# 防曬晴報員 P0 Screen Inventory 與畫面狀態矩陣

| 文件資訊 | 內容 |
| --- | --- |
| 對應 PRD | `防曬晴報員PRD.md` v3.10 |
| 對應交付清單 | `P0_RELEASE_MANIFEST.md` v0.3 |
| 文件版本 | 0.5 |
| 狀態 | P0 UX／UI 開發基準草案 |
| 建立日期 | 2026-07-29 |
| 最近更新 | 2026-08-07 |

> 本文件定義 P0 畫面、進入條件、主要操作及狀態呈現。提醒公式、事件因果、資料模型、安全邊界與正式驗收仍以 PRD 為準。若本文件與 PRD 衝突，以 PRD 為準並修正本文件。

---

## 1. 使用方式

本文件供產品、設計、前端、後端、內容與 QA 共用：

- 產品：確認 P0 沒有漏掉必要使用者任務。
- 設計：據此製作 wireframe、prototype 與 Design System 元件。
- 前端：據此建立 route、頁面狀態、操作與錯誤處理。
- 後端：確認公共 API 回應足以呈現所有 UVI 與時間狀態。
- 內容：補齊每個狀態需要的正式文案及審查標記。
- QA：將矩陣轉成頁面、狀態與轉場測試。

route 為目前建議識別名稱，可在 Technical Design 階段調整；頁面任務、狀態及安全行為不得因 route 改名而消失。

---

## 2. 共用畫面框架

### 2.1 手機導覽

一般 P0 頁面固定提供四個底部入口：

| 入口 | 目的地 | 核心內容 |
| --- | --- | --- |
| 首頁 | `/` | UVI、資料狀態、主要防護建議、開始／查看提醒 |
| 提醒 | `/reminder` | 進行中提醒或提醒空白狀態 |
| 產品 | `/products` | 目前使用與過去紀錄 |
| 更多 | `/more` | 說明、特殊狀況、安裝、顯示與資料設定 |

導覽列文字以實作為準（`BottomNavigation.vue`）：首頁／提醒／產品／更多。
本文件先前寫成「防曬品」，與畫面及 route meta 的「防曬產品」三處不一致。

規則：

- 圖示必須搭配文字。
- 點擊後停留在對應頁，不因沒有資料自動跳到其他分頁。
- 設定精靈、最終確認、事件提交、資料刪除確認等專注流程可隱藏底部導覽。
- 隱藏導覽時必須提供清楚的返回或取消，且不得遺失允許保存的草稿。
- 手機不得同時出現底部導覽與另一組重複的固定主要操作列。
  **例外**：route meta 為 `hideNavigation: true` 的專注流程（設定精靈）底部導覽本來就不存在，
  此時可使用固定底部操作列，不構成兩組競爭。此例外**不得**套用到有底部導覽的頁面，
  提醒頁（S-07）尤其不可比照。（2026-08-06 裁決，見 S-05 固定操作列。）

### 2.2 共用頂部區域

一般頁面依需要使用：

- 返回按鈕：回到上一個合理頁面，不依賴瀏覽器歷史中可能失效的外部頁。
- 頁面標題：白話描述當前任務。
- 次要選單：只放低頻或破壞性操作。
- 網路／離線提示：不遮住頁面主要標題與操作。

不得把登入、安裝、產品管理或公開內容入口做得比當前核心任務更醒目。

### 2.3 共用操作層級

- 每個畫面最多一個實心主要按鈕。
- 同層級至多一個直接可見的次要按鈕；其他選項進入文字連結、展開區或次要選單。
- 主要按鈕必須反映目前最需要處理的狀態，不得固定寫成 `已補擦`。
- 所有會改變時間、部位、產品、事件或 Session 的操作都必須在提交前確認。
- Loading 期間若不能安全重送，主要按鈕須停用並保留進度語意。
- API／IndexedDB 尚未確認寫入成功前，不顯示成功 toast 或完成狀態。

### 2.4 共用手機限制

- 發布基準：360、390、430 CSS px。
- 不得橫向捲動。
- 主要與高頻觸控目標至少 44 × 44 CSS px。
- 軟體鍵盤、瀏覽器列及安全區不得遮住必要欄位、錯誤或提交按鈕。
- 文字放大 200% 時仍可完成核心任務（全站以 `rem` 排版，滿足 WCAG 1.4.4）。
- 字級只使用 `packages/ui/src/styles.css` 的五個變數：
  `page-title`／`section-title`／`body`（14px）／`label`（12.8px）／`caption`（12px）。
  **caption 級只能承載輔助資訊**——使用者不讀也能完成任務的內容；
  沒有比 caption 更小的字級。
  （2026-08-05 裁決。原條文要求「一般文字至少 16 px」，與實作的 14 px 內文不符。
  16 px 並非 WCAG 要求，WCAG 沒有規定最小字級；此處改以實作為準，
  但保留 caption 的用途限制作為下限。表單 `input` 仍維持 16 px，
  避免 iOS Safari 聚焦時自動縮放頁面。
  尚待驗證：戶外強光下的實機可讀性。）
- **顏色不得是狀態的唯一載體**，必須另有文字、可及名稱或形狀承載相同資訊。
  （2026-08-05 裁決。原條文要求「同時使用文字、圖示、邊框及色彩」，
  指定手段而非結果，與「結構性區塊一律無框」的版面規則衝突。
  驗證方式：把畫面轉成灰階，狀態是否仍可辨識。）
- 尊重 `prefers-reduced-motion`。

---

## 3. 共用狀態元件

| 元件 | 用途 | 必要內容 | 禁止事項 |
| --- | --- | --- | --- |
| UVI 卡 | 顯示環境 UV 資料 | 數值、CWA 等級、來源種類、測站／區域、時間、freshness、建議 | 不得把預報稱為即時觀測；不可用時不得顯示 0 |
| 時間型提醒環 | 顯示可信的下一個時間行動 | 部位、相對時間、絕對時間、狀態、原因、非安全倒數說明 | 不得顯示安全百分比、負數或多個同等大型圓環 |
| 到期狀態卡 | 顯示目前已需處理 | 部位、狀況、原因、主要行動 | 不得閃爍 `00:00` 或整頁紅色警報 |
| 非時間型行動卡 | 缺乏可信期限或需要補資料 | 受影響部位、原因、立即下一步 | 不得補任意分鐘或借用其他部位期限 |
| 中性方式卡 | 衣物覆蓋等不需要產品倒數的狀態 | 實際方式、更新入口 | 不得換算 UPF／布料為安全時間 |
| 狀態 Banner | 離線、資料過期、尚未保存等跨頁狀態 | 簡短狀態、影響、替代行動 | 不得用 toast 取代持續存在的重要限制 |
| Inline Error | 欄位或區塊錯誤 | 問題、修正方法、關聯欄位 | 不得只在頁首顯示籠統錯誤 |
| Confirm Dialog／Sheet | 最終確認或破壞性操作 | 影響摘要、確認、取消 | 不得預設選取破壞性動作 |
| Success Summary | 成功提交後回饋 | 實際更新的部位／事件／時間、`更正` | 不得只顯示「成功」而使人誤認全部部位已更新 |

---

## 4. Screen Inventory 總表

| ID | Route | 畫面 | 底部導覽 | 實作狀態 | 主要結果 |
| --- | --- | --- | --- | --- | --- |
| S-01 | `/` | 首頁 | 顯示 | 已實作 | 開始設定，或直接執行 active Session 的最高優先操作 |
| S-02 | `/region` | 地區與定位 | 可隱藏 | 已實作 | 保存手動地區、本次定位結果或略過狀態 |
| S-03 | `/setup/context` | 設定：情境 | 隱藏 | 已實作 | 保存草稿情境 |
| S-04 | `/setup/timing?adjustProtection=1`；S-07 原地 sheet | 防護方式與部位（共用展開區） | 隱藏／依宿主 | 設定端已實作 | 調整部位及合法方法；設定端寫入草稿，提醒端寫入 active Session。不是必經頁面 |
| S-05 | `/setup/timing` | 設定：快速提醒、塗抹時間與開始提醒 | 隱藏 | 已實作，待改為兩步 | 自動套用建議部位；引用產品頁 snapshot、保存本次時間並原子建立 Session |
| ~~S-06~~ | ~~`/setup/review`~~ | ~~設定：最終確認~~ | — | **已廢除** | 內容併入 S-05；見下方 S-06 廢除說明 |
| S-07 | `/reminder` | 進行中提醒／提醒空白 | 顯示 | 已實作 | 查看狀態並開始下一個行動 |
| S-08 | `/reminder/reapply` | 記錄已補擦 | 隱藏 | Placeholder | 建立確認群組與 ApplicationEvents |
| S-09 | `/reminder/report` | 回報狀況 | 隱藏 | Placeholder | 建立活動、方法或產品安全事件 |
| S-10 | `/reminder/event/:id/correct` | 更正最近事件 | 隱藏 | Placeholder | 建立 replace／void 更正鏈 |
| S-11 | `/products` | 我的防曬裝備 | 顯示 | 已實作，待擴充品類 | 選擇、新增或管理防曬產品與裝備 |
| S-12 | `/products/new` | 新增防曬裝備 | 隱藏 | 未實作 | 建立本機產品或裝備 |
| S-13 | `/products/:id/edit` | 編輯防曬裝備 | 隱藏 | 未實作 | 更新、封存、刪除或另建新版 |
| S-14 | `/more` | 更多 | 顯示 | 已實作 | 前往說明與設定 |
| S-15 | `/help`；`/help/beach` | Q&A 總覽與海邊防曬 | 依入口 | 未實作 | 瀏覽主題總覽或閱讀單則內容並返回原流程 |
| S-16 | `/help/how-it-works` | 運作與倒數說明 | 依入口 | 未實作 | 理解限制並返回 |
| S-17 | `/special-situation` | 特殊狀況 | 隱藏 | 未實作 | 顯示拒答、轉介或急症主要行動 |
| S-18 | `/settings/display` | 顯示設定 | 顯示 | 未實作 | 保存本機顯示偏好 |
| S-19 | `/settings/data` | 本機資料管理 | 顯示 | 未實作 | 查看或清除本機資料 |
| S-20 | `/install` | 安裝到手機 | 顯示 | 未實作 | 顯示平台相符的安裝步驟 |

---

## 5. 逐頁規格

## S-01 首頁

### 目的

讓使用者快速理解目前 UV 資料狀態，並開始或返回核心提醒任務。

### 進入條件

- 初次開啟。
- 從底部導覽選擇首頁。
- Session 結束後返回。
- 從其他流程返回。

### 資訊順序

2026-08-05 依實作更新。原順序把 UVI 資料放在提醒之前，實際實作反過來：
使用者開 App 的目的是「還有多久要補擦」，不是查 UV，所以補擦提醒是
`<h1>`、排在最前面，UV 資訊退為次級（commit `d53fa3b`、`d81e066`）。

1. 補擦提醒主卡（`<h1>`）：有可信期限顯示倒數與唯一主要 CTA；
   無可信期限顯示 untimed 卡；沒有 active Session 顯示空狀態與「新增提醒」。
2. 各部位狀態（`<h2>`，依狀態分組的 chip 群組）。
3. 戶外資訊（`<h2>`）：目前地區與變更入口。
4. 18:00～05:59 且預報可用時，同一晚間區間一次的頁內預覽提示。
5. 結束本次提醒（有 active Session 時）。
6. 未來 5 天 UV（`<h2>`）。
7. 免責聲明。

視覺順序與 DOM 語意順序必須一致；調整排序時同步檢查標題層級，
不要讓螢幕閱讀器唸出的順序與畫面不同。

**尚未實作**：即時測站觀測（UVI 卡）。目前只有五日區域預報，
因此下方「主要狀態」表中的 `fresh observation`／`stale observation`／
代表測站／距離等狀態在 P0 目前的實作裡不會出現。

### 主要 CTA

| 條件 | CTA | 結果 |
| --- | --- | --- |
| 無 active Session | `開始防曬提醒` | 前往 S-03；不要求先提供地區 |
| 有 active Session | 依 `primaryAction.actionKind` 顯示相符操作 | 直接進入補擦、回報或相應確認流程；另提供次要入口前往 S-07 查看完整狀態 |

### 次要操作

- `使用目前位置`
- `手動選擇地區`
- `設定／變更地區`
- `安裝到手機`
- `資料來源與限制`

### 主要狀態

| 狀態 | 顯示 |
| --- | --- |
| 尚未提供地區 | 「目前沒有所在地 UV 資料」＋三種地區選項；主要提醒 CTA 仍可用 |
| 正在取得位置 | UVI 卡區域顯示定位進度；提醒 CTA 仍可用 |
| fresh observation | UVI、CWA 等級、代表測站、距離、觀測時間、觀測標籤 |
| stale observation | 保留數值但明顯標示資料較舊與時間 |
| valid forecast | 顯示區域預報、有效時段與發布狀態，不稱即時 |
| unusable／missing | 不顯示目前 UVI；顯示一般保守建議 |
| offline with usable snapshot | 顯示最後資料、原時間與離線提示 |
| offline with unusable snapshot | 只能作歷史資料說明，不顯示為目前 UVI |
| API error | 顯示取得失敗、重試與手動／略過替代方式 |
| five-day forecast ready | 顯示仍有效的下一個五個白日時段、CWA 分級、來源、更新時間及區域預報限制 |
| five-day no region／unusable | 不顯示數值；分別說明需要地區或目前沒有可用資料 |
| evening forecast prompt | 只在 18:00～05:59、有可用預報且本晚尚未關閉時顯示；不使用系統通知 |

### 驗收關聯

AC-01、02、03、04、19、21、32、54、55、59。

---

## S-02 地區與定位

### 目的

讓使用者理解資料用途後，主動選擇定位、手動地區或不提供地區。

### 進入條件

- 點擊首頁地區入口。
- 首次主動選擇 `使用目前位置`。
- 更新所在地。

### 資訊順序

1. 為何需要地區。
2. 精確位置如何使用及不保存的說明。
3. 三個選項。
4. 拒絕權限的影響及替代方式。

### 操作

| 操作 | 行為 |
| --- | --- |
| `使用目前位置` | 使用者點擊後才觸發瀏覽器 permission prompt |
| `手動選擇地區` | 顯示可搜尋／分層選擇的行政區 |
| `暫不提供地區` | 保存略過偏好並返回；不阻擋提醒 |
| `取消` | 保留原地區設定，不觸發權限 |

### 狀態

- Permission prompt 前說明。
- 定位中。
- 定位成功但 UV 查詢中。
- 權限拒絕。
- 瀏覽器不支援。
- 定位逾時。
- 無法對應行政區。
- 手動地區清單載入失敗。
- 地區已保存但 UVI 暫不可用。

定位錯誤不得自動改用不透明的中心座標，也不得重複彈出權限請求。

### 驗收關聯

AC-01、19、21、32、55、100。

---

## S-03 提醒設定：情境

### 目的

建立本次 Session 的初始活動情境，並決定後續需要顯示的欄位。

### 進入條件

- 首頁／提醒空白狀態點擊 `開始防曬提醒`。
- 沒有 active Session。
- 有未到期 SetupDraft 時可先顯示恢復提示。

### 首層選項

- 室內遠離直射陽光。
- 室內近直射窗邊。
- 一般戶外。
- 戶外運動。
- 水上活動。

水上活動再詢問：

- `準備下水`
- `已在水中`

### 主要操作

- 主要 CTA：`下一步`
- 次要 CTA：`取消`

### 狀態

| 狀態 | 行為 |
| --- | --- |
| 無選擇 | `下一步` 停用或提交時顯示就近錯誤 |
| 有最近設定 | 先顯示摘要與 `沿用並確認`／`修改設定` |
| 有 SetupDraft | 顯示 `繼續設定`／`重新開始` |
| 已有 active Session | 不進入本頁；顯示目前提醒衝突選項 |
| 儲存草稿失敗 | 保留表單記憶體狀態並顯示未保存 |
| 離線 | 可繼續；不要求地區或最新 UVI |

選擇 `準備下水` 不得建立耐水期限。選擇 `已在水中` 只調整後續欄位，本頁尚不建立事件。

### 驗收關聯

AC-33、41、42、79、88。

---

## S-04 防護方式與部位（設定與提醒頁共用的展開區）

### 目的

讓需要不同部位或不同防護方式的使用者進階調整；一般快速提醒不必先經過此區。

### 使用場合

2026-08-06 裁決：本區**不再是設定精靈專屬**，`ZoneProtectionForm` 由兩處共用。

| 場合 | 入口 | 差異 |
| --- | --- | --- |
| 設定精靈 | S-05 的 `調整追蹤部位或防護方式` | 寫入 SetupDraft，尚未建立 Session |
| 進行中提醒 | S-07 的 `確認防護方式`／`查看防護選項` | 寫入 active Session，需經事件與確認流程 |

兩處共用同一份合法方法組合規則與同一個表單元件；差別只在提交後寫入草稿或
寫入 Session。提醒頁的提交仍受既有 mutation 規則約束：不得略過確認、
不得在未確認前修改期限。

### 預設路徑

1. 進入 S-05 時依已選情境，將一組版本化建議組合自動寫入 SetupDraft。
2. 頁首以 `快速提醒（推薦）` 及白話大群組摘要清楚顯示已套用內容。
3. 時間選擇器立即可用，不要求額外按下接受推薦。
4. 提供 `調整追蹤部位或防護方式`，以 Bottom Sheet 開啟本區。

次要入口：`自己選擇部位`。

### 部位方法

主要選項：

- `已擦防曬產品`
- `被衣物完整遮住`

次要入口：

- `其他情況` → `其他外用產品`

衣物選項的選填補充：

- `衣物下方也有擦防曬產品`

### 揭露層次（2026-08-06 裁決）

PRD §5.2 第 5 點 早已規定上述層級，但實作把四個選項並列成第一層，且每個部位群組
常駐一組單選鈕。實測現況：**20 個常駐可見單選鈕、需捲動 3 屏**才能離開本區。
這是既有規格落差，不是新設計。

本區必須依下列層次揭露：

| 層次 | 內容 | 常駐可見的單選鈕 |
| --- | --- | --- |
| 收合（預設） | 每個部位群組一行：群組名稱＋目前方法的狀態文字 | **0 個** |
| 點開一組 | 只顯示兩個主要選項，加一個 `其他情況` 文字連結 | 2 個 |
| 展開 `其他情況` | 才出現 `其他外用產品` | 3 個 |

規則：

- 預設收合。收合狀態下不得有任何可見單選鈕。
- 一次只展開一個群組；選擇後就地更新該行狀態文字。
- 選 `已擦防曬產品` 後直接收合（無後續問題）。
- 選 `被衣物完整遮住` 後**不立即收合**，就地顯示選填追問
  `衣物下方也有擦防曬產品嗎？（選填）`，由使用者按 `完成` 收起。
- `衣物下方也有擦防曬產品` **不得**放進第一層並列。PRD §5.2 第 5 點 明文禁止
  在第一層另列語義模糊的「兩者都有」。
- 衣物覆蓋是中性正常狀態，狀態文字**不得**使用 untimed 語意色（紫）。
  untimed 在本專案代表「沒有可信期限、需要處理」，借用會讓正常狀態讀起來像有問題。
  使用次要文字色即可，在一排預設值中已足夠可辨識。

本裁決只改變揭露層次，`ZoneProtectionForm` 的提交邏輯、合法方法組合規則
（DT-METHOD-01）與資料模型均不變。因本元件由設定端與提醒端共用，改一次兩處同時受益。

### 主要操作

- 主要 CTA：`完成調整`
- 次要 CTA：`收合`
- 離開入口：`先離開`

### 狀態

| 狀態 | 顯示／行為 |
| --- | --- |
| 自動套用 preset | 展開為 SetupDraft 的 V3 原子部位，UI 維持大群組摘要；不建立方法事件、Application 或 Session |
| 尚未最終確認 | 可修改或取消草稿；只有 S-05 成功提交後才成為 Session 真值 |
| 調整部位 | 顯示群組；必要時才展開原子部位 |
| 群組內方法不同 | 強制展開差異，不得靜默批次覆蓋 |
| 無任何追蹤部位 | 顯示就近錯誤；不可繼續 |
| 任一部位沒有合法方法 | 顯示 `請先選擇目前已採用的方式` |
| 全部衣物覆蓋 | 可進入確認，不詢問塗抹時間 |
| 無法確認已採用方式 | 提供返回／離開，不建立空 Session |
| 舊 V2 最近設定 | 顯示 V3 群組轉換摘要並要求確認 |

### 禁止行為

- 不顯示可提交的 `尚未採取防護` 或 `不確定`。
- 不提供 `general_unlocalized`。
- 不因 UVI、定位或歷史推測靜默選取部位。
- 頭皮與嘴唇不因選擇臉部而自動加入。
- 雨傘、建築、樹蔭不得記為衣物覆蓋。

### 驗收關聯

AC-26、34、52、79、81、88、94。

---

## S-05 提醒設定：快速提醒、塗抹時間與開始提醒

### 目的

進入頁面時自動將情境建議部位寫入 SetupDraft，讓使用者確認本次實際塗抹時間，
並在同一頁完成 Session 建立。產品標示 snapshot 統一在 S-11 產品頁管理；
需要部位差異時，以 Bottom Sheet 開啟 S-04 調整表單。

**2026-08-06 裁決：設定流程由三步縮為兩步。** 原 S-06 最終確認頁廢除，
其必顯內容與提交行為併入本頁，CTA 改為 `開始提醒`，按下即為計時起點。

上游依據：PRD v3.10 §5.2 第 2、10 點。原 v3.9 要求三畫面與獨立最終確認頁，
已於 2026-08-07 修訂為兩畫面＋提交前確認區。**確認的實質要求沒有降低**——
使用者仍須在按下 `開始提醒` 前看過完整摘要，只是摘要改在同一畫面的按鈕上方。

裁決理由：使用者反映三步太麻煩，希望「步驟 2 送出後就開始計時」。
直接刪除步驟 3 會違反 AC-34 Scenario C（系統會追蹤使用者從未看過的部位），
因此改為併入而非刪除——AC-34 要求的是完整部位摘要**有被揭露**，
不是強迫使用者捲過它。

### 顯示條件

| 部位組成 | 本頁內容 |
| --- | --- |
| 全部只有衣物覆蓋 | 跳過產品與時間輸入，直接顯示摘要與 `開始提醒` |
| 至少一個 sunscreen | 顯示產品選擇、必要標示及時間 |
| 至少一個 other topical | 顯示產品身分／未知值與無期限結果說明 |
| 衣物下方有 sunscreen | 允許記錄原產品與原始塗抹時間，但提醒衣物覆蓋期間不倒數 |

### 產品入口

- 最多三個符合資格的最近產品。
- `選擇其他已保存產品`
- `新增產品`
- `這次先不保存產品`

### 最低必要欄位

- 包裝是否明確有防曬／SPF 標示。
- 曝曬前等待方式。
- 本次實際塗抹時間。
- 水上情境時的耐水 40／80 分鐘、無宣稱或未知。
- 若標示有更短的一般補擦分鐘，須保存於 snapshot。

### 時間選項

- 剛剛。
- 15 分鐘前。
- 30 分鐘前。
- 1 小時前。
- 自訂時間。

相對時間旁同步顯示換算後的絕對時間。

### 提交前必須揭露（原 S-06 必顯內容）

- 情境與適用的水上狀態。
- 追蹤部位大群組摘要。
- 每個部位的方法。
- 實際使用的產品或 Session-only snapshot 摘要。
- 只屬於外用產品部位的新塗抹時間。
- 曝曬前等待、較短補擦標示及水上耐水標示。
- 過期、異常、不適、身分未知或非防曬產品的顯眼警示。
- 「提醒期限不是安全曝曬時間」。

揭露方式（2026-08-06 裁決，經瀏覽器實測）：

- **部位摘要壓成一行**，例如 `追蹤 5 個部位：臉部、耳朵、頸部、手臂、手背　調整`。
  不展開列表。需要細節或修改的人才點 `調整` 進入 S-04 sheet。
- **警示排在版面最前面**，不靠強迫捲動。過期、身分未知、異常這些警示
  多數 Session 一個都沒有，沒有時不佔版位；真的有的時候放在第一眼位置。
  靠版位確保被看見，比靠摩擦力誠實。

### 固定操作列（2026-08-06 裁決）

主要操作固定於畫面底部，內容捲動時按鈕不移動。

| 情境 | 是否需要捲動 | 實測超出量 |
| --- | --- | --- |
| 一般情況 | 不用，一屏完成 | 0 px |
| 產品標示不完整（有警示） | 需要，但操作列不動 | 140 px |

合規性：本頁 route meta 為 `hideNavigation: true`，底部導覽本來就隱藏，
沒有第二組操作列與之競爭，符合 §2.1 的例外條款。
**此做法不得照搬到 S-07 提醒頁**，該頁有底部導覽。

### 主要操作

- 主要 CTA：`開始提醒`（按下即為計時起點）
- 次要 CTA：`返回修改`
- 離開入口：`取消設定`

**CTA 文字必須反映實際會發生的事**：產品身分未確認時
（`identity_unconfirmed` → 無期限資格）系統不會建立倒數，
此時按鈕須顯示 `開始提醒（不建立倒數）`。
本頁規格本就要求這種情況必須明示不建立產品期限，寫進按鈕是最難忽略的位置。

### 狀態

| 狀態 | 顯示／行為 |
| --- | --- |
| 已保存且未修改產品 | 不重問產品身分 |
| Session-only 產品 | `productId=null`，但必須有完整 snapshot |
| 包裝確認沒有防曬標示 | 可記錄，但明示不建立產品期限 |
| 身分不確定／看不清楚 | 可記錄，但使用無時間行動卡 |
| 產品過期 | 顯眼提示，不建立期限 |
| 產品異常／不適 | 不可作為合格期限產品 |
| 標示等待時間 | 在摘要中展開顯示 |
| 更短一般補擦標示 | 在摘要中展開顯示並採較短值 |
| 水上耐水未知 | 顯示保守結果及 Q&A 入口 |
| CLOCK_UNTRUSTED 線上 | 必須先成功重新校準 |
| CLOCK_UNTRUSTED 離線 | 不允許用不可信時間延長期限 |
| 自訂時間在未來 | 就近錯誤，不得提交 |

### 提交狀態（原 S-06）

| 狀態 | 行為 |
| --- | --- |
| 尚未提交 | 不建立 Session |
| 提交中 | 鎖定重複提交；保持可理解的進度 |
| 成功 | 導向 S-07 並顯示真實衍生狀態 |
| 驗證 422 | 回到相應欄位並保留草稿 |
| active Session 衝突 | 顯示查看、結束後重試或取消 |
| IndexedDB transaction 失敗 | 不留下部分 Session；顯示未建立 |
| 相同 idempotency key 重送 | 回傳同一完整結果 |

全為衣物覆蓋時 ApplicationConfirmationGroup／ApplicationEvents 必須為空。
仍使用 `StartSessionCommandV1` 原子建立 Session；併頁不改變命令與交易邊界。

### 驗收關聯

AC-05、16、17、18、23、33、34、35、39、40、41、42、43、47、48、50、53、79、85、87、88、97。
（併入原 S-06 的 AC-05、33、34、39、41、42、79、85、88。）

---

## S-06 提醒設定：最終確認（已廢除）

**2026-08-06 裁決廢除本頁。** 內容與提交行為併入 S-05，設定流程由三步縮為兩步。

- route `/setup/review` 不再是 P0 交付項目。
- 原「必須顯示」清單移到 S-05「提交前必須揭露」，一項未刪。
- 原「提交狀態」表移到 S-05「提交狀態（原 S-06）」，行為不變。
- 原 CTA `開始提醒` 移到 S-05 的固定操作列。

保留本節是為了讓既有的 S-06 交叉引用有落點。新規格一律寫在 S-05，
不要在此新增內容。實作移除 `/setup/review` route 時，
需一併處理 route meta `setupStep` 的步驟編號與返回路徑。

---

## S-07 進行中提醒／提醒空白

### 空白狀態

若沒有 active Session：

- 留在提醒頁。
- 顯示「目前沒有進行中的防曬提醒」。
- 主要 CTA：`開始防曬提醒`。
- 不自動跳回首頁。

### Active Session 資訊順序

1. 單一 `primaryAction` 狀態元件。
2. 目前狀態、受影響部位、原因與下一步。
3. 一個主要 CTA 與至多一個次要 CTA。
4. `需要處理` 部位。
5. `即將需要處理` 部位。
6. `其他狀態` 部位。
7. 最近事件時間軸。
8. 次要選單中的通知說明與 `結束本次提醒`。

### `primaryAction` 顯示矩陣

| 主要狀態 | presentation | 主要 CTA | 主要 CTA 目的地 | 次要 CTA |
| --- | --- | --- | --- | --- |
| REAPPLY_DUE | due card | `記錄已補擦` | S-08 | `回報狀況` |
| REAPPLY_SOON | timed ring | `記錄已補擦` | S-08 | `回報狀況` |
| TRACKING | timed ring | `回報狀況` | S-09 | `記錄已補擦` |
| UNRECORDED 舊資料狀態 | untimed action | `補上防護紀錄` | S-08（首次記錄變體） | `回報狀況` |
| unknown 舊資料狀態 | untimed action | `確認防護方式` | S-07 原地 sheet | `回報狀況` |
| none_reported 舊資料狀態 | untimed action | `查看防護選項` | S-07 原地 sheet | `更新防護紀錄` |
| WATER_START_UNKNOWN | untimed action | `處理入水時間` | S-09 | `記錄已補擦` |
| 產品安全事件 | untimed action | `改用其他防護` | S-08 | `查看處理說明` |
| LABEL_WAIT | untimed action，label_wait variant | `查看產品標示` | S-07 原地展開 | `回報狀況` |
| 衣物覆蓋 | untimed action，neutral_physical variant | `回報狀況` | S-09 | `更新防護方式` |
| CLOCK_UNTRUSTED 線上 | untimed action | `重新校準時間` | S-07 原地執行 | `查看已保存紀錄` |
| CLOCK_UNTRUSTED 離線 | untimed action | `查看保守提醒` | S-16 | `查看已保存紀錄` |
| 多種最高優先行動 | untimed action，multi_action variant | `查看需要處理的部位` | S-07 原地錨點 | `回報狀況` |

上表以「主要狀態」為索引，未涵蓋全部 `ActionKind`。權威對照見下一節；
`ActionKind` 是 `packages/contracts` 的 domain 契約，狀態描述只是它的呈現。

### 次要 CTA 目的地（2026-08-07 裁決）

四則次要 CTA 全數對應到既有畫面或 S-07 原地行為，**不新增畫面**——
與 2026-08-06 的 `ActionKind` 裁決同一原則。

| 次要 CTA | 出現於 | 目的地 | 理由 |
| --- | --- | --- | --- |
| `查看已保存紀錄` | CLOCK_UNTRUSTED 線上／離線 | S-07 最近事件時間軸（原地錨點） | 時鐘不可信時使用者要確認的是「我已經記了什麼」，資料就在本頁下方，離開頁面反而失去脈絡 |
| `查看處理說明` | 產品安全事件 | S-17 特殊狀況 | 產品異常／不適的處理指引屬醫療邊界內容，S-17 已承載拒答、轉介與急症分流 |
| `更新防護紀錄` | none_reported 舊資料狀態 | S-08（首次記錄變體） | 「補上紀錄」的本質是建立 Application，與 `complete_protection_record` 同一條路徑 |
| `更新防護方式` | 衣物覆蓋中性方式卡 | S-04 原地 sheet | 改的是方法不是時間，不需要補擦表單；`ZoneProtectionForm` 已由提醒端共用 |

`查看處理說明` 導向 S-17 時不得預先帶入症狀、產品名稱或任何使用者輸入，
S-17 的拒答邊界不因入口不同而放寬。

### `ActionKind` 目的地對照（2026-08-06 裁決）

13 個 `ActionKind` 全數對應到既有 P0 畫面或 S-07 原地行為，**不新增畫面**。

| ActionKind | 觸發條件（`reducer.ts`） | 目的地 |
| --- | --- | --- |
| `record_reapplication` | `reapply_due`／`reapply_soon` | S-08 |
| `resolve_cause` | `timingStatus === "untimed_action"` | S-08 |
| `complete_protection_record` | `recordStatus === "unrecorded"` | S-08（首次記錄變體） |
| `switch_protection` | 產品安全封鎖且皮膚外露 | S-08 |
| `report_context_event` | `tracking`／`not_applicable` 且追蹤中 | S-09 |
| `resolve_water_start` | `WATER_START_UNKNOWN` | S-09 |
| `confirm_protection_method` | `recordStatus === "unknown"` | S-07 原地 sheet |
| `view_protection_options` | `none_reported` 或 Application 不具資格 | S-07 原地 sheet |
| `view_conservative_reminder` | 時鐘不可信且離線 | S-16 |
| `view_product_label` | `timingStatus === "label_wait"` | S-07 原地展開 |
| `recalibrate_clock` | 時鐘不可信且在線 | S-07 原地執行 |
| `view_ended_state` | Session 已結束 | S-07 空白狀態 |
| `review_required_zones` | 最高層級同時存在多種動作 | S-07 原地錨點 |

裁決理由與實作注意事項：

- **`resolve_cause` → S-08**：由規則推導，非選擇。`RR-P0-CAUSE-002` 明訂
  只有嚴格較晚的合格 Application 才能解除原因，所以目的地必然是補擦表單。
- **`switch_protection` → S-08**：產品選單必須排除被安全事件封鎖的產品。
  改用衣物遮蔽者走 S-09 的 `更新衣物狀態`，屬次要路徑。
- **`complete_protection_record` → S-08**：表單欄位與補擦相同，但這是首次記錄，
  **不得沿用「記錄已補擦」標題**。需要新的標題文案並進 Copy Deck 審查。
- **`confirm_protection_method`／`view_protection_options` → S-07 原地 sheet**：
  兩者答案空間相同，合併同一目的地。複用 S-04 的 `ZoneProtectionForm`
  元件；該元件因此不再是設定精靈專屬，S-04 的定義需同步放寬。
- **`view_product_label` → 原地展開**：此狀態的語意是「正在等待，不要離開」，
  導航到 S-11 會打斷等待。標示 snapshot 就地摘要顯示即可。
- **`recalibrate_clock` → 原地執行**：系統操作而非使用者輸入，沒有表單。
  相依於尚未實作的 `useTrustedClock`。
- **`resolve_water_start` → S-09**：S-09 第二層確認已涵蓋水上 interval 狀態
  與實際發生時間；複用既有的 `WaterStartPicker` 元件。

### 其他狀態

- `尚未保存`：持續 Banner，不用短暫 toast。
- `離線`：顯示遠端資料不可用，但本機有效狀態可操作。
- Session 來自較早日期：可提示繼續或結束，不自動結束。
- Session 已在其他 context 更新：重讀最新 revision，不用舊畫面提交。
- Session 已結束：顯示已結束並返回空白狀態，不允許一般事件。

### 禁止行為

- 不提供暫停倒數、增加時間或無確認的重新開始。
- 不讓其他部位的時間替代最高優先的無時間狀態。
- 不把衣物覆蓋稱為安全。
- 不顯示多個同等醒目的大型提醒環。

### 驗收關聯

AC-05、07、09、12、23、36、38、42、51、57、64、65、66、67、81、82、85、86、95、96。

---

## S-08 記錄已補擦

### 目的

以批次預選降低操作，但只更新使用者最後確認的部位。

### 預選

- 預選已到期或即將到期部位。
- 預填這些部位目前產品。
- 預選 `剛剛`，但仍須提交確認。

### 操作選項

- `只選建議部位`
- `選擇所有本次追蹤部位`
- `自行調整`

### 最終確認

必須顯示：

- 最終部位。
- 各部位產品。
- 實際塗抹時間。
- 不同產品／snapshot 的分組。
- 不具期限資格部位的結果。

### 狀態

| 狀態 | 行為 |
| --- | --- |
| 沒有選部位 | 不可提交 |
| 同一部位出現在兩個產品群組 | 阻止提交 |
| 產品不同 | 建立同一 confirmation group 下互斥 ApplicationEvents |
| 自訂時間在未來 | 阻止提交 |
| CLOCK_UNTRUSTED | 依線上校準／離線保守規則 |
| 成功 | 顯示「已更新：部位，時間」與 `更正` |
| 部分寫入失敗 | 整批回滾，不顯示部分成功 |
| revision 衝突 | 重讀目前狀態並要求重新確認 |

### 驗收關聯

AC-07、28、37、39、40、45、65、82、85、87。

---

## S-09 回報狀況

### 目的

讓使用者快速回報會影響目前防護狀態的實際事件。

### 第一層事件

- 游泳／下水。
- 離水。
- 大量流汗。
- 擦毛巾。
- 明顯摩擦。
- 洗手。
- 進入室內。
- 返回戶外。
- 更新衣物狀態。
- 使用產品後感到不適。

### 第二層確認

依事件顯示：

- 受影響部位。
- 實際發生時間。
- 水上 interval 狀態。
- 離水後情境。
- 產品安全事件的產品／snapshot。

### 預選規則

| 事件 | 預選 |
| --- | --- |
| 洗手 | 目前外露的手背 |
| 下水 | 本次水中外露部位 |
| 離水 | 完整沿用對應 water_start 部位集合 |
| 擦拭／摩擦／流汗 | 要求使用者確認實際受影響部位 |

### 狀態

- 無 active Session：不允許建立事件，導向提醒空白狀態。
- 沒有可關閉的水上區間：不顯示可提交的離水事件。
- 已有 active 水上區間：不允許建立重疊起點。
- 入水時間未知：不製造耐水倒數。
- 事件時間在未來：阻止提交。
- 成功：顯示事件、部位與時間摘要及 `更正`。
- 寫入失敗：保留輸入並顯示未更新。

產品不適流程不得要求症狀、照片、病史、用藥或自由文字。

### 驗收關聯

AC-06、28、37、39、41、44、51、83、85、97。

---

## S-10 更正最近事件

### 目的

更正目前 Session 最近提交的事件，保留不可變稽核鏈。

### 可用操作

- 修改事件時間。
- 修改允許變更的受影響部位。
- 以 replace 建立後繼事件。
- 在允許情況下 void 事件。

### 規則

- 不直接編輯或刪除原事件。
- correction target 必須是目前唯一有效 leaf。
- 水上事件更正必須維持起終點配對、部位集合一致及區間不重疊。
- 更正後重建部位狀態、Session 彙總及本機提醒。

### 狀態

- 目標已被更正：顯示最新狀態，不允許建立第二個 successor。
- 更正造成非法水上區間：阻止提交。
- 更正造成未來事件：阻止提交。
- 成功：顯示更新後摘要。
- 失敗：原有效狀態保持不變。

### 驗收關聯

AC-28、39、44、45、85。

---

## S-11 我的防曬裝備

### 目的

查看及管理本機保存的防曬產品與裝備，不把產品管理變成核心提醒的前置門檻。

**2026-08-06 裁決：本頁由「提醒用產品主檔」擴為「防曬裝備清單」。**
使用者要的是一份完整的防曬裝備紀錄（含衣物、墨鏡、購買月份、到期日、備忘、過去用過），
不只是會進 reducer 的標示欄位。

上游依據：PRD v3.10 F-05。原 v3.9 只描述防曬產品的包裝標示、暱稱與使用狀態，
已於 2026-08-07 擴充納入四個裝備品類與購買月份、到期日、備忘、過去用過。

### 品類

| 品類 | 是否進 reducer | 提醒語意 |
| --- | --- | --- |
| `sunscreen` | **是** | 到期、耐水、補擦間隔、曝曬前等待都影響倒數 |
| `clothing` | **部分** | 是 methodComponent，覆蓋期間不倒數 |
| `eyewear` | 否 | 無對應 `BODY_ZONE`，純紀錄 |
| `other_gear` | 否 | 純紀錄 |

**UI 必須明示哪些欄位會影響倒數。** 使用者記錄一副墨鏡時，
不得讓人以為提醒行為會因此改變——這是本次擴充最主要的安全風險。

### 新增欄位

以下欄位**除到期日外皆不進 reducer**：

| 欄位 | 用途 | 進 reducer |
| --- | --- | --- |
| `purchaseMonth` | 購買月份 | 否 |
| `expiryDate` | 真實日期，取代／補充既有的 `expiryStatus` | **是** |
| `note` | 備忘 | 否 |
| `archivedAt` | 過去用過 | 否 |

`expiryDate` 取代 `expiryStatus` 時須保留既有「過期產品不建立期限」的行為，
並提供舊資料的 migration 路徑。

### 分區

- `目前使用`
- `過去紀錄`

### 操作

- 主要 CTA：`新增防曬裝備`
- 裝備列：查看／編輯。
- 符合條件的過去產品：恢復。
- 封存、停止使用或刪除：進入 S-13。

### 狀態

| 狀態 | 顯示 |
| --- | --- |
| 完全沒有裝備 | 空白狀態＋新增；說明建立提醒也可暫不保存產品 |
| 有目前產品 | 最近／目前使用產品優先 |
| 只有過去產品 | 顯示過去紀錄，不自動恢復 |
| 異常／不適產品 | 明確安全狀態，不提供直接恢復 |
| 只有非 sunscreen 裝備 | 明示目前沒有可建立倒數的防曬產品 |
| 本機資料不可用 | 顯示儲存問題，不假裝清單為空 |

### 驗收關聯

AC-35、43、50、53、55、97。

---

## S-12 新增防曬裝備

### 目的

以短表單建立可重用產品或裝備；非必要欄位可稍後補充。

### 必要欄位

- 品類（`sunscreen`／`clothing`／`eyewear`／`other_gear`）。
- 產品暱稱。
- 產品身分確認（僅 `sunscreen` 與 `clothing` 需要）。

選擇非 `sunscreen` 品類時，表單須收起所有只對 sunscreen 有意義的標示欄位，
並明示此筆紀錄不影響提醒倒數。

### 選填／情境欄位

- 品牌／品名。
- SPF、PA、廣效／UVA 文字（僅 `sunscreen`）。
- 曝曬前等待（僅 `sunscreen`）。
- 一般補擦間隔（僅 `sunscreen`）。
- 耐水標示（僅 `sunscreen`）。
- 購買月份（`purchaseMonth`）。
- 到期日（`expiryDate`）。
- 開封日期。
- 備忘（`note`）。
- 私人備註。

### 狀態

- 無防曬標示：可保存為非防曬紀錄。
- 看不清楚：可保存為身分未知。
- 私人備註：顯示不要輸入疾病、症狀、用藥或聯絡資料提示。
- 重複暱稱：可允許，但不可用暱稱推定同產品。
- 儲存失敗：保留表單，不返回列表。

### 驗收關聯

AC-18、35、47、48、55。

---

## S-13 編輯防曬裝備

### 目的

更新產品主檔或改變使用狀態，不回溯改寫既有 Session snapshot。

### 操作

- 儲存修改。
- 移至過去紀錄（寫入 `archivedAt`）。
- 封存。
- 停止使用。
- 再次購買／建立新版。
- 刪除。

品類不可任意變更：已被 active Session 或既有事件引用的 `sunscreen`
不得改為純紀錄品類，否則會讓既有倒數失去產品依據。需要改品類時另建新紀錄。

### 狀態與限制

| 狀態 | 可用操作 |
| --- | --- |
| 一般目前使用 | 編輯、封存、停止、刪除 |
| 已用完／封存且無安全問題 | 可恢復 |
| user_reported_abnormal | 不直接恢復；不同批次／新包裝可另建新版 |
| user_reported_discomfort | 不直接恢復；同配方新批次不可繞過封鎖 |
| 被 active Session 引用 | 可修改主檔，但不改寫既有 snapshot |
| 刪除 | 顯示最近設定可能失去產品參照的影響 |

刪除與安全狀態操作需要確認 dialog。

### 驗收關聯

AC-43、50、53、87、97。

---

## S-14 更多

### 目的

容納非高頻但必要的說明與設定，不與核心提醒競爭。

### P0 項目

- Q&A 總覽（`/help`）。
- 防曬晴報員如何運作。
- 倒數如何運作。
- `我有特殊狀況`。
- 安裝到手機。
- 顯示設定。
- 登入／帳號：只顯示尚未提供或 P1 說明，不建立假入口。
- 隱私與個資告知。
- 本機資料管理。
- 服務限制與資料來源。

P0 不顯示空白的完整 UV知識庫入口。

### 帳號的 P0 定位（2026-08-06 裁決）

使用者問卷顯示兩種需求並存：**有人不想註冊直接使用，有人想註冊保留防曬產品資訊。**

P0 的處理方式是**不做登入**，改以三件事回應「想保留資料」的需求：

1. PWA 安裝——讓資料留在裝置上且入口穩定。
2. 本機資料管理（S-19）——可查看與清除。
3. 誠實告知——明說資料只在這台裝置，清除瀏覽器資料會一併消失。

帳號體系維持在 P1。S-14 的帳號項目只做說明，不建立假入口，
也不得暗示「註冊後就能跨裝置同步」這種 P0 做不到的事。

**2026-08-07 追加裁決：本機匯出納入 P0。** 讓不想註冊的使用者也有備份手段，
入口在 S-19，不新增畫面。詳見 S-19「本機匯出」。

### 驗收關聯

AC-46、57、58、60、63。

---

## S-15 Q&A 總覽與海邊防曬

### 目的

提供經審查的最低衛教內容，不修改提醒狀態。

**2026-08-06 裁決：本項由單一主題頁擴為「總覽＋主題」兩層，且只做 App 內。**

### 範圍界線

Q&A **只做 App 內**，不做公開索引、SEO 或 GEO。理由：
SPA 要做 SEO 需要加 SSG／預渲染，是架構層級的變更；
而 `P0_RELEASE_MANIFEST.md` §8 已明確排除公開 SEO／AEO 與公開文章。
公開內容留在 P0.5，屆時另立 ADR。

### 結構

| Route | 內容 | P0 狀態 |
| --- | --- | --- |
| `/help` | Q&A 總覽，列出可用主題 | 新增 |
| `/help/beach` | 海邊防曬（`FAQ_BEACH_SUN_V1`） | 既有 |
| `/help/how-it-works` | 運作與倒數說明（S-16） | 既有 |
| 其他主題 | **待定**，不在 P0 交付 | — |

總覽只列出**已通過審查且可發布**的主題。未核准的主題不佔位、不顯示灰階項目，
避免使用者以為內容即將出現。

每則 Q&A 都需要 Copy Deck 審查狀態（`CONTENT_REVIEW`／`MEDICAL_REVIEW`／`MARINE_REVIEW`），
規則與既有內容一致。

### 進入來源

- 更多（總覽）。
- 準備下水。
- 已在水中。
- 耐水標示未知。
- 水上事件流程。

後四項直接進 `/help/beach`，不強迫先經過總覽。

### 必要行為

- 顯示 `FAQ_BEACH_SUN_V1` 已核准版本。
- 提供資料來源及審查資訊。
- 返回原操作位置並保留 SetupDraft／事件草稿；產品頁偵測到未完成 SetupDraft 時顯示返回設定橫幅。
- 開啟、閱讀、關閉均不得建立或延長期限。

### 不可發布狀態

- `reviewed_at=null`
- `next_review_at=null`
- 任一必要醫療／海洋／法務審查未通過
- 內容過期且無核准替代版本

### 驗收關聯

AC-46、47、48、49、61、63。

---

## S-16 運作與倒數說明

### 目的

說明 CWA 資料、逐部位提醒、絕對時間戳、PWA 及通知限制。

### 必須涵蓋

- 地區可略過。
- 觀測與區域預報的差異。
- UVI 不是個人曬傷倒數。
- 期限是檢查／補擦提醒。
- 不同部位可有不同期限。
- 關閉頁面後不執行背景視覺倒數。
- P0 不保證關閉頁面後通知。
- 重新開啟後依時間戳恢復。

內容必須可從提醒頁及更多開啟，返回時不改變 Session。

### 驗收關聯

AC-05、09、12、31、46、61、63、66。

---

## S-17 特殊狀況

### 目的

對醫療或急症問題停止一般提醒推論，提供經核准的功能邊界與下一步。

### 第一層選項

只能使用經醫療／法務審查的白話分類，不要求自由文字。至少涵蓋：

- 已有醫師避光指示、用藥或療程。
- 想詢問能否曝曬或使用特定產品。
- 皮膚破損、起泡或嚴重曬傷相關情境。
- 可能全身性或熱疾病的急症警示情境。

### 結果

| 結果類型 | 主要動作 |
| --- | --- |
| 一般醫療邊界 | 依醫囑或詢問醫師／原療程院所 |
| 產品個別適用問題 | 依產品標示並詢問藥師／醫療專業人員 |
| 急症 red flag | 經審查的 119／緊急就醫指引 |

### 禁止事項

- 不蒐集疾病、病理、處方、病史、症狀自由文字或照片。
- 不判定疾病、過敏、治療或可否曝曬。
- 急症主要行動不得被一般免責聲明延後。
- 一次性特殊狀況不得送入第三方分析或 SetupDraft。

### 驗收關聯

AC-20、56、58、99。

---

## S-18 顯示設定

### 目的

讓使用者選擇可讀的顯示模式。

### 選項

- 日間高對比。
- 跟隨手機。
- 夜間模式。

### 規則

- 首次使用預設日間高對比。
- 選擇後立即預覽並保存於本機。
- 不依賴 Ambient Light Sensor。
- 夜間模式不能用簡單反相產生。
- 所有模式通過文字、非文字對比及狀態非顏色驗證。

### 狀態

- 偏好寫入失敗：恢復前一個已保存值並顯示未保存。
- 系統主題變更：只有選擇跟隨手機時自動反映。
- Reduced motion：獨立遵守，不因顯示模式關閉。

### 驗收關聯

AC-38、67、68、69、70、71。

---

## S-19 本機資料管理

### 目的

讓 Guest 查看資料範圍、匯出備份並清除目前裝置資料。

### 必須顯示

- 目前裝置保存的產品數量。
- 是否有 active Session。
- 已結束 Session 歷史概況。
- SetupDraft 是否存在。
- 最後氣象快照與時鐘校準資料。
- 清除資料對提醒、產品及 PWA 使用的影響。

### 操作

- 查看本機資料說明。
- **匯出本機資料**（2026-08-07 裁決納入 P0）。
- 清除草稿。
- 結束目前提醒。
- 清除產品與歷史。
- 清除全部本機資料。

### 本機匯出（2026-08-07 裁決）

P0 不做帳號，資料只在這台裝置上。使用者問卷同時存在「不想註冊直接使用」與
「想保留防曬產品資訊」兩種需求；匯出是在不引入帳號體系的前提下，
唯一能讓前者也有備份手段的做法。

| 項目 | 規格 |
| --- | --- |
| 觸發 | 使用者主動點擊，不自動執行 |
| 內容 | 本機產品、Session、事件與偏好 |
| 排除 | 金鑰、精確座標、裝置識別碼 |
| 傳輸 | 不上傳、不經後端、不進分析 |
| 格式 | 單一檔案，格式版本化且可讀 |
| 匯入 | **不在 P0 範圍**；恢復能力留待 P1 與帳號遷移政策一併設計 |

規則：

- 匯出入口與清除入口同置本頁；**清除前必須提示可先匯出**。
- 不得描述為「備份到雲端」或暗示跨裝置同步。
- 匯出失敗時不得顯示成功，也不得因匯出失敗阻擋清除。

### 規則

- active Session 不可用一般刪除直接消失；先明確結束或在「清除全部」確認中說明。
- 清除全部需要二次確認及明確範圍。
- 成功後重新建立乾淨的必要 App metadata。
- 失敗時不得顯示已清除。
- 不把 P1 帳號同步或匯入描述為 P0 已提供；P0 只有匯出。

### 驗收關聯

AC-12、19、22、39、55、100。

---

## S-20 安裝到手機

### 目的

依實際平台能力提供安裝方式，不把安裝當成使用 P0 的條件。

### 平台狀態

| 狀態 | 顯示 |
| --- | --- |
| Android／Chrome 可觸發安裝 | 主要 CTA `安裝到手機` |
| Android 無 install prompt | 顯示瀏覽器選單教學 |
| iPhone／Safari | 顯示分享 → 加入主畫面 → 新增 |
| 已安裝 | 顯示已可由主畫面開啟 |
| 不支援／無法判定 | 顯示一般瀏覽器仍可使用 |
| Desktop | 顯示瀏覽器支援時的安裝方式或略過 |

### 必須說明

- 不安裝仍可使用 P0。
- P0 不因安裝而取得關閉頁面後保證通知。
- 清除網站資料或解除安裝可能移除 Guest 本機資料。
- 選單名稱可能因裝置與瀏覽器不同。

### 驗收關聯

AC-12、31、32、54、59。

---

## 6. 跨頁面狀態矩陣

### 6.1 網路與資料狀態

| 狀態 | 首頁 | 設定流程 | 提醒頁 | 防曬品 | 說明／設定 |
| --- | --- | --- | --- | --- | --- |
| Online | 取得最新可用 UVI | 正常；適用時校準時間 | 正常 | 正常 | 正常 |
| Offline | 顯示最後資料及時間 | 可建立本機 Session；不依賴 UVI | 依本機狀態重算 | 可用本機資料 | 已快取內容可用 |
| CWA failure | 一般建議＋重試 | 不阻擋 | 不修改期限 | 無影響 | 顯示資料限制 |
| Time API failure | 不影響 UVI | 無有效校準時限制延長期限 | 顯示 CLOCK_UNTRUSTED | 無影響 | 可查看說明 |
| Cached UVI stale | 明確標示 stale | 不改變提醒規則 | 不改變期限 | 無影響 | 可查看來源 |
| Cached UVI unusable | 不顯示為目前 UVI | 不阻擋 | 不改變期限 | 無影響 | 可顯示歷史說明 |

### 6.2 權限狀態

| 權限／能力 | 未詢問 | 允許 | 拒絕 | 不支援 |
| --- | --- | --- | --- | --- |
| 定位 | 只顯示用途與選項 | 本次選站 | 手動地區／略過 | 手動地區／略過 |
| 系統通知 | P0 不詢問 | P0 不使用 | 不影響 P0 | 不影響 P0 |
| 相機 | P0 不詢問 | P0 不使用 | 不影響 P0 | 不影響 P0 |
| Audio | 預設關閉 | 使用者測試後可嘗試短提示 | 視覺提醒 | 視覺提醒 |
| Vibration | 預設關閉 | 支援且測試後可嘗試 | 視覺提醒 | 視覺提醒 |
| IndexedDB | feature detect | 正常保存 | 顯示無法保存 | 不宣稱資料已保存 |

### 6.3 Session 與 route 行為

| Session 狀態 | 首頁 CTA | 提醒頁 | 開始設定 route | 事件／補擦 route |
| --- | --- | --- | --- | --- |
| 無 Session | 開始防曬提醒 | 空白狀態 | 允許 | 不允許 |
| DRAFT only | 開始／繼續設定 | 空白狀態＋可恢復提示 | 可恢復或重開 | 不允許 |
| TRACKING | 依 `primaryAction.actionKind` 顯示操作 | Active UI | 阻止第二個 Session | 允許 |
| ATTENTION_REQUIRED | 依 `primaryAction.actionKind` 顯示需處理操作 | Active UI，需處理優先 | 阻止第二個 Session | 允許 |
| ENDED | 開始防曬提醒 | 空白狀態 | 允許新草稿 | 舊 route 只顯示已結束 |

### 6.4 表單離開與草稿

| 情境 | 行為 |
| --- | --- |
| 返回上一步 | 保留目前 SetupDraft 允許欄位 |
| 關閉分頁／重新開啟 | 24 小時內可提示恢復 |
| 明確取消 | 刪除 SetupDraft，不建立 Session |
| 草稿到期 | 刪除，不當成防護紀錄 |
| 開啟 Q&A | 保留原位置與草稿，返回後繼續 |
| 儲存草稿失敗 | 保留記憶體內輸入；明示未保存 |
| 已輸入塗抹時間但尚未完成產品設定 | 寫入 `pendingTiming`，不建立 Application 或 Session |
| Guest 清除資料 | 草稿與本機資料依確認範圍刪除 |

### 6.5 提交結果

| 結果 | UI 行為 |
| --- | --- |
| 成功 | 顯示具體更新摘要及更正入口 |
| 驗證錯誤 | 就近標示欄位並聚焦第一個問題 |
| Revision conflict | 取得最新狀態，顯示差異並要求重新確認 |
| Duplicate submission | 以 idempotency 結果顯示同一成功狀態 |
| Transaction abort | 不改變 UI 真值；顯示未保存並允許重試 |
| Storage quota | 顯示儲存空間問題與資料管理入口 |
| Context updated elsewhere | 重讀最新 revision，避免舊表單覆蓋 |
| Session ended elsewhere | 顯示已結束，不重開或提交舊事件 |

---

## 7. Loading、Empty、Error 規格

### 7.1 Loading

- 首次頁面不得只顯示空白畫面。
- UVI loading 不得阻擋 `開始防曬提醒`。
- 本機 Session 恢復期間不得先短暫顯示錯誤的「沒有提醒」。
- 提交 loading 必須防止重複 mutation。
- 超過合理時間後提供重試或替代流程。

### 7.2 Empty

空白狀態必須包含：

1. 現在沒有什麼。
2. 為什麼可能沒有。
3. 一個可執行的下一步。

禁止自動導向其他底部分頁來掩蓋空白狀態。

### 7.3 Error

- 欄位錯誤放在欄位附近。
- 跨頁錯誤使用持續可見 Banner 或區塊。
- 錯誤文案說明影響及下一步，不顯示內部代碼、堆疊或 token。
- 無法安全重試時，先保留本機輸入並說明。
- 健康或急症內容錯誤時，不得以一般免責替代最低必要分流。

---

## 8. 無障礙狀態矩陣

| 項目 | P0 要求 |
| --- | --- |
| 焦點 | route 轉換後移到頁面標題；錯誤提交後移到錯誤摘要或第一個錯誤欄位 |
| 提醒環 | 有可存取名稱、目前值及 `aria-valuetext` 等效語意 |
| Live region | 只在即將到期、到期、事件變更或使用者主動聚焦時更新 |
| 顏色 | 每個狀態都有文字、圖示與邊框；不以綠色表示安全 |
| Reduced motion | 停用旋轉、脈衝、閃爍及連續填色動畫 |
| 表單 | label、說明、錯誤與控制項以程式化關聯 |
| Dialog | 鎖定焦點、可取消、關閉後回到觸發控制項 |
| Bottom nav | 有當前頁語意，圖示搭配文字 |
| 文字放大 | 200% 下不截斷主要狀態、時間、錯誤或提交 |
| 鍵盤 | 所有操作可完成；人體圖不是 P0 必要或唯一輸入 |

---

## 9. 狀態文案占位需求

後續 Copy Deck 至少需要為下列狀態提供核准文字：

1. 無所在地 UVI。
2. fresh／stale／forecast／unusable／offline UVI。
3. 定位前告知、拒絕、逾時及不支援。
4. Session 空白、草稿恢復及 active Session 衝突。
5. TRACKING、REAPPLY_SOON、REAPPLY_DUE。
6. 尚未記錄、方法未知及非防曬產品。
7. 衣物覆蓋、衣物移開及遮蔭環境。
8. LABEL_WAIT。
9. WATER_START_UNKNOWN。
10. 洗手、離水、流汗、擦拭及摩擦原因。
11. 產品過期、異常及使用後不適。
12. CLOCK_UNTRUSTED 線上與離線。
13. 儲存失敗、quota、transaction abort 及多 context 衝突。
14. 成功補擦、局部補擦、事件回報及更正摘要。
15. Session 結束與舊 route。
16. 急症分流、醫療拒答及依醫囑提示。
17. 安裝、不支援及 P0 通知限制。
18. 本機資料刪除與失敗。

---

## 10. Prototype 必測任務

1. 第一次使用，暫不提供地區，完成快速提醒。
2. 拒絕定位後手動選地區。
3. 接受通勤建議組合後移除耳朵。
4. 全部部位以衣物覆蓋建立 Session，不輸入時間。
5. 使用 Session-only 防曬產品建立一般戶外提醒。
6. 產品身分看不清楚時理解為沒有數字期限。
7. 已在水中但不知道入水時間。
8. 回報洗手並只處理手背。
9. 局部補擦手背，確認臉部期限不變。
10. 回報擦毛巾並修正受影響部位。
11. 回報產品使用後不適並改用其他防護。
12. 重新開啟 App 後理解恢復的提醒狀態。
13. CLOCK_UNTRUSTED 時理解為何不能正常延長。
14. IndexedDB 寫入失敗時理解資料尚未保存。
15. 結束目前提醒，再建立新的 Session。
16. 在流程中閱讀海邊 Q&A 後返回且草稿保留。
17. 在 360 CSS px、鍵盤開啟及文字放大 200% 下完成設定。

---

## 11. 畫面完成定義

一個畫面只有在以下條件皆成立時才可標記完成：

1. 正常流程、返回、取消與目的地明確。
2. Loading、Empty、Error、Offline 狀態齊全。
3. 權限拒絕與不支援有替代流程。
4. IndexedDB／API 失敗不顯示假成功。
5. 與 active Session、草稿及多 context 的行為明確。
6. 主要 CTA 依狀態正確變化。
7. 沒有可信期限時不顯示時間型提醒。
8. 360、390、430 CSS px 及鍵盤安全區通過。
9. 鍵盤、螢幕閱讀器、文字放大與 reduced motion 通過。
10. 相關文案已進入 Copy Deck，需審查者已標記。
11. 相關 PRD AC 已對應至測試案例。
12. 不納入的 P1／P2 能力沒有以假按鈕或誤導文案出現。

---

## 12. 下一步

完成本 Screen Inventory 後，下一份應建立：

1. `P0_REMINDER_RULE_DECISION_TABLE.md`
2. `P0_COPY_DECK.md`
3. `P0_REQUIREMENT_TRACEABILITY_MATRIX.md`

其中 Reminder Rule Decision Table 應先於正式 reducer 實作，避免產品文案、畫面狀態、前端本機計算與未來 P1 後端產生不同結果。
