# UVAlert 圖示系統

**狀態**：核心組已產出，等 wireframe 定案後補齊其餘分組
**產生器**：[`tools/icon-system/generate-icons.mjs`](../../../tools/icon-system/generate-icons.mjs)
**預覽**：[`icon-system-preview.svg`](icon-system-preview.svg)

舊的 `ICON_DESIGN_SYSTEM.md` 已隨舊設計系統封存到 `docs/archive/2026-08-pre-redesign/visual-design/`，不再作為依據。本文件取代它。

## 一、造型語言

圖示的視覺 DNA 來自 06 播報印記 Logo：**實心圓點＋膠囊狀線條**。

- 畫布 24×24，四周保留約 2px 光學留白。
- 線條寬度固定 `2.5`，端點與轉角一律 `round`——渲染出來就是膠囊形，呼應 Logo 的射線。
- 需要強調的元素用實心填色，不用加粗線條來製造層級。
- 不使用尖角、不使用陰影、不使用漸層。

## 二、四項已確認決策

| 決策 | 結論 |
|---|---|
| **圖示色彩** | 混合制：導覽與更多頁卡片用雙色（墨咖結構＋琥珀金 `#C1832E` 重點）；狀態類一律單色 `currentColor` |
| **身體部位** | 衛教模組畫 7 個抽象部位示意；提醒頁的 16 個追蹤部位用文字標籤＋狀態圖示，不畫部位圖示 |
| **UV 五級** | 漸進填滿格數（1–5 格），形狀一致只有填滿量不同 |
| **產出時機** | 先做規格已鎖定的核心組，其餘等 wireframe 定案 |

### 為什麼狀態類不用琥珀金

倒數狀態在 `packages/ui/src/styles.css` 裡各有語意色（`--color-tracking` 藍、`--color-soon` 琥珀、`--color-due` 紅）。狀態圖示若寫死琥珀金，會與語意色衝突且深色模式要另做一份。單色 `currentColor` 讓圖示自動繼承外層語意色，一份幾何走遍所有狀態。

## 三、不依賴顏色的區分方式

`docs/design/current-direction.md` 明訂「狀態必須同時有色彩、圖示與文字」。因此：

- **倒數／部位四狀態**用剩餘量計量表：追蹤中（三格）→ 即將到期（一格）→ 已到期（空）→ 未計時（空＋斜線）。格數承載意義，灰階與色盲情況下仍可區分。語意刻意採用電量計的日常直覺——**滿＝時間還多、空＝該補擦**，不需要學習。
- **斜線一律代表「停用／不適用」**，目前用在「未計時」與「通知未開啟」。
- **UV 五級**用相同邏輯的格數填滿，與倒數狀態共用「填滿量＝強度」的心智模型。

## 四、核心組（20 個，已完成）

### 下排導覽（雙色）
`nav-reminder` 提醒／`nav-gear` 裝備／`nav-more` 更多

### 狀態（單色）
`state-tracking` 追蹤中／`state-soon` 即將到期／`state-due` 已到期／`state-untimed` 未計時／
`state-notification-off` 通知未開啟／`state-notification-pending` 背景通知尚未完成／
`state-offline` 目前離線／`state-online` 背景通知已恢復／
`state-success` 已儲存／`state-warning` 警告／`state-unverified` 標示尚未確認

### 更多頁卡片（雙色）
`more-notifications` 通知設定／`more-education` 防曬衛教／`more-data` 本機資料與隱私／
`more-feedback` 問題回報／`more-install` 安裝到主畫面／`more-about` 說明與關於

## 五、待補（等 wireframe 定案）

- 裝備 6 品類：防曬乳、防曬衣物、太陽眼鏡、帽子、陽傘、其他裝備
- 衛教 6 分類，以及三步流程用的小圖示（規格 4.6 明訂）
- 設定流程 6 種情境：室內離窗、室內近窗、一般戶外、戶外運動、預備下水、水中活動
- 事件回報 4 類：大量流汗、擦拭、摩擦、洗手
- 衛教 7 部位抽象示意
- UV 五級格數圖示
- 工具型：返回、關閉、展開收合、新增、編輯、刪除、分享、搜尋、收藏、重新整理

## 六、使用規則

- 顏色用 `currentColor`（單色）或既有 token，不在元件內寫死 hex。
- 圖示必須承擔資訊；純裝飾的圖示直接不放。
- 尺寸從 16／20／24 三個檔位選，不要再發明新數值（舊系統散成 12 種值是明確的反面教材）。
- 動畫只用 `opacity`，不用位移或縮放。

## 七、重新產生

```bash
node tools/icon-system/generate-icons.mjs
```

## 八、造型概念的取捨紀錄

這幾個決定是實際畫出來、看過之後才收斂的，記下來避免之後重蹈覆轍。

### 倒數四狀態：剩餘量計量表

| 試過的概念 | 為什麼不用 |
|---|---|
| 圓環＋圓餅切片 | 讀起來像圖表，不像時間 |
| 圓形水位填滿 | 兩種中間狀態在 16px 幾乎一樣；且「越滿＝越該補擦」與電量計的日常直覺相反 |
| 沙漏 | 概念最具體，但沙子多寡的差異在 18px 看不出來，四個狀態長得像同一個圖 |
| 環形進度弧線 | 辨識度不錯，但「即將到期」的短弧看起來像斷掉的碎片 |
| **剩餘量計量表**（採用） | 四狀態在 18px 差異最大，且滿／空的語意不需要學習 |

### 通知類：鈴鐺

| 試過的概念 | 為什麼不用 |
|---|---|
| 放射弧線 | 那是 WiFi／訊號的既有語彙，會被直接誤讀成連線狀態 |
| 角落徽章圓點 | 圓點與方框邊線交疊產生亂線，構圖偏向對角，看起來歪斜 |
| 卡片＋未讀圓點 | 構圖是正的，但太抽象，讀不出「通知」 |
| **鈴鐺**（採用） | 最直覺的通知符號 |

Logo 規格的「不使用警報鈴」寫在**第六節「禁止方向」**，約束對象是品牌標記，不是 UI 狀態圖示（同一份清單也禁止驚嘆號，但警告圖示仍需要它）。三種狀態靠鈴舌區分：

| 狀態 | 造型 |
|---|---|
| 通知設定（已啟用） | 完整鈴鐺＋琥珀金鈴舌 |
| 背景通知尚未完成 | 鈴口斷開一段＋鈴舌 |
| 通知未開啟 | 完整鈴鐺＋鈴舌＋斜線 |

鈴舌的兩次失敗嘗試：**空心鈴舌**——線寬 2.5 畫 r=1.55 的圓，外徑會變成 2.8（實心點只有 1.8），中間的洞只剩 0.3，看起來就是一顆更大的實心點；**不畫鈴舌**——大小不一的問題解決了，但變得跟「通知設定」的完整鈴鐺太接近。最後改由鈴口的缺口承載「尚未完成」，鈴舌一律相同。缺口是用兩段獨立線條畫的，不是虛線，位置完全可控。

結論：**2.5 線寬做不出小尺寸的圓環**，需要「空心」語意時要換其他方式。

### 警告：圓角三角形＋驚嘆號

曾考慮換成圓形，但三角形本身承載「警示」語意，正好滿足「狀態不能只靠顏色判斷」的要求；換成圓形後輪廓會與「已儲存」「標示尚未確認」完全相同，只剩顏色可區分。

驚嘆號貼近三角形邊緣的問題，是靠**圓角三角形**解決的：把尖角切掉之後內部可用寬度變大，驚嘆號左右的間隙從約 0.6 增加到約 1.5。另一個候選方案是「只留一個實心圓點」，雖然最乾淨，但那是靠拿掉辨識度最高的元素來迴避擁擠，警示強度會弱一階。

圓角同時讓它更融入整體——其餘 19 個圖示全是圓端點與圓轉角，三個尖角的三角形原本才是異類。

### 更多：2×2 網格

原本是水平三點，但量測是 17.5×4.5，與另外兩個導覽圖示（約 13×19）差距太大，放在一起不像同一組。改為 2×2 網格後是 14.1×14.1，量體相當；語意上也剛好對應「更多」頁本身就是卡片網格。

### 提醒：沙漏

原本用「圓環＋中心實心點」，但那與狀態類的圓環家族太接近，也不具體指向這一頁的內容。改用沙漏：直接對應提醒頁的核心（補擦倒數），且與裝備（瓶身）、更多（三點）在輪廓上完全不同。

### 琥珀金的兩條使用規則

1. **琥珀金一律出現在上半部**：瓶蓋、2×2 網格的右上點、沙漏上層的沙、資料庫中間帶、下載箭頭、資訊點都在上半部。唯一的例外是鈴舌，因為鈴舌天生就在鈴鐺底部。
2. **琥珀金是重點，不是結構**：衛教是墨咖書本＋琥珀金書脊；資料庫是墨咖圓柱＋琥珀金中間帶。早期版本讓資料庫的頂部橢圓直接是琥珀金，等於讓重點色承擔結構，量體明顯比其他圖示重。
3. **琥珀金畫在下層**：先畫琥珀金、再畫墨咖結構，讓重點色在交會處收進結構底下，而不是蓋在上面截斷它。沙漏的沙、書脊、資料庫中間帶都照這個順序。

### 其他修正

- 裝備原為雙層方塊，讀起來像電池，改為瓶身加琥珀金瓶蓋。
- 本機資料原為實心琥珀金橢圓頂，視覺過重像杯子，改為輪廓式資料庫圓柱。
- 防曬衛教原為方框加橫線，與一般表單無法區分，改為翻開的書。
- 安裝到主畫面原為手機加箭頭，箭頭被壓縮，改為下載箭頭進托盤。
- 斜線只有一個定義（45 度、以 12,12 為中心、長度與線寬固定），用在未計時、通知未開啟、目前離線三處。早期版本各寫各的，角度與長度都不同。
- 安裝到主畫面的箭桿原本末端超過箭頭尖端：箭桿的圓端點會往外延伸 1.25，比尖端還低，看起來就是沒對齊。箭桿必須縮短到讓圓端點收在尖端內。
- 防曬衛教的書本左右其實是對稱的，歪的是上下——外緣佔 5.5–17.4、書脊卻是 7.6–19.5，整個往下錯開 2.1，視覺上像被剪切。落差縮到 1.4 後恢復平衡。
- 沙漏上層的沙原本頂端停在 5.4，但上緣橫桿的內側在 5.15，中間留下一道白邊看起來像沒填滿。填色形狀必須略微伸進線條底下（現為 5.1），不能只對齊到內側邊界。
- 兩個鈴鐺量測寬度其實相同，但「尚未完成」的鈴口缺口原本寬達 6.2，吃掉三分之一的鈴口，讓它看起來比較窄。缺口縮到 3.2 之後兩者視覺寬度一致。
- 「目前離線」的斜線原本套用系統共用的置中版本，但雲朵本身偏低、偏右，不是以 (12,12) 對稱的形狀，導致斜線右上端伸出雲朵邊緣的量明顯比左下端多。這個圖示改用專屬的斜線（左下端再往外推 1.4），兩端伸出的量才一致。共用的 `slash()` 定義本身沒有問題，只是不能無腦套用在非對稱造型上——斜線該伸多長要看它疊加的形狀，不是固定值。
- 標示尚未確認的問號原本起筆位置偏軸，字符在圓環內傾斜，已改成以 x=12 為軸的完整 270 度弧。
- 背景通知已恢復原本是雲朵內加圓點，圓點與雲朵本身偏右的量體互相打架，已改為與「目前離線」共用同一朵雲，只差斜線。

## 九、兩個渲染陷阱

實作時踩過、值得記下來：

**虛線已完全從系統移除**，因為它同時有三個問題：

1. **圓端點會吃掉間隙**。`stroke-linecap="round"` 讓每段虛線兩端各延伸線寬的一半（1.25），間隙 2.6 會被完全填滿，虛線渲染成實線。
2. **加大間隙又會斷成一顆顆**，看不出是同一條線。
3. **封閉曲線上的虛線會讓造型歪斜**。虛線相位決定斷點落在哪，封閉路徑上無法控制，量測顯示會造成單邊墨跡缺口（實測 x 偏移 0.95）。

需要表達「不啟用／未完成」時，改用斜線（可控、對稱）或明確畫出的缺口（如鈴口斷開，用兩段獨立線條而非虛線）。

另外，**外框置中不等於視覺置中**。20 個圖示的外框中心都準確落在 (12,12)，但三角形、書本、對話框這類量體偏底部的造型仍會看起來下沉，需要靠 `nudgeY` 校正。校正值來自把 SVG 點陣化後量測實際墨跡邊界，不是目測。
