# 「本機資料管理」與「跨裝置同步」合併回一張卡

**日期**：2026-08-29（Asia/Taipei）
**狀態**：**已裁決並已實作完成（2026-08-29）**——採用合併；標籤定為「本機資料與隱私」；說明文字見第九節；實作與量測見第十節。
**用途**：「更多」頁有一組卡片可以合併，而且合併不是新提案——是回到 `DESIGN.md` 與 Sitemap 本來就訂好的設計。
**範圍**：只動「更多」頁的入口卡與 `/settings/*` 的頁面組織，不改任何同步或本機資料的行為、契約與資料模型。
**權威性**：低於 `DESIGN.md` 與 `2026-08-15-redesign-sitemap-userflow-current.md`。本文件指出實作與它們的落差。

## 一、落差：文件訂 6 張卡，程式碼拆成 8 張

`DESIGN.md` 第五節「更多」訂的排序：

> 通知設定 → 防曬衛教 → **本機資料與隱私** → 問題回報與意見回饋 → 安裝到主畫面 → 說明與關於

Sitemap §2.4 講得更具體：

> 通知設定與本機資料與隱私都採**單頁分區**；**本機備份放第一層，Google 同步放次要區塊**。

但實作把「本機資料與隱私」拆成了兩張卡、兩個頁面，底下還掛第三頁：

```
更多 → 本機資料管理  /settings/data          （328 行）
更多 → 跨裝置同步    /settings/sync          （218 行）
            └→ 登入與雲端資料 /settings/account-data （175 行）
```

Sitemap §2.4 已經記過「實作是八張卡不是六張」這個落差，但當時只是**記錄**，沒有裁決要不要收回去。

## 二、這個拆分有具體成本

`DataSettingsPage.vue` 上有一張卡（`cloud-data-link-title`）是**純粹為了處理拆分而存在的**：

> **雲端資料請到另一頁管理**
> 本頁只處理這台裝置的本機資料。若你有登入並使用同步，請到登入與雲端資料頁管理。
> 〔管理登入與雲端資料〕

它沒有任何功能，全部的工作就是解釋「為什麼你要找的東西不在這裡」。**如果兩頁是一頁，它不需要存在。** 這是拆分製造出來的問題，然後又花了一整張卡的版面去補救。

## 三、對「更多」頁清爽度的實際效果

「更多」頁目前**實際只渲染 6 張卡**——`/help` 與 `/special-situation` 被 `MorePage.vue` 的 publishable gate 過濾掉了（兩者內容都還沒上線）。

| 做法 | 版面節省 |
| --- | --- |
| 合併掉一張卡 | **一整張卡 82px ＋ 間距** |
| 刪掉 B9 分類表裡唯一能刪的那段說明 | 約 24px |

**合併一張卡的效果，大於把所有可刪的說明文字都刪掉。** 這也是為什麼這件事應該排在 B9 之前——否則 B9 才剛把兩張卡的圖示放大、文字調好，合併時全部重來。

## 四、建議

**把「跨裝置同步」併進「本機資料與隱私」，`/settings/account-data` 維持巢狀。**

合併後的分區，直接對應 Sitemap 的「本機備份放第一層，Google 同步放次要區塊」：

| 區塊 | 內容 | 來源 |
| --- | --- | --- |
| 這台裝置儲存了什麼 | 資料概況 | `DataSettingsPage` |
| 匯出 | 本機備份 | `DataSettingsPage` |
| 清除 | 三種清除範圍 | `DataSettingsPage` |
| ─── 次要區塊 ─── | | |
| 跨裝置同步 | 登入模式、同步狀態、內容預覽 | `SyncSettingsPage` |
| 登入與雲端資料 | 仍是獨立頁，從同步區塊進入 | `AccountDataPage`（不動） |

`account-data` 保持獨立頁是**照 Sitemap 原本就訂的**——§2.4：「另有 `/settings/account-data`（登入與雲端資料），**從『跨裝置同步』頁進入，不是獨立入口卡**」。合併後它改從同步區塊進入，層級不變。

### 為什麼不三頁全併

三頁合計 721 行。全併會做出一個很長的頁面，而 `account-data` 的內容（登出、清除雲端資料）是**不可逆操作**，把它和本機資料的清除操作放在同一頁，兩組「清除」會互相混淆——這正是 `DESIGN.md` 第六節「每頁只保留一個最主要任務」要避免的。

## 五、另一組：現在不要動

**常見問題（`/help`）＋ 特殊狀況（`/special-situation`）** 在原本的 6 張清單裡對應「說明與關於」一張，看起來可以合併。但：

- **兩張現在根本沒顯示**——被 publishable gate 過濾
- `/help` 的兩則主題 2026-08-23 已核准，但 `HelpTopicPage.vue` 還沒接內容（只有 `v-if="!publishable"`，沒有 `v-else`）
- `/special-situation` 的文案是 `BLOCKED`，等台灣醫療與法務核准

**現在合併等於在對不存在的東西做資訊架構。** 等內容真的要上線時再一起決定。

## 六、不建議動的四張

通知設定、防曬衛教、安裝到手機桌面、問題回報——各自是獨立的關注點，沒有可合併的對象。

## 七、影響範圍

| 檔案 | 改動 |
| --- | --- |
| `MorePage.vue` | 移除「跨裝置同步」卡；「本機資料管理」改名為「本機資料與隱私」 |
| `DataSettingsPage.vue` | 移除 `cloud-data-link` 卡；吸收同步區塊 |
| `SyncSettingsPage.vue` | 刪除 |
| `AccountDataPage.vue` | 返回連結 `/settings/sync` → `/settings/data` |
| `router/index.ts` | 移除 `/settings/sync`，**不留轉址**（沿用 2026-08-08、2026-08-24 移除路由的既有處理方式） |
| `SyncSettingsPage.test.ts` | 斷言移進 `DataSettingsPage` 的測試 |
| `DESIGN.md` 第五節、Sitemap §2.4 | 回寫成合併後的實際結構 |

## 八、與 B9 的關係

**這件事應該排在 B9 第一輪之前。** B9 的說明文字分類表（`2026-08-29-b9-icon-first-more-page.md`）目前把「本機資料管理」與「跨裝置同步」列為兩列；合併後要改成一列，而且合併後那張卡的說明文字要重新定。

先做 B9 再合併的話，B9 的圖示放大與文字調整會有一半白做。

## 九、裁決結果（2026-08-29，使用者裁決）

| # | 裁決點 | 結果 |
| --- | --- | --- |
| 1 | 是否採用合併 | **採用**。「跨裝置同步」併進「本機資料與隱私」，`/settings/account-data` 維持巢狀獨立頁 |
| 2 | 合併後的標籤 | **「本機資料與隱私」**——採 `DESIGN.md` 第五節與 Sitemap §2.4 的用詞，程式碼跟著文件改，不是文件跟著程式碼改 |
| 3 | 合併後的說明文字 | 見下方 |

### 裁決 3：合併後那張卡的說明文字

```
本機資料與隱私
資料留在這台裝置；要跨裝置同步才需登入，同步前會先讓你確認內容。
```

**為什麼是這句**——它把兩段舊文字裡真正不可省的部分接成一句，而不是把兩段黏在一起：

| 分句 | 承接自 | 為什麼保留 |
| --- | --- | --- |
| 資料留在這台裝置 | 舊「本機資料管理」 | 這是本機優先的隱私承諾，也是新標籤裡「隱私」二字的具體內容。原文的「查看、匯出與清除」被 B9 分類表判為「進去就看得到」的重複說明，**刪除** |
| 要跨裝置同步才需登入 | 舊「跨裝置同步」的「選擇性登入」 | B9 §3 的決策條件，且免登入是產品核心承諾。改成因果句而非「選擇性登入」，是因為合併後標籤不再出現「同步」，這句同時要負責讓找同步的人知道入口在這張卡 |
| 同步前會先讓你確認內容 | 舊「跨裝置同步」 | **逐字保留**。使用者採取動作前必須知道的條件（`DESIGN.md` 第五節不可隱藏清單） |

**與「不新增文案」約束的關係**：B9 計畫禁止新增文案。這句是合併**必然**觸發的例外——兩張卡變一張，不可能兩段都留。本裁決即為該例外的核准依據；B9 第一輪實作時直接採用，不再自行改寫。

**連帶效果（給 B9 Task 1）**：合併後「跨裝置同步」的單色 `state-online` 圖示隨卡片一起消失，合併卡沿用雙色 `more-data`。B9 Task 1 那個「兩張單色卡怎麼處理」的裁決點因此**只剩「特殊狀況」`state-warning` 一張**——而它目前被 publishable gate 擋住不會渲染，可以留到內容上線時再決定。

### 回寫落點

| 檔案 | 改動 |
| --- | --- |
| `MorePage.vue` | 移除「跨裝置同步」卡；「本機資料管理」→「本機資料與隱私」，`description` 換成上面那句 |
| `docs/superpowers/plans/2026-08-29-b9-icon-first-more-page.md` | 分類表八列 → 七列；合併卡標為「必須常駐」（含隱私決策條件），文字已裁決 |
| `DESIGN.md` 第五節、Sitemap §2.4 | 回寫成合併後的實際結構 |

其餘檔案改動見第七節。

## 十、實作結果（2026-08-29）

實作照 `docs/superpowers/plans/2026-08-29-settings-data-sync-merge.md` 執行，`pnpm check` 全綠（typecheck、738 個測試、eslint、stylelint）。

### 改了什麼

| 檔案 | 改動 |
| --- | --- |
| `DataSettingsPage.vue` | h1 改為「本機資料與隱私」；刪除 `cloud-data-link` 整張補救卡；吸收同步的三個狀態區塊與接線 |
| `DataSettingsPage.test.ts` | 新建。承接原 `SyncSettingsPage.test.ts` 的兩條斷言，另加兩條守住補救卡不復活、`/settings/account-data` 仍進得去 |
| `SyncSettingsPage.vue`／`.test.ts` | 刪除 |
| `router/index.ts` | 移除 `/settings/sync`，不留轉址；`/settings/data` 的 meta title／heading 改名 |
| `AccountDataPage.vue` | 返回連結 `/settings/sync` → `/settings/data` |
| `MorePage.vue` | 移除「跨裝置同步」卡；改名並套用裁決 3 的說明文字 |

### 次要區塊的層級怎麼做的

**層級差異做在「有沒有卡片外框」，不做在字級。** 第一層的概況／匯出／清除是 `app-card`（有底色 `rgb(247,237,225)`、圓角、內距）；同步群組背景透明、直接落在頁面底色上，只有一條 hairline 起手。群組標題與第一層卡片標題同一個 typography role（同字級同字重）。

不縮字是刻意的：同步區塊裡有登入與上傳雲端的決策資訊，縮字會變成看不清楚，而不是變次要。狀態標題用 `<strong>` 而非 `<h3>`，沿用本頁 `.clear-row` 既有的做法，避免在同一個群組裡多開一層標題階層（也讓 `typographyRoles.test.ts` 的 heading role 規則不必為此開例外）。

### 量測（390 寬度族群，實測 375×812）

| 項目 | 合併前 | 合併後 |
| --- | ---: | ---: |
| 「更多」頁實際渲染卡數 | 6 | **5** |
| 入口清單總高 | 594px | **521px** |
| 合併涉及的卡片高度 | 82 + 82（兩張） | 103（一張） |

淨省 **73px**，約 12.3%。合併後那張卡因為說明變兩行而高了 21px，但仍遠小於省掉的一整張卡加間距——這正是本文件第三節「合併一張卡的效果大於把所有可刪的說明文字都刪掉」的實測驗證。

其他檢查：

- 點擊範圍最小 82px，仍 ≥ `{layout.tap-target}` 的 44px
- 375 寬度無水平溢出
- `/settings/sync` 落到既有的「找不到這個頁面」，沒有轉址
- `/settings/data` ↔ `/settings/account-data` 雙向往返正常
- 桌面寬度下 `/settings/data` 只剩 3 張 `app-card`（補救卡確實消失）

### 一個不是這次造成的既有問題

200% zoom（等效視窗 188px 寬）時 `document.scrollWidth` 是 320px，會有水平溢出。**但同樣的 320px 也出現在這次沒動過的 `/more` 與 `/settings/notifications`**——這是全站的最小寬度限制，不是合併造成的迴歸。要處理的話是另一件事，不在本次範圍。
