# 2026-08-30 待裁決事項總表

**為什麼有這份文件**：2026-08-30 的介面稽核（`2026-08-30-ui-audit-findings.md`）列了六節，其中三節結尾是「待討論」「待決」。使用者明確要求「要給我決定的先一起決定好後記錄下來，我怕又漏掉」——所以這裡把**所有**還沒定案的事項集中成一張表，決一項就在這裡標一項，不要再散落在各自的段落結尾。

已完成的部分見本文件最後一節。

---

## 一、衛教首頁版面：三套規格互相矛盾

**狀態：✅ 已裁決並實作（2026-08-30）——採 A，但只做 hero 卡那一層**

實作 commit `559a5ac`。實測（390×844）：hero 28px 襯線／其餘五張 18px，hero 191px／其餘 175px，無橫向捲動；hero 內文字對比度 kicker 6.11、說明 6.11、標題 9.88，全數通過 WCAG AA。**後續要做**：把 B 的 plan／spec 標記為「首頁部分已由 A 取代」，避免下一個 session 又撿起來做。

這不是「文件過期」，是三份**都還有效**的來源指向三種版面：

| 來源 | 版面                                                                                    | 文件地位                                                                                                                                                                                                       |
| ---- | --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A    | 「了解今天的 UV」放大成 hero 卡＋「先從這裡開始」標籤，其餘五張小卡（雜誌式不對稱）     | 2026-08-09 訪談裁決 → 2026-08-14 再確認 → 寫進 `docs/decisions/2026-08-15-redesign-sitemap-userflow-current.md` §4.5 第 2 點（**現行 UX／IA 基準**），`DESIGN.md` 第 712 行的 `education-hero-card` 也是這一套 |
| B    | 精選導讀（了解今天的 UV）＋**四張**主題卡，「特殊情況」與「查看所有分類」降為低強度連結 | `docs/superpowers/specs/2026-08-28-education-editorial-redesign-design.md`＋同名 plan（**最新的一份**）                                                                                                        |
| C    | 六張齊平的分類卡                                                                        | **目前的程式碼**（2026-08-30 加上 32px 圖示）                                                                                                                                                                  |

**B 的執行狀態**：38 個 checkbox **全部未打勾**。只有文章頁的 takeaway block 與波浪落地了（`bb9d389`、`b4c8c7b`、`faa7aff`），首頁那部分從未實作。

### 建議：走 A，但只做 hero 卡那一層，不做雜誌式不對稱網格

三個理由：

1. **A 被裁決過兩次而且進了現行基準。** 2026-08-09 訪談定案、2026-08-14 再確認一次、2026-08-15 寫進現行 sitemap。B 雖然日期較新，但它是一份**未執行的計畫**，不是裁決。

2. **精緻感的來源是層級，不是不對稱網格。** 這是 mobile-first PWA，390px 單欄是主要視窗；A 的原始裁決自己就寫了「手機版需將不對稱版面轉為可理解的順序，不依賴卡片大小傳達唯一資訊意義」——等於承認不對稱在手機上傳達不了東西。在單欄下，hero 卡用 `{colors.surface-cream-strong}` 底色＋襯線 `page-title`＋「先從這裡開始」eyebrow，已經和下面五張 `surface-card` 拉開足夠層級，不需要靠寬度差。不對稱網格留給桌面版當可選增強。

3. **B 的風險最高。** 它把「特殊情況」降成低強度連結，而那是健康邊界內容（敏感反應、孕期、嬰幼兒、藥物），與 `DESIGN.md` 第五節「健康／安全邊界一律常駐」的不可隱藏清單有直接張力。B 還重新命名了分類（「補擦時機」等），要動內容產生器與六條 canonical 路由。

### 決定之後要回寫的地方

- 選 A：實作 hero 卡；`DESIGN.md` 第 712 行的 `education-hero-card` 從落差變成落地；把 B 的 plan／spec 標記為「首頁部分已由 A 取代」，避免下一個 session 又撿起來做
- 選 B：`DESIGN.md` 第 712 行與 `2026-08-15` 現行 sitemap §4.5 都要改；要先解決「特殊情況降級」與不可隱藏清單的衝突
- 選 C（維持現況）：`DESIGN.md` 第 712 行的 `education-hero-card` 條目要刪除或改寫，並在第十節記一筆「落差已由放棄 hero 卡消解」；B 的 plan／spec 同樣要標記作廢

---

## 二、`/setup` 右上角叉叉改「回上一頁」

**狀態：✅ 已裁決並實作（2026-08-30）——改「回上一頁」，草稿保留**

實作 commit `b945902`。按鈕同時從右上移到左上（返回是導航，導航在左上是慣例），圖示 `tool-close` → `tool-arrow-left`，emit `cancel` → `back`。新增守門測試「返回鈕發出 back 而不是 cancel」，已驗證會紅。`controller.cancel()` 自此沒有 UI 呼叫者，未移除但已加註解。**尚未做視覺實測**——`/setup` 有 `requiresNoActiveSession` 守衛，這台裝置有活動的測試 session，進不去；不擅自結束他人的 session 資料。

**現況（已讀碼確認，不是推測）**：

- 按鈕的 `aria-label` 是「取消設定」（`SetupStepShell.vue:56`），**語意與行為是一致的**
- `cancel()` → `setup.cancel()` → `deleteDraft(draftId)`，**真的把草稿刪掉**，然後 `router.replace({ name: "home" })`（`SetupPage.vue:211`、`createSetupController.ts:482`）
- 版面問題實測（390×844）：`main` 頂端 y=72、叉叉 y=96（44×44）、`<h1>` 頂端 y=95。叉叉與標題幾乎同高，卻各佔一列，把標題往下推

### 建議：改「回上一頁」，草稿保留

1. **「回上一頁」在任何 App 裡都是純導航，使用者不會預期它刪東西。** 若改了文案卻仍刪草稿，就是用一個看起來安全的按鈕做破壞性動作——那比現在的叉叉更糟，現在的叉叉至少還誠實地標著「取消設定」。

2. **續作路徑已經蓋好了，不需要新機制。** `SetupPage.vue:294` 已經有完整的「繼續未完成的設定？／繼續設定／重新開始」回復卡，`resumeDraft()` 與 `restartDraft()` 都在，`SetupDraftV1.currentStep` 也還在契約裡。保留草稿只是讓這套既有機制真正被用到——現在它被叉叉的刪除行為繞過了。

3. **取消草稿的能力不會消失**，它移到回復卡的「重新開始」上，那裡的語意是明確的。

**實作時必須驗證的前提**：離開再回到 `/setup` 時，`recoveryPendingState` 要真的觸發回復卡。若沒觸發，使用者會看到一個「半填好但沒說明為什麼」的表單，那比刪掉更糟。這一條要當成守門測試寫。

---

## 三、首頁「各部位狀態」8 個 pill

**狀態：✅ 已裁決並實作（2026-08-30）——兩個都做**

實作 commit 見下方。**實測把根因看得更清楚了**：8 個部位全是 `tracking`（倒數正常跑），`reasonCodes` 都只有 `PRODUCT_IDENTITY_UNKNOWN`——所以畫面上是同一句話重複八次，而「N 個原因」這個數字不告訴使用者任何事（不知道是什麼原因、也不知道要不要處理）。

先修根因，再加收合。

- **根因**：那個「・1 個原因」是 `PRODUCT_IDENTITY_UNKNOWN`（標示尚未確認）。對不填防曬乳的使用者，它會**永遠**顯示在每一個部位上。單純收合只是把它遮住，雜亂的來源還在。
- **順序**：先讓這個原因不逐部位重複，再把 pill 收成狀態摘要、需要時展開。

### 實作結果

1. **移除 pill 的「・N 個原因」**，改成群組層級的共有說明——只有**每一個**部位都帶該原因時才顯示，少一個就不顯示（寧可不說，也不要說得比實際情況廣）。
2. **只做 `PRODUCT_IDENTITY_UNKNOWN` 一個碼**，不做 21 個 ReasonCode 的全表：其餘的碼要嘛是事件型（流汗、毛巾、碰水），效果已經反映在 `timingStatus`／群組標題上；要嘛會讓部位落到別的群組。自創 21 條健康相關文案的風險也不划算。
3. **收合只給 `tracking` 與 `not_applicable`**（「現在不用做事」的狀態），且部位數 > 1 才做。需要行動的狀態常駐——把「建議現在補擦」藏進 disclosure 正是 `DESIGN.md` 第五節展開收合契約要避免的事。
4. 觸發器用契約允許的「標籤化按鈕」，`aria-expanded` ＋ `aria-controls` 齊備，chevron 換圖示 name 不用 `transform: rotate`。

**已知未做**：`reminderPresentation.ts` 裡 `PRODUCT_IDENTITY_UNKNOWN` 的文案仍寫「暫時無法建立補擦倒數」「未計時」，與 2026-08-30 的保守倒數規則矛盾。目前**不會顯示**（那段只在真的沒有倒數時走到），所以不是活躍 bug，但是個陷阱。

---

## 四、裝備區（`/products/new`）簡化

**狀態：✅ 已裁決（2026-08-30）——走大幅簡化**

裝備區退回純紀錄：記錄買過的防曬乳（期限、價格、好不好用），把倒數相關的欄位整批拿掉。

**前置條件已具備**：`2026-08-30-countdown-without-product-label.md` 已經移除「裝備要餵資料給倒數引擎」這個責任，沒有產品標示也會建立 120 分鐘保守倒數。

**規格草案已寫**：`docs/superpowers/specs/2026-08-30-gear-simplification-design.md`（2026-08-30，尚未動工）。它回答了下面三個問題，並補上一個探索後才發現、會改變這件事性質的第四點——**使用者要的「價格」與「好不好用」目前 schema 裡都不存在**，所以這是「刪倒數欄位 ＋ 新增紀錄欄位」，不是只有刪；而且**刪掉包裝標示會讓倒數變得比較不保守**（標示只會讓間隔變短），耐水期限會直接消失。草案第五節那三件事**已於 2026-08-30 裁決**：耐水期限保留（收合成選填）、「好不好用」用三檔（好／普通／不好）、價格不記幣別。因此包裝標示欄位一個都不移除，**不需要資料遷移**，schema 改動只剩新增 `priceTwd` 與 `usageRating` 兩個選填欄位。

**範圍大，動手前要先立規格**，至少要回答：

1. 「包裝標示確認」整張卡是不是全部拿掉？SPF／PA 還留不留（留的話是純展示）？
2. 已經存了標示資料的使用者，那些欄位怎麼處理？（Zod 預設 strip 未知欄位，移除欄位通常不需要遷移——見 `DESIGN.md` 第十節）
3. 「這件裝備不會建立補擦倒數」那張 124px 的卡直接刪（品類格下方已有一行在講同一件事）

稽核先前實測的三個具體問題（可當簡化後的驗收基準）：

- 一張 124px 的卡只為了說「這裡沒有東西」
- SPF 在兩張卡出現但意義不同（展示用 vs 進倒數計算），畫面上看不出差別
- 四張等重的卡，必填與選填視覺重量相同

頁面總高：1633px（防曬乳）／1294px（太陽眼鏡）。

---

## 五、Logo 要調整（2026-08-31 使用者補上三個具體方向）

**狀態：方向已明確，待實作。全部是「尺寸與位置」，不是造型——不需要動 Illustrator 幾何。**

| #   | 使用者說法                | 落點                                              | 待確認                       |
| --- | ------------------------- | ------------------------------------------------- | ---------------------------- |
| 5.1 | 「右上角 Logo 再小一點點」                 | 推測是 `BrandHeader` 的橫式 lockup                  | **位置對不上，見下方** |
| 5.2 | App icon 裡的標記往右移                    | PWA app icon（`apps/web/public/` 的 icon PNG／SVG） | 無（2026-08-31 已用箭頭圖確認） |
| 5.3 | 瀏覽器分頁的圖示放大一點                   | favicon                                             | 無                     |

### 5.1 的位置對不上，動手前要問

畫面上 **Logo 在左上角**，右上角是「臺中市西區 高量級」的地區／UV 文字。使用者說的是「右上角 Logo」。

三種可能，不要自己選一邊：

1. 指的其實是左上角的 `BrandHeader` lockup（最可能，順口說錯方位）
2. 指的是右上角那串地區文字太大（那不是 Logo）
3. 指的是別的畫面

`BrandHeader` 的高度 2026-08-30 才從 `3.25rem` 調到 `2rem`（因為新版 lockup 的長寬比從 2.89 變成 5.33，不改會把「前往地區設定」擠成兩行）。**再往下調之前要重測那一行不會換行**——這是同一個約束，不是新的。

### 5.2／5.3 是同一個根因的兩面

兩者都是**圖形在畫布內的佔比**問題，不是圖形本身的問題：

- **5.2**：squircle 底板裡的標記偏左，需要往右移到視覺中心。注意這是**視覺**置中不是幾何置中——標記左側是實心圓點、右側是三道發散線條，幾何置中會看起來偏右。
- **5.3**：favicon 在 16px 下四周留白太多，圖形本身太小。要縮小 padding 讓圖形佔更滿。

**檔案落點已查（2026-08-31）**：

- `docs/design/logo/` 只有**橫式 lockup**（`.ai` ＋ `.svg`），沒有 app icon 的來源。使用者 2026-08-30 修正的是這一份，所以它**沒有涵蓋到** app icon 與 favicon——這很可能就是 5.2／5.3 的成因。
- App icon／favicon 的產線是 `tools/logo-concepts/generate-logo-concepts.mjs`，畫布是 `viewBox="0 0 64 64"`，標記透過 `transform` 放進去。5.2（往右移）與 5.3（放大）都是改那個 transform 的位移與 scale，**不動 path 幾何**。
- 產出物：`apps/web/public/` 的 `favicon.svg`／`favicon.ico`／`apple-touch-icon.png`／`icon-192.png`／`icon-512.png`／`icon-512-maskable.png`。

**maskable 那一張要單獨檢查**：Android 會把它裁成圓角方形，放大標記會壓縮安全邊界。5.3 的放大**不應該無差別套到 maskable**。

---

## 十一、裝備表單（`/products/new`）版面太長（2026-08-31）

**狀態：使用者要建議，方向待裁決。**

**先講一個不方便的事實：頁面比簡化之前更長了。** 2026-08-30 稽核記錄 1633px，2026-08-31 實測 **1848px**。簡化砍掉了 `.no-effect-note`，但新增的「我的紀錄」區塊（購買月份／到期日／價格／評價／備註）比砍掉的多。§4 那次裁決是「退回純紀錄」，紀錄欄位本來就會變多——這不是實作偏差，是裁決的必然結果，但**沒有人先算過總高**。

實測四塊的高度（390px 寬）：

| 區塊              | 高度  |
| ----------------- | ----- |
| 品類選擇 fieldset | 307px |
| 裝備暱稱＋SPF／PA | 318px |
| 包裝標示（可收合）| 303px |
| 我的紀錄          | 528px |

### ✅ 已裁決（2026-08-31）：甲乙丙三刀都做，先做甲

| 刀   | 內容                                            | 省下   | 狀態                          |
| ---- | ----------------------------------------------- | ------ | ----------------------------- |
| 甲   | 「我的紀錄」整塊從**新增**流程拿掉，只在編輯出現 | 545px  | ✅ 已實作（PR #40）           |
| 乙   | 包裝標示與我的紀錄合成一個收合區                | ~300px | 待做（甲之後範圍會變，見下）  |
| 丙   | SPF／PA 移進包裝標示裡                          | ~80px  | 待做                          |

**甲的實測**：`/products/new` 1848px → **1303px**（-29.5%）。

**甲的理由不是版面而是時序**：新增一瓶防曬乳時使用者知道的只有名字與品類；購買月份、價格要翻收據，而「好不好用」在用之前不可能知道，等於一個永遠只能選「未評價」的下拉。詳情頁的「編輯」才是這些欄位的自然位置，那個按鈕本來就在。

**甲的已知副作用（已接受）**：到期日是這塊裡唯一會進 reducer 的欄位，也一起移走了。新增當下若那瓶已過期，第一次仍會建立倒數，要進編輯頁補到期日才會停。

**做乙之前要重新評估範圍**：甲之後「我的紀錄」在新增流程已經不存在，乙原本的「兩塊選填視覺重量不一致」在**新增頁**已經自動消解——乙剩下的價值只在**編輯頁**（那裡兩塊仍然並存，實測 2130px）。要不要為編輯頁單獨做乙，值得再想一次。

### 順帶發現：SPF 的 placeholder `50` 與價格是同一個問題

2026-08-31 已拿掉價格的 `placeholder="690"`（數字欄位裡的灰字會被讀成已填好的值）。SPF 的 `50` 完全一樣，**但使用者只指了價格**，所以沒有一起改。

`PA` 的 `PA++++` 不同——那是在提示**格式**，留著合理。要不要一併處理 SPF，等使用者裁決。

**提醒**：Logo 用的是圖示配色（墨咖 `#33291F` ＋ 琥珀金 `#C1832E`），與介面配色（暖象牙 ＋ 深杏桃 `#9F5E42`）是兩套，不要混用。幾何的真實來源是 Illustrator（`docs/design/icon-system/README.md`），不要手改 SVG path——但 5.1／5.2／5.3 都只動尺寸與位置，碰不到 path。

---

## 六、首頁主 CTA 的份量：「記錄狀況」不該是主要 CTA

**狀態：使用者 2026-08-30 提出，方向待裁決。**

**先講結論：這件事先前沒有被記錄，而且現有文件寫的是相反的。** 使用者問「之前有記錄到嗎」——查了 `docs/` 全部非 archive 文件，沒有任何一條說首頁不該有主 CTA，能找到的都要求它存在：

| 出處                                                                                | 寫的是什麼                                 |
| ----------------------------------------------------------------------------------- | ------------------------------------------ |
| `2026-08-15-redesign-sitemap-userflow-current.md` 第 199 行（**現行 UX／IA 基準**） | 「首屏維持不捲動就看得完倒數與**主 CTA**」 |
| `docs/design/2026-08-17-home-wireframe-variants-design.md` 第 29 行                 | 「單一主要 CTA『記錄補擦』」               |
| `DESIGN.md` 第 736、834 行                                                          | 「每頁只有一個主要 CTA」                   |

所以這是**推翻既有裁決**，不是補一個漏掉的待辦。

### 一個對裁決有影響的區分

首頁那顆按鈕的文字不是固定的，它跟著 `primaryAction.actionKind` 走（`reminderPresentation.ts` 的 `ACTION_LABELS`，共 13 種）。實務上最常見的兩種是：

| 狀態                                       | 按鈕文字     | 這時它該不該是主 CTA                                                                             |
| ------------------------------------------ | ------------ | ------------------------------------------------------------------------------------------------ |
| `tracking`（倒數進行中，例如還有 82 分鐘） | 「記錄狀況」 | **這正是使用者質疑的**——還有 82 分鐘，沒有要使用者現在做任何事，卻用深杏桃實心大按鈕佔滿整個寬度 |
| `reapply_due`（該補擦了）                  | 「記錄補擦」 | 這時它**就是**當下最主要的任務，降級反而不對                                                     |

**所以「要不要當主 CTA」很可能不該一刀切，而是跟著狀態走。** 這與 `2026-08-23-wireframe-copy-fixes.md` 第 208 行對裝備詳情頁下的裁決是同一個模式（「主 CTA 依狀態切換，不是固定文字」）。

### 好消息：不違反設計系統

`DESIGN.md` 的「每頁只有一個主要 CTA」是**上限不是下限**——`2026-08-23-wireframe-copy-fixes.md` 第 145 行已經明文解釋過，並據此讓夜間頁完全沒有主 CTA。所以首頁在某些狀態下不放主 CTA，**不需要改設計系統**。

### 動手前要裁決的

1. **是所有狀態都降級，還是只有 `tracking`？** 建議只降 `tracking`（與 `not_applicable` 之類「現在不用做事」的狀態），`reapply_due`／`reapply_soon` 維持主 CTA。
2. **降級成什麼？** `.button--quiet`（既有的次要樣式）？還是文字連結？還是移到「最近事件」附近而不是倒數正下方？
3. **`2026-08-15` 現行 sitemap 第 199 行要跟著改**——它現在寫「首屏維持不捲動就看得完倒數與主 CTA」。

### 使用者補充的方向（2026-08-30）

> 「因為此 APP 核心功能是提醒補擦，事件現在是深色按鈕，會一直注意到這個事件的存在。我希望降級，或者變成是『有大量流汗的狀況嗎？提醒您記得補擦防曬』類似的次要提醒。」

這個方向本質是**把命令式的主 CTA 換成條件式的提問**，而且它跟 domain 的語意是相符的——情境事件（流汗／毛巾／摩擦／洗手／下水）**本來就是條件觸發**的，不是每次都要做。深杏桃實心滿寬按鈕讀起來是「你現在該做這件事」，與「如果剛好有發生，才需要點」不是同一件事。

### 文案規範上的兩個硬性限制（查證後）

1. **不能用「您」。** `2026-08-23-wireframe-copy-fixes.md` §2.3 有實測紀錄：`apps/web/src/` 之中「您」出現 **0 次**、「你」出現 **41 次**，全站一致用「你」。所以「提醒**您**記得補擦防曬」要改成「你」，或乾脆不用第二人稱。
2. **「記錄補擦」是 copy-audit 訂的核心動作詞彙**（`2026-08-17-copy-audit.md`「本次統一規則」），`reapply_due` 狀態下的文字不該自行改寫。

### 建議方案

**依狀態切換份量，不要一刀切**——與 `2026-08-23-wireframe-copy-fixes.md` 第 208 行對裝備詳情頁的裁決同一個模式。

| 狀態                       | 現況                           | 建議                                                     |
| -------------------------- | ------------------------------ | -------------------------------------------------------- |
| `tracking`／`reapply_soon` | `.button--primary`「記錄狀況」 | **降級**：改次要樣式，文案改條件式提問                   |
| `reapply_due`              | `.button--primary`「記錄補擦」 | **維持主 CTA**——這時它就是當下最主要的任務，降級反而不對 |

文案方向（避開「您」、涵蓋五種狀況而不只流汗）：

- 「流汗、碰水或擦過了嗎？」＋次要按鈕「記錄狀況」
- 或「剛才有流汗、碰水或擦拭嗎？記錄後會重算補擦時間。」

**這兩句都還沒有經過裁決，是提案。** 涵蓋範圍要對得上 `ReportContextEventPage` 實際提供的五個選項（大量流汗／擦毛巾／明顯摩擦／洗手／游泳下水）。

### 動手前仍要裁決的

1. 降級成 `.button--quiet`，還是做成一張帶問句的提示卡（像使用者描述的）？
2. 位置留在倒數正下方，還是移到「各部位狀態」之後？
3. `2026-08-15` 現行 sitemap 第 199 行要跟著改。

**尚未動工。**

---

## 七、`/settings/data` 資料概況卡：提案的三個元素沒有應用

**狀態：待裁決（2026-08-30）。**

使用者提供了一張設計圖，問「圖上類似的樣式有做嗎？另一個 AI 有提出但好像沒有應用」。

**答案：卡片本身做了，圖上三個具體元素沒做。**

`DataSettingsPage.vue` 第 151 行的 `data-summary-title`（「這台裝置儲存了什麼」）在 2026-08-29 的 `settings-data-sync-merge` 裁決裡就被列為第一層區塊，label／value 左右對齊也已經用 `.summary-grid` 做了。

圖上有、程式碼沒有的：

| 元素                                                                                             | 現況                                                   | 評估                                                                                                                                  |
| ------------------------------------------------------------------------------------------------ | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------- |
| **標題下的說明句**：「以下數量只代表這台裝置上的本機紀錄，不包含其他裝置或尚未下載的雲端資料。」 | **沒有**（grep 全 repo 零結果，2026-08-29 裁決也沒訂） | **最有價值的一項。** 它講的是真實限制，而且正是 2026-08-29 那次合併要解決的「本機 vs 雲端」混淆。建議採用（把「您」類客服腔避掉即可） |
| **每列之間的分隔線**                                                                             | 沒有，目前只靠 `--space-3` 間距分隔                    | 純視覺。六列資料加 hairline 會更好掃讀，但要用 `--border-subtle` token，不可寫死                                                      |
| **標題用襯線**                                                                                   | 現況是 `card-title`（黑體 18px）                       | **不建議照做**：襯線在 `DESIGN.md` 是留給 `page-title` 的，卡片標題改襯線會破壞字體階層                                               |

另外圖上與現況的文案差異：「這台裝置**目前**儲存了什麼」、「有一個」vs 現況「有一個進行中」。圖上只列 3 項，現況有 6 項（多了設定草稿、氣象快照、時鐘校準）——**不建議砍到 3 項**，那三項是隱私揭露的一部分。

**尚未動工。**

---

## 八、稽核後續四項（2026-08-30 已裁決執行順序）

使用者裁決的順序：**A 類 → 粗體 → B 類 → DESIGN.md 稽核**。理由是 A 類與粗體都會動到 `DESIGN.md`，先做完再做全面稽核，才不會稽核完又被自己改掉。

來源：`2026-08-30-unused-declaration-audit.md`。

### 8.1 深色表面體系：規範有效但整批未套用（**原本被誤判為「失效可收掉」**）

**狀態：需要重新裁決。使用者原本裁決「A 類收掉」，但動手前查證發現前提不成立。**

稽核報告原本把這 4 個 token 歸成「因 2026-08-23 設計變更而失效」。**那個判斷是錯的**：

- `DESIGN.md` 第 423 行：「`{colors.surface-dark}` 主要深色表面。**2026-08-23 起不再用於首頁倒數，改用於設定流程外框與資料強調**」
- 第 399 行列了三個落點：**設定流程的步驟外框、產品標示編輯、五日預報的強調欄**

深色面板**沒有被廢棄，只是換了用途，而新用途一個都還沒做**。實測現行程式碼：`--surface-inverse` 被引用 0 次、`--color-surface-dark` 只在 `--surface-inverse` 的定義裡出現。

`surface-dark-elevated`（深色區塊內的控制項）、`surface-dark-soft`（深色卡片內的資料區塊）、`on-dark-soft`（深色表面上的次要標籤）都是主體的配套——主體沒套用，配套自然也沒有。

**所以 8 個 token 全部是「規範有效但未套用」，跟 8.3 的 blush／amber 同一類，沒有一個該被收掉。** 收掉等於刪除還有效的設計規範。

唯一真正過時的是 `accent-apricot` **用途描述裡的「倒數進度環」**（第 410 行）——進度條 2026-08-29 已改用狀態色。apricot 的另外兩個用途（插圖筆觸、小面積高光）仍有效。

#### 要你重新裁決

| 選項             | 說明                                                                                                                                |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| **甲、套用**     | 照 `DESIGN.md` 把設定流程外框、產品標示編輯、五日預報強調欄做成深色表面。**範圍大、會大幅改變畫面**，而且深色表面上的文字對比要重驗 |
| **乙、明確放棄** | 裁決不做深色表面，把 4 個 token 連同 `DESIGN.md` 第 399／423／424／425／441 行一起收掉。**這是刪規範，要你點頭**                    |
| **丙、記為落差** | token 與規範都留著，在 `DESIGN.md` 第十節記一筆「深色表面體系尚未套用」，之後再排                                                   |

**建議丙**：甲的範圍太大不該夾在這批收斂裡做；乙會刪掉 2026-08-23 才剛重新定位過的規範，太可惜。

**唯一可以現在就做的**：把第 410 行 `accent-apricot` 的「倒數進度環」用途拿掉，那個確定過時。

### 8.2 粗體：3 處量表外字重 ＋ 沒有全域 `strong` 規則

**狀態：待做（順序 2）。**

實測分布：`font-weight: 500` 29 處、`600` 12 處、`var(...)` 8 處、**`700` 3 處**。

規則本身在 `DESIGN.md` 第三節就有而且明確（page-title 400、section-title 500、內文 400、標籤與強調 500、`stat-figure` 600，只載入 400／500／600），問題有兩個：

1. **3 處 `font-weight: 700` 是量表外的值**——`ReapplicationProductAssignments`、`ReapplicationZoneSelector`、`BottomNavigation`。`DESIGN.md` 第 975 行明文寫過後果：「Noto Serif TC subset 只有 400 字重，元件若寫 `font-weight: 600` 會觸發瀏覽器合成假粗（faux bold），把筆畫無差別加厚，中文字會糊。」已查證 `@font-face`：`Noto Serif TC Subset` 確實是單一 400、`Inter Subset` 是 100–900 可變，所以拉丁字 700 沒事，**中文要看 fallback 到哪支字型**。
2. **沒有全域 `strong` 規則**——與 2026-08-30 修過的 `<small>` **完全同型**。`<strong>` 落到瀏覽器預設 700，各元件再各自覆寫成 500（`.entry strong`、`.education-category-card strong` 等），漏寫的地方就是 700。

### 8.3 B 類：套用兩個規範有效但從未使用的顏色

**狀態：✅ 已完成（2026-08-30）。`--color-amber` 已套用；`--color-blush` 經裁決不套用，文件衝突已收乾淨。**

先講一個對兩者都成立的限制：**這兩個顏色只能當底色，不能當文字色。** 實測對比度（畫布 `#FAF5EC` 上）：

| 用法                  | 對比度 | AA（4.5） |
| --------------------- | -----: | --------- |
| blush 當文字          | 2.17:1 | ✗         |
| amber 當文字          | 2.07:1 | ✗         |
| 深咖文字放在 blush 底 | 6.11:1 | ✓         |
| 深咖文字放在 amber 底 | 6.41:1 | ✓         |

這與 `DESIGN.md` 對同族的 `accent-apricot` 寫的「此色上必須用深咖文字」是同一條規則。

#### ✅ `--color-amber` 已套用

落點是 `GearForm.vue` 的 `.affects-badge`（「會影響倒數」），就在 SPF 與到期日旁邊——正對應 `DESIGN.md` 寫的「徽章、SPF 標記」。改成 amber 底 ＋ 深咖文字，實測 6.41:1。邊框一併移除（有實底色之後再加深色邊框會變成兩層框）。

**沒有做成「SPF 值的徽章」**：`ProductDetailPage` 與 `GearListItem` 的 SPF 目前是純文字摘要的一部分（`SPF 50・PA++++`），要做成徽章等於新增視覺元素，不是「把既有規範套到既有元素上」，超出這批收斂的範圍。

#### ✅ `--color-blush`：裁決維持現況，三方衝突已收乾淨

`DESIGN.md` 對「衛教引言」指派了**兩個不同的顏色**，而程式碼用了**第三個**：

| 出處                          | 指派                                                                             |
| ----------------------------- | -------------------------------------------------------------------------------- |
| 第 411 行                     | `{colors.accent-blush}` `#D79A92` → 「**衛教引言**與分類細節；永遠不作為行動色」 |
| 第 420 行                     | `{colors.surface-soft}` `#F7EDE1` → 「區塊分隔、**衛教引言底**、來源區塊」       |
| `EducationArticleSummary.vue` | `--color-surface-cream-strong` `#EFD0BC`                                         |

**三方不一致，而且是文件自己先矛盾。** 依既有慣例（設計來源衝突先裁決、不由實作端選邊）沒有擅自套用，改為請使用者裁決。

**裁決（2026-08-30）：維持現況 `cream-strong`。** 程式碼一行沒動，三處文件改成一致：

| 行                          | 改動                                                                                                      |
| --------------------------- | --------------------------------------------------------------------------------------------------------- |
| 411（accent-blush）         | 拿掉「衛教引言與分類細節」，改標「目前沒有落點，保留色票備用」，並記下它只能當底色（文字 2.17:1 不到 AA） |
| 420（surface-soft）         | 拿掉「衛教引言底」                                                                                        |
| 422（surface-cream-strong） | 補上「衛教文章引言底」——這才是實際在用的                                                                  |

`--color-blush` 因此仍會出現在未使用掃描裡，但第 411 行現在明講原因，不會再被誤判成死碼。

### 8.4 DESIGN.md 全面稽核

**狀態：待做（順序 4）。**

使用者要求：「仔細看 `DESIGN.md` 的內容，過舊的要更新，以免其他 AI 誤判。」

已知會誤導的例子（本輪發現的）：

- 第 712 行 `education-hero-card` 曾長期與程式碼不符（2026-08-30 已實作，現已相符）
- 第 424／441 行仍描述深色表面 token 的用途，但深色面板 2026-08-23 就放棄了（＝ 8.1）
- 第 410 行把 `accent-apricot` 寫成「倒數進度環」，但進度條現在用狀態色（**2026-08-30 已修**）
- **第 527 行與第 613 行對底部導覽字重自相矛盾**：typography 表寫 nav-label 字重 `500`，元件規格卻寫「未選取 400／選取 700」。程式碼各取一半——未選取實測 500（繼承 `--font-weight-nav-label`）、選取 700。**要裁決未選取到底是 400 還是 500**，然後讓兩處一致
- **`--font-weight-*` 缺「強調短語」角色**：DESIGN.md 第三節訂「標籤與強調短語 500」，但 7 個 `--font-weight-*` token 裡沒有對應的名字。2026-08-30 補全域 `strong` 規則時只能寫字面值 500（借用 `--font-weight-caption` 會誤導——caption 指的是短註腳、時間戳、eyebrow、badge，不是行內強調）

**稽核要放在 8.1–8.3 之後**，否則會稽核完又被自己的改動推翻。

### 8.5 掃描要不要納入 CI

**狀態：待裁決。建議加，但做成獨立指令，不要放進 `pnpm check`。**

- 加的價值明確：這次抓到 8 個，而 `tokens.test.ts` 完全看不到這一類
- 不放進 `pnpm check`：它會擋住「先定義 token、之後才套用」這個正常流程——B 類的兩個顏色就正是這種狀態，不該讓 PR 紅
- 具體做法：`pnpm audit:unused` 輸出清單但不決定成敗；或納入 CI 但只發警告不擋合併

---

## 九、殘留 worktree（不是設計裁決，但別漏掉）

`.worktrees/codex-supabase-uv-forecast` 是**已註冊**的 git worktree，在 `codex/supabase-uv-forecast` 分支（`bd1ea04`），不是被遺忘的資料夾。

**沒有動它，也不建議在沒問過之前動。** 依 `CLAUDE.md` 的 Session 衛生，清理前要同時做兩件事：`git status` 確認乾淨、`ListAgents` 確認沒有其他 session 正在那個路徑工作。2026-08-22 就是只做了第一項，弄丟了另一個 session 的未 commit 變更。

---

## 十、2026-08-31 截圖回饋（五項，設計建議已給，尚未動工）

使用者在關機前提出，先記在這裡避免漏掉。**兩項已裁決**：卡片內距「可以再緊一點」（`--card-padding` 已收斂成單一 token，改一行即可）、按鈕高度**不動**（44px 是 WCAG SC 2.5.5 下限）。

| #   | 項目                                             | 性質       | 狀態                |
| --- | ------------------------------------------------ | ---------- | ------------------- |
| 1   | 塗抹時間的調整按鈕限定 120 分鐘以內              | 規格       | ✅ 方向已同意，待實作 |
| 2   | 裝備清單收合前只顯示名稱，以圖示為主             | 版面       | ✅ 方向已同意，待實作 |
| 3   | 裝備詳情 `dt` 直排跑版                           | **Bug**    | ✅ 已定位，待修       |
| 4   | 五日 UV 預報重複文字、日曆排版、當日最高標示     | 版面＋文案 | ✅ 方向已同意，待實作 |
| 5   | `/setup` 出現「設定內容目前無法儲存」            | **Bug**    | 先補診斷，再談修     |

**2026-08-31 使用者已同意下列各項的建議方向。**

### 1 的做法

上限做成 slider／stepper 的**硬上限**，不要「可以輸入但事後報錯」。

理由不是 UI 便利，是安全邊界：沒有產品標示時系統採 120 分鐘保守倒數，讓使用者宣稱「3 小時前擦的」會讓倒數一開始就在過期狀態。超過 120 分鐘的正確動作是**重新塗抹**不是記錄，文案往這個方向寫：「超過 2 小時建議重新塗抹再開始計時」。

### 2 的做法

現在收合狀態塞了四層：品類、名稱、**一整段兩行的說明**、個人附註。那段說明是每一張防曬乳卡都一樣的固定文字——重複度 100%、資訊量 0。

收成兩行：32px 圖示 ＋ 名稱為主、品類為輔，右側 chevron。固定說明移到**清單標題下方講一次**，或只留在詳情頁。個人附註不出現在收合狀態。

### 4 的做法

三件事：

1. **免責聲明講了兩次**，字幾乎一樣。留下面那句、刪上面那句。
2. **標題也重複**：「五日 UV 預報」＋「未來 5 天 UV 預報」，刪掉其中一個。
3. **卡片內排版**：現在日期／數字／等級三層各自搶注意力。改成數字最大、等級縮成小字或純色點、日期最小置頂——一眼要先看到嚴重度。

**當日最高：標，但不要用文字。** 今天那張現在是紅框＋紅底，語意是「今天」而不是「最高」，兩件事混在同一個視覺記號上，使用者會以為紅色代表最高。今天用外框、最高值用小三角或加粗數字，兩者分開。

### 3 的成因（已定位，不需要再查）

`ProductDetailPage.vue` 的 `.spec-row` 是 `display: flex` ＋ `justify-content: space-between`，但 `dt` 沒有 `flex-shrink: 0`。`dd` 的文字一長就把 `dt` 壓到最小內容寬度，於是「補擦提醒」被擠成一行一個字。

長標籤配長內容本來就不該並排——建議整列改成上下堆疊（既有的 `.spec-row--full` 就是這個形狀），或至少給 `dt` 一個 `flex: 0 0 auto` 與 `min-width`。

### 5 為什麼查不出原因（重要）

`SetupPage.vue:116` 的 `catch` 是**空的**——它把例外整個丟掉，只留一句固定文案：

```ts
} catch {
  localError.value = "設定內容目前無法儲存，請重新整理後再試一次。";
}
```

所以「為什麼」這個問題，**程式碼目前的結構讓它答不出來**。同一句文案至少涵蓋三種完全不同的失敗：草稿不存在（`requireDraft` 丟 `SetupValidationError`）、IndexedDB 寫入失敗、跨分頁 invalidation 撞上同一筆草稿。

`SetupPage.vue` 自己的註解記載過同一條路徑在 2026-08-24 的前一次事故（例外沒被接住變成 unhandled rejection，看起來像點了沒反應）。當時的修法是「接住並顯示訊息」，但沒有留下任何可診斷的痕跡。

**建議先做這件事，再談修 bug**：在 catch 裡 `console.error` 原始例外，並依錯誤類型分出不同文案（草稿遺失 → 提示重新開始；儲存失敗 → 提示重試）。否則每次重現都只能猜。

---

## 已完成（2026-08-30 這一輪）

| 項目                                                           | 稽核章節 | 結果                                    |
| -------------------------------------------------------------- | -------- | --------------------------------------- |
| `<small>` 補上字級，13px → 14px（badge 用 12px）               | §2       | 已完成，commit `bdcaa10`                |
| 移除 `.stat-figure--inline`，16.8px → 16px                     | §2       | 已完成，commit `6957245`                |
| 衛教分類卡接上 32px 圖示                                       | §3       | 已完成，commit `eba3332`                |
| 衛教 6 個圖示移到已確認板                                      | §3       | 已完成，commit `0fe856a`                |
| 「了解今天的 UV」改為 hero 卡（本文件 §1）                     | —        | 已完成，commit `559a5ac`                |
| `/setup` 叉叉改左上「回上一頁」，離開不刪草稿（本文件 §2）     | §4       | 已完成，commit `b945902`                |
| 部位狀態：移除「N 個原因」改群組層級說明＋收合（本文件 §3）    | §5       | 已完成，見 `claude/zone-status-reasons` |
| 底部導覽在 320px 撐出 16px 橫向捲軸（WCAG SC 1.4.10 基準寬度） | —        | 已完成，見 `claude/bottom-nav-overflow` |

§1（token 紀律）稽核結論是「不需要再做一輪」。
