node_modules/
.vercel/
.pnpm-store/
.worktrees/
dist/
coverage/
*.tsbuildinfo
.DS_Store
Thumbs.db
.codex-*.log
tools/region-data/source/*.zip
tools/region-data/source/extracted/
# 字型原始檔（數 MB）不進 repo，只 commit subset 後的產物。
# 重新取得方式見 tools/fonts/README.md。
tools/fonts/source/

# Claude Design 匯出的交接 bundle 不進 repo（下游產物、非權威、會漂移）。
# 2026-08-26 移除了 uvalert-design-system/ 與 防曬補擦流程設計/，見
# docs/superpowers/plans/2026-08-26-codebase-consolidation-audit.md 的 C2。
# 之後的匯出放這個資料夾或 repo 外。
*.dc.html
design-exports/

# 部署產物與本機祕密。
#
# 2026-08-29：.env.local 先前是「未追蹤但也沒被忽略」，一次 git add -A 就把
# Vercel CLI 產生的 VERCEL_OIDC_TOKEN commit 進這個 public repo。那個 token
# 當下其實已經過期（OIDC 是 12 小時短效憑證，無法手動撤銷、只能等它到期），
# 所以沒有造成實際暴露，但同樣的洞會讓下一個真的還活著的祕密溜出去。
.env*
.vercel/

# brainstorm 預覽伺服器的本機狀態（含 localhost 專用 token 與 pid）。
.superpowers/brainstorm/
