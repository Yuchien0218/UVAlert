# 產品需求文件（PRD）— 防曬晴報員（Sunshield Advisor）Web App

| 文件資訊 | 內容 |
| --- | --- |
| 版本 | 3.10（兩步設定流程與防曬裝備清單修訂版） |
| 狀態 | 可進入產品、工程、醫療內容、海洋環境內容與法務聯合審查；尚未核准公開上線 |
| 更新日期 | 2026-08-07 |
| 主要平台 | 手機板優先的響應式 Web／可安裝 PWA |
| 主要市場 | 台灣，繁體中文 |
| 文件負責人 | 待指定 |

> **重要定位**：本產品提供所在地紫外線風險資訊、防護衛教與補擦事件提醒，不是醫療服務、診斷工具或治療建議；不預測個人何時曬傷、不計算「安全曝曬時間」，也不能取代產品標示、醫師或其他醫療專業人員的建議。已有皮膚疾病、病理症狀、醫囑、用藥或療程者，應以醫師／醫療專業人員的個別指示為優先。

---

## 1. 專案概述

「防曬晴報員」是一款以手機單手操作為優先、免下載即可使用、亦可選擇安裝為 PWA 的紫外線風險與防曬提醒工具。中文品牌名稱結合「晴天／晴況」與「情報員／播報員」的意象，角色定位為提供日光風險情報、操作建議及適時提醒，不代表氣象預報機構或醫療專業人員。

系統在使用者同意定位後，依所在地選取最近可用的中央氣象署（CWA）紫外線觀測站；若使用者拒絕定位，則提供手動選擇地區或 `暫不提供地區`。未提供地區時不顯示假 UVI，也不得阻斷補擦提醒。介面必須清楚標示資料屬於「測站觀測」或「區域預報」、資料時間、測站／區域及資料是否過期。

產品根據以下資訊提供保守、可操作的提醒：

- 當前 UVI 風險等級。
- 使用者自陳的上次塗抹時間與實際塗抹部位。
- 防曬產品標示，例如 SPF、PA 等級、廣效防護宣稱、曝曬前使用方式及耐水 40／80 分鐘。
- 使用者主動回報的游泳、大量流汗、擦毛巾或明顯摩擦事件。
- 本次活動情境，例如室內、一般戶外、運動或水上活動。

本產品不以 SPF、膚色、疾病或藥物推算個人可安全曝曬的分鐘數。UVI 用於呈現環境風險與防護建議，不用來延長補擦間隔。

### 1.1 問題陳述

使用者常遇到的問題包括：

1. 忘記上次何時塗抹或補擦。
2. 不知道所在地目前的 UV 風險與資料是否仍然有效。
3. 不清楚游泳、流汗、擦拭後是否應重新塗抹。
4. 不清楚不同 UVI 等級應搭配哪些遮蔭、衣物、帽子與太陽眼鏡。
5. 現有固定提醒無法區分「正在戶外」與「長時間待在遠離直射陽光的室內」。

### 1.2 產品目標

- 讓使用者在 30 秒內看到所在地 UV 風險；拒絕定位時可手動選擇地區，也可先不提供地區並直接開始提醒。
- 讓使用者以訪客模式在手機上直接完成核心操作，不必先註冊、登入或輸入聯絡方式。
- 讓使用者用最少輸入啟動一次補擦提醒。
- 讓首次使用者以三個簡短階段完成提醒設定，並讓再次使用者重用上次的部位、方法與產品設定；任何新的塗抹時間仍須逐次確認。
- 在游泳、流汗、擦拭等事件發生後提供清楚、保守的重新塗抹提示。
- 所有健康文案可追溯至權威或原始來源，並記錄審查日期。
- 在瀏覽器、權限或網路能力不足時，明確降級，不宣稱仍能「100% 正常運作」。
- 只蒐集提供所選功能所必要的資料；P0 提供本機資料查看、更正與刪除，帳號資料匯出屬 P1 功能，法定個資權利另依第 16 節完整告知。

### 1.3 非目標

本版本不提供：

- 個人曬傷時間、MED 或「安全曝曬倒數」。
- 皮膚癌、白內障、掉髮、唇疱疹或皮膚疾病的診斷、治療或預防保證。
- 對皮膚疾病、皮膚症狀、病理結果、藥物或療程提供個別化診斷、分級、處方、治療建議或「可／不可曝曬」判定。
- 依疾病、醫美療程、藥物、性別或 Fitzpatrick 膚型計算分鐘權重。
- 背景持續定位、Web geofencing 或零按鍵交通工具辨識。
- 關閉網頁後仍可保證準時的離線本機通知。
- PWA 圖示逐分鐘倒數。
- 純 Web 自動註冊 Siri App Intents 或 Google App Actions。

### 1.4 用語規範

品牌命名：

- 中文完整名稱：`防曬晴報員`。
- 介面短稱：`晴報員`；只有在上下文明確時使用。
- 品牌標語：`記得防曬，也記得適時補擦`。
- 英文工作名稱暫保留 `Sunshield Advisor`，待品牌、商標、網域與英文語感確認後定稿；不得由本文件推定已完成商標查核。
- `晴報員` 是提供 UV 資料來源標示、防曬知識與事件提醒的產品角色，不得自稱官方氣象預報員、醫療專業人員，或暗示自行產製官方 UVI。

允許使用：

- 「當前 UV 風險」
- 「建議補擦時間」
- 「各部位上次記錄塗抹距今時間」
- 「資料為最近測站觀測／區域預報」
- 「風險較低，但不是零風險」
- 「通知可能因裝置、網路或系統設定而延遲」

禁止使用：

- 「精準曬傷倒數」
- 「安全剩餘時間」
- 「您現在絕對安全」
- 「保證不曬傷」
- 「物理全防禦／UV＝0」
- 「100% 離線正常」
- 「絕不外洩」
- 「現在的太陽更毒」

---

## 2. 目標使用者與使用情境

### 2.1 核心使用者

1. **一般通勤與戶外活動成人**：需要快速查看 UVI、記錄上次塗抹時間及接收補擦提醒。
2. **戶外運動與水上活動使用者**：需要針對流汗、游泳、擦拭和耐水標示的事件提醒。
3. **室內外頻繁切換者**：需要在重新外出前檢查各外露部位距離上次記錄塗抹是否已過久。
4. **有醫師特別避光指示者**：只提供一般風險資訊與轉介提示，不以疾病資料推算提醒時間。

### 2.2 溝通語氣

防曬晴報員固定採用「溫和明確、簡潔有行動感」的提醒語氣，不依性別推定，也不提供品牌人格切換。

每則重要提醒原則上按照以下順序編寫：

1. 說明現在的狀況。
2. 簡短交代提醒原因。
3. 提供一個可以立即執行的主要行動。

語氣規則：

- 使用日常、容易理解的繁體中文，重要資訊先說結論；補充原理放在可展開區域。
- 優先使用「建議」、「可以」、「請留意」等提醒式用語，不因忘記補擦而責備使用者或製造焦慮。
- 一般操作避免命令、恐嚇、過度賣萌、過度擬人及過度角色扮演。
- 角色用語只作輕量提示，例如 `晴報員提醒`，不得妨礙資訊理解；資料不足時應直接說明尚未確認的項目。
- 每則主要提醒原則上只提供一個主要行動，其他選項使用次要按鈕或展開內容。
- 涉及高風險、警示徵象或需要專業協助時，可以提高嚴肅程度，但仍須保持清楚、冷靜，並依有效審查內容引導使用者遵循醫囑或詢問醫師。
- 不使用 `保證安全`、`絕對不會曬傷`、`防曬已完全失效` 等無法由系統證實的說法。

標準句型範例：

- 一般提醒：`晴報員提醒：接近建議補擦時間，可以準備補擦了。`
- 洗手事件：`手背可能因洗手而需要重新塗抹。`
- 資料不足：`防護方式尚未確認，目前會採用保守提醒。`
- 提交完成：`回報已更新，提醒時間已重新計算。`
- 系統限制：`通知可能因手機、網路或系統設定而延遲。`

### 2.3 代表性使用情境

- 使用者準備出門，查看目前 UVI 與建議裝備，記錄已塗抹防曬。
- 使用者在戶外運動中大量流汗，主動點選事件後收到重新塗抹提醒。
- 使用者游泳，系統依產品標示的 40／80 分鐘與擦毛巾事件提醒。
- 使用者長時間待在遠離陽光直射的室內，系統不持續製造焦慮；再次外出時重新檢查上次塗抹時間。
- 使用者拒絕定位與通知權限，仍可手動選地區並在前景使用提醒功能。

---

## 3. 產品安全原則

1. **不製造錯誤安全感**：任何倒數只能代表「提醒期限」，不得代表歸零前不會曬傷。
2. **手動操作永遠可用**：定位、通知、相機與感測權限被拒絕時，不得阻斷核心流程。
3. **自動化只能保守**：感測或推測可建議提前提醒，但未經使用者確認不得延長提醒時間。
4. **事件優先於推測係數**：游泳、流汗、擦拭與摩擦使用明確事件規則，不使用未驗證的 0.7、0.8、1.2、1.3 等倍率。
5. **產品標示優先**：耐水 40／80 分鐘、使用方式與警語以實際產品標示為準。
6. **資料來源透明**：不得把測站觀測、區域預報、插值或快取資料混稱為「即時精準值」。
7. **健康資料最小化**：疾病、藥物與醫療療程不屬於 MVP 必填資料。
8. **通知是輔助**：通知採盡力送達；App 內顯示的狀態才是最終真值。

### 3.1 易用性原則

1. **核心任務優先**：無進行中 Session 時，首頁只保留一個視覺上最主要的 CTA：`開始防曬提醒`；已有 Session 時，首頁直接依目前 `primaryAction.actionKind` 顯示最高優先操作，例如 `記錄已補擦`、`回報狀況` 或相應的資料確認動作。完整部位、原因與事件仍由提醒頁承載。登入、產品管理與進階設定不得與核心 CTA 競爭。
2. **漸進式揭露**：先顯示目前最需要處理的部位、原因與動作，再讓使用者展開全部部位、產品標示與技術細節。
3. **系統記住，使用者確認**：可重用最近一次仍保存在本機或帳號中的設定，但不得重用先前的 `lastAppliedAt`，也不得在未確認今天實際外露部位、方法及新塗抹時間前建立 Session。
4. **批次優先、例外調整**：先讓使用者把同一防護方式套用至多個部位，再個別修改例外；不得要求每個部位從零重複填寫相同資料。
5. **權限即時詢問**：只有在使用者主動選擇目前位置、開啟通知、拍攝標籤或跨裝置同步時才要求相應權限或登入。
6. **不確定也是有效輸入**：產品標示、部位方法或事件範圍不確定時必須有可繼續的保守路徑，不得迫使使用者猜測。
7. **可理解且可更正**：畫面使用白話狀態、說明提醒原因，關鍵提交前提供摘要；提交後提供明確更正入口並保留事件稽核。
8. **手機優先、桌面適配**：先以 360–430 CSS px 寬的單手使用情境設計資訊層級、觸控目標與固定操作列；桌面版可擴充版面，但不得要求手機使用者縮放、橫向捲動或完成桌面專屬操作。
9. **簡潔且白話**：每個畫面只突出一個主要任務與一個主要按鈕；標題、狀態與按鈕使用日常用語，避免在一般畫面顯示 Session、CTA、schema、ruleset 等內部術語。必要說明先用一句話摘要，詳細內容由使用者主動展開。
10. **可預期的導覽**：點選底部導覽後留在對應頁面，不因目前沒有資料而自動跳到其他分頁；空白狀態須說明目前狀況並提供下一個可執行動作。
11. **使用者體驗高於搜尋曝光**：產品決策的固定優先順序為 `安全與正確性 → 使用者完成核心任務 → 易讀與無障礙 → SEO／AEO 可發現性 → 曝光與互動指標`。任何 SEO／AEO 做法若增加啟動提醒步驟、遮住主要 CTA、造成內容重複、干擾閱讀或降低健康資訊正確性，必須刪除或降級，不得以排名、AI 引用或停留時間為理由保留。
12. **P0 商業中立**：P0 不顯示廣告、業配、聯盟分潤購買連結、付費排名或偽裝成中立建議的產品推薦。「我的防曬品」只協助使用者保存與重用自己的產品標示，不推定品質優劣。未來若研究商業合作，須另做法務、利益揭露、健康宣稱、信任與易用性評估，不得直接混入提醒結果。
13. **狀態感知主要行動**：主要按鈕依目前最需要處理的狀態改變，不得在尚未記錄、防護方式不確定、未採取防護或只使用衣物／遮蔭時，仍一律把「已補擦」顯示為主要行動。若同層級部位需要不同動作，主要按鈕改為 `查看需要處理的部位`。
14. **沒有可信期限就不顯示倒數**：只有目前主要行動具有可信、非空且可解釋起點與終點的時間時，才顯示時間型提醒環。尚未記錄、防護方式不確定、未採取防護、只使用衣物／遮蔭或時鐘不可信時，以相同版位的非時間型行動卡取代，不得用假的 `00:00`、任意分鐘數或其他部位的較晚期限冒充主要狀態。

---

## 4. 功能範圍與優先級

### 4.1 P0 Core Beta：提醒互動與規則驗證

| 編號 | 功能 | 說明 |
| --- | --- | --- |
| F-01 | 訪客優先首頁 | 訪客模式為預設入口；無須註冊、登入或輸入 Email、電話、姓名、生日即可查看可用的 UVI、一般防護建議並啟動 App 內前景提醒。 |
| F-02 | 定位、手動地區與略過 | 定位需使用者主動同意；拒絕時可手動選擇縣市／地區，也可暫不提供地區並直接開始提醒。 |
| F-03 | CWA 觀測資料 | 經後端快取取得最近有效測站 UVI，顯示測站、距離、觀測時間與資料狀態。 |
| F-04 | UV 風險卡 | 顯示官方 UVI 分級、防護建議及「風險較低不等於零風險」提示。 |
| F-05 | 我的防曬裝備 | 使用者只在新增、複製、編輯已保存產品，或首次建立 Session-only 產品 snapshot 時確認包裝是否有防曬／SPF 標示，並可保存產品暱稱、SPF、PA、廣效宣稱、曝曬前等待方式、耐水標示、使用狀態與個人備註。同一個未修改的已保存產品不重複詢問產品身分；過去買過或已用完的產品可封存並僅供本人查看。**（2026-08-07 v3.10 擴充）** 本頁同時作為防曬裝備清單，支援 `sunscreen`、`clothing`、`eyewear`、`other_gear` 四個品類，並可保存購買月份、到期日、備忘與「過去用過」狀態。只有 `sunscreen` 參與補擦期限計算；`clothing` 為防護方式組成，覆蓋期間不倒數；`eyewear` 與 `other_gear` 為純紀錄，不影響任何提醒。介面必須明示哪些欄位會影響倒數，不得讓使用者以為記錄裝備會改變提醒行為。 |
| F-06 | 塗抹紀錄與部位狀態 | 記錄使用者自陳的塗抹時間與部位；每個追蹤部位各自保存最後紀錄與提醒期限，不把紀錄當成防護充分的證明。 |
| F-07 | 活動與事件 | 手動選擇室內／戶外／運動／水上活動，並回報游泳、流汗、擦拭、摩擦。 |
| F-08 | 補擦提醒引擎 | 依產品標示與事件產生保守提醒，不計算安全曝曬時間。 |
| F-09 | 前景計時與狀態恢復 | 以絕對時間戳計算；重新開啟頁面時可恢復，不以背景 interval 累減作真值。 |
| F-10 | 權限與錯誤降級 | 定位、網路、CWA 或 App 內提醒恢復失敗時提供替代流程與清楚狀態；P1 遠端通知錯誤由 F-15 驗收。 |
| F-11 | 基本離線降級 | 已載入後可查看 App Shell、最後資料與 App 內提醒狀態；明示資料可能過期。 |
| F-12 | 基本個資告知 | 在蒐集資料前分層告知目的、類別、接收者、期間、權利及不提供的影響。 |
| F-UX-01 | 低摩擦操作框架 | 提供最近設定重用、依情境自動套用的一鍵建議部位組合、追蹤部位常用組合、批次套用防護方式、相對時間快捷選項、進行中 Session 狀態感知快捷動作及情境式欄位；預設不展開完整原子部位清單，所有建議仍須在提交前確認區中由使用者確認後才能建立 Session。 |
| F-UX-03 | 戶外高對比顯示 | 預設提供適合陽光下閱讀的高對比淺色介面，並支援跟隨系統／日間高對比／夜間模式；重要狀態同時使用文字、圖示、邊框與色彩，不以綠色表示安全。 |

#### 4.1.1 P0 Content Minimum：安全使用所需內容

| 編號 | 功能 | 說明 |
| --- | --- | --- |
| F-CONTENT-01 | 最低內容治理 | P0 即建立健康宣稱登錄表、規則證據連結、來源與審查日期欄位及未通過審查不得發布的 gate；不要求先完成 P1 的完整知識庫管理介面。 |
| F-CONTENT-02 | 最低海邊防曬 Q&A | 提供經審查的下水前、離水後、擦拭、耐水標示及純椰子油核心問答；內容可由水上情境就近開啟，不得用問答內容建立或延長提醒期限。 |
| F-CONTENT-03 | 最低 PWA 與運行說明 | 提供 Android／iPhone 安裝方式、防曬晴報員運行原理、倒數原理及通知限制；不得宣稱關閉頁面、離線或系統限制下仍能保證送達。 |

#### 4.1.2 P0.5 Public Launch：公開內容與可發現性

P0.5 不阻擋封閉式 Core Beta；只有公開網站正式發布時才成為必要範圍。

| 編號 | 功能 | 說明 |
| --- | --- | --- |
| F-CONTENT-04 | 完整 UV知識庫 | 在 `更多` 提供獨立衛教專區，依生活問題整理防曬乳、衣物、帽子、眼鏡、陽傘、遮蔭、情境攻略及迷思破解；採短答案、行動建議與可展開原理的分層內容。 |
| F-19 | 物理防護指南 | 依 UVI 風險提供經審查的遮蔭、衣物、寬邊帽及 UVA／UVB 防護太陽眼鏡內容；屬一般衛教，不推算個人醫療風險。 |
| F-LAUNCH-01 | 公開 SEO／AEO 與 Schema | 完成公開內容資訊架構、索引邊界、Article／Breadcrumb 結構化資料、技術 SEO 與使用者優先的 AEO；不得增加核心提醒步驟。 |

### 4.2 P1：驗證核心價值後

| 編號 | 功能 | 說明 |
| --- | --- | --- |
| F-13 | 可選帳號與同步 | Guest 可升級帳號；登入提供使用者明確選擇的雲端備份、跨裝置、帳號歷史、資料匯出及已實作的遠端通知，不得把登入描述為會提高 UVI 或提醒公式準確性，也不得自動上傳本機資料。 |
| F-14 | 歷史紀錄 | 顯示塗抹與提醒事件；支援匯出與刪除。 |
| F-15 | 一種遠端通知管道 | P1 開發前以有紀錄的產品決策在 Web Push 與 LINE 中選定一種首發管道，完成端到端驗證；未選管道不進入本次 P1 驗收。若選 Web Push，可依使用者偏好請求系統通知聲或震動，但不得保證裝置一定響鈴、震動或準時送達。 |
| F-16 | LINE 帳號綁定 | 只有 LINE 被選為 P1 首發管道時才屬 P1 必做；正確處理 Login、Messaging API、Official Account、好友／可投遞狀態與解除綁定。未被選中時移至後續里程碑。 |
| F-17 | 鄉鎮白日時段 UVI 預報卡 | 使用 `F-D0047-091` 的逐 12 小時／白日時段 UVI；不畫逐時曲線，也不推薦分鐘級「最佳出門時間」。 |
| F-18 | 內容治理後台 | 在 P0 最低發布 gate 之上，提供健康宣稱的查找、版本、族群、限制、審查者、審查日期與到期再審管理介面；公開閱讀內容仍屬 P0.5。 |
| F-UX-02 | 易用性增強 | 提供可存取的人體部位圖、個人可調整的常用部位組合及更完整的操作歷史更正介面；若 P0 一鍵建議組合仍無法解決拒絕記錄部位者的放棄問題，再研究具明確限制的 `general_unlocalized` 整體提醒模式。 |

#### 4.2.1 F-17 提前實作決策（2026-07-30）

使用者已明確採用 `1A＋2A`，因此 F-17 的 Web 前端切片提前實作，規則固定如下：

1. 只提供 App 開啟期間的頁內晚間提示，不要求系統通知權限，也不建立 Web Push、NotificationJob 或關閉頁面後投遞承諾。
2. 晚間固定依裝置當地時間 `18:00～05:59` 判斷，不使用所在地日落時間。
3. 同一個跨午夜晚間區間最多顯示一次；使用者關閉後，到隔日 06:00 前不得再次顯示。
4. 五日卡取仍有效的下一個五個白日時段；白天可包含當日，晚間自然從下一個白日時段開始。
5. 只有已設定地區且有仍可使用的預報時才顯示數值與晚間提示。無地區、API 未接通、資料缺測或已過期時不得顯示假 UVI。
6. 五日 UVI 只用於環境風險預覽，不參與、延長或縮短逐部位補擦期限。
7. 前端 contract、IndexedDB 快照、controller 與畫面可先完成；正式資料仍必須經同源後端代理 `F-D0047-091`，CWA 授權碼不得進入瀏覽器 bundle。

### 4.3 P2：研究與有限平台增強

| 編號 | 功能 | 說明 |
| --- | --- | --- |
| F-20 | AI 標籤辨識 | 僅提出候選值與信心程度，使用者確認後才儲存；模型由後端可替換。 |
| F-23 | 前景情境建議 | 在支援且授權時，以定位／動作資料提出建議，必須由使用者確認。 |
| F-24 | 里程碑角標 | 支援平台上於「即將到期／已到期」等節點更新，不承諾逐分鐘倒數。 |

`F-21`（兒童模式）與 `F-22`（家庭／隊友模式）已由產品範圍移除，編號保留且不得重新指派給其他功能，避免舊版決策與追蹤資料混淆。

### 4.4 不列入 Web 驗收範圍

- 背景持續 GPS、geofence、自動辨識機車／捷運。
- 監聽一般耳機、車機或安全帽藍牙斷線。
- 關閉 PWA 後仍保證準時的離線本機通知。
- Siri App Intents、Google App Actions 的原生整合。
- Live Activities、持續鎖定畫面倒數與原生 Widget。

若商業需求必須包含上述能力，應另立 iOS／Android 原生 App 專案與驗收標準。

---

## 5. 核心使用流程

### 5.1 首次進入

1. 顯示產品用途與限制，不先要求登入、通知或相機權限。
2. 首頁 UV 資料卡提供 `使用目前位置`、`手動選擇地區` 與 `暫不提供地區`；只有點選 `使用目前位置` 才呼叫定位權限。點選 `暫不提供地區` 後不重複追問，並保留日後補設地區的次要入口。
3. 已取得地區時，UV 資料卡顯示 UVI、來源、資料時間、資料狀態及一則當下最重要的風險建議。未提供地區或資料不可用時，顯示「目前沒有所在地 UV 資料」，不得顯示 0、沿用過期值或阻斷提醒流程。
4. `開始防曬提醒` 不依賴定位、手動地區或有效 UVI。無進行中 Session 時，它始終是首頁唯一主要 CTA；已有 Session 時，首頁改為直接顯示目前 `primaryAction.actionKind` 對應的最高優先操作，並保留前往提醒頁查看完整狀態的次要入口。流程內再讓使用者記錄已擦防曬產品、被衣物完整遮住，或次要的其他外用產品情況；無法確認其中任一方式時可返回或離開，不建立 Session。
5. 使用者可從「我的防曬品」直接選擇目前使用產品、手動新增，或選擇 `這次先不保存產品`。不得依賴 AI 掃描。產品身分只在新增、複製、編輯已保存產品，或首次建立 Session-only snapshot 時確認；同一個未修改的已保存產品不重問，未知標示不得阻斷流程，系統改採保守提示。
6. P0 Guest 只提供不需系統通知權限的 App 內前景提醒，不要求瀏覽器通知權限。前景提醒固定有視覺狀態；短提示音與震動預設關閉，只有使用者在設定頁明確啟用並完成目前裝置測試後才嘗試播放／震動。能力不支援、被瀏覽器阻擋或裝置設定不允許時安靜降級，不得阻斷核心提醒。只有 P1 遠端管道已實作、使用者看過登入與資料上傳範圍並完成帳號 Session 建立／遷移後，才依所選管道提供 Web Push 或 LINE 啟用流程；拒絕或不支援時仍可使用前景提醒。

### 5.2 啟動提醒

啟動流程提供 `沿用最近設定` 與 `快速開始` 兩條路徑；`快速開始` 再以 `快速提醒（推薦）` 為預設，`自己選擇部位` 為次要選項。兩條路徑均不得沿用舊塗抹時間，也不得要求先登入、建立完整產品檔案或提供地區。

1. 若有最近一次仍可用的設定，先以部位大群組顯示情境、防護方式與產品摘要，提供 `沿用並確認` 及 `修改設定`。使用者仍須確認本次追蹤部位與目前方法；只有實際使用防曬或其他外用產品的部位才要求確認本次塗抹時間。不得用任何舊塗抹時間直接建立 Session。
2. 首次使用或選擇修改時，以**兩個簡短畫面**快速開始：`情境` → `快速提醒／塗抹時間／開始提醒`（2026-08-07 v3.10 修訂，原為三畫面並含獨立的 `最終確認` 畫面）。進入第二個畫面時，系統依使用者已主動選擇的情境，將一組版本化常見追蹤部位自動寫入 SetupDraft，並以白話摘要清楚顯示；只有點選 `調整追蹤部位或防護方式` 才以 Bottom Sheet 展開進階設定。自動套用只代表設定草稿的預設值，不代表使用者已確認塗抹、不建立 Application 或 Session；**使用者必須在按下 `開始提醒` 之前，於同一畫面看過部位、方法、產品與時間的完整摘要**，按下後才成為真值。確認的實質要求不因併頁而降低：摘要必須在按鈕上方揭露，不得摺疊隱藏，也不得預設送出。第二個畫面只詢問本次實際塗抹時間；包裝是否有防曬／SPF 標示、曝曬前等待、一般補擦與耐水標示統一在產品頁管理，Setup 引用當下保存的 current-product snapshot，不重複詢問。全部改為衣物完整覆蓋時跳過產品與時間輸入，直接顯示摘要與 `開始提醒`。若所選部位沒有任何一項已確認的方法，顯示 `請先選擇目前已採用的方式`、`返回修改` 與 `先離開`，不得建立空 Session、依定位／UVI／未確認歷史代選，或要求使用者填寫不符合現況的答案。每個畫面只顯示建立當次有效狀態所必要的欄位，返回上一步時保留已輸入內容。
3. 選擇目前情境：室內遠離直射、室內近直射窗邊、一般戶外、戶外運動、水上活動。選擇水上活動時再區分 `準備下水` 與 `已在水中`；前者只調整欄位與提示，不得提前建立耐水期限。後者須確認受影響部位，並可選擇實際入水時間或 `不確定`；不得用提交當下時間冒充未知的實際入水時間。
4. `快速提醒（推薦）` 依使用者選定的情境自動套用一個 `BODY_ZONE_PRESET_V3` 建議組合，以「一般外出建議先追蹤：臉部、耳朵、頸部、手背」等大群組摘要呈現，主要流程直接顯示時間選擇，並提供次要按鈕 `調整追蹤部位或防護方式`。系統可將 preset 展開為 `BODY_ZONE_V3` 原子部位並寫入 SetupDraft，但不得建立 Application／Session、宣稱使用者已塗抹，或省略 S-05 提交前確認區的完整摘要。選擇自行調整時，才以 `BODY_ZONE_GROUP_V3` 選擇外露皮膚及希望記錄為衣物覆蓋的部位。預設畫面不直接展開額頭、鼻頰、前頸與後頸等所有原子代碼；只有群組內存在不同方法、產品、時間或狀態時才展開細部差異。耳朵與手背保持可獨立選取。
5. 「這個部位目前怎麼防護？」只提供兩個主要選項：`已擦防曬產品`、`被衣物完整遮住`；`其他外用產品` 放在次要的 `其他情況`，不與主要選項競爭。正式設定介面不顯示 `尚未採取防護` 或 `不確定`，也不在第一層另列語義模糊的 `兩者都有`。選擇 `被衣物完整遮住` 後，才以不阻斷流程的選填項目詢問 `衣物下方也有擦防曬產品`。只有布料、帽子或其他穿戴物完整遮住該 zone 時才可選衣物覆蓋；雨傘、建築或樹蔭屬 Session 環境，不能代替此選項。若同一 UI 大群組只有部分被衣物遮住，應引導 `調整部位` 分開記錄；使用者不調整時不得把整個群組標成衣物覆蓋，也不得建立該群組的提醒。底層仍以 `skinExposureStatus` 與集合型 `methodComponents[]` 保存實際組成；批次操作不得未提示便覆蓋個別修改。
6. 若任一部位使用防曬產品，先顯示「目前使用」與最近產品，再提供 `這次先不保存產品` 或新增產品。已保存且未修改的產品只詢問「這次用了哪一款」，不得再次詢問是否有 SPF 標示。Session-only 產品可令 `productId=null`，但必須保存經使用者確認的 `productLabelSnapshot`；一般戶外最低只確認包裝是否明確有防曬／SPF 標示、曝曬前等待方式及實際塗抹時間，只有水上情境才優先詢問耐水 40／80 分鐘或未知。Session 建立後才以非阻斷提示詢問是否保存至「防曬品」，略過不得影響目前提醒。
7. 新增、複製、編輯或選擇 Session-only 產品時，不得從暱稱、品牌、「天然」或成分名稱自動判定產品身分。回答「沒有」或「不確定／看不清楚」可保存為私人非防曬／未知紀錄，也可建立 `ruleEligibilityAtApplication` 不合格的使用事件；相關部位保留 `other_topical` 或 `unknown` 的實際方法並顯示無時間行動卡，不得取得 120／40／80 分鐘期限。PA 與 broad-spectrum 分開呈現，不同部位可以使用不同產品。
8. 只有在目前情境或事件涉及水上活動時，才優先展開耐水標示；其他進階標示預設收合但可隨時查看。未知值必須可提交並觸發保守規則。會改變當次規則的曝曬前等待標示、一般補擦數字指示、目前水上活動的耐水標示或未知狀態，必須在提交前確認區展開顯示。`準備下水`、`已在水中`、耐水標示未知及水上事件畫面須提供不阻斷流程的 `海邊防曬常見問題` 入口；開啟或關閉問答不得修改 Session、產品或事件資料。
9. 只有記錄實際防曬或其他外用產品的部位才詢問使用者自陳的新實際塗抹時間及適用部位；只選 `被衣物完整遮住` 時不要求時間。衣物下方也有擦防曬者可選擇記錄產品與原始塗抹時間，但衣物仍完整覆蓋期間不顯示補擦倒數。快捷選項至少包含 `剛剛`、`15 分鐘前`、`30 分鐘前`、`1 小時前`、`自訂時間`；相對選項以第 8.1 節的 `trustedCurrentTime` 換算並同時顯示絕對時間。`剛剛` 可以視覺預選，但必須由使用者提交確認；`CLOCK_UNTRUSTED` 時線上須先成功重新校準，離線只能記錄不晚於目前保守時間的過去事件，不得用未校準裝置時間延長期限。
10. 提交前確認區（S-05 `開始提醒` 按鈕上方）先以大群組顯示本次情境、追蹤部位、各部位方法、產品，以及只屬於實際外用產品部位的新塗抹時間；群組內有差異時展開原子部位。過期／異常產品、產品標示無法確認或只使用非防曬外用產品的部位須以顯眼文字列出，不得藏在收合區內。摘要可壓縮為單行呈現，但必須在按鈕上方可見，不得摺疊隱藏；顯眼警示排在確認區最前，沒有警示時不佔版位。產品身分未確認而不會建立倒數時，主要按鈕須明示 `開始提醒（不建立倒數）`。
11. 若產品標示要求在曝曬前等待，顯示剩餘標示等待時間；等待期間只顯示「已記錄，請依產品標示等待並搭配遮蔽」，不得顯示防護已生效。等待結束也不得宣稱一定安全。
12. 使用者提交確認後，以同一 `StartSessionCommandV1` 原子建立含 `initialContext` 的 `SessionStartedEvent`、每個追蹤部位的 `ZoneMethodEvent`，並只在至少一個部位記錄實際外用產品時建立必要的 `ApplicationConfirmationGroup`／`ApplicationEvent`，再衍生 `ProtectionZoneState` 與 Session 彙總；全為衣物覆蓋時，Application group／events 必須為空。P0 新設定流程不得提交 `methodCertainty=none_reported` 或 `unknown`；任一步驗證失敗時不得留下空 Session 或部分事件。若選擇 `已在水中`，同一操作另建立具新 `activityIntervalId` 及非空 `zoneInstanceIds[]` 的 `water_start`。已知入水時間時保存可信的 `activityStartedAt`；不確定時保存 `startTimeConfidence=unknown`、不建立 40／80 分鐘期限，相關部位立即進入保守注意狀態。
13. 顯示各部位的實際方式與距離上次記錄塗抹；只有目前外露且具合格防曬 Application 的部位可顯示最早建議檢查／補擦時間及「不是安全曝曬倒數」。衣物完整覆蓋顯示「已記錄衣物遮住，不計算補擦時間」及 `更新衣物狀態`，不得把 UPF、布料種類或覆蓋時間換算成可曝曬分鐘數。其他沒有期限的情形顯示具體的無時間狀態與下一步，不得補上任意時間。

#### 5.2.1 不想逐一選擇部位的低摩擦路徑

P0 的首要解法是「不要求逐項選，但仍讓使用者確認一組可理解的部位」，而不是移除部位真值：

1. 入口先顯示 `快速提醒（推薦）` 與 `自己選擇部位`；不得先攤開完整人體部位清單。
2. `快速提醒` 只根據使用者已選情境提出一個版本化建議組合，不依定位、UVI、帳號資料或未確認的歷史推測。建議卡以大群組顯示，不顯示內部代碼。
3. 建議卡顯示已自動套用的大群組摘要與 `調整部位`；進入本頁即可把 preset 展開為 SetupDraft 的 zone instances，但不得建立方法事件、Application 或 Session。
4. SetupDraft 仍依既有 `BODY_ZONE_V3` 展開成原子部位，使最終提交後的局部補擦、洗手、摩擦及水上事件可正確作用；介面不要求使用者看到展開細節，除非群組內有差異。
5. 若使用者返回或取消，不得把建議組合保存為其個人偏好；只有經 S-05 提交前確認並成功提交的 Session 可成為日後 `沿用最近設定` 的來源。

若易用性測試證實仍有一群使用者因「完全不願記錄任何部位」而放棄，可在 P1 研究 `general_unlocalized` 整體提醒模式，流程只詢問情境、防護方式、適用時的產品與實際塗抹時間。此模式必須遵守：

- 首屏固定標示 `整體提醒` 與「尚未設定個別部位，因此無法指出是哪個部位需要處理」；通知及完成訊息也不得冒用部位名稱。
- 洗手、擦拭、摩擦或游泳只能顯示一般保守提示，例如「手背可能受洗手影響」，不得暗中建立手背狀態或修改不存在的逐部位期限；可提供 `從現在開始追蹤手背` 等明確轉換入口。
- `general_unlocalized` 必須使用獨立、版本化的 reminder scope／state／event schema；不得新增假 `bodyZoneCode=general`、空 `zoneInstanceIds[]` 或把整體狀態塞入 `ProtectionZoneState`。
- 在新的 command、reducer、通知 scope、事件更正、遷移及驗收案例完成前，不得把 `我不想記錄部位` 顯示成可提交的正式功能。P0 只上線一鍵建議組合，並以實測放棄率判斷是否值得開發整體模式。

新建立的 Session 使用 `BODY_ZONE_V3`；既有 V2 Session 依原版本完成，不在進行中靜默遷移。V3 必須前後端共用並版本化：

```text
face_forehead
face_nose_cheeks
face_lower
ears
lips
scalp
neck_front
neck_back
shoulders
torso_front
torso_back
hand_backs
arms
legs
feet
custom
```

`custom` 使用獨立 `zoneInstanceId`，同一 Session 可建立多個自訂部位；選填顯示標籤不得作分析維度、寫入 URL、第三方分析或遠端通知。通知只能顯示「其他部位」，不能傳送自訂文字。輸入需長度限制與安全編碼。未來新增部位不得改變舊版本代碼的意義。

`BODY_ZONE_GROUP_V3` 只作介面選取與摘要，不取代原子部位狀態：

| UI 大群組 | 展開後的 `BODY_ZONE_V3` |
| --- | --- |
| 臉部 | `face_forehead`, `face_nose_cheeks`, `face_lower` |
| 耳朵 | `ears` |
| 頸部 | `neck_front`, `neck_back` |
| 手臂 | `arms` |
| 手背 | `hand_backs` |
| 身體 | `shoulders`, `torso_front`, `torso_back` |
| 腿部 | `legs` |
| 腳背／外露腳部 | `feet` |
| 頭皮 | `scalp` |
| 嘴唇 | `lips` |
| 其他 | 一個或多個 `custom` instance |

P0 常見組合使用版本化的 `BODY_ZONE_PRESET_V3`：

| UI 顯示名稱 | preset key | 展開內容 |
| --- | --- | --- |
| 臉／耳／頸（不含頭皮與嘴唇） | `face_ears_neck` | 臉部、耳朵、頸部 |
| 通勤常見追蹤部位 | `commute_tracked` | 臉部、耳朵、頸部、手臂、手背 |
| 臉頸＋手腳 | `broad_tracked` | `commute_tracked` 加上腿部、腳背／外露腳部 |
| 海邊常見部位 | `beach_tracked` | 臉部、耳朵、頸部、肩膀／身體、手臂、手背、腿部、腳背／外露腳部 |
| 自行選擇 | `custom_selection` | 無預選 |

套用後先顯示白話大群組，使用者可取消任一群組並在需要時展開原子部位。群組摘要取其中最需要注意的狀態；若有無期限注意狀態，不得只顯示較晚數字期限。頭皮與嘴唇不得因選擇臉部而自動加入。`hand_backs` 僅指手背，不含手掌；洗手快捷只預選手背。舊 V2 最近設定可轉成 V3 群組摘要供新 Session 確認，但群組內有不同產品、方法或時間時不得靜默合併。更改 group／preset mapping 必須增加版本。

### 5.3 活動事件

使用者可隨時記錄：

- 開始／結束戶外活動。
- 游泳或持續水中活動。
- 大量流汗。
- 擦毛巾。
- 口罩、衣物或裝備造成明顯摩擦。
- 洗手，預設只影響手背，提交前可取消或調整。
- 已記錄補擦所有所選部位。
- 已記錄局部補擦，並選擇實際部位。
- 某部位由外露改為衣物覆蓋、由覆蓋改為外露，或防護方法改變。
- 使用產品後感到不適；此為產品安全入口，不是症狀診斷事件。

每個事件均分開保存使用者確認的實際發生時間、裝置建立時間及適用時的伺服器接收時間。局部補擦只更新所選部位，不以「全身 60%」換算。

`water_start` 預設勾選本次所有水中外露部位，提交前必須讓使用者確認／修改；API 不接受空的 `zoneInstanceIds[]`。目前水上情境是 Session 全域狀態，同一 Session 最多只有一個未關閉的水上區間；對應的 `water_end` 必須沿用起點的完整部位集合，若起點集合選錯，只能以配對更正同時修正起點與終點。只有方法含防曬產品且耐水標示有效的部位能建立 40／80 分鐘期限，其他受影響部位顯示相應的保守提示。大量流汗、擦拭與摩擦事件則由使用者明確確認受影響部位。`hand_wash` 預設只勾選目前外露的 `hand_backs`；若該部位記錄為使用防曬產品，事件後立即顯示檢查／補擦提示，不影響臉、耳、頸或其他部位期限。

進行中 Session 在主要狀態元件與原因摘要後立即提供狀態感知操作，不放在完整內容底部，也不另設重複固定操作列。點選 `記錄已補擦` 時可預先勾選已到期或即將到期的部位，並預填這些部位目前產品與 `剛剛`；使用者仍須確認或修改實際部位、產品及時間。不同產品或產品標示 snapshot 必須以同一 `applicationConfirmationId` 分成一筆或多筆 `ApplicationEvent`；同組事件的 `zoneInstanceIds[]` 必須彼此互斥，且每個最後確認部位恰好出現一次。現行模型不支援同一部位同次疊擦多項產品；若需要，須另立多產品部位模型，不得依事件順序猜測主要產品。點選 `回報狀況` 後，先以游泳／下水、離水、大量流汗、擦毛巾、明顯摩擦、洗手、進入室內、返回戶外等白話選項呈現，再確認受影響部位。

所有快捷事件均使用第 8.1 節的可信時間。`water_start`、`water_end`、大量流汗、擦拭、摩擦及室內外切換不得直接以未校準的 `Date.now()` 作為事件真值；`CLOCK_UNTRUSTED` 時線上須先成功重新校準，離線則維持較短的保守狀態。單純按下確認不得把不可信時鐘改成可信。

事件或補擦提交後顯示時間與部位摘要並提供 `更正` 入口。P0 至少支援更正目前 Session 最近提交的事件；P1 歷史介面再提供較完整的歷史事件查找與更正。更正不得刪除不可變資料：補擦確認使用 `correctionOfGroupId`，部位方法與情境事件使用 `correctionOfEventId`，並依第 14.1 節重建衍生狀態。

`使用產品後感到不適` 不要求症狀名稱、照片、病史、用藥或自由文字。使用者確認產品後，系統將產品移至 `停止使用`、停止提示相關部位補擦同一產品、取消尚未投遞的同產品通知，並顯示經醫療內容審查的「停止使用、依包裝警語處理、需要時尋求醫療協助」文字，以及改選其他已確認產品或衣物覆蓋的入口。此操作不得產生過敏、疾病或治療判定，也不得送入第三方分析、通知內容、AI 或其他非必要服務；若帳號版由使用者另外明確選擇同步粗粒度產品安全狀態，必要受託處理者、欄位、目的與保存期間必須列入個資告知。若使用者同時選擇急症情境，改由第 5.6 節急症流程優先。

### 5.4 室內外切換

- 切換至遠離直射陽光的室內時，可抑制不必要的遠端通知，但 App 內期限與狀態仍持續計算，不得修改各部位原始 `lastAppliedAt` 或延長 `zoneDueAt`。
- 再次切換到戶外時，若任一外露部位尚未記錄塗抹或已達提醒門檻，立即提示相應部位。
- 靠近陽光直射窗邊時，只提供 UVA 可能穿透的情境提示，不使用固定 1.2 倍衰減或「室內安全四小時」。

### 5.5 訪客模式與升級帳號

1. 預設進入 `訪客模式`；首頁以 `開始防曬提醒` 為主要 CTA，`登入／建立帳號` 置於次要層級，不得打斷第一次操作。
2. 訪客模式不得要求 Email、電話、姓名、生日、性別、精確位置或支付資料；地區可手動選擇，定位與通知均為可選權限。
3. 訪客可使用 UVI、手動地區、我的防曬品、本機 Session、前景提醒、產品與事件的本機查看／清除等核心功能。
4. 只有在使用者已完成一次核心任務，或主動需要跨裝置同步、資料備份、帳號歷史、資料匯出或 P1 遠端通知時，才就近顯示登入的具體功能價值與資料上傳範圍；不得用籠統的「解鎖完整功能」或健康風險施壓。
5. 使用者完成登入後，讓其明確選擇是否合併本機資料；不得自動上傳。若帳號已有進行中 Session，依第 8.2 節 `ACTIVE_SESSION_CONFLICT` 流程讓使用者選擇；不得靜默覆寫或合併兩個 Session 的事件。

#### 5.5.1 訪客與登入功能差異

登入與否不得改變 CWA UVI、提醒公式、期限資格、醫療轉介或一般衛教內容；登入增加的是保存、同步與投遞能力，不是更高的防護準確性。

| 能力 | 訪客模式 | 登入模式（P1 相應功能已上線後） |
| --- | --- | --- |
| 開始使用 | 不需註冊，立即開始 | 使用當次已實作的快速登入方式 |
| UVI 與一般防護資訊 | 可使用 | 可使用，內容與訪客相同 |
| App 開啟時的提醒 | 可使用 | 可使用，公式與訪客相同 |
| 安裝 PWA | 可安裝 | 可安裝 |
| 防曬品、Session 與事件 | 只保存在目前裝置 | 先由使用者選擇，再同步至帳號 |
| 清除網站資料、解除安裝或換裝置 | 可能遺失本機資料；可先自行匯出檔案備份 | 已成功同步的資料可依保存政策恢復 |
| 跨裝置 | 不支援；匯出檔案可由使用者自行保管與攜帶 | 支援已同步資料及衝突處理 |
| 關閉頁面後的遠端通知 | 不提供；重新開啟後恢復 App 內狀態 | 完成 Session 遷移、管道設定與權限後，可使用已上線管道的盡力送達通知 |
| 帳號歷史與自助匯出 | 不提供帳號功能；可查看、清除及**匯出**本機資料 | 可提供帳號歷史、自助匯出、刪除及法定權利入口 |
| 資料最小化 | 不要求帳號個資，資料主要留在裝置 | 需處理登入識別及使用者選擇同步的資料，須先告知接收者、期間與用途 |

訪客模式的主要價值是「最快開始、最少個資、先體驗核心功能」；登入模式的主要價值是「備份已選資料、跨裝置、歷史／匯出及已實作的遠端通知」。所有登入價值文案都必須連到實際已上線能力；功能尚未上線時顯示 `即將推出` 或不顯示，不得以未完成能力誘導登入。

**本機匯出（2026-08-07 v3.10 納入 P0）**：訪客可將本機產品、Session、事件與偏好匯出為單一檔案自行保管。P0 不做帳號，使用者問卷同時存在「不想註冊直接使用」與「想保留防曬產品資訊」兩種需求；匯出是在不引入帳號體系的前提下，唯一能讓前者也有備份手段的做法。規則：

- 匯出由使用者主動觸發，不自動上傳、不經後端、不進分析。
- 檔案內容限於使用者自己輸入或系統為其建立的本機資料，不含金鑰、精確座標或裝置識別碼。
- 匯出入口與清除入口同置於本機資料管理頁；清除前必須提示可先匯出。
- 匯入不在 P0 範圍。P0 只保證匯出檔案格式版本化且可讀，恢復能力留待 P1 與帳號遷移政策一併設計。
- 不得把匯出描述為「備份到雲端」或暗示跨裝置同步。

#### 5.5.2 鼓勵登入的時機與文案

1. **首次進入**：`開始防曬提醒`／`免登入，立即開始` 保持主要 CTA，`快速登入並同步資料` 為次要入口，旁邊只列出當次已上線的兩至三項價值。
2. **第一次成功建立提醒後**：若 P1 遠端管道已上線，可顯示非阻斷卡片：「想在關閉頁面後也接收提醒嗎？登入並完成通知設定後，可啟用支援的遠端提醒。」提供 `快速登入並設定`、`先不用`。
3. **保存防曬品後**：顯示一次性提示：「目前只保存在這台裝置。登入後可選擇備份，避免清除網站資料或換手機後遺失。」提供 `登入並選擇備份內容`、`繼續只存在本機`。
4. **主動要求帳號能力時**：使用者點選跨裝置、帳號歷史、匯出或遠端通知時，才將登入設為完成該任務的主要 CTA，並先顯示必要資料與限制。
5. **頻率限制**：使用者點選 `先不用` 後，同一價值提示至少在該 Session 內不再出現；不得每頁重複、遮住提醒主要行動、使用倒數壓力或將關閉按鈕弱化。
6. **不得暗示健康優勢**：禁止使用「登入後提醒更準」「登入才不會曬傷」「未登入可能錯過安全時間」等文案；訪客與登入必須使用同一 ruleset 與 reducer。
7. **登入前預覽遷移內容**：至少分列防曬品基本標示、目前 Session、過去事件、私人備註與粗粒度產品安全狀態。私人備註、產品安全狀態、SetupDraft 及一次性特殊狀況預設不勾選；只有規格允許且使用者另行明確選擇時才同步。

### 5.6 一次性特殊狀況入口

「我有特殊狀況」常駐入口放在底部導覽的 `更多` 頁，不放在首頁首屏。提醒設定與進行中提醒若出現可能相關的情境，可在當下畫面提供次要的就近入口。選擇只用於當下顯示對應內容，預設不保存、不上傳，也不改變數字權重：

1. **有醫師避光指示、光敏感疾病、藥物或近期療程**：在該特殊狀況頁不套用一般提醒推論，提示遵循醫師、藥袋或療程院所指示。
2. **皮膚已破損、明顯起泡或嚴重曬傷**：不把一般補擦流程套用到破損處，顯示由醫療專業人員審核的照護與就醫提示。
3. **可能的嚴重全身性反應或熱疾病紅旗**：只有命中有效醫療審查版本的 `redFlagCodes` 才顯示獨立急症流程；可能納入的例子如體溫持續上升、呼吸困難或意識持續不清，確切條件、台灣 119／就醫文字及分流均須在上線前由醫療專業人員核准，工程與內容人員不得自行擴寫。

不得把以上三類狀況合併成同一個「縮短提醒時間」或籠統就醫訊息。此入口不得要求使用者輸入疾病名稱、病理報告、處方、照片或病史。由於選擇預設不保存，它只控制當下特殊狀況頁的內容，不得暗中建立永久 `safety_hold` 或修改 Session；使用者可另行明確選擇 `結束本次提醒`。一般轉介文字為「本服務不提供診斷或治療建議；請依您的醫囑，或詢問醫師／原療程院所」。

急症流程優先於上述一般轉介與通用免責。分流順序固定為：先比對當次有效醫療審查版本的 `redFlagCodes`；命中時走 AC-99，未命中才走 AC-56 的一般拒答／醫師轉介。當使用者選擇符合有效醫療審查版本的急症情境時，第一個主要行動必須是核准的緊急就醫／台灣 119 指示；`開始提醒`、`記錄已補擦` 或產品選擇不得成為主要 CTA。通用免責只能放在緊急行動之後。最低必要急症文字必須隨 PWA 靜態內容保存，離線時仍能顯示；紅旗條件、電話與分流文字不得由工程人員自行補寫，必須綁定有效醫療審查版本。

若使用者主動詢問「我這個症狀／疾病能不能曬太陽、能不能使用某產品、應等多久」等個別醫療問題，系統不得根據症狀、照片或自述作判定；僅可說明本服務無法判斷，並建議依醫囑或向醫師、皮膚科醫師或原療程院所詢問。不得因使用者未回報疾病而暗示其適合一般提醒規則。

### 5.7 介面資訊架構與互動規格

#### 5.7.1 首頁

- 首屏依序呈現：UVI 數值與官方分級、觀測／預報標籤、資料時間與地區、最重要的一則防護建議、單一主要 CTA。
- 無 Session 時主要 CTA 為 `開始防曬提醒`；有進行中 Session 時直接依 `primaryAction.actionKind` 顯示目前最高優先操作，並以次要入口前往提醒頁查看完整部位、原因與事件。`手動選地區`、登入與設定採次要層級；`我有特殊狀況` 不放在首頁首屏，改由 `更多` 常駐提供。
- 未提供地區或無法取得 UVI 時顯示「目前沒有所在地 UV 資料」，仍保留 `開始防曬提醒`、手動地區與日後使用定位的入口，不得用舊資料或 0 假裝目前值。

#### 5.7.2 進行中 Session

進行中頁面的第一個元件統稱為 `主要狀態元件`，不一律假設為倒數圓環。它依 `primaryAction` 採三種呈現：

1. **時間型提醒環**：最高優先的可操作部位具有可信、非空的 `zoneDueAt`，狀態為 `TRACKING` 或 `REAPPLY_SOON`，且能說明進度起點與終點。
2. **到期狀態卡**：最高優先部位為 `REAPPLY_DUE`。顯示「建議現在補擦」、部位、原因及動作，不顯示持續停在 `00:00` 的圓環。
3. **非時間型卡片**：最高優先部位為 `UNRECORDED`、`unknown`、`none_reported`、產品不具提醒資格或 `CLOCK_UNTRUSTED` 時，顯示需要處理的行動卡；只有衣物覆蓋且狀態已確認時，改顯示中性的「已記錄衣物覆蓋」狀態卡，不得列為錯誤或需要處理。兩者都不產生假的剩餘時間。

- 手機頁面由上到下固定為：`主要狀態元件` → `目前狀態與原因` → `狀態感知主要／次要動作` → `需要處理的部位` → `其他部位` → `最近事件時間軸`。不得要求先捲過完整部位清單或歷史才能操作。
- 時間型提醒環中央同時顯示白話相對時間及絕對時間，例如「約 1 小時 15 分後／15:40」，旁邊固定顯示「這是補擦提醒，不是安全曝曬時間」。圓環不是防護剩餘量、有效度或安全百分比。
- 圓環的起點必須是目前期限來源的可信 anchor，例如一般期限的 `appliedAt` 或水上期限的 `activityStartedAt`；終點為該部位目前有效的 `zoneDueAt`。立即事件、未知起點或無法定義 anchor 時改用靜態卡。主要部位切換時依新部位的起訖重新繪製並標示部位，不以動畫接續舊部位進度。
- 主要狀態元件下先顯示目前狀態、受影響部位、原因及建議動作。例如：「手背建議現在補擦；原因：剛才記錄洗手。」使用者必須先看得到原因，再看到提交操作。
- 狀態摘要後依第 5.7.5 節顯示一個實心主要按鈕與至多一個次要按鈕；主要按鈕不得固定為「已補擦」。預選只能減少操作，任何動作都不得在未確認部位、產品、狀況與時間前修改期限。
- 其他部位預設收合，但 `尚未記錄防護`、`尚未採取防護`、`不確定` 或 `REAPPLY_DUE` 不得被收合到使用者看不見；Session 彙總仍以最需要注意的部位為準。
- 各部位使用大群組摘要、文字卡或可存取的人體部位圖顯示 `提醒進行中`、`即將需要檢查`、`建議現在補擦`、`尚未記錄`、`衣物覆蓋` 或 `不確定`；不得只靠顏色。群組內有差異時才展開原子部位，人體圖不是唯一入口。
- 最近事件使用水平或垂直時間軸顯示塗抹、下水、離水、流汗、擦毛巾、摩擦、洗手及補擦，標示事件時間與受影響部位，讓使用者理解狀態為何改變。時間軸不得把推測事件畫成已發生事實。
- 若有提醒時間，同時顯示相對時間與絕對時間，並提供觸發原因；不得只顯示沒有語義的秒數倒數。視覺可依分鐘更新，避免不必要的每秒動畫、閃爍或焦慮。
- 到期後不得顯示負數、持續閃爍的 `00:00` 或「時間用完」；改用到期狀態卡。一般追蹤、即將到期與已到期可分別使用藍紫、琥珀、珊瑚紅等視覺層級，但不得使用綠色表示安全，也不得讓顏色成為唯一線索。
- 不提供一般計時器的 `暫停`、`增加時間`、`延長` 或未確認的 `重新開始`。進入室內只影響不必要通知，不改寫塗抹時間；只有使用者完成 `記錄已補擦` 流程並確認部位、產品與實際時間後才能建立新期限。
- 頁面不重複放置另一組意義相同的固定操作列；手機底部已保留主導覽，避免雙層固定按鈕遮住內容或造成誤觸。
- 畫面可用固定文字說明「這是補擦提醒，不是安全曝曬時間」，但不能依賴免責文字抵銷誤導性的顏色、圖示或文案。

##### 5.7.2.1 逐部位計時的簡化呈現

P0 正式保留每個原子部位分開記錄與計時，但採用「底層分開計時、首屏集中行動、大群組摘要」原則。每個追蹤部位均可有自己的 `lastAppliedAt`、`zoneDueAt`、`zoneNextActionAt`、狀態與原因；首屏不得因此出現多個同等醒目的大型倒數圓環。

1. **主要行動與數字期限分離**：`sessionNextDueAt` 只是所有非空補擦期限的最小值，不等同 `primaryAction`。沒有期限的注意狀態若優先度較高，首屏顯示該行動卡；較晚數字只放在其他部位摘要。只有 `primaryAction` 為可信時間型狀態時顯示一個提醒環。
2. **最需要處理的部位排序**：有效產品安全事件與 `CLOCK_UNTRUSTED` 優先，其次為 `尚未採取防護／不確定／尚未記錄`、未知水上起點或未解除的一般事件原因、`REAPPLY_DUE`、`LABEL_WAIT`、`REAPPLY_SOON`、`TRACKING`，最後為衣物覆蓋且無產品計時。相同層級先依 `primaryAction.actionAtNullable` 排序，有時間者依較早時間優先、null 排在有時間值之後，再依 `BODY_ZONE_V3` 穩定顯示順序；此排序只決定介面下一步，不稱為醫療風險排名。
3. **其他部位分成三組**：使用者可展開 `需要處理`、`即將需要處理`、`其他狀態`。需要處理的部位必須展開並留在首屏主要操作附近；後兩組可收合。衣物覆蓋、沒有產品計時但不需採取行動的部位放入中性的 `其他狀態`，不得暗示稍後一定要處理。有可信期限時，每列顯示部位、白話狀態、相對／絕對時間及最近變更原因；沒有期限時改顯示原因與適用的下一步，不留空也不補假時間。各列均不顯示每秒更新的獨立圓環。
4. **批次操作、局部更新**：點擊 `記錄已補擦` 時，預選目前到期或即將到期部位，提供 `只選建議部位`、`選擇所有本次追蹤部位` 與 `自行調整`；送出前必須顯示最終部位、產品與實際時間。只有最後確認的部位建立 `ApplicationEvent`；其中只有當下具期限資格者重新計時，其他顯示相應無時間狀態。未選部位完全不變。
5. **事件預選合理範圍**：洗手預選手背；游泳／下水預選本次水中外露部位；毛巾擦拭、摩擦與大量流汗要求確認實際受影響部位。預選是減少操作，不是系統代替使用者判定，提交前都可取消或修改。
6. **不預設拆分左右側**：P0 以 `BODY_ZONE_V3` 既有原子部位為單位，不將左右手、左右腿或臉部左右側新增為必填計時器。若研究證明左右側具有穩定任務價值，再以新版 schema 與遷移規則處理。
7. **沿用設定但不沿用時間**：再次開始時，可沿用最近的追蹤部位、方法與產品組合；只有實際外用產品部位須重新確認本次塗抹時間，且一律不得沿用舊時間。第一次使用優先提供大群組與常見組合，避免逐項建立計時器。
8. **完成後提供可更正回饋**：局部補擦或事件提交成功後顯示「已更新：手背，14:20」等摘要及 `更正`；不得只顯示籠統成功訊息，避免使用者誤以為所有部位都已重新計時。

手機首屏的概念順序：

```text
到期狀態卡
手背建議現在補擦
原因：剛才記錄洗手

[記錄已補擦]  [回報狀況]

需要處理
・手背　建議現在檢查

其他部位（可展開）
・臉部、頸部　約 1 小時 12 分後
・手臂　　　　約 1 小時 35 分後
```

上述分鐘數僅為版面示意，不是固定產品規則或安全曝曬時間。實際畫面必須由第 6 節規則與有效事件計算，不得把示意值寫死。

若最高優先部位沒有可信期限，概念順序改為：

```text
非時間型行動卡
耳朵尚未記錄防護方式
下一步：補上防護紀錄

[補上防護紀錄]  [回報狀況]

其他部位（可展開）
・臉部　約 40 分鐘後
```

##### 5.7.2.2 通知與 Session 控制

`暫停通知`、`關閉通知` 與 `結束本次提醒` 是不同操作：

1. **暫停通知**：只在 P1 遠端通知已啟用時提供。暫時停止系統通知投遞，但 App 內期限、部位狀態與事件仍持續計算，不得凍結或延長 `zoneDueAt`。恢復時不補送多則已過期通知，只依目前真實狀態重建後續工作。
2. **關閉通知**：關閉所選遠端管道，直到使用者再次開啟；App 內狀態與 Session 不變。
3. **結束本次提醒**：P0 與 P1 均提供。確認後 Session 轉為 `ENDED`、停止接受一般事件並取消待送通知，但保留產品及依政策保存的事件。結束不代表已補擦、防護完成或安全。

`結束本次提醒` 放在提醒頁次要選單，不與主要行動競爭。若已有未結束 Session 又要求開始新的提醒，顯示 `查看目前提醒`、`結束目前提醒並開始新的`、`取消`；只有明確結束成功後才建立新草稿，且不得帶入舊塗抹時間、事件或期限。

跨日或長時間未操作的 Session 不得自動標記為已補擦或自動建立新 Session。系統可依版本化的 UX 門檻提示「這可能是先前的提醒」，讓使用者選擇繼續或結束；該門檻只用於管理 Session，不得描述為健康或產品有效期限。

#### 5.7.3 最近設定與常用選項

- `沿用上次設定` 可由最近一筆仍留存的 Session 衍生；Guest 僅使用本機資料，登入帳號依同步與隱私設定使用帳號資料。刪除該資料後，建議也必須消失。
- 若已有進行中 Session，只顯示 `查看目前提醒`，不得再提供會建立第二個 Session 的沿用入口；需要變更時進入既有 Session 的部位／方法更新流程。
- 沿用內容只包括情境建議、追蹤部位、防護方式與產品選擇，不包括舊的事件時間、提醒期限或防護狀態。
- 若最近設定引用的產品已刪除，只能顯示「原產品已刪除，請重新選擇」；不得從歷史 `product_label_snapshot` 靜默重建產品或恢復已刪除資料。
- P0 的最近產品最多顯示 3 個：只取最近 90 天內、經更正鏈歸納後仍有效之已確認 `ApplicationEvent` 引用且目前仍存在的產品，以每個產品最近一次可信 `appliedAt` 由新到舊排序，依 `productId` 去重；時間相同時以穩定 `eventSequence` 再以 `productId` 排序。已刪除、停用、過期、回報異常或目前 owner 無權存取的產品不顯示。
- P0 提供上述最近使用產品及固定的常見部位組合；P1 才加入產品收藏、可自訂組合與可存取的人體部位圖。
- `快速提醒` 的草稿可在本機暫存 `suggestedPresetId／suggestedPresetVersion`，用來還原畫面及排查建議組合；它們不得直接成為使用者已確認資料。建立 Session 時只保存中性的 `setupEntryMode`、`presetDecision` 與版本號，只有最終提交的原子部位集合是 Session 真值。
- 常見組合只是 UI 快捷選取，提交內容仍須展開成 `BODY_ZONE_V3` 的原子部位／zone instance 集合，並經使用者確認。第三方分析不得收到可反推出身體部位的 preset ID 或版本對照；只可記錄入口類型、接受／調整／取消、選取項目數量及完成狀態，不得上傳實際部位、自訂文字或產品資料。

#### 5.7.4 權限、錯誤與離開流程

- 權限說明必須先說明用途、拒絕影響與替代方式，再由明確按鈕觸發瀏覽器權限提示；不得在頁面載入時連續彈出多個權限請求。
- 欄位錯誤就近顯示，不得只在頁面頂端顯示籠統失敗。網路或 API 失敗時保留尚未提交的本機草稿，避免使用者重新填寫。
- 使用者離開未完成精靈再返回時，可恢復本機草稿並明確標示尚未建立 Session；草稿逾期或被清除時不得被當成有效防護紀錄。
- `SetupDraft` 只存於目前裝置，預設 24 小時到期；提交、明確取消、手動清除或到期後刪除。草稿不得保存精確座標、一次性特殊狀況或尚未確認的塗抹時間，不進入 Guest 升級上傳，也不得把草稿內容送至分析服務。
- `不確定`、`看不清楚`、`稍後設定` 等選項須說明會採用何種保守處理，不得設計成錯誤或低品質答案。
- P0 必須提供 `結束本次提醒`。結束前顯示影響摘要；完成後取消待送通知並回到無進行中提醒狀態。若 IndexedDB／API 結束交易失敗，不得先在畫面顯示已結束。

#### 5.7.5 白話狀態與無障礙標示

Session 狀態：

| 內部狀態 | 使用者可見文字 |
| --- | --- |
| `DRAFT` | 尚未完成設定 |
| `TRACKING` | 提醒進行中 |
| `ATTENTION_REQUIRED` | 有部位需要處理 |
| `ENDED` | 本次提醒已結束 |

逐部位記錄狀態：

| 內部狀態 | 使用者可見文字 |
| --- | --- |
| `unrecorded`／`UNRECORDED` | 尚未記錄防護方式 |
| `sunscreen_recorded` | 已記錄使用防曬產品 |
| `physical_method_reported` | 已記錄衣物覆蓋；遮蔭另以 Session 環境顯示 |
| `mixed` | 已記錄防曬產品與衣物覆蓋 |
| `none_reported` | 尚未記錄具防曬標示的產品或衣物覆蓋；若使用其他外用產品，仍不視為已記錄防曬 |
| `unknown` | 防護方式不確定 |

逐部位計時狀態：

| 內部狀態 | 使用者可見文字 |
| --- | --- |
| `label_wait`／`LABEL_WAIT` | 請依產品標示等待，期間加強遮蔽 |
| `tracking`／`TRACKING` | 提醒進行中 |
| `reapply_soon`／`REAPPLY_SOON` | 即將需要檢查或補擦 |
| `reapply_due`／`REAPPLY_DUE` | 建議現在補擦 |
| `untimed_action` | 目前無法計算可信時間，請依顯示原因處理 |
| `not_applicable` | 目前沒有產品補擦計時，並顯示實際記錄方式 |

計時可信度：

| 內部狀態 | 使用者可見文字 |
| --- | --- |
| `CLOCK_UNTRUSTED` | 裝置時間可能不正確，請重新連線校準；目前採較短的保守狀態 |

內部代碼不得直接作為一般畫面或通知文案。所有狀態須同時使用文字，必要時再搭配圖示與顏色；顏色不能是唯一區別方式。

主要行動依目前 `primaryAction` 呈現：

| 目前主要狀態 | 主要 CTA | 次要 CTA | 主要狀態元件 |
| --- | --- | --- | --- |
| `REAPPLY_DUE` | `記錄已補擦` | `回報狀況` | 到期狀態卡 |
| `REAPPLY_SOON` | `記錄已補擦` | `回報狀況` | 時間型提醒環 |
| `TRACKING` | `回報狀況` | `記錄已補擦` | 時間型提醒環 |
| `UNRECORDED` | `補上防護紀錄` | `回報狀況` | 非時間型行動卡 |
| `unknown` | `確認防護方式` | `回報狀況` | 非時間型行動卡 |
| `none_reported` | `查看防護選項` | `更新防護紀錄` | 非時間型行動卡 |
| `untimed_action`＋`WATER_START_UNKNOWN` | `處理入水時間` | `記錄已補擦` | 非時間型行動卡；開啟後可補上／更正實際入水時間，仍不確定時只能查看保守建議，不得要求編造時間或顯示回報時刻形成的假倒數 |
| `untimed_action`＋產品安全事件 | `改用其他防護` | `查看處理說明` | 非時間型行動卡；不得催促再次使用同一產品 |
| `LABEL_WAIT` | `查看產品標示` | `回報狀況` | 等待狀態卡；可顯示標示等待時間但不得稱為安全倒數 |
| 衣物覆蓋且無產品期限 | `回報狀況` | `更新防護方式` | 中性非時間型狀態卡；顯示「已記錄衣物覆蓋」 |
| `CLOCK_UNTRUSTED` | 線上：`重新校準時間`；離線：`查看保守提醒` | `查看已保存紀錄` | 非時間型行動卡；不得提供可手動解除狀態的確認按鈕 |
| 多種最高優先行動並存 | `查看需要處理的部位` | `回報狀況` | 非時間型彙總卡 |

工程端不新增第四種 `presentationType`：表中的「等待狀態卡」「非時間型彙總卡」與「中性非時間型狀態卡」均使用 `untimed_action_card`，並分別以 `variant=label_wait／multi_action／neutral_physical` 區分。`variant` 只控制可見文案與視覺，不得改變期限或狀態優先序。

每個畫面只可有一個實心主要按鈕。主要與次要按鈕在 360 CSS px 寬下可改為垂直堆疊，不得壓縮成難讀文案。所有會改變時間、部位、產品或狀況的操作仍須在提交前確認，不得因 CTA 已依狀態預選而直接修改資料。

#### 5.7.6 手機底部導覽與空白狀態

P0 手機版固定提供四個底部入口：`首頁`、`提醒`、`防曬品`、`更多`。圖示必須搭配文字，不得只靠圖示讓使用者猜測。

- `首頁`：顯示所在地 UVI、資料時間、主要防護建議；無 Session 時顯示 `開始防曬提醒`，有 Session 時直接顯示 `primaryAction.actionKind` 對應的最高優先操作。
- `提醒`：有進行中提醒時顯示目前狀態、`記錄已補擦`與`回報狀況`；沒有進行中提醒時留在本頁，顯示「目前沒有進行中的防曬提醒」及單一主要按鈕 `開始防曬提醒`。點擊後才進入設定流程，不自動跳回首頁。
- `防曬品`：顯示目前使用與過去紀錄，提供新增、編輯、封存，以及只對符合第 12.3 節安全條件的產品提供恢復。
- `更多`：P0 提供最低必要 Q&A／運行說明、`我有特殊狀況`、說明中心、安裝到手機、顯示設定、登入／帳號、隱私與本機資料清除；P0.5 才另外顯示獨立的 `UV知識庫`。即使納入 `UV知識庫`，也不增加第五個底部導覽項目。

底部導覽不得在同一頁重複放置意義相同的主要按鈕。設定精靈等需要專注完成的流程可暫時隱藏底部導覽，但須提供清楚的返回與取消方式，且不得遺失草稿。底部導覽已隱藏時，該流程可使用固定底部操作列；有底部導覽的頁面不得比照。

#### 5.7.7 戶外高對比色彩與字體

P0 預設使用高對比淺色介面，優先確保陽光直射、樹蔭、低亮度、霧面／防窺保護貼及配戴太陽眼鏡時仍可辨識。不得以室內設計稿看起來柔和作為降低對比的理由。

日間色彩 token：

| token | 用途 | 色碼 | 主要對比／限制 |
| --- | --- | --- | --- |
| `surface.page` | 頁面背景 | `#FFFDF7` | 溫暖米白，不放背景照片或低對比漸層。 |
| `surface.card` | 卡片背景 | `#FFFFFF` | 卡片不透明，重要邊界不可只靠陰影。 |
| `text.primary` | 主要文字 | `#172033` | 對 `surface.page` 約 15.99:1。 |
| `text.secondary` | 次要文字 | `#5F6B7A` | 對 `surface.page` 約 5.33:1；不得再以 opacity 降低。 |
| `brand.primary` | 主要按鈕／品牌 | `#233B6E` | 白字對比約 10.92:1。 |
| `status.tracking` | 提醒進行中 | `#3C5AA9` | 對 `surface.page` 約 6.40:1；不得稱為安全。 |
| `status.soon.text` | 即將檢查文字／邊框 | `#8B5A00` | 搭配 `#FFF4CC` 淺琥珀背景；亮黃不得承載白字。 |
| `status.due.text` | 建議現在處理 | `#9B2C2C` | 搭配 `#FDE8E7` 淺紅背景；不得整頁閃紅。 |
| `status.unknown` | 尚未記錄／不確定 | `#5F6B7A` | 搭配 `#F2F4F7`，另加問號或虛線圖示。 |
| `border.strong` | 圓環輪廓／重要邊框 | `#7A8497` | 對 `surface.page` 約 3.70:1。 |

狀態必須同時包含色彩、圖示、文字及適用時的原因：

- `提醒進行中`：藍紫＋時鐘圖示。
- `即將需要檢查`：深琥珀文字／邊框＋提醒圖示。
- `建議現在檢查／補擦`：深紅文字／邊框＋驚嘆圖示。
- `尚未記錄／不確定`：灰藍＋問號或虛線輪廓。
- `衣物／遮蔽`：深藍灰＋衣服、帽子或陽傘圖示。

不得使用綠色表示 `安全`、`防護有效` 或 `完成後無風險`；不得以顏色作唯一狀態線索。禁止白字搭配亮黃／淺橘、低透明度文字、細淡灰字、文字直接疊在照片上、大面積毛玻璃、只靠陰影分隔卡片及低對比粉彩按鈕。

視覺提醒環使用 `status.tracking` 作主要進度、`border.strong` 作可辨識輪廓，線寬 12–16 CSS px、手機直徑目標 200–240 CSS px；中央保留相對時間、絕對時間及語意標籤。到期改用靜態 `status.due.text`、圖示與文字，不閃爍。圓環剩餘軌可使用較淡裝飾色，但有意義的進度本身及總輪廓須符合非文字對比要求。

主要按鈕使用 `brand.primary` 背景＋白字；次要按鈕使用白底＋`brand.primary` 文字及至少 2 CSS px 邊框。兩者不得只以顏色深淺區分，並沿用至少 44 × 44 CSS px 的觸控要求。

字體最低規格：圓環主要時間 44–56 CSS px／700；狀態標題 20–24 CSS px／700；一般文字至少 16 CSS px／400–500；按鈕 16–18 CSS px／600；必要次要說明至少 14 CSS px／500；重要圖示筆畫至少 2 CSS px。文字放大 200% 時不得截斷主要狀態、時間及操作。

`更多 → 顯示設定` 提供 `跟隨手機`、`日間高對比`、`夜間模式`。首次使用預設 `日間高對比`，使用者可改為跟隨手機；日間淺色方案是戶外測試與發布基準。不得依賴 Ambient Light Sensor 自動切換；夜間色票須另行通過相同文字與非文字對比驗證，不得用簡單反相產生。

---

## 6. UV 風險與提醒規則

### 6.1 UVI 分級

| UVI | CWA 顯示等級 | 基本訊息 |
| --- | --- | --- |
| 0–2 | 低量級 | 一般情況下短時間戶外風險較低，但不是零風險；長時間活動或有醫師避光指示者仍需防護。 |
| 3–5 | 中量級 | 建議遮蔭、衣物、帽子、太陽眼鏡及依產品標示使用防曬。 |
| 6–7 | 高量級 | 減少高曝曬時段停留，增加遮蔽並留意補擦事件。 |
| 8–10 | 過量級 | 優先調整活動時間、尋找遮蔭並採取完整防護。 |
| 11 以上 | 危險級 | 儘量降低非必要曝曬；無法避免時採取多重防護。 |

台灣 CWA 資料預設使用「低量級／中量級／高量級／過量級／危險級」。內部可保存 `low／moderate／high／very_high／extreme` 標準化代碼，但不得把其他中文譯名混稱為 CWA 原始標籤。不得為任何等級顯示全體適用的固定「幾分鐘後曬傷」，也不得使用 CWA 防護示意分鐘建立個人倒數。補水與熱傷害提示必須依氣溫／熱指數另行判定，不能只由 UVI 推導。

產品資訊與提醒狀態必須分開呈現：

- `已記錄塗抹` 不等於系統驗證已足量、均勻或完整覆蓋。
- SPF、PA 與 broad-spectrum／UVA 宣稱分欄顯示；未知值不得默認為足夠。
- 提醒使用者依產品標示使用，均勻涵蓋本次外露部位，並搭配遮蔭、衣物、帽子與太陽眼鏡等多重措施。
- 新設定流程允許使用者記錄「未使用防曬產品，只採衣物完整覆蓋」；遮蔭另記為環境情境。若使用者無法確認已擦防曬、衣物完整覆蓋或其他外用產品，保留一般 UV 與防護資訊，但不建立 Session 或虛構補擦期限。

### 6.2 提醒規則

提醒引擎不使用 SPF 乘法。所有期限以部位為單位；`zoneDueAt` 與彙總的 `sessionNextDueAt` 只代表提醒截止時間，不代表安全時間或實際防護效力。

規則來源與資格：

1. 只有 `productIdentityStatus=confirmed_sunscreen`、產品標示 snapshot 合法，且本次塗抹時 `ruleEligibilityAtApplication=eligible` 的 `ApplicationEvent` 可建立產品期限。純植物油、一般身體油、沒有防曬／SPF 標示、身分未知、已過期、使用者回報異常或使用後不適的產品可保留私人／使用紀錄，但不得建立或更新 120／40／80 分鐘期限。
2. 一般提醒以 120 分鐘為保守上限；若產品標示 snapshot 有使用者確認且更短的 `reapplicationInterval.minutes`，採較短值。自由文字 `rawInstructionText` 不得直接參與 reducer。標示比 120 分鐘長時不得自動延長；標示未知或沒有明確數字時採 120 分鐘一般上限，但不得稱為產品保證。
3. 包裝有效期限已知且早於本次實際塗抹時間時，不建立新期限。進行中才回報產品異狀／使用後不適時，追加不可變 `ProductSafetyEvent`，使同一產品／snapshot 目前仍受影響的期限失效並轉為無時間注意狀態；顯示改選產品及衣物／遮蔭選項，不得要求繼續使用同一產品。有效期限未知且未回報異狀時，不自行套用通用開封年限，也不得誤稱已過期。
4. 尚未記錄、只有衣物覆蓋、只有其他非防曬外用產品、明確未採取防護或狀態未知的部位，不建立虛構產品期限；分別顯示實際狀態與多重防護建議。衣物完整覆蓋是條件型狀態，不是時間型防護：UPF 或布料資訊可供衛教與顯示，但不得換算成倒數、有效分鐘或安全曝曬時間。遮蔭只改變環境提示與適用時的遠端通知強度，不延長期限。
5. 產品標示的曝曬前等待時間另存為 `labelReadyAt`，只用於標示操作提示與 `zoneNextActionAt`，不代表等待結束後即安全。
6. 水上活動以 `water_start` 與 `water_end` 組成一段不重疊區間。耐水 40／80 分鐘期限只可由可信 `activityStartedAt`、該起點前已存在的合格 Application、有效耐水 snapshot 及相符部位共同建立。起點未知時不得用回報時間冒充入水時間，也不得建立完整耐水期限。
7. 已在水中但起點未知時，方法含防曬產品的相關部位設為 `untimed_action`，原因碼為 `WATER_START_UNKNOWN`；顯示無法判定剩餘耐水時間、建議立即確認／重新塗抹的保守警示，但不得用回報時間製造 `REAPPLY_DUE` 的數字期限。明確不耐水、未見耐水宣稱或標示未知時，不建立水中期限，並提示不可依賴水中防護。
8. 同一 active 水上區間中補擦不自動重設 40／80 分鐘期限。必須先有有效 `water_end`、確認重新塗抹，再建立新的 `water_start`，才能取得新水上期限。
9. `water_end`、擦毛巾、明顯摩擦、洗手及大量流汗是可提前處理的事件原因；洗手預設只影響手背，摩擦只影響確認部位。系統不使用汗水或摩擦倍率。
10. 室內遠離直射時保留既有期限，但可抑制遠端通知；返回戶外時立即重驗全部追蹤部位。室內窗邊、UVI 變高或變低只改變風險與裝備提示，不修改既有產品期限。
11. 某部位由 `clothing_covered` 改為 `exposed` 時必須由使用者明確執行 `更新衣物狀態` 並追加 ZoneMethodEvent，不得由時間經過或感測器推定。若該部位的 `sunscreen` component 從未被移除、原 Application 仍合格且產品未受安全封鎖，重新外露後以原 `appliedAt` 重建期限，不得從衣物移開時間重新起算；原期限已過即顯示需要補擦，尚未到期則顯示原剩餘時間。若衣物下未記錄防曬、方法已被移除或狀態不確定，改為無時間行動卡，不得復活更早的 Application。

`minNonNull(values)` 忽略 null；至少一個非空值時回傳最小值，全部為 null 時回傳 null。每個部位依目前有效事件流計算：

```text
confirmedLabelInterval =
  currentApplication.snapshot.reapplicationInterval.minutes
  only when status = explicit_minutes

generalInterval =
  minNonNull(120 minutes, confirmedLabelInterval)

currentSunscreenMethodEnabledAt(zone) =
  先找出目前這一輪 sunscreen activation：
    初始即含 sunscreen 時為 StartSessionCommand；
    後續則為 latest tracking_started 與 latest
    「不含 sunscreen → 含 sunscreen」轉換中較晚的一次。
  若同一 activation command 為該部位建立一筆新 Application，
    取該 Application.appliedAt；
  否則取該 activation 的 effectiveOccurredAt。
  同一 command 以共用 idempotencyKey／原子操作關聯，
  不得只因舊 Application 的時間較晚便誤認為同一輪 activation。

generalDueAt =
  currentApplication.appliedAt + generalInterval
  only when current skinExposureStatus = exposed
  AND current methodComponents contains sunscreen
  AND currentApplication exists
  AND currentApplication.ruleEligibilityAtApplication = eligible
  AND no activeProductSafetyBlock(zone)

confirmedWaterInterval =
  currentApplication.snapshot.waterResistance.minutes
  only when status = water_resistant_40 or water_resistant_80

activeWaterDeadline =
  activeWaterInterval.activityStartedAt + confirmedWaterInterval
  only when current skinExposureStatus = exposed
  AND current methodComponents contains sunscreen
  AND startTimeConfidence = confirmed
  AND currentApplication exists
  AND currentApplication.appliedAt <= activityStartedAt
  AND currentApplication.ruleEligibilityAtApplication = eligible
  AND no activeProductSafetyBlock(zone)

zoneDueAt = minNonNull(
  generalDueAt,
  activeWaterDeadline,
  eventTriggeredDeadline
)

zoneNextActionAt = minNonNull(
  activeLabelReadyAt,
  zoneDueAt
)

sessionNextDueAt =
  minNonNull(zoneDueAt of all zones whose latest tracking state is active)
```

事件原因不得永久卡住，也不得被較早補登的塗抹錯誤解除：

```text
currentApplication(zone) =
  該部位所有未被 replace／void 的 Application 中，
  其原子 command order 不早於目前 sunscreen activation command，
  appliedAt >= currentSunscreenMethodEnabledAt(zone)，
  不先依期限資格篩選，依 appliedAt 與穩定 effective order
  排序後的最後一筆

latestEligibleApplicationForCause(zone) =
  該部位所有未被 replace／void 且
  ruleEligibilityAtApplication = eligible 的 Application 中，
  依 appliedAt 與穩定 effective order 排序後的最後一筆

unresolvedOrdinaryCause(zone) when:
  cause 尚未被 replace／void
  AND cause 不是 ProductSafetyEvent
  AND (
    latestEligibleApplicationForCause(zone) 不存在
    OR cause.effectiveOccurredAt
       >= latestEligibleApplicationForCause(zone).appliedAt
  )

ordinaryCauseApplicable(zone, cause) when:
  latest tracking state = active
  AND current skinExposureStatus = exposed
  AND current methodComponents contains sunscreen

eventTriggeredDeadline =
  earliest effectiveOccurredAt among unresolved ordinary causes
  whose deadlineEligibility = known_time
  AND ordinaryCauseApplicable(zone, cause)

activeProductSafetyBlock(zone) when:
  current methodComponents contains sunscreen or other_topical
  AND the latest product / snapshot Application selected
      for that current topical method
      matches any still-effective ProductSafetyEvent

untimedActionReason =
  highest-priority of:
    unresolved ordinary causes whose deadlineEligibility = untimed
      AND ordinaryCauseApplicable(zone, cause)
    activeProductSafetyBlock(zone)
```

`command order` 由 P1 的 `serverAppliedSequence` 或 P0 共用 reducer 的等效本機穩定序號判定；同一原子 command 的事件共用 `idempotencyKey`。它只用來界定哪一輪方法啟用可引用哪些新紀錄，不得取代 `appliedAt／effectiveOccurredAt` 的業務時間或以 HTTP 抵達順序重排事件因果。

初始 Session 同一原子操作內的新 Application 可早於 Session 建立時間，例如 10:00 建立 Session、使用者確認 09:30 已塗抹；該 Application 自身的 `appliedAt` 即為初始 activation 邊界。移除目前方法中的 `sunscreen` 或停止追蹤時，`generalDueAt` 與事件型行動立即不適用；日後重新加入 `sunscreen` 或重新追蹤時，只有同一 activation command 新建的 Application，或不早於新 activation 邊界的後續 Application 可建立期限，舊事件不得復活。

一般破壞原因在衣物覆蓋或停止追蹤期間只是「目前不適用」，不是已解除；日後再次外露且方法仍需產品補擦時，若仍沒有嚴格晚於該原因的合格 Application，原原因重新成為待處理狀態。`WATER_START_UNKNOWN` 固定屬一般 `deadlineEligibility=untimed`；有效 `ProductSafetyEvent` 則只依獨立的 `activeProductSafetyBlock` 判定，兩者都不能取得數字期限。被回報異常／不適的產品或 snapshot 仍禁止建立新期限；較晚改用另一個當下合格的產品／snapshot，或把目前方法改為衣物覆蓋後，該部位可不再顯示這張安全事件行動卡，但原產品的安全封鎖不得因此清除。

對洗手、離水、擦拭、摩擦及大量流汗等一般事件原因，只有 `appliedAt` 明確晚於原因發生時間的新塗抹才能解除。補登於原因之前或同一時間的塗抹不得解除；同時點無法確認先後時採事件原因仍未解除的保守結果。產品安全事件依上一段的更嚴格規則處理。更正／撤銷事件後，必須重建受影響部位、Session 彙總及通知工作／Bundle。

情境與期限矩陣：

| 情境／方法 | 一般期限 | 水上期限 | 事件原因 | 通知處理 |
| --- | --- | --- | --- | --- |
| 一般戶外＋合格 Application | 建立 | 無 | 取未解除原因 | 正常 |
| 戶外運動＋合格 Application | 建立 | 無 | 取未解除流汗／摩擦等原因 | 正常 |
| 室內近直射窗邊 | 保留，不延長 | 保留既有值 | 保留 | 正常顯示 |
| 室內遠離直射 | 保留，不延長 | 保留既有值 | 保留 | 可抑制遠端通知；返回戶外立即重驗 |
| 準備下水 | 建立／保留 | 不建立 | 保留 | 顯示準備提示 |
| 已入水＋可信起點＋40／80 標示 | 建立／保留 | 建立 | 取未解除原因 | 取最早期限 |
| 已入水但起點未知 | 建立／保留 | 不建立 | 建立無時間立即注意原因 | `untimed_action`；不顯示數字期限 |
| 已入水但無／未知耐水標示 | 建立／保留 | 不建立 | 離水或擦拭時建立原因 | 顯示不可依賴水中防護 |
| 衣物完整覆蓋且無合格 Application | 不建立 | 不建立 | 只處理適用事件 | `not_applicable`；顯示衣物狀態，不倒數 |
| 衣物完整覆蓋且衣物下有合格 Application | 暫不建立；保留原 `appliedAt` 供重新外露時重驗 | 暫不建立；保留合法水上起點供重新外露時重驗 | 只處理適用事件 | `not_applicable`；顯示衣物狀態，不以覆蓋時間重新起算 |
| 尚未防護／方法未知 | 不建立 | 不建立 | 可有 | 靜態注意狀態 |
| UVI 改變 | 不變 | 不變 | 不變 | 只改風險與裝備提示 |

`sessionNextDueAt` 只供真正的時間型補擦排程，不直接決定首屏。首屏依第 6.3 節衍生單一 `primaryAction` 物件，並使用其中的 `actionKind`、`affectedZoneInstanceIds[]`、`actionAtNullable` 與 `reasonCodes[]`，不另建第二套平行欄位。本節所有公式只用於排定行動提醒，不描述皮膚保護百分比或安全曝曬時間。每項會建立、提前、取消或阻止期限的正式規則都必須有 `ruleId`，並依第 13.2 節連到核准證據。

### 6.3 Session 與部位狀態

```text
Session:
DRAFT → TRACKING ↔ ATTENTION_REQUIRED
TRACKING → ENDED
ATTENTION_REQUIRED → ENDED

Per zone:
UNRECORDED
  → LABEL_WAIT（產品有曝曬前等待標示時）
  → TRACKING
  → REAPPLY_SOON（距 zoneDueAt 30 分鐘內）
  → REAPPLY_DUE
  → TRACKING（使用者新增該部位 ApplicationEvent 後）
```

規則：

- `ApplicationEvent` 是不可變事件，不是 Session 狀態；新增事件不刪除先前紀錄，也不重設當日曝曬歷史。
- 每個部位各自更新。局部補擦不得改變其他部位的 `lastAppliedAt`、`zoneDueAt` 或狀態。
- Session `overallStatus` 取所有本次追蹤部位中最需要注意的狀態；`unrecorded`、`none_reported`、`unknown`、產品不具提醒資格或任一 `REAPPLY_DUE` 均使 Session 成為 `ATTENTION_REQUIRED`。只記錄衣物覆蓋或遮蔭環境時不建立產品期限，但仍顯示經審查的裝備與覆蓋提示。
- `primaryAction` 是由目前有效狀態衍生的介面資料，不是健康風險分數；至少包含 `presentationType: timed_ring／due_card／untimed_action_card`、`variantNullable: label_wait／multi_action／neutral_physical`、`actionKind`、`affectedZoneInstanceIds[]`、`actionAtNullable`、`reasonCodes[]` 與 `derivedFromEventRefs[]`。`CLOCK_UNTRUSTED` 是 Session 層級最高優先狀態：線上引導重新校準，離線顯示保守提醒，不能用手動確認解除。最高優先部位需要不同動作時，顯示 `查看需要處理的部位`，不得任選其中一個動作冒充全部。
- 「已記錄塗抹」只代表使用者自陳紀錄，不是系統驗證防護充分。
- 同一操作必須使用 idempotency key，避免多裝置重複補擦或重複通知。
- Session 結束只能由明確 end operation 建立不可變 `SessionEndedEvent`；`TRACKING` 與 `ATTENTION_REQUIRED` 均可直接進入 `ENDED`，結束後不可由一般離線事件重開。結束優先於任何會延長期限的操作；其他衝突由 `revision` 做 optimistic concurrency control。

### 6.4 特殊情況

- **UVI＝0**：顯示「觀測值為 0（低量級）」，不執行除法，也不暗示零風險；只有另有可靠日照／時間判定時才可標示夜間。
- **UVI 缺值或過期**：顯示無法取得有效觀測，改用明確標示的預報或一般保守建議。
- **醫師避光指示、疾病、藥物或近期療程**：由一次性入口觸發，不計算倍率，提示遵循醫師／藥袋／院所指示。
- **破損、起泡或嚴重曬傷**：使用獨立皮膚狀況流程，不在破損處套用一般提醒。
- **可能全身性或熱疾病症狀**：使用獨立急症流程，不與一般曬傷文案合併。
- **口罩**：一般口罩不得視為 UV＝0；只有可信 UPF 標示且完整、乾燥覆蓋時才顯示產品提供的遮蔽資訊。
- **產品過期、異常或使用後不適**：不建立新的合格產品期限，不催促繼續使用同一產品；顯示改選產品、衣物／遮蔭及經審查的產品安全／就醫提示，不作過敏或疾病判定。

---

## 7. CWA 資料整合

### 7.1 資料來源

- 現況觀測首選：CWA `O-A0003-001` 10 分鐘綜觀氣象資料中的有效 `UVIndex`。
- 區域 fallback：CWA `F-D0047-091` 鄉鎮天氣預報；資料每 6 小時更新，UVI 為較粗的逐 12 小時／白日時段預報。不得用它生成逐分鐘或逐小時 UVI，也不得據此宣稱「16:30 精準最佳活動時間」。
- 觀測與預報必須使用不同標籤，不得在介面上混稱「即時值」。

### 7.2 後端取得與快取

- CWA API 授權碼只存於後端 secrets manager。
- 後端依 dataset／測站共用抓取與快取，不讓每位瀏覽器各自高頻輪詢 CWA。
- 快取需記錄 `sourceDataset`、來源種類、測站／預報區域、觀測或發布／有效時間、`fetchedAt`、`freshUntil`、`usableUntil`、品質與缺值原因。`freshnessState` 必須在每次讀取時計算，不持久化成不會自行更新的固定值。
- 觀測政策：`observedAt` 距今不超過 30 分鐘為 `fresh`；超過 30 分鐘至 120 分鐘為 `stale`；超過 120 分鐘為 `unusable`。
- `F-D0047-091` 預報政策：只在 `validFrom ≤ now < validTo` 時適用；使用最新發布版本。`freshUntil` 設為下一個預期 6 小時更新時間加 2 小時容許值，`usableUntil` 不得晚於 `validTo`，且發布時間超過 14 小時仍未更新時一律 `unusable`。
- `stale` 可作為明確標示的最後資料；`unusable` 不得繼續顯示成當前 UVI，只能改用合格預報或一般建議。

### 7.3 選站規則

1. 先把使用者座標對應至 CWA 行政區代碼，再載入該版本 `RegionUvMapping`；避免使用全台任意遠端測站冒充所在地資料。
2. 從 mapping 的 `preferredStationIds` 中，只選 `UVIndex` 有效且未過期的測站；若有多站，選與座標最近者。
3. 顯示測站名稱、距離、觀測時間與「代表測站觀測」標籤，不得稱為使用者所在點的直接量測。
4. mapping 內沒有可用站時，直接降級至 `F-D0047-091` 對應鄉鎮預報，不另猜測距離門檻。
5. 定位只用於本次行政區與選站判定；目前 P0／P1 範圍均不保存精確座標或移動軌跡。未來若確有保存需求，須另立目的、必要性、告知、法律依據、安全措施及保存期限評估，不能只以一般服務同意或單一 checkbox 擴張。

### 7.4 手動地區映射

- 手動選擇使用 CWA 行政區代碼，不以不透明的行政區中心座標臨時計算。
- 後端維護有版本的 `RegionUvMapping`，包含 `regionCode`、依序排列的代表測站 `preferredStationIds`、`forecastRegionId`、`mappingVersion` 與審查日期。
- 系統依序選第一個 `fresh` 或可接受的 `stale` 代表站，介面標示「所選地區代表測站觀測」，不得稱為使用者所在地直接量測。
- 沒有可用代表站時，改用已核准的區域預報並標示「區域預報」；兩者皆無時只顯示一般建議。
- 映射變更必須版本化並可回溯，讓相同版本的驗收結果可重現。

### 7.5 失敗處理

| 情況 | 系統行為 |
| --- | --- |
| 使用者拒絕定位 | 提供手動地區選擇，依 `RegionUvMapping` 顯示代表站觀測或區域預報，不阻斷使用。 |
| CWA timeout／5xx | 只在資料未達 `unusable` 前顯示最後資料及其狀態；超過 hard expiry 則改用合格預報或一般建議。 |
| UVIndex 為缺測碼／非數字 | 不參與選站，改找下一個有效站。 |
| 所有附近站皆無效 | 顯示區域預報或「目前無有效資料」，不得自行填 0。 |
| 使用者離開台灣 | 顯示目前僅支援台灣資料，提供手動提醒但不顯示 CWA UVI。 |

### 7.6 紫外線資料準確度與呈現

- PWA 只負責取得及呈現資料，不得將平台形式描述為提升紫外線準確度；準確度取決於來源、資料時間、空間解析度、觀測站距離、預報模型及當地雲量、地形與海拔。
- `O-A0003-001` 的有效測站觀測可標示為「測站觀測」，但不得描述為使用者所在位置的實地量測；必須同時顯示測站名稱、觀測時間及距離。
- `F-D0047-091` 等鄉鎮預報須標示為「區域預報」，顯示預報代表區域與發布／有效時間，不得與測站觀測混稱為即時值。
- 每日紫外線最大值資料只可作當日最高風險或歷史資訊，不得呈現為「現在 UVI」。
- 當資料過期、缺測、來源切換或位置只能映射至較大行政區時，介面必須顯示相應不確定性，不得自行補 0、插入假值或使用「精準」字樣。
- UVI 用於環境風險分級及一般防護建議，不得單獨推算個人安全曝曬時間、曬傷倒數或延長產品補擦期限。
- 建議顯示格式至少包含：`UVI 數值與分級`、`觀測／預報`、`測站或區域`、`資料時間`、`資料來源`、`資料狀態`，並附註實際情況可能因位置、雲量、遮蔭、地形及海拔而不同。

---

## 8. 通知、計時與多裝置同步

### 8.1 計時真值

- 所有持久化時間使用 UTC RFC 3339；介面再依使用者時區顯示。
- 事件時間分成：`effectiveOccurredAt`（使用者確認的實際發生時間）、`clientCreatedAt`（操作在裝置建立的時間）、`serverReceivedAt`（伺服器收到時間）、`clientSequence`（同裝置／Session 單調序號）及 P1 `serverAppliedSequence`（伺服器套用事件流的單調序號）。塗抹另以 `appliedAt` 明確表示實際塗抹時間；水上區間使用 `activityStartedAt`／`activityEndedAt`。更正的建立時間不得冒充被更正事件的有效發生時間。
- 同一有效時間先依已驗證的相依關係排序，再依 `clientSequence`、`serverAppliedSequence`／本機穩定序號與事件 ID。塗抹與破壞事件時間完全相同且無法確認先後時，採破壞事件仍未解除的保守結果。
- **線上登入帳號**：伺服器保存事件與部位期限，API 回傳 `serverNowUtc`；伺服器狀態為權威。
- **線上 Guest**：產品、Session 與事件只存 IndexedDB。無狀態 `GET /v1/time` 只提供時間校準，不保存 Session；前端保存 `serverOffset`、`calibratedAt`、RTT 與校準版本。
- **離線 Guest／登入帳號**：以最後可信 offset 與裝置時間暫時計算；頁面保持開啟時可用 monotonic clock 輔助，但不可假設關閉後仍存在。
- 畫面每次顯示時以各部位 `zoneDueAt - trustedCurrentTime` 計算，不把 `setInterval` 累減值當作資料真值。
- `GET /v1/time` 請求必須帶客戶端每次隨機產生的 `clientNonce`；回應使用 `Cache-Control: no-store, no-cache, must-revalidate` 與 `Pragma: no-cache`，回傳 `serverNowUtc`、原樣 echo 的 `clientNonce` 及供伺服器追蹤的 `requestId`。客戶端同時保存請求／回應 wall-clock 樣本及 monotonic 樣本：monotonic clock 只用於 RTT 與偵測 wall-clock 跳變，UTC offset 由 `serverNowUtc` 與客戶端 wall-clock 請求／回應中點估算。Nonce 不符、RTT 超過版本化上限、請求期間 wall clock 與 monotonic clock 差異異常或多個樣本差異過大時，不得標記 trusted。
- 回到前景、重新連線及重新開啟時立即檢查時間。只有 `sessionRevision`、`zoneDueAt`、`activeCauseFingerprint` 與 `rulesetVersion` 均未改變時，才可比較 `lastRenderedRemaining`；若剩餘時間比上次增加超過 2 分鐘、時鐘向後跳或校準超過 24 小時，標記 `CLOCK_UNTRUSTED` 並保留較短結果。`activeCauseFingerprint` 由排序後的目前有效 cause IDs、rule IDs 與 deadline eligibility 以版本化序列化方式產生，集合任何變化都必須更換 fingerprint。合法補擦、事件更正或期限重建後須重設比較基準，不得因此誤判時鐘異常。
- `CLOCK_UNTRUSTED` 時不得以未校準裝置時間或單純按下「確認」延長期限。線上時以合格時間樣本解除；離線時維持保守較短狀態，只允許記錄不晚於目前保守時間的過去實際事件，待重新連線後校準。

### 8.2 Guest 與帳號的儲存責任

- P0 訪客模式的產品、Session、部位狀態及事件只存在 IndexedDB；後端只提供無狀態的 UV lookup、地區資料與時間校準。首次使用可產生僅限本機的隨機 visitor ID 作資料分區，不得是可跨網站追蹤的識別碼，也不得傳至後端。
- 訪客模式不要求帳號、Email、電話、姓名、生日或性別；清除瀏覽器網站資料、解除安裝或換裝置可能移除本機資料，介面須在首次建立資料與清除操作前以白話提示。
- 第 5.5.1 節的功能差異必須由 feature flag／實際帳號能力產生，不可寫死未上線功能。登入完成不等於資料已備份；只有 migration／sync 成功並顯示最後同步時間後，介面才可說「已同步」或「可於其他裝置恢復」。
- `/v1/me/products`、`/v1/me/sessions`、遠端通知與多裝置同步皆屬 P1 登入帳號 API。
- Guest 升級時透過專用 migration API 上傳明確選取的資料，不將一般離線操作佇列直接當成帳號資料。
- 每個 owner 同一時間只允許一個未結束 Session。若帳號與 Guest 各有一個，伺服器回傳 `409 ACTIVE_SESSION_CONFLICT` 與兩邊摘要，使用者必須選擇「保留帳號 Session」「以 Guest Session 取代並結束帳號 Session」或「取消合併」；不同 Session 的事件不自動混合。
- P0 IndexedDB 以單一 transaction 維護固定 active-session key、revision compare-and-swap 與事件 append；一般分頁、另一分頁及已安裝 PWA 同時操作時，只有成功 commit 的操作可顯示「已更新」。使用跨 context 更新通知讓其他畫面重讀狀態，不得以最後寫入者任意覆蓋。

### 8.3 登入帳號的離線 outbox

Guest 不建立伺服器同步 outbox。只有已登入且曾成功同步的帳號可以暫存離線 mutation：

```text
operationId, syncBatchId, ownerId, sessionId,
operationType, payload, baseRevision,
clientSequence, dependsOnOperationIdNullable,
effectiveOccurredAtUtc, clientCreatedAtUtc, deviceId,
status, attemptCount, nextAttemptAt,
lastError, createdAt
```

- `operationId` 使用客戶端 UUID，同時作 idempotency key。
- 每個裝置、每個 Session 維護單調遞增 `clientSequence`；相依操作另保存 `dependsOnOperationId`，不得只依可被修改的客戶端時間排序。
- 同一離線期間、相同 `baseRevision` 的待同步事件組成一個 `syncBatchId`，依 `clientSequence` 送至 batch API。伺服器只在批次開始時驗證一次 base revision，並在單一交易中按序套用全部事件；全部成功才提交並回傳各 operation 結果與最終 revision。
- Batch 採冪等；若網路中斷後重送相同 `syncBatchId`，伺服器回傳先前結果，不重複套用。
- 發生衝突時回傳 `409`，至少包含 `currentRevision`、`currentState`、`reason` 與 `allowedActions`；前端停止後續相依操作並要求使用者選擇。
- 已結束的 Session 不因離線事件自動重開。使用者可將事件記錄到新 Session，或捨棄操作。
- 結束優先於延長；所有重試採指數退避並設上限，不可無限重送。
- 登入使用者離線建立的事件在同步完成前標記 `pending_sync`。畫面必須說明「遠端提醒仍可能依上次同步狀態發送」；恢復連線後先同步事件、使舊 revision 工作／Bundle 失效，再建立目前排程。通知點擊時若本機仍有 pending outbox，須同時顯示本機待同步狀態，不得只用伺服器舊資料覆蓋。

### 8.4 通知管道

- App 內提醒：P0，前景可靠顯示視覺狀態。使用者可另行啟用一次性短提示音及支援裝置的短震動；兩者均為注意提示，不得循環播放、持續震動或模擬鬧鐘。
- 前景聲音：只能在瀏覽器允許播放且已有必要使用者互動時嘗試；被自動播放政策、靜音、音量或系統設定阻擋時不得反覆播放、提高音量或顯示成故障。P0 不使用背景音訊維持頁面存活。
- 前景震動：只能在 feature detection 確認 `navigator.vibrate` 可用時嘗試單次短震動；不支援時 no-op。不得用裝置／瀏覽器名稱推定支援，也不得用重複計時器製造持續震動。
- Web Push：P1 首發候選，需要權限、網路與平台支援，採盡力送達。使用者要求聲音時可在支援平台送出非靜音通知請求；要求震動時只把相應選項視為平台提示。作業系統、瀏覽器、通知頻道、音量、靜音、專注／勿擾及省電設定永遠優先。
- LINE：P1 首發候選，需要獨立綁定 Messaging API 與可投遞狀態。
- P1 開發前必須在 Web Push 與 LINE 中選定一種並記錄決策、目標平台與驗收矩陣；只有被選管道納入 P1 必要 AC，另一管道移至後續里程碑。
- Email：後續低優先備援研究項目，不屬 P0 或首發 P1 驗收，也不承諾分鐘級送達。
- Web App 不提供自訂通知鈴聲、持續響鈴、強制震動、繞過靜音／專注模式或「響到使用者處理為止」的鬧鐘式能力。跨平台正式文案只能寫「可依裝置與系統設定發出通知聲或震動」，不得寫「一定響鈴／一定震動」。

### 8.5 通知工作與投遞

通知拆成「逐部位邏輯工作、合併訊息、管道目的地、逐目的地投遞」四層，避免把多個部位或多台裝置誤當成同一資料列：

```text
NotificationJob:
id, userId, sessionId, reminderType, zoneInstanceId,
scheduledAt, expiresAt, sessionRevision,
rulesetVersion, causeFingerprint,
status, idempotencyKey, createdAt, cancelledAt

NotificationBundle:
id, userId, sessionId, channel, sessionRevision,
scheduledAt, expiresAt, bundleKey,
mergeWindowVersion, status,
createdAt, cancelledAt

NotificationBundleJob:
bundleId, jobId, channel
FOREIGN KEY (bundleId, channel)
  REFERENCES NotificationBundle(id, channel)

NotificationDestination:
id, userId, destinationType, status,
verifiedAt, lastSuccessAt, revokedAt

NotificationDelivery:
id, bundleId, destinationId, status,
attemptCount, nextAttemptAt, lastAttemptAt,
lastErrorCode, providerMessageId,
claimedAt, claimExpiresAt
```

- Web Push 與 LINE 使用 channel-specific 子表；LINE 不得被要求填入 `deviceId`、endpoint、`p256dh` 或 `auth`。Web Push endpoint 與密鑰材料、LINE Messaging user ID 均加密保存且不得寫入一般分析日誌。
- Worker 以原子 claim 取得 Delivery；只有 `pending／retry_wait` 或 `claimed` 且 `claimExpiresAt` 已過期的 Delivery 可被 claim／reclaim。Worker 中斷時不得永久卡住工作。採 at-least-once 投遞與應用層冪等，不宣稱外部供應商 exactly-once。
- 每次送出與重試前重新驗證 Session revision、來源工作、部位狀態、期限與目的地。Session／部位已更新、工作過期或目的地撤銷時直接取消或重建摘要。
- 預設最多重試 3 次，使用 1、5、15 分鐘退避；超過 `expiresAt` 不再送出。正式數值可依 beta 調整，但須版本化。
- Session 更新後，舊 revision 的待送工作與 Bundle 必須取消或在送出前失效。Logical Job 以 `userId + sessionId + sessionRevision + reminderType + zoneInstanceId + causeFingerprint` 唯一；同一 Job 每個 channel 最多屬於一個有效 Bundle，Bundle 以版本化合併鍵冪等；每個目的地 Delivery 以 `bundleId + destinationId` 唯一。
- Quiet hours 預設不啟用。啟用時必須選定 IANA time zone、當地開始與結束時間；跨午夜區間由時區資料庫計算。一般內容延後；時間敏感的補擦提醒只有在使用者另行同意時才可於 quiet hours 發送，否則只更新 App 內狀態。偵測到旅行或瀏覽器時區改變時只提示更新，不得靜默改變通知時區。
- Provider 回覆成功不等於使用者實際看到通知；分析事件應分成「提交、供應商接受、已知失敗、使用者開啟」。
- 通知不得顯示疾病、產品個人備註或其他敏感內容；預設文案為「防曬提醒時間到了，請開啟防曬晴報員查看」。
- 同一 Session 有多個工作落入短合併視窗時，以 `NotificationBundle` 產生一則訊息。Bundle 的 `scheduledAt` 永遠等於來源工作中最早時間，只可納入最早時間至 `最早時間 + mergeWindow` 的有效工作，不得為等待其他部位延後最早通知。第一個部位可寫「現在需要處理」，較晚部位只能寫「也即將需要處理」。來源工作全部失效時取消 Bundle；部分失效時重建摘要，不得讓已補擦部位留在舊通知。部位名稱最多列兩個標準安全名稱，自訂標籤不外送，其餘以數量摘要。

### 8.6 快捷動作

- Web Notification action 僅作漸進增強，不是唯一操作入口。
- LINE 的「記錄已補擦全部所選部位／記錄局部補擦」按鈕位於聊天室訊息內，不承諾出現在作業系統鎖定畫面。
- 快捷動作開啟具驗證的頁面或使用具簽章、短效且冪等的 POST；不得以未驗證 GET 直接修改 Session。
- 通知點擊後導向相應 Session 與受影響部位，並先重驗最新 revision、狀態與期限；若 Session 已結束、部位已更新或通知已過期，只顯示目前真實狀態，不得執行舊動作。
- 通知 URL 只攜帶短效、不透明且不可枚舉的開啟識別碼；不得放入 `zoneInstanceId`、`bodyZoneCode`、`customLabel`、產品名稱、可直接枚舉的 Session ID 或其他敏感摘要。驗證完成後再取得目前狀態。

### 8.7 關閉頁面後的提醒運作

- PWA 技術可行，但「關閉頁面後仍提醒」只在 P1 遠端通知管道啟用後成立；P0 仍只承諾 App 內前景提醒與重新開啟後的狀態恢復。
- 不得使用頁面內 `setTimeout`、`setInterval` 或持續背景倒數作為關閉後通知的排程依據，因背景分頁可能被節流、凍結或回收。
- Web Push 被選為 P1 首發管道時，啟用順序為：說明登入、Session 上傳與送達限制 → 使用者登入 → 明確選擇 Guest migration → 伺服器成功建立／接管目前 Session 且解決衝突 → 由直接操作要求通知權限 → 建立 destination → 測試通知成功。之後提醒流程為：確認新塗抹／狀況 → 建立 `NotificationJob`／`NotificationBundle` → Worker 重驗 → 推送服務傳送 → Service Worker 顯示系統通知。
- LINE 被選為 P1 首發管道時，不要求瀏覽器通知權限或安裝 PWA；完成登入、Guest migration、Messaging 綁定、好友／可投遞狀態與測試訊息後才建立排程。拒絕任一步驟時維持 P0 App 內提醒。
- 使用者關閉瀏覽器分頁或將 PWA 從最近使用畫面滑掉後，只要訂閱、權限、網路與平台推送服務仍有效，後續 Web Push 原則上仍可送達；不得將此描述為保證送達。
- 使用者把單一通知滑掉，只移除該則通知，不等於取消後續排程。介面須另提供明確的 `暫停通知`、`結束本次提醒` 與 `關閉通知` 操作；暫停通知不得停止 App 內狀態或延長期限，恢復時不得一次補送多則已過期通知。
- 關閉系統通知權限、清除網站資料、解除安裝 PWA、撤銷訂閱、強制停止相關程式、裝置離線或系統省電／專注模式限制時，通知可能無法送達或延遲。
- iPhone／iPad 的 Web Push 僅在支援的系統版本、使用者將網站加入主畫面並由直接操作允許通知後啟用；一般瀏覽器分頁不得顯示成已完成 iOS 背景通知設定。
- 通知設定完成後須提供 `傳送測試通知` 及狀態檢查，至少區分：`尚未安裝`、`平台不支援`、`尚未授權`、`已啟用`、`已封鎖`、`訂閱失效`。測試完成只能確認當次通知是否已知提交／送達及由使用者自行回報是否聽見或感到震動；API、Push provider 或 Service Worker 不得因成功顯示通知就宣稱裝置實際發聲或震動。
- Web Push 設定頁對支援管道提供 `系統通知`、`聲音（跟隨手機設定）`、`震動（支援的裝置）` 與 `傳送測試通知`。LINE 管道的聲音與震動由 LINE App 及作業系統管理，不得顯示成防曬晴報員可控制。

---

## 9. PWA 與離線降級

### 9.1 可承諾的離線能力

- 開啟已快取的 App Shell。
- 查看最後一次成功取得的氣象資料及其時間；若已達 `unusable`，只能以歷史資料標示呈現，不得顯示成目前 UVI。
- 查看本機保存的產品、進行中 Session 與各部位狀態。
- 依最後可信時間資訊在前景恢復提醒狀態；時鐘不可信時採不延長原則並顯示警示。
- Guest 操作繼續保留在本機；已登入帳號可依第 8.3 節暫存 outbox，恢復連線後同步。

### 9.2 不可承諾的離線能力

- 取得最新 CWA 資料。
- AI 影像辨識。
- LINE、Email 或 Web Push。
- 網頁關閉或系統休眠後準時執行本機通知。

### 9.3 儲存方式

- Cache Storage：HTML、CSS、JavaScript、圖示與靜態衛教內容。
- IndexedDB：產品、Session、`ProtectionZoneState`、事件、最後氣象快照、`clockCalibration` 與有期限的 `SetupDraft`；登入帳號另保存離線 outbox。
- HttpOnly、Secure、SameSite Cookie：登入工作階段。
- 不在 LocalStorage 保存 access token、健康資料、精確位置或長效憑證。
- IndexedDB 寫入、revision compare-and-swap 及 active-session key 必須在同一 transaction；以 `BroadcastChannel` 或等效跨 context 機制通知其他分頁／PWA 重讀。若瀏覽器停用 IndexedDB、容量不足、資料遭系統清除或 transaction abort，畫面須顯示未保存並保留可重試草稿，不得先顯示「回報已更新」。

畫面離線時固定顯示：

> 目前離線。以下為最後一次取得的資料（時間：YYYY-MM-DD HH:mm），可能已與目前環境不同；遠端通知與同步暫時不可用。

### 9.4 手機安裝與平台引導

- 首頁及說明中心提供 `安裝到手機` 入口。首頁安裝入口永遠是次要層級，不得取代 `開始防曬提醒` 或 active Session 的最高優先操作；只有進入安裝說明頁後，平台支援且尚未安裝時，`安裝到手機` 才可成為該頁主要 CTA。
- Android／Chrome：優先使用瀏覽器提供的安裝提示；若未出現，教學引導使用者開啟瀏覽器選單並選擇 `安裝應用程式` 或 `加到主畫面`。實際文字可能因品牌及瀏覽器版本不同，教學須保留「名稱可能略有不同」提示。
- iPhone／Safari：教學引導使用者點選 `分享` → `加入主畫面` → 確認 `新增`，再從主畫面開啟防曬晴報員。若需 Web Push，須在主畫面版本內由使用者點選 `開啟通知` 後才要求權限。
- 不得在使用者首次開啟頁面時連續要求定位、安裝與通知權限。P0 只提供前景提醒；P1 Web Push 的順序依第 8.7 節為完成核心體驗 → 選擇登入／上傳 Session → 選擇安裝 → 從已安裝版本主動開啟通知。若首發為 LINE，不要求安裝或瀏覽器通知權限。
- 安裝不是查看 UVI 或使用 P0 前景提醒的必要條件；拒絕安裝時仍可繼續使用一般網頁。
- 教學須包含解除安裝、關閉通知與重新啟用的入口，並說明清除網站資料可能移除 Guest 本機 Session、產品及通知訂閱。

### 9.5 電力與背景資源策略

- 目標為低背景耗電：關閉或背景狀態下不持續執行畫面倒數、不持續定位、不以分鐘級輪詢 CWA，也不維持只為提醒用途的 WebSocket。
- 位置只在使用者主動選擇目前位置、重新整理所在地或明確啟用需定位的功能時取得；MVP 不使用持續 GPS。
- CWA 資料由後端依來源更新頻率集中取得並快取，前端不得讓每台裝置高頻直接輪詢。
- P1 Web Push 採事件驅動；Service Worker 僅在推送、必要同步或使用者開啟時短暫執行，不以 Periodic Background Sync 承諾分鐘級準時提醒。
- 頁面進入背景後暫停動畫、視覺倒數更新及非必要請求；回到前景時再以絕對時間戳重算畫面。
- Beta 須在代表性 Android 與 iPhone 裝置實測前景 30 分鐘、背景 2 小時及一日通知情境，記錄網路請求、背景喚醒、CPU 時間及相對電量變化；未完成量測前不得宣稱「不耗電」。

---

## 10. 帳號、登入與 LINE 綁定

### 10.1 帳號策略

- P0 以訪客模式為預設，不強制登入或註冊；介面用語採「以訪客身分開始」，避免暗示使用者已建立帳號或資料已同步。
- 登入／註冊僅為可選升級入口，須在按下前清楚說明可增加跨裝置同步、帳號歷史、資料匯出及相容時的遠端通知；不得將這些功能錯寫成訪客模式已具備。
- P1 先選一種登入方式完成上線品質，再加入第二種。
- 若保留密碼登入，必須包含 Email 驗證、密碼重設、登入限速、工作階段撤銷與安全雜湊；較建議先採 passwordless 或成熟 OAuth。
- Google／LINE OAuth 必須使用 `state`、PKCE、nonce 及明確的帳號合併流程。

### 10.2 LINE

- `line_login_user_id` 與 `line_messaging_user_id` 分欄保存。
- Login Channel 與 Messaging API Channel 的 Provider 關係必須依 LINE 規格配置。
- 必須處理未加好友、封鎖、解除綁定、不可投遞與重新授權。
- LINE 登入成功不得自動視為已開啟 LINE 通知。
- 使用者解除 LINE 後，撤銷對應 token、取消通知工作並刪除非必要識別碼。

### 10.3 Guest 合併與帳號刪除

- 合併前顯示會上傳的資料範圍。
- 進行中 Session 衝突不得靜默覆寫。
- 刪除帳號時，Profile、產品、Session、通知 token、待送工作與 OAuth 關聯必須依政策刪除。
- 提供資料匯出及刪除進度／完成結果。

---

## 11. AI 防曬標籤辨識（P2）

### 11.1 使用原則

- PRD 不寫死特定 Gemini 型號、免費額度或單次固定成本。
- 透過後端 Model Adapter 使用仍在供應商支援期的穩定模型。
- API 金鑰不得放在前端。
- 每季檢查模型生命週期、費率、配額、資料使用條款與地區可用性。

### 11.2 資料流程

1. 拍照前清楚告知圖片會傳至哪個處理者及用途。
2. 引導使用者只拍攝瓶身標籤，避免人物、地址或背景。
3. 前端裁切標籤並移除 EXIF。
4. 後端以短效物件處理；原圖預設不保存，暫存最長 24 小時後自動刪除。
5. AI 回傳候選欄位、原始辨識文字、信心程度與可驗證區域。
6. 使用者逐項確認後才能存入產品資料。

可辨識候選：

- SPF，例如 `50+`
- PA 等級，例如 `PA++++`
- 其他廣效／UVA 防護宣稱及其原文；不得自動換算成 PA
- 曝曬前使用時間或其他標籤使用指示
- 耐水：明確不耐水／未見宣稱／`40 min`／`80 min`／看不清楚
- 產品名稱

「物理／化學」不得只靠成分猜測後自動定案；混合配方或不確定時顯示未知。

### 11.3 失敗與備援

- AI timeout、配額或低信心時回到手動輸入。
- Barcode Detection 只能取得條碼內容；若要換算產品屬性，必須有可稽核的產品資料庫。
- AI 結果不得直接改動進行中 Session 或延長提醒。

---

## 12. 我的防曬品與個人紀錄（P0）

### 12.1 目的與資訊架構

- 避免每次啟動提醒都詢問「是否為有 SPF 標示的防曬產品」。產品身分只在新增、複製、編輯已保存產品，或首次建立 Session-only snapshot 時確認；同一個未修改的已保存產品不重問。
- 提供 `目前使用` 與 `過去紀錄` 兩個主要檢視；已用完、停止使用或封存的產品不出現在快速選擇，但可由本人查閱。
- P0 優先保存在本機；登入後才依使用者選擇同步。產品清單、個人備註、購買紀錄與粗粒度產品安全狀態不公開，也不放入通知或分析事件；私人備註與產品安全狀態預設不隨 Guest migration 上傳。
- 查看 UVI 不要求先建立產品。建立提醒時可選現有產品，或使用 `這次先不保存產品` 的 Session-only snapshot；不得強迫使用者先填寫完整產品檔案。

### 12.2 產品欄位與狀態

保存至「防曬品」時採短表單，產品暱稱為必填；Session-only snapshot 不要求暱稱。以下欄位除產品身分及當次規則確實需要的標示外均可稍後補充：

- 品牌／品名、購買日期、開封日期、有效期限與個人備註。
- 產品身分：`確認有防曬／SPF 標示`、`確認沒有`、`不確定／看不清楚`。
- SPF：可先選 `<15`、`15–29`、`30 以上`、`不確定`；包裝數字可選填。SPF 不直接換算成補擦分鐘。
- PA、廣效宣稱及 UVA 文字分欄保存。
- 曝曬前等待：`明確分鐘`、`未見數字指示`、`不確定`，只有明確分鐘可進入計算。
- 一般補擦間隔：`明確分鐘`、`未見數字指示`、`不確定`；自由文字只供顯示與人工確認。
- 耐水標示：`明確不耐水`、`未見宣稱`、`40 分鐘`、`80 分鐘`、`看不清楚`。
- 標示市場／司法管轄、語言、使用者確認時間及原始說明文字。
- 使用狀態：`目前使用`、`未開封`、`已用完`、`停止使用`、`封存`。
- 產品安全狀態：`未回報問題`、`使用者回報異常`、`使用後感到不適`；只保存阻止再次推薦同一產品所需的粗粒度狀態，不保存變色、異味、結塊、分層、症狀、病史、照片或自由文字細節。

只有產品身分為 `確認有防曬／SPF 標示`、本次標示 snapshot 合法、未在本次塗抹前過期且未回報異常／不適時，才可套用一般產品提醒規則。其餘產品，包括無防曬宣稱的純椰子油，仍可作私人產品／使用紀錄並標示「非防曬紀錄」或「身分不確定」，也可建立 `ruleEligibilityAtApplication` 不合格的 ApplicationEvent；但不得產生 120／40／80 分鐘期限。

產品有效期限未知時不自行套用通用開封年限。產品後來過期、被標記異常或修改標示，不回溯改寫過去合法 Application snapshot；下一次新增／補擦時依當時產品狀態重新判定。使用者選擇 `使用產品後感到不適` 時，將使用狀態改為 `停止使用`，並只保存 `user_reported_discomfort` 作為阻止再次推薦的安全狀態；不保存具體停止原因、症狀、病史、照片或自由文字。

### 12.3 低摩擦操作

- 啟動提醒時先顯示最多三款目前使用或近期使用的產品；點一下即可套用，再於最終摘要確認。
- 同一產品在標示未修改時不得重問產品身分。使用者編輯標示、複製成新版或表示包裝已更換時，才重新確認。
- `再次購買` 可複製暱稱與非安全性個人欄位，但產品身分、SPF、耐水與使用方式必須重新確認，避免不同批次或版本被誤認為相同標示。
- 個人備註只供本人閱讀，不用來推導產品有效性、醫療結論或購買推薦。欄位旁須提示不要輸入疾病、症狀、用藥、聯絡方式或其他敏感個資；P0 僅保存於本機，Guest migration 預設不勾選私人備註。若 P1 允許同步，須另行明確選擇。
- 使用者可將產品移至過去紀錄或刪除；只有 `conditionStatus=no_issue_reported`、未被產品安全事件封鎖，且只是一般 `已用完／封存／自行停止` 的產品可恢復為目前使用。`user_reported_abnormal` 或 `user_reported_discomfort` 不提供直接恢復。若是 `product_abnormal_reported`，不同批次或新包裝可建立獨立新版並重新確認實際標示；若是 `product_discomfort_reported`，僅換新包裝或同配方新批次不得自動繞過封鎖，只有改用另一款產品，或使用者能明確確認配方不同時才可建立獨立紀錄，且介面不得宣稱系統已判定適合使用。帳號資料匯出屬 P1。刪除、封存或恢復均不得回溯改寫既有 Session 的產品標示 snapshot。

---

## 13. 衛教與科學內容治理

### 13.1 內容原則

- 不以恐懼式敘述提升遵從度。
- 不把關聯研究寫成個人必然結果。
- 不把總太陽短波輻射研究外推成「台灣現在 UV 更毒」。
- 不將單一族群或小樣本研究外推至全台人口。
- 不宣稱一般防曬乳可以預防掉髮。
- 涉及疾病、症狀、病理、藥物、醫美或其他療程時，只能說明本服務的功能邊界與就醫／依醫囑方向；不得寫成個別診斷、治療、處方、預後、可否曝曬或產品適用判定。
- 免責文字須以白話放在特殊狀況入口與相關結果畫面，但不得被當成省略醫療內容審查、風險提示或產品 intended use 評估的理由。
- 眼睛防護以合格太陽眼鏡與帽子為主要可操作建議。
- UVI 低於 3 稱為一般風險較低，不稱為安全。
- 不把「天然」「植物來源」「不含特定 UV 過濾成分」直接寫成已證實有效防曬或已證實對珊瑚／海洋生物安全。
- 明確區分單一原料與完成 SPF、UVA／廣效及耐水測試的最終成品；成品含椰子油不代表由椰子油提供主要防曬力，純椰子油也不得沿用成品標示。

### 13.2 健康宣稱登錄表

每一則健康或科學文案至少記錄：

```text
claim_id
display_text
source_title
publisher
source_url / DOI
source_type
source_published_at
source_effective_at
source_accessed_at
jurisdiction
study_population
effect_or_recommendation
known_limitations
reviewer_id
reviewer_role
reviewer_credential_scope
conflict_of_interest
reviewed_at
next_review_at
status: draft / approved / retired
```

未完成審查的內容不得發布。審查者不得只保存姓名；須能確認角色、資格適用範圍及利益衝突。

所有會建立、提前、取消或阻止 120／40／80 分鐘期限的規則另建立證據連結：

```text
ReminderRuleEvidenceLink:
ruleset_version
rule_id
rule_description
trigger
deadline_expression
claim_ids[]
jurisdiction
approved_by[]
approved_at
effective_from
next_review_at
status: draft / approved / retired
```

每個啟用中的 `ruleId` 至少連到一筆適用市場的 approved claim。來源失效、被撤回或逾再審日期時，不得自動改成更寬鬆期限；必須重新審查，或降級為不產生產品期限的標示／物理防護提示。每個 Session 保存 `rulesetVersion` 及實際使用的 `ruleIds[]`，新規則不得回溯改寫既有歷史。

涉及海洋環境、珊瑚或 `reef-safe`／海洋友善宣稱時，另須記錄：

```text
claim_domain: human_health / marine_environment
jurisdiction
tested_material: ingredient / finished_product
test_context: laboratory / field / evidence_review
tested_species_or_endpoint
concentration_or_exposure_context
certification_scope
```

不得將單一成分的實驗室危害直接外推為所有完成品在真實海域必然有害，也不得以未檢出、缺乏研究或行銷標章反向推論為已證實安全。

### 13.3 需要移除的舊版敘述

- 「全球變亮證明現在的太陽更毒辣」。
- 「規律防曬確定降低 50% 黑色素瘤」。
- 未附代表性來源的台灣 Fitzpatrick III／IV 人口比例。
- 「防曬堆疊必然嚴重堵塞毛孔」。
- 「汗水與摩擦在 20–30 分鐘完全瓦解防護膜」。
- 「口罩下半臉 UV＝0」。
- 「頭皮防曬抗掉髮」。
- 由室外 UVI 推算窗邊 UVA 固定衰減倍率。

### 13.4 海邊防曬 Q&A（P0）

本節定義 `FAQ_BEACH_SUN_V1` 的繁體中文標準內容，適用於一般成人的海邊及水上活動衛教。內容須在說明中心常駐，並可從 `water_preparing`、`water_active`、耐水標示未知及水上事件流程就近開啟；入口不得以強制彈窗阻斷使用者記錄事件。

```text
content_id: FAQ_BEACH_SUN_V1
locale: zh-TW
audience: general_adult
status: draft
evidence_searched_at: 2026-07-22
reviewed_at: null
next_review_at: null
required_review: medical_content / marine_environment / legal_compliance
```

`reviewed_at` 與 `next_review_at` 在核准前保持 null，任一為 null 均阻擋發布；核准者須依 Claim Registry 的證據有效期填入日期。上線文案須保留各答案的核心限制與來源關聯；可以為版面縮短句子，但不得把「尚無直接證據」改寫為「已證實安全」，也不得把研究中的 SPF 數字換算成個人安全曝曬時間。詳細來源由 `查看資料來源` 展開，不在主要答案中堆疊技術名詞。

#### Q1：下水前可以用純椰子油取代防曬產品嗎？

不建議。2021 年研究對受測純椰子油進行 ISO 人體 SPF 測試，結果約為 SPF 1.2，屬於非常低、近乎可忽略的 UVB 防護。這是特定受測樣品的結果，不代表每瓶椰子油都有固定 SPF；但截至 `evidence_searched_at` 所檢索的證據，不支持把純椰子油當成海邊防曬替代品。本 Q&A 引用的受測樣品／研究也未提供可供系統採用的 UVA、廣效或標準耐水結果；沒有最終成品的明確防曬與耐水標示時，系統不得顯示已完成產品防護或建立耐水期限。

#### Q2：為什麼網路常說椰子油有 SPF 7～8？

這個數字主要來自 2010 年的體外研究。研究將油製成稀釋液，在 290～320 nm 範圍量測吸光度，再用公式推算 SPF；它不是將純椰子油作為最終成品塗在人體上的完整 SPF 試驗，也沒有驗證 UVA、耐水性或實際海邊使用效果。2021 年依較嚴謹方法進行的研究指出，早期數字遭到高估，並測得受測椰子油的人體 SPF 約為 1.2。因此不得把舊研究的 SPF 7～8 當成產品標示、廣效證明或安全曝曬時間。

#### Q3：椰子油不容易溶於水，是否代表它能耐水防曬？

不代表。疏水或油滑的觸感不能證明入水後仍能均勻留在皮膚上，也不能證明仍保有 UVA／UVB 防護。耐水 40 或 80 分鐘是最終防曬成品經標準浸水及浸水後 SPF 測試所得的標示；截至 `evidence_searched_at`，本 Q&A 引用的受測樣品／研究未提供可供系統採用的標準耐水結果。沒有最終成品的明確耐水標示時，系統不得自行套用 40／80 分鐘期限。

#### Q4：純椰子油是天然的，所以一定比較海洋友善嗎？

目前不能這樣下結論。現有海洋研究主要評估特定 UV 過濾成分、粒子或完整防曬配方，未足以直接證明泳客塗抹的純椰子油對珊瑚及其他海洋生物安全；目前資料也不足以直接認定正常使用量的純椰子油一定會傷害珊瑚。美國國家科學、工程與醫學院指出，`reef-safe`／珊瑚友善沒有可確保科學準確性的統一監管定義。產品不得以「天然」或未含某一成分代替完整環境證據。

#### Q5：擔心產品進入海水時，應如何兼顧皮膚與海洋環境？

優先用衣物及遮蔽減少需要塗抹防曬的裸露面積，例如穿著有正式 UPF 標示、適合下水的長袖水母衣或泳衣，並在岸上搭配帽子、太陽眼鏡、遮蔭及避開強 UV 時段。仍然裸露的皮膚應依所在地規定及實際產品標示，選擇具有可辨識 SPF、UVA／廣效與水上活動所需耐水標示的最終成品。「礦物型」「有機型／化學型」「天然」「不含某一成分」或 `reef-safe` 包裝文字，都不能單獨證明產品對所有珊瑚及海洋生物零影響；所在地限制特定成分時，以當地主管機關最新規定為準。

#### Q6：防曬成品含有椰子油，也代表保護力不足嗎？

不一定。完整防曬成品可能同時含有經允許的 UV 過濾成分、成膜劑及其他配方；椰子油在其中可能只是潤膚或質地成分。是否可依賴產品防曬力，應看最終成品是否具有可辨識的 SPF、UVA／廣效、耐水標示及完整使用說明，而不是只看其中是否含椰子油。反過來說，單有椰子油也不能沿用含椰子油防曬成品的 SPF 或耐水結果。

#### Q7：海邊或游泳前後應該何時補擦？

依實際產品標示，在曝曬或入水前預留標示要求的等待時間並足量、均勻塗抹。戶外活動至少依標示定期補擦；若產品標示耐水 40／80 分鐘，從可信的實際入水時間追蹤相應期限。達到標示時間、離水後擦毛巾、明顯擦拭或摩擦時，應依標示重新塗抹。耐水標示未知、未見耐水宣稱或入水時間不確定時，系統不建立水中期限，並採第 6.2 節的保守提示。任何倒數都只是補擦提醒，不是安全曝曬時間。

#### Q8：可以把椰子油混入防曬，或塗在防曬上增加效果嗎？

不要自行把純椰子油混入防曬產品。產品標示只適用於原本經測試的完整配方；自行混合或改變使用方式後，不能假設原有 SPF、UVA／廣效及耐水結果仍然成立。若把椰子油疊在防曬上，該組合也通常沒有完成相同的最終成品測試，不得宣稱可提高 SPF 或延長耐水時間；需要保濕時，優先選擇本身含潤膚成分且具有完整防曬標示的成品，並遵循其使用說明。

### 13.5 產品與 PWA Q&A（P0）

本節定義一般使用者可在說明中心查看的安裝及運行說明。內容須依平台能力進行 feature detection 後顯示，不得僅依裝置名稱猜測，也不得把 P1 遠端通知能力寫成 P0 已提供功能。

#### Q1：如何把防曬晴報員安裝到手機？

**Android／Chrome**

1. 使用 Chrome 開啟防曬晴報員。
2. 點選頁面上的 `安裝到手機`；若沒有顯示，開啟 Chrome 選單。
3. 選擇 `安裝應用程式` 或 `加到主畫面`，再依畫面確認。
4. 安裝後可從手機主畫面開啟。選單名稱可能因手機品牌及瀏覽器版本略有不同。

**iPhone／Safari**

1. 使用 Safari 開啟防曬晴報員。
2. 點選 Safari 的 `分享` 按鈕。
3. 選擇 `加入主畫面`，再點選 `新增`。
4. 從主畫面開啟防曬晴報員。若當次版本已提供 P1 Web Push，系統會先說明登入、Session 遷移與送達限制；完成後再由使用者主動點選 `開啟通知` 並依系統畫面允許。只有建立 P0 本機提醒時不會要求通知權限。

不安裝也能使用一般網頁及 P0 前景功能；安裝主要提供類似 App 的開啟方式、離線 App Shell，以及在 P1 啟用 Web Push 後所需的平台能力。關閉通知、清除網站資料或解除安裝後，遠端提醒可能停止，Guest 本機資料也可能被移除。

#### Q2：防曬晴報員是怎麼運作的？

1. 使用者同意定位或手動選擇地區後，系統取得中央氣象署可用的測站觀測或區域預報；也可選擇 `暫不提供地區` 並直接使用提醒。
2. 有地區資料時，畫面顯示 UVI 分級、資料來源、測站／區域、資料時間及是否過期；未提供地區時顯示資料不足，不猜測 UVI。這些都是環境風險資訊，不是個人曬傷倒數。
3. 使用者自行確認活動情境、外露部位、防護方式、產品標示及新塗抹時間。
4. 系統依實際產品標示與游泳、流汗、擦毛巾、摩擦等事件，計算各部位的建議檢查／補擦時間；UVI 不會用來延長補擦期限。
5. P0 在網頁前景顯示提醒，重新開啟後以保存的絕對時間戳恢復狀態。若 P1 啟用 Web Push，後端會保存提醒排程，到時重新驗證最新狀態，再由手機系統顯示通知。
6. 通知是輔助功能，可能受通知權限、網路、系統省電、專注模式、清除資料或解除安裝影響；App 內最新狀態才是最終真值。

簡化流程：

```text
選擇地區／定位，或暫不提供地區
    ↓
有地區時取得並標示 CWA 紫外線資料
    ↓
確認情境、外露部位、產品與塗抹時間
    ↓
依產品標示及使用者回報狀況建立部位提醒
    ↓
前景顯示狀態；P1 可由後端排程發送 Web Push
    ↓
補擦或事件回報後重新計算
```

#### Q3：倒數時間是怎麼運作的？

倒數顯示的是「距離建議檢查或補擦的時間」，不是防曬失效的精確時間，也不是在歸零前一定不會曬傷的安全曝曬時間。

運作方式如下：

1. 使用者確認本次實際塗抹時間、適用部位、防護方式及產品標示後，系統只為具有合格產品 snapshot 與可信時間的部位建立建議檢查／補擦期限 `zoneDueAt`；資料不足、尚未防護、只採衣物／遮蔭或不具期限資格時改顯示無時間行動卡。
2. 系統保存的是「到期的絕對時間」，例如 `2026-07-25 15:30`，不是在背景每秒減一的數字。
3. 畫面開啟時，以 `zoneDueAt - 目前可信時間` 即時計算剩餘時間。因此關閉頁面後畫面雖然不會持續執行倒數，重新開啟時仍能依時間戳恢復正確狀態。
4. 不同部位可以有不同期限。局部補擦只會更新已確認補擦的部位，不會自動延長其他部位。
5. 游泳、大量流汗、擦毛巾或明顯摩擦等事件可能讓相關部位提前需要補擦；系統會依使用者回報的事件重新計算或直接標示 `建議現在補擦`。
6. 若防曬產品標示耐水 40／80 分鐘，只有在使用者確認實際入水時間後，系統才會依標示建立水中期限。入水時間不確定或耐水標示不明時，不猜測剩餘耐水時間，改採保守提醒。
7. 到達期限後，畫面停止倒數並改成 `建議現在檢查／補擦` 的靜態卡，不持續顯示 `00:00` 或負數。即使尚未到期，只要已游泳、流汗、擦拭、防曬被摩擦掉，或產品標示要求提早處理，也應提前檢查。
8. UVI 用於說明當下環境風險及防護強度，不會用來延長倒數；較低的 UVI 也不代表可以忽略產品標示或事件提醒。
9. P0 只在網頁前景顯示狀態；重新開啟後會恢復倒數。若 P1 啟用 Web Push，後端會依相同的到期時間安排通知，但通知可能因網路、權限、系統省電或專注模式而延遲，所以 App 內狀態才是最終依據。

簡化範例：

```text
使用者確認 13:30 已塗抹臉部與手臂
    ↓
系統依產品標示及提醒規則建立各部位 zoneDueAt
    ↓
畫面用 zoneDueAt 減去目前時間顯示剩餘時間
    ↓
14:20 使用者回報擦毛巾
    ↓
只讓受影響部位提前成為「建議現在補擦」
    ↓
確認補擦後，只更新實際補擦的部位與時間
```

固定提示文案：

> 此倒數是防曬檢查／補擦提醒，不代表安全曝曬時間。若已游泳、大量流汗、擦毛巾或發生明顯摩擦，請提早檢查並依產品標示補擦。

### 13.6 UV知識庫（P0.5 Public Launch）

#### 13.6.1 定位與入口

`UV知識庫` 是一般防曬衛教的獨立內容專區，常駐於底部導覽的 `更多`，不新增第五個底部導覽項目。內容涵蓋防曬產品、穿戴與遮蔽方式、不同生活情境、常見迷思及需要轉介專業人員的界線。

除常駐入口外，系統可在使用者需要資訊的當下提供次要就近入口：

- 選擇產品時：`SPF、PA 怎麼看？`
- 水上活動或耐水標示不明時：`耐水 40／80 分鐘怎麼看？`
- 選擇手背或記錄洗手時：`洗手後需要補擦嗎？`
- 查看倒數時：`為什麼 SPF 不是補擦時間？`
- UVI 高或尚未採取產品防護時：`除了防曬乳，還能怎麼防護？`

就近入口不得使用強制彈窗、阻斷事件紀錄或自行修改產品、Session、部位狀態及提醒期限。閱讀完成後須返回原操作位置並保留草稿。

#### 13.6.2 內容分類

第一版使用六個白話分類，不以光生物學、防曬劑機轉等學科名稱作第一層導覽：

1. **今天怎麼防曬**：目前 UVI、陰天、通勤、室內窗邊、出門前準備及何時檢查／補擦。
2. **看懂防曬乳**：SPF、PA、廣效宣稱、耐水 40／80 分鐘、曝曬前等待、用量、劑型、保存及效期。
3. **不只防曬乳**：正式 UPF 衣物、水母衣、帽子、太陽眼鏡、陽傘、遮蔭、口罩、布料覆蓋及車窗／室內窗戶。
4. **不同場合怎麼做**：日常通勤、上班／上課、開車／搭車、騎車、戶外運動、登山、海邊、游泳、帶妝、大量流汗與洗手後手背。
5. **防曬迷思破解**：SPF 與補擦時間、陰天、季節、純椰子油、油滑感與耐水、海洋友善宣稱及一次塗抹是否足夠。
6. **曬後與特殊狀況**：一般曬後注意、停止一般提醒的情境及依醫囑／詢問醫師的轉介界線；不得提供疾病診斷或個別治療建議。

文章可屬於多個分類，但每篇必須有一個主要分類。第一層最多六類；若內容增加，優先使用搜尋、標籤與情境篩選，不持續增加首頁分類數量。

#### 13.6.3 每篇內容格式

每篇內容依序呈現：

1. 使用者會說的問題式標題。
2. 預估閱讀時間：`30 秒`、`1 分鐘` 或 `3 分鐘`。
3. `10 秒看答案`：一句話先回答，不用術語鋪陳。
4. `我現在可以怎麼做`：提供可執行但不保證結果的行動建議。
5. `容易誤會的地方`：說明條件、例外與不可推論之處。
6. `想知道為什麼`：由使用者主動展開原理與較完整證據。
7. 適用時顯示 `什麼情況應依醫囑或詢問醫師`。
8. 資料來源、內容類型、審查者、最後審查日期及下次再審日期。

列表卡只顯示問題式標題、10 秒答案摘要、閱讀時間及內容狀態，不在卡片堆疊完整文章。搜尋須比對標題、白話同義詞與標籤，例如 `防水` 可找到 `耐水標示`，但不得將搜尋字詞送往第三方分析服務。

#### 13.6.4 有趣但不失真的互動

允許使用以下非強迫式內容形式：

- **先猜猜看**：單題選擇後立即顯示答案與原因；不需要登入、不建立健康分數，也不公開排名。
- **迷思判決**：使用 `大致正確`、`只對一半`、`證據不支持`、`要看情境`、`目前無法確定`，避免把有條件的證據硬分為對／錯。
- **情境攻略卡**：依通勤、海邊、運動、洗手等事件分成可勾選步驟；勾選只代表閱讀或自陳完成，不得當成防護已被驗證。
- **晴報員一句話**：用輕鬆、尊重的短句提醒一個重點；不得以恐嚇、責備或羞辱提高互動。
- **延伸挑戰**：提供下一篇相關內容或一題複習，不以連續登入、倒數壓力或失去獎勵迫使閱讀。

可使用生活比喻、反問與適度幽默，但笑點不得建立在疾病、外貌、曬傷、使用者答錯或未完成防護之上。不得使用 `擊敗紫外線`、`完全防禦`、`現在安全` 等會造成錯誤安全感的遊戲文案。

#### 13.6.5 內容治理、來源與著作權

- 每篇內容沿用第 13.1、13.2 節的宣稱登錄與發布 gate；醫療、產品標示及海洋環境內容分別由相應專業人員審查。
- 可學習其他衛教網站的分類方式與溝通手法，但不得複製第三方文章、標題、圖片、插圖或具辨識度的系列名稱。外部資料只以摘要、合法引用或直接超連結使用，並保留來源及存取日期。
- `曬後與特殊狀況` 不得變成症狀自我診斷工具；涉及疾病、病理、藥物或療程時，依第 5.6 節停止一般推論並轉介醫師／原療程院所。
- 小測驗答案、文章閱讀與分類瀏覽預設只作本機互動；若日後分析內容成效，只能使用不含帳號、產品、Session 或健康內容的短效假名化流程事件，記錄內容 ID、開啟／完成狀態與版本，不記錄自由文字、疾病、部位、產品或個別答案組合。未完成合理的無法再識別評估前不得稱為匿名。
- 內容下架或審查過期時，既有連結顯示 `內容更新中` 及安全替代說明，不得繼續呈現失效健康宣稱。

#### 13.6.6 公開 SEO 資訊架構與索引邊界

公開網站的 SEO 架構以「防曬提醒」為第一優先；`今日UV` 是支援提醒判斷的環境資訊，不得在首頁首屏、搜尋落地頁或主要導覽中取代 `開始防曬提醒`／`查看目前提醒` 的主要行動。

公開可索引的主題層級：

```text
防曬提醒（第一優先）
├─ 如何開始防曬提醒
├─ 建議補擦時間如何運作
├─ 已補擦與回報狀況
├─ 洗手、流汗、游泳與擦拭後怎麼做
└─ PWA 通知、背景運行與送達限制

今日UV（支援資訊）
├─ 所在地 UV 資料
├─ UVI 等級與資料時間怎麼看
├─ 測站觀測與區域預報的差異
└─ 陰天、室內窗邊與重新外出的注意事項

UV知識庫
├─ 今天怎麼防曬
├─ 看懂防曬乳
├─ 不只防曬乳
├─ 不同場合怎麼做
├─ 防曬迷思破解
└─ 曬後與特殊狀況

說明中心
├─ 手機安裝方式
├─ 防曬晴報員運行原理
├─ 倒數時間運作原理
├─ 通知權限與限制
└─ 隱私、免責與資料來源
```

App 內底部導覽仍依第 5.7.6 節維持 `首頁｜提醒｜防曬品｜更多`，不得為 SEO 另增第五個主入口。公開內容頁可使用麵包屑、分類頁與相關內容連結建立可檢索層級，但不得把個人 Session、產品私人備註或帳號資料暴露為可索引頁。

索引規則：

- 可索引：公開品牌首頁、防曬提醒說明頁、不含當前數字的穩定 `今日UV` 地區解讀頁、UV知識庫分類／文章、公開說明中心。
- 原則上 `noindex`：登入／註冊、帳號、個人產品、進行中提醒、歷史紀錄、設定、權限回呼、錯誤頁、站內搜尋結果、獨立的當前數字 UVI 路由及帶有個人化／暫時參數的頁面。
- 當前 UVI 數字不得放入可長期快取的 `<title>`、meta description、JSON-LD、Open Graph 或靜態摘要。若穩定地區解讀頁在可見頁面中動態顯示最新值，必須同時顯示來源與時間，且搜尋 metadata 只描述資料用途與查看方式，避免搜尋或 AI 摘要引用過期數字。
- `robots.txt` 不作個資保護機制；敏感或個人頁面必須同時具備正確存取控制、快取政策與 `noindex`，未登入不得由網址直接取得內容。
- 每個可索引頁提供唯一、描述性的 `<title>`、meta description、canonical URL、單一清楚 H1、可爬取的 `<a href>` 內部連結及一致的繁體中文名稱。
- 新文章使用可讀且穩定的永久網址，不以無意義流水號作主要識別；網址變更須 301 轉址、更新 canonical、站內連結與 XML sitemap。

#### 13.6.7 SEO 文章模板與內部連結

每篇公開 UV知識庫文章依序包含：

1. 麵包屑：`首頁 → UV知識庫 → 主要分類 → 文章`。
2. 單一問題式 H1，不使用誇張、恐嚇或無證據的效果承諾。
3. 撰寫者、專業審查者、初次發布日期、最後實質審查日期及利益／商業關係揭露。
4. `10 秒看答案`，直接回應主要搜尋問題。
5. 可點擊的頁內目錄；長文以語意正確的 H2／H3 分層，不使用空白標題或只靠圖片承載文字。
6. `我現在可以怎麼做`、條件／例外、不可推論範圍及適用時的醫療轉介。
7. 宣稱附近的直接來源，以及文末完整參考資料、研究限制與查閱日期。
8. 一個與內容直接相關的工具入口；若文章與補擦有關，主要 CTA 優先為 `開始防曬提醒`，`查看今日UV` 為次要入口。
9. 2～4 篇真正相關的知識內容，不以大量「最新文章」或無關標籤稀釋主題。

分類與標籤規則：

- 每篇只有一個主要分類，可有少量經治理的次要標籤；同義標籤合併，不建立只有一篇文章或沒有獨立說明的薄弱索引頁。
- 內容應回答真實使用者任務並提供原創整理、情境或分析，不為堆疊關鍵字大量改寫相同文章。
- `防曬提醒`、`補擦提醒`、`多久補擦`、`洗手後防曬`、`流汗後補擦` 等主題優先連向提醒說明或開始流程；`今日UV` 文章再連回相應防護行動，不讓 UV 數字成為沒有下一步的孤立資訊。

#### 13.6.8 技術 SEO、結構化資料與成效驗證

- 公開文章使用與可見內容一致的 `BlogPosting`／`Article` JSON-LD，至少評估 `headline`、`description`、`image`、`author`、`datePublished`、`dateModified`、`publisher` 與 `mainEntityOfPage`；只有實際完成內容更新與再審時才修改 `dateModified`。
- 麵包屑可加入 `BreadcrumbList`；組織與網站名稱資料只在真實一致時加入。FAQ 結構化資料不作曝光保證，也不得標記頁面上看不到的問答。
- 資訊圖片提供描述內容用途的替代文字；純裝飾圖片使用空 `alt`。比較資料優先使用 HTML 文字／表格，圖片設定尺寸並使用適當壓縮、響應式來源及延遲載入。
- 以行動版為索引與驗收基準；公開頁須檢查 LCP、INP、CLS、鍵盤／螢幕閱讀器、文字對比、圖片位移及第三方程式負擔。
- 維護 XML sitemap、canonical、重新導向與 404／410 政策；用 Google Search Console 監控索引、查詢、曝光、點擊率、內容失效及結構化資料錯誤。
- SEO 成效不得只看排名。至少分別追蹤公開內容的有效索引率、非品牌自然搜尋曝光／點擊、文章到 `開始防曬提醒` 的短效假名化流程轉換，以及內容審查仍有效的比例；分析不得上傳個人產品、部位、疾病、精確位置或提醒歷史。

#### 13.6.9 使用者優先的 AEO 內容規格

本文件將 AEO（Answer Engine Optimization）定義為：在不犧牲使用者體驗、內容正確性與隱私的前提下，讓公開內容更容易被搜尋引擎、AI 摘要及問答系統正確理解、擷取與引用。AEO 不視為獨立排名捷徑，也不保證被任何 AI 系統收錄、引用或置於答案前列。

AEO 與產品的優先關係：

1. 使用者必須先能看懂現在狀況並完成 `開始防曬提醒`、`記錄已補擦` 或 `回報狀況`。
2. AEO 只調整公開說明與 UV知識庫內容，不得增加提醒設定欄位、要求登入、改變倒數規則或在進行中提醒首屏插入長篇內容。
3. 若一項 AEO 改版使核心任務完成率下降、操作時間增加、誤觸／更正率上升或無障礙閱讀變差，即使自然搜尋或 AI 引用增加，也不得直接發布；須回復、簡化或重新測試。

每個重要問題可使用以下「答案單元」，但只在對讀者有幫助時採用，不強迫所有段落套相同格式：

1. **問題標題**：使用者真正會問的一個問題，避免為每個同義詞建立不同頁面。
2. **直接答案**：前 1～3 句先回答結論，不先放品牌故事、長篇前言或產品推銷。
3. **適用條件**：說明答案在什麼情境成立，例如是否游泳、流汗、擦拭、洗手，以及產品是否具有耐水標示。
4. **可以怎麼做**：提供一個清楚的下一步；涉及補擦時，優先連到 `開始防曬提醒`。
5. **限制與不確定性**：明示資料不足、不能推論或需依產品標示／醫囑的部分，不把一般資訊寫成個人醫療判定。
6. **來源與時間**：重要健康、產品標示、UV或海洋環境敘述在宣稱附近提供來源，並顯示實質審查日期。

內容清晰度規則：

- 一個段落原則上處理一個主要概念；使用短段落、描述性 H2／H3、清楚清單及必要時的比較表，但不為機器擷取把內容切成失去上下文的碎片。
- 名稱、單位與實體保持一致，例如 `UVI`、`SPF`、`PA`、`耐水 40 分鐘`、`耐水 80 分鐘`、`防曬提醒`；第一次出現時提供白話定義。
- 比較內容使用相同欄位與單位，表格前後仍提供文字結論，使行動裝置與螢幕閱讀器不必只靠橫向表格理解。
- FAQ 只收錄真實使用者研究、客服、站內搜尋或易用性測試中反覆出現的問題；避免為了涵蓋長尾關鍵字大量製造近義問答。
- 提供防曬晴報員自身能產生的原創價值，例如提醒運作圖解、情境流程、部位差異、產品標示範例及經專業審查的決策界線，不只摘要其他網站。
- 圖片、影片與圖解只在能幫助理解時加入，並提供等價文字；不得把關鍵答案只放在圖片、Canvas或影片中。

禁止的 AEO 做法：

- 不建立只供搜尋引擎或 AI 讀取、使用者看不到的隱藏答案、關鍵字或結構化資料。
- 不把相同答案改寫成大量城市、族群、季節、產品或問句變體頁；沒有獨立價值時合併至主要頁並使用 canonical／重新導向。
- 不為 AI 強制建立 `llms.txt`、特殊 AI schema、額外 Markdown 副本或極小內容切片；若未來特定服務有明確用途，須另行驗證及記錄，且不得當成 Google 搜尋曝光要件。
- 不購買虛假提及、偽造專業作者、虛構引用、偽造使用經驗或自動發布未經人工與專業審查的健康內容。
- 不以 AI 引用次數、排名、停留時間或頁面瀏覽數取代核心產品 KPI；AEO 指標只能作次要成效。

AEO 成效可在不含個人與健康資料的前提下，觀察：

- Search Console 的生成式 AI／Web 搜尋曝光、查詢與點擊資料（依當時實際提供的報表為準）。
- Bing Webmaster Tools 可用時的引用頁面、引用次數及 grounding query 樣本；引用次數不解讀為權威分數或排名。
- 從公開內容進入 `開始防曬提醒` 的短效假名化流程轉換、使用者是否快速找到答案，以及答案後仍需重複搜尋或求助的比例。
- 核心任務完成率、時間、錯誤率、放棄率及無障礙測試結果必須與 AEO 指標並列，且前者具有發布否決權。

---

## 14. 資料模型

### 14.1 核心資料表

#### Users（P1）

```text
id, email_nullable, status, locale, created_at, updated_at, deleted_at
```

#### UserIdentities（P1）

```text
id, user_id, provider: email / google / line_login,
provider_subject_ciphertext, provider_subject_hash,
verified_at, revoked_at_nullable,
created_at, updated_at

UNIQUE (provider, provider_subject_hash)
```

LINE Login identity 只用於帳號驗證；LINE Messaging 的可投遞識別另存於 `LineDestination`，兩者不得假設相同。

#### UserProfiles（P1）

```text
user_id, default_region,
preferred_time_zone_iana, time_zone_confirmed_at,
privacy_version, created_at, updated_at
```

MVP 不保存性別、Fitzpatrick 膚型、疾病、藥物或醫美療程作為提醒權重。

#### SunscreenProducts

```text
id, owner_type, owner_id, nickname, brand_name_nullable,
product_identity_status:
  confirmed_sunscreen / confirmed_no_sunscreen_claim / unknown,
spf_band: under_15 / 15_to_29 / 30_plus / unknown,
spf_label_nullable, pa_rating,
broad_spectrum_claim: present / absent / unknown,
uva_claim_text,
label_schema_version,
pre_exposure_wait_status:
  explicit_minutes / no_instruction / unknown,
pre_exposure_wait_minutes_nullable,
reapplication_interval_status:
  explicit_minutes / no_numeric_interval / unknown,
reapplication_interval_minutes_nullable,
water_resistance_status:
  not_water_resistant / no_claim / 40 / 80 / unknown,
label_market_or_jurisdiction_nullable,
label_language_nullable,
label_confirmed_at,
raw_instruction_text_nullable,
usage_status:
  current / unopened / used_up / stopped / archived,
purchase_date_nullable, opened_date_nullable, expiry_date_nullable,
condition_status:
  no_issue_reported / user_reported_abnormal /
  user_reported_discomfort,
condition_reported_at_nullable,
condition_sync_scope: local_only / account_opt_in,
private_note_nullable,
private_note_sync_scope: local_only / account_opt_in,
source: manual / ai_confirmed / catalog,
source_text,
status: active / deleted,
created_at, updated_at, deleted_at_nullable
```

`explicit_minutes` 時 minutes 必填且為正整數，其他狀態時必須為 null；水上 minutes 只由明確 40／80 狀態取得。PA 與其他 broad-spectrum／UVA 宣稱分欄保存，不互相推導。`private_note` 與 `condition_status` 不得進入通知、分析、一般錯誤紀錄或預設 Guest migration；帳號同步前須各自明確選擇。產品能否建立期限須同時依身分、標示 snapshot、效期與狀況判定，不可只檢查 `product_identity_status`。

每筆 ApplicationEvent 保存不可變的 `ProductLabelSnapshotV1`，白名單如下：

```text
snapshot_version
product_identity_status
spf_display_text_nullable
pa_rating_nullable
broad_spectrum_claim
uva_claim_text_nullable
pre_exposure_wait_status
pre_exposure_wait_minutes_nullable
reapplication_interval_status
reapplication_interval_minutes_nullable
water_resistance_status
water_resistance_minutes_nullable
raw_instruction_text_nullable
label_market_or_jurisdiction_nullable
label_language_nullable
label_confirmed_at
source_product_id_nullable
source_product_updated_at_nullable
captured_at
rule_eligibility_at_application:
  eligible / expired / abnormal /
  user_discomfort / no_sunscreen_claim /
  identity_unconfirmed
eligibility_reason
```

Snapshot 不包含產品暱稱、品牌、購買紀錄或私人備註。`raw_instruction_text` 只供顯示與人工確認，不參與 reducer；產品日後修改或刪除不得回溯改變既有事件。

#### ProtectionSessions

```text
id, owner_type, owner_id,
default_product_id_nullable,
body_zone_schema_version,
setup_entry_mode: reuse / quick_preset / self_select,
preset_decision: accepted / adjusted / not_shown,
suggested_preset_version_nullable,
overall_status: tracking / attention_required / ended,
started_at, session_next_due_at_nullable,
current_context: indoor_away / indoor_window /
  outdoor_general / outdoor_exercise /
  water_preparing / water_active,
shade_context: none / partial / full / unknown,
weather_snapshot_id_nullable,
ruleset_version, applied_rule_ids,
revision,
created_at, updated_at,
ended_at_nullable,
ended_reason_nullable:
  user_ended / replaced_by_new_session /
  account_conflict_resolution
```

`session_next_due_at` 是所有目前追蹤部位非空 `zone_due_at` 的最小值，只供時間型排程，不直接代表首屏應顯示的動作。`primaryAction` 每次依目前 revision、有效事件、部位狀態與 ruleset 衍生，不作為可獨立修改的真值；若為效能而快取，快取鍵必須包含 `session_id + revision + ruleset_version`。

#### ProtectionZoneStates

此表是事件衍生的可重建狀態；SessionStartedEvent、ZoneTrackingEvents、ZoneMethodEvents、ApplicationEvents 與 ContextEvents 才是稽核真值。

```text
session_id, zone_instance_id,
body_zone_code, custom_label_nullable,
tracking_status: active / ended,
skin_exposure_status: exposed / clothing_covered / unknown,
method_certainty: confirmed / none_reported / unknown,
record_status:
  unrecorded / sunscreen_recorded /
  physical_method_reported / mixed /
  none_reported / unknown,
method_components[] (set):
  sunscreen / clothing / other_topical,
timing_status:
  label_wait / tracking / reapply_soon /
  reapply_due / untimed_action / not_applicable,
last_applied_at_nullable,
label_ready_at_nullable,
zone_due_at_nullable,
zone_next_action_at_nullable,
product_id_nullable,
active_cause_refs, active_rule_ids,
derived_from_event_refs,
updated_at

PRIMARY KEY (session_id, zone_instance_id)
UNIQUE (session_id, body_zone_code)
  WHERE body_zone_code != custom
```

標準部位在同一 Session 只能有一個 instance；`custom` 可有多個 instance，必須以穩定的 `zone_instance_id` 區分。自訂標籤只在本機或已授權的帳號畫面顯示，不得放進遠端通知、分析事件或一般日誌。

`method_components[]` 是不可重複的集合欄位，不得用單一 `mixed` 值取代實際組成；`mixed` 只可作衍生 `record_status`。合法組合如下：

- `method_certainty=confirmed` 時集合必須非空。`skin_exposure_status=exposed` 時不得含 `clothing`；`skin_exposure_status=clothing_covered` 時必須含 `clothing`，可同時含使用者確認曾塗於衣物下方的產品。
- `method_certainty=none_reported` 時 `skin_exposure_status=exposed` 且集合為空，表示舊版資料曾回報尚未採取防護。
- `method_certainty=unknown` 時集合為空，`skin_exposure_status` 可為 `exposed` 或 `unknown`，表示舊版、遷移或損壞復原資料的方法無法確認。
- `skin_exposure_status=unknown` 只能搭配 `method_certainty=unknown`。其他交叉組合回傳驗證錯誤，不得由工程端猜測。

已建立的資料必須讓每個 active 部位符合上述其中一種組合；但當前 P0 新建 Session 的每個部位只接受 `method_certainty=confirmed`。`none_reported`、`unknown` 與 `unrecorded` 僅供既有 Session 重播、舊版遷移、SetupDraft 或資料損壞復原，不是目前 StartSession 的正常輸出，也不得出現在 `沿用最近設定` 的直接提交內容。`record_status`、`timing_status` 與期限由目前有效的 ZoneTrackingEvent、ZoneMethodEvent、ApplicationEvent、產品標示 snapshot、ContextEvent 與規則共同重建，不能只靠最近一筆方法事件。遮蔭屬於 Session／情境資料，不是身體部位的固定防護方法。Session 的 `overall_status` 只取 `tracking_status=active` 部位中最需要處理者；任何單一部位的補擦都不得覆蓋其他部位狀態。

#### 事件共通時間與排序欄位

所有不可變事件與 ApplicationConfirmationGroup 至少保存：

```text
effective_occurred_at,
client_created_at,
server_received_at_nullable,
client_sequence,
server_applied_sequence_nullable,
idempotency_key,
created_at
```

- `effective_occurred_at`：使用者確認「實際發生」的時間，是期限與因果判斷的業務時間。
- `client_created_at`：裝置建立操作的時間，只供同步、稽核及問題排查，不可取代實際發生時間。
- `server_received_at`：後端收到時間；P0 本機模式可為 null。
- `client_sequence`：同一裝置／同步批次內的穩定順序。
- `server_applied_sequence`：伺服器成功套用後的全序；P0 reducer 必須產生等效的穩定順序。

排序先依 `effective_occurred_at`，同時點再依已確認的因果關係與穩定 sequence。會解除某原因的 Application 必須嚴格晚於該原因；同時點視為未解除，採保守狀態。晚到、離線重播及更正都須重跑相同 reducer，不能依 HTTP 抵達順序決定結果。

#### SessionStartedEvents／ZoneTrackingEvents／ZoneMethodEvents

```text
SessionStartedEvent:
id, session_id, body_zone_schema_version,
tracked_zone_instance_ids,
initial_context: indoor_away / indoor_window /
  outdoor_general / outdoor_exercise /
  water_preparing / water_active,
shade_context: none / partial / full / unknown,
effective_started_at,
<事件共通時間與排序欄位>

ZoneMethodEvent:
id, session_id, zone_instance_id, body_zone_code,
skin_exposure_status: exposed / clothing_covered / unknown,
method_certainty: confirmed / none_reported / unknown,
method_components[] (set):
  sunscreen / clothing / other_topical,
correction_of_event_id_nullable,
correction_action_nullable: replace / void,
source: user / confirmed_suggestion,
<事件共通時間與排序欄位>

ZoneTrackingEvent:
id, session_id, zone_instance_id, body_zone_code,
event_type: tracking_started / tracking_ended,
correction_of_event_id_nullable,
correction_action_nullable: replace / void,
source: user,
<事件共通時間與排序欄位>
```

`SessionStartedEvent.effective_started_at` 是方便查詢的重複欄位，資料庫／共用驗證器必須強制它等於同一事件的 `effective_occurred_at`；兩者不符即拒絕，不得選其中一個作真值。

Session 建立時必須為每個 `tracked_zone_instance_ids` 寫入初始 ZoneMethodEvent；`SessionStartedEvent` 的初始清單保持不可變。之後外露狀態或方法改變時追加新 ZoneMethodEvent，不直接覆寫歷史。若使用者新增追蹤部位，須以明確命令原子建立／啟用 zone instance、`tracking_started` 與完整初始方法事件；停止追蹤時追加 `tracking_ended`。重新追蹤同一標準部位可沿用該 Session 內的穩定 instance 並追加新的 `tracking_started` 與完整方法事件，不得讓結束前的舊期限直接復活。`sessionNextDueAt` 與 `primaryAction` 只納入 latest tracking state 為 active 的 instance。

ZoneTrackingEvent／ZoneMethodEvent 更正時 `zone_instance_id` 與 `body_zone_code` 不可改變。任何 replace／void batch 完成後，每個目前 active 部位都必須仍可歸納出唯一 tracking state 及合法的外露狀態與方法；不得單獨 void 唯一的初始方法事件。若最初選錯部位，須在同一原子命令中合法追加舊 instance 的 `tracking_ended`，並建立／啟用新 instance、`tracking_started` 與完整初始方法。P0 IndexedDB reducer 與 P1 伺服器均須拒絕會讓狀態缺漏、互相衝突或無法唯一重建的更正。

#### ApplicationConfirmationGroups／ApplicationEvents

```text
ApplicationConfirmationGroup:
id, session_id,
confirmed_zone_instance_ids,
group_action: record / replace / void,
correction_of_group_id_nullable,
applied_at,
<事件共通時間與排序欄位>

ApplicationEvent:
id, session_id,
event_type: application_recorded,
application_confirmation_id,
confirmation_scope: all_selected / partial,
product_id_nullable,
product_label_snapshot_v1,
zone_instance_ids,
applied_at,
label_ready_at_nullable,
source: user / confirmed_suggestion,
<事件共通時間與排序欄位>
```

Session 初始塗抹與後續補擦確認都建立 `group_action=record`；同組 ApplicationEvents 的 zone instance 集合必須互斥且聯集等於 group 的 `confirmed_zone_instance_ids`。`ApplicationConfirmationGroup.applied_at`、同組每一筆 `ApplicationEvent.applied_at`，以及各自的 `effective_occurred_at` 必須全部相等；資料庫／共用驗證器須強制此限制，不能拿點擊提交時間冒充實際塗抹時間，也不能讓同一確認組出現多個時間真值。

`product_id=null` 不代表沒有產品：臨時使用產品可以只保存已由使用者確認的 `ProductLabelSnapshotV1`。是否能建立期限依 snapshot 的 `rule_eligibility_at_application` 判定。純椰子油、身分不明、過期、異常或使用後不適的產品仍可留下使用紀錄，但 snapshot 必須標為不具期限資格，不能產生一般或耐水倒數。

ApplicationEvent 不提供逐筆 replace／void；更正產品、時間或部位時，以客戶端產生的新 group ID 建立 `group_action=replace`、`correction_of_group_id` 與完整的新確認集合及事件分區。Reducer 只使用唯一更正鏈的目前有效 leaf group，原 group 的任何事件不得與 replace group 混合歸納。完全撤銷該次誤記使用 `group_action=void`，使整個原 group 失效且不需要偽造空的 ApplicationEvent。舊 group 與事件保留稽核但不再參與目前狀態。Group correction 必須同 owner／Session、不可形成循環；每次 record／replace 後都重新驗證新有效 group 內部位互斥且聯集完整，失敗時整個 batch 拒絕。P0 IndexedDB reducer 與 P1 伺服器必須使用相同測試向量。

#### ProductSafetyEvents

```text
id, owner_type, owner_id,
session_id_nullable,
source_product_id_nullable,
product_snapshot_fingerprint,
event_type:
  product_abnormal_reported /
  product_discomfort_reported,
zone_instance_ids,
source: user,
<事件共通時間與排序欄位>
```

此事件只保存阻止再次建議所需的粗粒度狀態，不保存異常細節、症狀、病史、照片、用藥或自由文字。對已保存產品回報時，須在同一原子操作追加 ProductSafetyEvent、把產品安全狀態改為不可建立新期限，並重建所有使用同一產品的進行中部位；Session-only 產品則以 snapshot fingerprint 找到當次受影響部位。既有 Application snapshot 保持不可變，但安全事件發生後的目前期限失效並轉為無時間注意狀態。P0 不提供直接「清除不適／恢復使用」：異常事件可讓不同批次或新包裝建立新紀錄；不適事件不得只靠新包裝或同配方新批次繞過，只有另一款產品或明確不同配方可建立獨立紀錄，且仍由使用者依標示與專業建議決定是否使用，系統不判定個人適用性。

#### ContextEvents

```text
id, session_id,
event_type: context_change / water_start / water_end /
  heavy_sweat / towel / friction / hand_wash,
context_value_nullable: indoor_away / indoor_window /
  outdoor_general / outdoor_exercise /
  water_preparing / water_active,
shade_context_nullable: none / partial / full / unknown,
activity_interval_id_nullable, related_event_id_nullable,
activity_started_at_nullable,
activity_ended_at_nullable,
start_time_confidence_nullable: confirmed / unknown,
zone_instance_ids,
correction_of_event_id_nullable,
correction_action_nullable: replace / void,
source,
<事件共通時間與排序欄位>
```

`initial_context` 是 Session 的初始情境真值；其後的 `current_context` 與 `shade_context` 由初始值及 ContextEvents 重建。`context_change` 必須帶 `context_value`；遮蔭可以隨情境改變，但不得回推修改產品方法或延長期限。`water_start` 將情境設為 `water_active`，`water_end` 關閉區間並須帶 `activity_ended_at` 及離水後的 `context_value`。`water_preparing` 不建立耐水期限；初始 `water_active` 必須與同一原子建立操作中的 `water_start` 對應。

每個 `water_start` 建立新的 `activity_interval_id`；因 `current_context` 是 Session 全域狀態，同一 Session 同時間最多只有一個未關閉的水上區間，不因 zone instance 集合不同而允許重疊。對應的 `water_end` 必須沿用相同 ID 與起點的完整 zone instance 集合；要修改集合只能以配對更正處理。起點為 `confirmed` 時，必須滿足 `activity_ended_at >= activity_started_at`；起點為 `unknown` 時 `activity_started_at=null`，可保存合法的 `activity_ended_at`，但不得計算水中分鐘或建立耐水期限。所有終點均須滿足 `activity_ended_at <= effective_occurred_at` 且不得晚於可信現在。區間不得重疊、重複結束、留下孤兒或把多次入水分鐘數靜默相加。相同 `idempotency_key` 重送只能回傳原結果。

ZoneTrackingEvent、ZoneMethodEvent 與 ContextEvent 的更正本身也是不可變事件，且必須與 `correction_of_event_id` 指向的原事件屬於同一 owner、Session 與事件家族／資料表；相容 subtype 可以改變。`correction_of_event_id` 與 `correction_action` 必須同時為空或同時有值。`replace` 必須帶完整替代 payload，並以更正內容取代原事件參與目前狀態歸納；`void` 可省略一般業務 payload，其受影響範圍由目標事件取得，使原事件不再參與目前狀態歸納，但原事件與更正鏈仍保留在稽核歷史。Application 使用上一段的 group-level correction。所有 event／group 更正只能指向該鏈目前有效且尚無後繼的 leaf；同一 target 最多一個直接 correction successor。系統以唯一限制及 Session revision／`If-Match` 序列化競爭更正，後到者回傳 `409 CORRECTION_CONFLICT`，不得建立分支。系統亦須拒絕不存在、跨 Session、跨事件家族、形成循環或重複作廢的更正鏈，並從唯一 leaf 重建受影響部位、Session 彙總、NotificationJob 與 NotificationBundle。

水上事件更正後的有效事件流必須符合：每個有效 `water_end` 恰好對應同一 `activity_interval_id` 的一個有效 `water_start`；不得留下孤兒、重複結束或錯誤區間。更換 `activity_interval_id`、作廢起點／終點或改變相關情境時，所有受影響的 water／context 事件必須在同一原子 batch 一併更正，否則拒絕。初始 `water_active` 的起點不得單獨作廢；若最初其實未入水，須在同一更正 batch 作廢錯誤起點並建立同時點的合法非水中 `context_change`，使有效初始狀態一致。P0 IndexedDB reducer 與 P1 伺服器必須使用相同事件流驗證測試向量。

#### SessionEndedEvents

```text
id, session_id,
end_reason:
  user_ended / replaced_by_new_session /
  account_conflict_resolution,
<事件共通時間與排序欄位>
```

結束 Session 必須追加不可變的 SessionEndedEvent，再衍生更新 `ProtectionSessions.ended_at` 與 `ended_reason`。結束後所有尚未投遞的通知工作與 bundle 失效；舊通知若被點開，只能顯示「這次提醒已結束」，不得重啟或修改 Session。

#### WeatherSnapshots

```text
id, source_kind: observation / forecast,
dataset_id, station_id_nullable, station_name_nullable,
region_id_nullable, station_distance_km_nullable,
mapping_version_nullable,
uvi_nullable, temperature_nullable,
observed_at_nullable,
forecast_issued_at_nullable,
valid_from_nullable, valid_to_nullable,
fetched_at, fresh_until, usable_until,
quality_state: valid / missing / estimated,
missing_reason_nullable
```

`freshnessState` 是以 `source_kind` 對應政策及目前時間動態計算的衍生值，不寫死在資料列中。`quality_state=missing` 時 `uvi` 必須為 null，不得用 0 代替。

#### RegionUvMappings

```text
region_code, preferred_station_ids,
forecast_region_id, mapping_version,
reviewed_at, active_from, active_to_nullable
```

#### ReminderRuleSets／ReminderRuleEvidenceLinks

```text
ReminderRuleSet:
version, status: draft / approved / retired,
effective_from, approved_at, approved_by,
change_summary, created_at

ReminderRuleEvidenceLink:
ruleset_version, rule_id, claim_id,
evidence_scope, reviewer_decision,
reviewed_at, reviewer_id

PRIMARY KEY (ruleset_version, rule_id, claim_id)
```

任何會建立、提前、取消或阻止期限的正式規則都必須存在於已核准 ruleset，並至少連到一筆仍有效的 Claim Registry 證據。規則與證據變更不得靜默套用到進行中 Session；需以明確 ruleset 升版及遷移／重算政策處理。

#### LocalReminderPresentationPreferences（P0；裝置端）

```text
device_local_id,
foreground_sound_enabled,
foreground_vibration_enabled,
last_foreground_test_at_nullable,
sound_attempt_capability:
  available / blocked_or_unknown,
vibration_capability:
  supported / unsupported,
updated_at
```

前景聲音與震動預設為 false，只保存在目前裝置，不隨 Guest migration 上傳。`sound_attempt_capability=available` 只表示程式可嘗試播放，不代表未被靜音或實際可聽見；`vibration_capability=supported` 只表示 API 存在且測試未立即失敗，不代表每次通知一定震動。

#### NotificationPreferences（P1）

```text
user_id, channel, enabled,
quiet_hours_start_local, quiet_hours_end_local,
time_zone_iana, time_zone_source: user / browser_confirmed,
allow_time_sensitive_in_quiet_hours,
request_system_sound_nullable,
request_vibration_nullable,
updated_at
```

`request_system_sound` 與 `request_vibration` 只在被選定且平台支援的 Web Push 管道表示使用者偏好／發送提示，不是送達或硬體結果。LINE、Email 或其他管道不得套用這兩欄；其聲音與震動由對應 App、瀏覽器與作業系統設定管理。

#### SessionNotificationOverrides（P1）

```text
user_id, session_id,
delivery_state: active / paused,
paused_at_nullable, resume_at_nullable,
updated_at

PRIMARY KEY (user_id, session_id)
```

`paused` 只停止遠端投遞，不暫停 App 內時間與狀態。恢復時依目前 revision 重建尚有意義的工作，不補送一串已過期通知。

#### NotificationDestinations（P1）

```text
NotificationDestination:
id, user_id,
destination_type: web_push / line_messaging,
status: pending_verification / active / revoked / invalid,
verified_at, last_success_at_nullable,
revoked_at_nullable, created_at, updated_at

WebPushDestination:
destination_id, device_id,
endpoint_ciphertext, p256dh_ciphertext, auth_ciphertext,
endpoint_hash,
user_agent_family_nullable

UNIQUE (device_id, endpoint_hash)
ACTIVE UNIQUE (endpoint_hash)

LineDestination:
destination_id,
line_messaging_user_id_ciphertext,
line_messaging_user_id_hash,
linked_at, followed_status: followed / blocked / unknown

UNIQUE (line_messaging_user_id_hash)
```

`ACTIVE UNIQUE (endpoint_hash)` 代表同一 Web Push endpoint 在所有未撤銷 destination 中只能有一位 owner；實作可採 active binding table 或等效的資料庫限制，不得只靠應用程式先查再寫。重新安裝、登出後改用另一帳號或重新綁定時，必須在同一交易先撤銷舊 owner 關聯，再建立／啟用新 destination，避免重複投遞或舊帳號殘留。Web Push subscription 與 LINE Messaging recipient 使用不同建立、驗證、撤銷流程。LINE Login 的 identity 不得直接當作 Messaging destination；只能在核准的 link／follow／webhook 流程確認後建立。

#### NotificationJobs／NotificationBundles／NotificationDeliveries（P1）

```text
NotificationJob:
id, user_id, session_id, reminder_type,
zone_instance_id,
scheduled_at, expires_at, session_revision,
ruleset_version, cause_fingerprint,
status: pending / bundled / cancelled / expired,
idempotency_key, created_at, cancelled_at_nullable

UNIQUE (user_id, session_id, session_revision,
        reminder_type, zone_instance_id, cause_fingerprint)

NotificationBundle:
id, user_id, session_id,
channel: web_push / line_messaging,
scheduled_at, expires_at,
session_revision, bundle_key, merge_window_version,
status: pending / sent / cancelled / expired,
created_at, rebuilt_at_nullable, cancelled_at_nullable

UNIQUE (id, channel)

NotificationBundleJob:
bundle_id, job_id, channel

PRIMARY KEY (bundle_id, job_id)
FOREIGN KEY (bundle_id, channel)
  REFERENCES NotificationBundle (id, channel)

NotificationDelivery:
id, bundle_id, destination_id,
status: pending / claimed / retry_wait /
  sent / cancelled / failed / expired,
attempt_count, next_attempt_at, last_attempt_at,
last_error_code, provider_message_id,
claimed_at, claim_expires_at

UNIQUE (bundle_id, destination_id)
```

`NotificationJob` 是單一部位的邏輯提醒，因此 `zone_instance_id` 一律非空；P1 目前不以此資料表發送 Session 層級行銷、系統公告或一般服務訊息，未來若需要須另立訊息類型與同意／退訂規格，不得用 null zone 混入。`NotificationBundle` 是可顯示於一則通知的合併結果；`NotificationDestination` 是管道目的地；`NotificationDelivery` 是對該目的地的一次投遞狀態，四層不得混為同一資料列。`NotificationBundleJob.channel` 必須由複合外鍵保證等於所屬 Bundle 的 channel；資料層與重建交易另須保證同一 job 在每個 channel 同時間最多只有一個有效 Bundle membership，取消的舊 membership 可保留稽核。Bundle 的 `scheduled_at` 必須等於成員 job 中最早時間，合併不得延後最早提醒。任何成員因 Session revision、事件更正或結束而失效時，須取消舊 bundle 並以仍有效 job 原子重建；已被 worker claim 的 Delivery 也必須在實際送出前再次比對 revision。`claimed` 必須同時具有未來的 `claim_expires_at`；逾時後才可由另一 worker 原子 reclaim。

#### OfflineOperations（P1）

```text
operation_id, sync_batch_id, owner_id, session_id,
operation_type, payload, base_revision,
client_sequence, depends_on_operation_id_nullable,
effective_occurred_at_utc, client_created_at_utc,
device_id,
status: pending_sync / syncing / synced /
  conflict / rejected / abandoned,
attempt_count, next_attempt_at,
last_error, created_at
```

`payload` 必須採版本化命令 schema，不可保存未篩選的 UI form。當 operation 仍為 `pending_sync` 時，介面必須明示「尚未同步」；若遠端可能仍保有舊排程，另顯示「舊通知可能仍會送達」，不得誤稱所有裝置已更新。

#### ClockCalibration（裝置端）

```text
calibration_request_id,
client_nonce, echoed_client_nonce,
server_time_utc,
request_started_wall_utc, response_received_wall_utc,
request_started_monotonic_ms, response_received_monotonic_ms,
rtt_ms,
server_offset_ms, calibrated_at_utc,
source: time_api / authenticated_api,
status: trusted / expired / clock_untrusted,
last_rendered_remaining_by_zone,
comparison_baseline:
  session_revision / zone_due_at /
  active_cause_fingerprint (activeCauseFingerprint) /
  ruleset_version
```

只採用 nonce 原樣 echo，且通過最大 RTT、回應新鮮度與 wall-clock／單調時間一致性檢查的校準結果。`server_offset_ms` 由 UTC 伺服器時間與 wall-clock 請求／回應中點計算；monotonic 樣本沒有 UTC epoch，只能量測 RTT 與偵測裝置 wall clock 跳變，不得單獨用來計算 offset。合法補擦、期限重建、事件更正或 ruleset 變更後必須重設比較基準，避免把正常延後誤判為裝置時鐘異常。

#### SetupDrafts（裝置端）

```text
id, local_draft_flow_id,
current_step, context_nullable,
shade_context_nullable,
body_zone_schema_version,
setup_entry_mode: reuse / quick_preset / self_select,
suggested_preset_id_nullable,
suggested_preset_version_nullable,
preset_decision_nullable: accepted / adjusted / cancelled,
tracked_zone_instance_ids, zone_method_components,
product_refs, expires_at, updated_at
```

`SetupDraft` 不保存尚未確認的塗抹時間、精確座標或一次性特殊狀況。`suggested_preset_id_nullable` 只留在本機草稿，不得複製到 ProtectionSession、Guest migration 或第三方分析；建立 Session 後只保留不含部位內容的入口、決定與 preset 版本。`local_draft_flow_id` 只用於目前裝置恢復草稿，不得重用或對應第 16.5 節的分析 `flowId`。提交、取消、手動清除或到期後刪除，不進入 Guest migration 或第三方分析 payload。

#### ConsentRecords

```text
id, user_id_or_guest_id, purpose,
policy_version, granted, occurred_at,
withdrawn_at, evidence_ref
```

Guest 的 ConsentRecord 只存在本機；升級帳號時只上傳仍有效且與所選同步功能相關的紀錄，並再次呈現帳號版告知。

### 14.2 資料生命週期

| 資料 | 預設政策 |
| --- | --- |
| 精確座標 | 僅在記憶體中用於本次選站，不寫入 URL、access log、分析事件或後端資料庫。 |
| Guest Session | 留在裝置端；進行中資料保留至明確結束或手動清除，已結束歷史預設 90 天後自動刪除。使用者可隨時清除全部本機資料；正式天數於可用性研究後確認。 |
| SetupDraft | 只留在目前裝置，預設 24 小時到期；提交、取消或手動清除時立即刪除，不隨 Guest 升級上傳。 |
| 帳號 Session 歷史 | 預設 12 個月；使用者可立即刪除，正式期限待法務確認。 |
| 通知投遞紀錄 | 30 天後刪除。若要保留彙總統計，須先完成可重識別風險評估與資料最小化，不得只因移除姓名就稱為匿名。 |
| OfflineOperations | 成功同步後 7 天內刪除；失敗或捨棄紀錄最長 30 天，且不得包含非必要敏感資料。 |
| AI 原圖 | 預設不保存；必要暫存最長 24 小時。 |
| 產品安全狀態 | 預設只留本機，僅保存阻止再次建議所需的粗粒度狀態；不含症狀或自由文字。若帳號版要同步，須另行明確選擇並完成法務／隱私審查。 |
| 防曬品私人備註 | 預設只留本機；帳號使用者另行明確選擇後才同步。不進入 Guest migration、通知、分析或一般日誌，刪除產品、本機資料或帳號時依範圍刪除。 |
| OAuth／Push token | 解除綁定、撤回或刪帳時立即撤銷並排程刪除。 |

---

## 15. API 與後端規格摘要

### 15.1 建議端點

```text
Public / stateless:
GET    /v1/time
GET    /v1/regions
POST   /v1/uv/lookup
GET    /v1/uv/current?regionCode=

Authenticated / P1:
GET    /v1/me/products
POST   /v1/me/products
PATCH  /v1/me/products/{id}
DELETE /v1/me/products/{id}
POST   /v1/me/sessions                  # StartSessionCommandV1
GET    /v1/me/sessions/current
GET    /v1/me/sessions/{id}
POST   /v1/me/sessions/{id}/events
POST   /v1/me/sessions/{id}/events:batch
POST   /v1/me/sessions/{id}/end
DELETE /v1/me/sessions/{id}
POST   /v1/me/migrations/guest
GET    /v1/me/notification-destinations
POST   /v1/me/notification-destinations/web-push       # 該管道納入時
POST   /v1/me/notification-destinations/line-link-intents # 該管道納入時
POST   /v1/me/notification-destinations/{id}:test
DELETE /v1/me/notification-destinations/{id}
GET    /v1/me/notification-preferences
PATCH  /v1/me/notification-preferences
PATCH  /v1/me/sessions/{id}/notification-state

GET    /v1/me/export                    # P1 自助匯出功能
DELETE /v1/me/account

Provider callback / webhook:
GET    /v1/oauth/line/callback
POST   /v1/webhooks/line
```

### 15.2 API 共通要求

- 所有 mutation 支援 `Idempotency-Key`。
- Batch event API 以 `syncBatchId` 冪等，帶單一 `baseRevision` 與按 `clientSequence` 排序的事件；整批必須原子成功或完全不套用。
- Session 更新需帶 `If-Match` 或 revision。
- 更正 event 或 ApplicationConfirmationGroup 時，`correctionOfEventId`／`correctionOfGroupId` 只能指向目前 revision 下尚無後繼的有效 leaf；資料層對非空 correction target 建立唯一後繼限制。併發或離線重播企圖建立第二個 successor 時整批回傳 `409 CORRECTION_CONFLICT`，P0 reducer 亦須拒絕同一 target 的第二個 successor。
- 伺服器接受事件時，必須在同一資料交易中追加不可變事件、重建受影響部位狀態、更新 Session 彙總與 revision，並寫入通知取消／重建 outbox；worker 只可依已提交的 outbox 建立 Job／Bundle。實際投遞前仍須再次比對 Session revision。
- 回報產品異常／使用後不適時，命令只接受產品或 snapshot 指紋、受影響 zone instance 與粗粒度事件類型，不接受症狀、病史、照片或自由文字；須原子追加 ProductSafetyEvent、更新已保存產品安全狀態、使目前同產品／snapshot 期限失效並寫入通知取消 outbox。Session-only 產品不因 `productId=null` 而漏掉處理。
- 建立 Session 必須提交版本化的 `StartSessionCommandV1`，至少包含 `bodyZoneSchemaVersion=BODY_ZONE_V3`、`initialContext`、`shadeContext`、完整 `trackedZoneInstances[]`，以及每個 instance 的 `skinExposureStatus`、`methodCertainty=confirmed` 與非空集合型 `methodComponents[]`。當前新建命令若含 `none_reported`、`unknown` 或 `unrecorded` 必須回傳 `422 METHOD_CONFIRMATION_REQUIRED`；這些值只可由版本化舊資料重播、遷移或復原流程處理。只有實際記錄外用產品的部位才提交 ApplicationConfirmationGroup／ApplicationEvents；若全部為衣物覆蓋，兩者必須為 null／空集合。可選擇 `productId=null`，但只要建立產品 Application 就必須帶完整 `ProductLabelSnapshotV1`。命令可另帶不影響規則的 `setupEntryMode`、`presetDecision` 與 `suggestedPresetVersion`；伺服器不得用這些 UX metadata 推定部位、方法或期限。缺少任一追蹤部位的合法方法組合，或已提交 Application 的 snapshot 資格無法判定時回傳 `422`，不得以已塗抹部位代替。
- `StartSessionCommandV1` 在一個原子交易中完成：檢查 owner 目前 Session、建立 ProtectionSession、SessionStartedEvent、zone instances、初始 ZoneMethodEvents、適用時的初始 Application group／events、必要的 `water_start`、衍生狀態、revision 與通知 outbox；任一步驟失敗時不得留下半套 Session。相同 `Idempotency-Key` 重送回傳同一結果。P0 IndexedDB 必須使用同一命令 schema、驗證器與 reducer 測試向量。
- 新增／停止追蹤部位必須使用版本化的 `ZoneTrackingCommand`：新增／重新啟用時原子建立或啟用 instance、`tracking_started` 與完整初始 ZoneMethodEvent，並可在同一命令附上新 Application group；該 Application 的使用者確認 `appliedAt` 可早於命令提交時間但不得晚於可信現在，並作為這一輪 activation 邊界。停止時追加 `tracking_ended` 並使該 instance 退出 `sessionNextDueAt`、`primaryAction` 與待送通知。不得直接修改 `SessionStartedEvent.trackedZoneInstanceIds`，也不得引用前一輪 tracking 的舊 Application。
- `initialContext=water_active` 時，同一 StartSessionCommand 必須帶合法且時間可信的 `water_start`；若起點未知，仍可開始 Session，但不得建立 40／80 分鐘期限，首屏顯示需立即確認的無時間行動。
- 同一次補擦確認產生的所有 ApplicationEvents 必須連同持久化的 ApplicationConfirmationGroup，以同一 `applicationConfirmationId` 在單一原子 batch 提交；`record`／`replace` group 帶完整 `confirmedZoneInstanceIds[]`，各事件的 `zoneInstanceIds[]` 非空且彼此互斥，聯集必須恰好等於該集合。`void` group 不建立新的有效 ApplicationEvent。不符合時整組回傳 `422 APPLICATION_ZONE_PARTITION_INVALID`，不得依接收順序選產品。
- 一般 Application、大量流汗、擦拭、摩擦、洗手與水上活動事件的 payload 必須包含非空 `zoneInstanceIds[]`；`correctionAction=void` 例外，其影響範圍由目標事件取得。水上活動另須包含 `activityIntervalId`。`water_start` 若 `startTimeConfidence=confirmed`，`activityStartedAt` 必填且不得晚於 `effectiveOccurredAt` 或當下可信時間；若為 `unknown`，`activityStartedAt` 必須是 null。只有 confirmed 且通過驗證的起點可以建立 40／80 分鐘期限。
- 每個有效 `water_end` 必須對應同 Session 且仍開啟的唯一 `water_start`，繼承其完整 zone instance 集合並帶合法離水後情境。起點為 confirmed 時才驗證 `activityEndedAt >= activityStartedAt`；起點為 unknown 時允許 `activityStartedAt=null`，但不得計算區間分鐘。兩者皆須驗證 `activityEndedAt <= effectiveOccurredAt` 且不晚於可信現在。伺服器拒絕任何 Session 內的重疊區間、孤兒終點、重複結束或跨部位集合關閉；replace 後重新執行完整驗證。P0 reducer 與 P1 API 使用相同測試向量。
- `context_change` 可不附部位但必須帶合法 `contextValue`；情境或遮蔭切換不得改寫各部位的 `lastAppliedAt` 或延長期限。
- 伺服器保存 `effectiveOccurredAt`、`clientCreatedAt`、`serverReceivedAt`、`clientSequence` 與 `serverAppliedSequence`。事件業務時間不得晚於 `serverNowUtc + 2 分鐘`；超出時回傳 `422 FUTURE_EVENT_TIME`。離線客戶端為 `CLOCK_UNTRUSTED` 時不得提交會延長期限的未來時間；合法、已驗證且不在未來的 Application 可以建立新的正常基準。
- 晚到但時間較舊的事件可保留為歷史，預設不得覆蓋同部位較新的衍生狀態；ZoneTracking／ZoneMethod／Context 使用 `correctionOfEventId` 與 `correctionAction=replace|void`，Application 使用 `correctionOfGroupId` 與 group action，均須依第 14.1 節驗證及重建狀態。事件與 confirmation group ID 由客戶端先產生並在同步後保持不變；若原資料與更正都尚未同步，須以穩定 ID 或 `dependsOnOperationId` 在同一原子 batch 解析引用。
- 只有嚴格晚於事件原因的有效 Application 才能解除該原因；時間相同採保守狀態。伺服器與 P0 reducer 不得以封包抵達順序或最後寫入覆蓋此因果規則。
- 每個 owner 只允許一個未結束 Session；衝突時回傳 `409` 與 `currentRevision`、`currentState`、`reason`、`allowedActions`，不得靜默覆寫。
- `POST /sessions/{id}/end` 必須追加 SessionEndedEvent 並使尚未投遞的 Job／Bundle 失效；只能刪除已結束 Session。刪除產品不回溯修改既有 ApplicationEvent 的產品標示 snapshot。
- Guest 不建立伺服器 NotificationDestination 或遠端排程。使用者登入後，只有在明確選擇遷移進行中 Session、完成通知管道設定並同意必要資料處理後，才可建立遠端通知。
- Guest migration 使用版本化白名單與預覽；`privateNote`、`conditionStatus`、SetupDraft、一次性特殊狀況及原始搜尋字詞預設不包含，只有規格允許且使用者對該欄位明確選擇時才可上傳。
- Web Push 與 LINE Messaging 各自驗證 destination；測試、撤銷與錯誤不得跨管道修改另一類 destination。Web Push endpoint 重新綁定至不同帳號時，須在單一交易撤銷舊 owner 後才能啟用新 owner，並以 active endpoint hash 唯一限制拒絕競爭寫入。LINE Login 的 subject 不可直接作為 Messaging API 收件者。
- `PATCH /v1/me/notification-preferences` 只有 `channel=web_push` 可接受 `requestSystemSound`／`requestVibration`；其他管道帶入時回傳 `422 CHANNEL_PRESENTATION_PREFERENCE_UNSUPPORTED`，不得靜默保存無效偏好。回應只能表示偏好已保存，不得包含或推定 `actuallyPlayedSound`、`actuallyVibrated`。
- `PATCH /sessions/{id}/notification-state` 只改變遠端投遞狀態，不得凍結 App 內狀態、改寫事件或延長 `zoneDueAt`。
- OAuth 與 webhook 驗證 state／簽章並防重播。
- CWA、AI、LINE Messaging、Email 及其他第三方服務金鑰只存後端。
- UV lookup／region 回應必須提供 `sourceKind`、dataset、測站／區域、相應觀測或預報有效時間、`freshnessState`；地區查詢另回傳 `mappingVersion`。此要求不套用產品或 Session API。
- `POST /v1/uv/lookup` 使用 `Cache-Control: no-store`。座標只在記憶體中用於本次選站，不寫入 access log、WAF／CDN 分析事件或資料庫；若基礎設施無法排除 request body 記錄，須先完成遮罩再上線。
- `GET /v1/time` 請求必須帶隨機 `clientNonce`，回應 UTC `serverNowUtc`、原樣 echo 的 `clientNonce` 及伺服器 `requestId`，並設定 `Cache-Control: no-store, no-cache, must-revalidate`、`Pragma: no-cache`；CDN、Service Worker 與瀏覽器不得快取。客戶端保存 wall-clock 與 monotonic 請求／回應樣本，以 monotonic 計算 RTT、以 wall-clock 中點估算 UTC offset；快取、nonce 不符、回應重播、wall-clock 跳變或 RTT 超標時不得標記可信。
- 錯誤訊息不得洩漏 token、內部堆疊或個資。
- 對登入、掃描、邀請、通知及帳號操作設定限速。

---

## 16. 隱私與資安

### 16.1 分層告知

蒐集前至少說明：

- 蒐集者與聯絡方式。
- 蒐集目的及資料類別。
- 使用期間、地區、對象與方式。
- 第三方處理者，例如 Google、LINE、推播、Email 或 AI 服務。
- 使用者依法得行使的權利：查詢或請求閱覽、請求製給複製本、請求補充或更正、請求停止蒐集／處理／利用，以及請求刪除；實際程序、聯絡方式、處理期限及依法得拒絕的例外須在正式告知事項說明。
- 產品另提供的自助資料匯出功能及其範圍；不得把「匯出」誤寫成取代前述法定權利。
- 不提供資料的影響與替代流程。

P0 已在使用者主動選擇 `使用目前位置` 時短暫取得精確座標以完成本次選站；即使只在記憶體處理、不落地保存，仍涉及個人資料之蒐集及後續處理／利用。呼叫定位權限前必須就近告知目的、資料類別、使用期間與方式，以及 `手動選擇地區／暫不提供地區` 的替代流程。疾病、症狀細節與照片若在未來版本加入，須另行評估告知、同意與法律依據，不得混在一般服務條款中以單一 checkbox 概括。

### 16.2 建議隱私文案

> 我們只會在提供您所選功能的必要範圍內處理資料。您可不提供精確定位，改用手動地區或暫不提供地區；不提供地區仍可使用防曬提醒。第三方登入、通知或影像辨識服務可能接收執行該功能所需的資料，詳見《個資告知事項》。您可依法行使查詢或閱覽、取得複製本、補充或更正、停止蒐集／處理／利用及刪除等權利；本產品也另提供自助資料匯出功能。我們採取傳輸加密、儲存加密與存取控制降低風險，但任何系統均無法保證零風險。

### 16.3 安全控制

- 全站 HTTPS／TLS。
- 敏感資料靜態加密，金鑰與應用程式分離管理。
- 最小權限 RBAC 與管理操作稽核。
- Cookie 使用 HttpOnly、Secure、SameSite。
- OAuth 使用 state、PKCE、nonce。
- Webhook 使用供應商簽章、timestamp 與 replay protection。
- 帳號登入、密碼重設、邀請碼與掃描 API 實施限速。
- CSP、輸入驗證、輸出編碼及 CSRF／XSS／IDOR 防護。
- 日誌禁止寫入 access token、完整定位、健康資料、圖片或通知內容。
- 建立資安事件偵測、通報、處理與必要的當事人通知流程。

### 16.4 法規與產品屬性

- 每次上線前由台灣法務檢查當次功能範圍的個資告知與資料保存政策；只有實際納入跨境或第三方傳輸時，才追加相應專項審查，不以尚未開發的 P1／P2 功能阻擋 P0。
- 由 TFDA 法規專業人員評估產品 intended use 與宣傳文案是否涉及醫療器材軟體屬性。
- 免責聲明不能取代對核心功能與宣稱的實質評估。
- 上線前由法務與醫療內容審查者核准「特殊狀況」入口、個別醫療問題的拒答／轉介文案與急症分流文字；若產品日後蒐集症狀、照片、病理或醫療資料，須另立資料最小化、告知、同意、安全與法規評估，不得沿用一般防曬提醒規格。

### 16.5 分析資料的匿名與假名化界線

- `flowId` 為隨機、單一流程使用的短效假名識別碼，`expiresAt = createdAt + 24 小時`，到期即刪除且不得續期；不得跨裝置或與帳號、Session、產品、通知識別碼串接成個人輪廓。
- 在完成可重識別風險評估前，一律稱為「短效假名化分析」，不得宣稱匿名或去識別化。移除姓名但仍含固定 cookie、裝置 ID、完整 IP、User-Agent 組合、第三方廣告識別碼或可長期連結的 storage key，不視為匿名。
- 分析端不得接收原始站內搜尋字詞、自訂部位名稱、產品名稱、私人備註、健康狀況、精確位置或提醒時間線。站內搜尋字詞不得寫入 URL query、referrer、access log 或第三方 SDK；若需改善搜尋，只可先在本機歸納成經審查的無敏感分類後再送出。
- 安全／稽核日誌與產品分析目的、欄位、權限及保存政策分開；不得把安全日誌另作行銷、廣告或個人化用途。

---

## 17. 非功能需求

### 17.1 支援平台

至少建立以下測試矩陣：

- Android Chrome 與安裝後 PWA。
- iOS Safari 一般分頁。
- iOS 加入主畫面的 Web App。
- Desktop Chrome／Edge 最新兩個主要版本。
- 通知、定位、相機被拒絕的情境。

手機版為發布阻擋驗收的基準：360、390 與 430 CSS px 寬下，從首頁、選部位、選產品、設定時間到確認 Session 均須單手可完成、無橫向捲動、無被鍵盤或瀏覽器列遮住的必要控制項。桌面版為相容與效率增強，不得反向決定手機版資訊架構。每項實驗性 API 使用前均需 feature detection；不支援時回到手動流程。

### 17.2 無障礙

- 目標符合 WCAG 2.2 AA。
- 一般文字與背景至少 4.5:1，大型文字至少 3:1；具功能或傳達狀態的圖示、圓環、邊框、焦點及控制項至少 3:1。主要狀態與核心操作在日間色票盡量以 7:1 為產品目標，但不得用未達 7:1 推定不符合 AA，仍依適用成功準則驗收。
- 公式、狀態與圖表具有文字替代，不以顏色作唯一訊息。
- 倒數變化不得造成螢幕閱讀器每秒重複朗讀。
- 視覺提醒環使用可存取名稱與目前值，並以 `aria-valuetext` 或平台等效語意提供「約 15 分鐘後建議檢查，預計 15:40」等白話文字；只有進入即將到期、已到期、事件改變或使用者主動聚焦時更新 live status，不逐秒宣告。
- 尊重 `prefers-reduced-motion`：停用旋轉、脈衝、閃爍與連續填色動畫，只保留靜態圓環位置、文字、圖示及必要的分鐘級更新。
- 行動版主要與高頻觸控目標以至少 44 × 44 CSS px 為產品設計目標；同時依 WCAG 2.2 AA 驗證例外、間距與等效操作。
- 觸控目標、焦點順序、鍵盤操作、螢幕閱讀器名稱、錯誤訊息、文字放大與對比需通過測試。
- P1 人體部位圖不得是唯一選擇方式，必須提供功能等價的文字核取清單；圖片縮放、載入失敗或無法使用指標裝置時仍可完成流程。
- 行動版固定底部動作須避開瀏覽器工具列、裝置安全區與螢幕鍵盤，不得遮住最後一個欄位、錯誤訊息或確認按鈕。

### 17.3 效能

- 行動網路下首頁核心內容目標 LCP ≤ 2.5 秒（p75，正式門檻由實測確認）。
- CWA 資料經後端共用快取，避免每位使用者對官方 API 高頻請求。
- 圖片上傳前壓縮與裁切；設定尺寸、格式與 timeout 上限。

### 17.3.1 戶外可讀性實機測試

公開測試前須在代表性 Android 與 iPhone 實機完成：正午直射陽光、樹蔭、螢幕自動亮度、50% 亮度、低電量模式、一般／霧面或防窺保護貼、太陽眼鏡、日間／夜間模式及色覺差異模擬。至少覆蓋 360、390、430 CSS px 寬。

任務至少包含：看懂 UVI 與資料時間、辨識主要狀態元件、找到需處理部位、區分 `記錄已補擦` 與 `回報狀況`、看懂即將到期／已到期／不確定、切換顯示模式。若受測者需遮住螢幕、反覆調角度或猜測顏色才能完成，記為可讀性失敗並修訂，不得只以自動對比檢查通過結案。

### 17.4 可觀測性

- 監控 CWA 成功率、資料過期率、最近有效站距離分布。
- 監控通知建立、取消、供應商接受、已知失敗與使用者開啟。
- 監控 Session 衝突、重複通知、離線同步失敗與帳號刪除完成率。
- 任何分析事件不得包含精確位置、疾病、產品名稱、私人備註或圖片。
- 易用性分析只記錄流程節點、耗時、選取項目數量與錯誤類型；不得記錄實際部位代碼、自訂部位文字、產品名稱、標籤內容或自由輸入文字。
- 第三方產品分析不得接收帳號 ID、Session ID、產品 ID 或通知識別碼；同一次設定漏斗以短效隨機 `flowId` 關聯並於到期後失效。產品分析與安全／營運稽核資料須使用不同目的、權限及保存政策。

---

## 18. KPI 與分析事件

### 18.1 產品 KPI

- 首次進入至看到有效 UVI 的完成率與中位時間。
- 拒絕定位後完成手動地區流程的比例。
- 啟動提醒的完成率、中位完成時間及各設定階段流失率；首次設定與沿用上次設定必須分開統計。
- 從 `記錄已補擦`／`回報狀況` 到成功提交的完成率、中位操作時間與更正率。
- 游泳／流汗／擦拭事件後的提醒確認率。
- 通知選擇加入率及各管道已知失敗率。
- 7 日回訪率。
- 資料過期顯示率與 CWA fallback 比例。
- 刪除／匯出請求完成率。

### 18.2 安全 KPI

- 使用者回報「誤以為倒數代表安全曝曬」的件數。
- 自動建議錯誤延長提醒的件數，目標必須為 0。
- 批次操作更新未經最後確認部位的件數，目標必須為 0。
- 未確認所有外露部位方法便建立 Session 的件數，目標必須為 0。
- 首屏隱藏 `REAPPLY_DUE`、尚未記錄、尚未採取防護或不確定部位的件數，目標必須為 0。
- 重複或過期通知率。
- 健康宣稱無來源或逾期未審查的發布件數，目標必須為 0。
- 隱私事件數與處理時效。

上線前由產品負責人依 beta 基準設定各 KPI 的正式數值目標；不得臆造「已達成」的成效。

### 18.3 易用性驗證假設

以下是公開測試前應驗證的設計目標，不代表目前已達成：

- 地區與 UVI 已載入後，首次設定提醒的中位完成時間不高於 30 秒。
- 無設定變更時，沿用最近設定並確認新的塗抹時間，其中位完成時間不高於 10 秒。
- 從進行中 Session 回報常用事件，以 `開啟事件選單 → 選擇事件 → 確認受影響部位` 為主要路徑，不超過三個必要動作；需要修正部位或時間時可展開進階操作。
- `已補擦` 流程預選應處理部位，但提交前必須有一次明確確認；縮短步驟不得以誤記所有部位為代價。
- 任一目標未達成時，先查看設定階段流失、返回、`不確定`、更正與錯誤事件，再決定調整介面或正式 KPI，不得把快速完成直接解讀為正確理解。

#### 18.3.1 初始測量協定

- 首輪基準測試至少招募 10 位不屬於產品開發團隊、符合第 2.1 節核心成人使用情境的參與者；首次設定與沿用設定各須取得至少 10 筆有效觀察。樣本不足只能標示為探索性結果，不得據此宣稱達標。
- 以實體手機或 360–430 CSS px 寬的受支援行動瀏覽器測試，記錄裝置、OS、瀏覽器版本、viewport、網路狀態、是否使用輔助科技及參與者過往使用經驗。首次設定計時只在地區與 UVI 已載入後開始，因此不得把 API 載入時間混入 30 秒指標。
- 首次設定的開始事件為使用者啟動 `開始防曬提醒`；沿用流程為啟動 `沿用並確認`。結束事件均為 Session 成功建立且首個真實狀態已渲染。只看見成功 toast 不算完成。
- 任務成功定義為：不接受主持人步驟提示、依測試情境正確確認全部外露部位／方法／時間並建立 Session，且兩分鐘內沒有因誤解而作出的關鍵更正。失敗與放棄必須納入完成率；時間中位數與 p75 同時報告，並清楚標示採全部樣本或僅成功樣本。
- `必要動作` 指一次有意義的按鈕、核取方塊、選項、鍵盤啟用或提交；捲動、系統自動導頁與純等待不計。若流程要求輸入自由文字，須另報文字輸入，不得以忽略鍵擊美化動作數。
- 測試腳本、資料初始狀態、任務順序、是否允許返回、主持人介入規則與原始觀察表在測試前定版。沿用流程使用已準備好的最近設定且不得有進行中 Session；順序效應須以分組或明確記錄方式處理。

### 18.4 探索性功能需求問卷（2026-07）

本節記錄產品概念尚未完成時的需求探索結果，用於確認問題方向、建立設計假設與安排後續研究；它不是市場代表性調查，也不能單獨證明某項功能必須開發或已具產品市場契合度。

#### 18.4.1 資料範圍與有效性

- 來源：`網頁製作的功能需求問卷（回覆）`，回覆期間為 2026-07-22 至 2026-07-28，共 27 份。
- 27 份回覆均完成所有必填題；排除時間戳後，未發現整份答案完全相同的回覆。
- 問卷沒有受訪者識別碼、作答時間、招募管道與防重複機制，因此不能證明 27 份來自 27 位不同人士，也不能排除方便抽樣或自我選擇偏差。
- 性別分布為女性 23 人（85.2%）、男性 4 人（14.8%）；未蒐集年齡、地區、職業、裝置與無障礙需求。結果只能視為方向性線索，不得外推為所有使用者或台灣人口比例。
- 第 4 題限制最多選 3 項，但 11 份超選；嚴格遵守題意的有效樣本為 16 份。該題完整票數只可用來觀察候選需求，不作精確排序。
- 第 15 題限制最多選 3 項，但 6 份超選；嚴格遵守題意的有效樣本為 21 份。採用動機百分比須以 `n=21` 計算。
- 第 12 題有 1 份同時勾選多項「不需要」與「以上功能都有可能需要」，屬邏輯矛盾；分析個別不需要項目時可保留其勾選，但不得把該份的總體態度分類為明確需要或不需要。
- 第 7 題有語意近似的戶外活動／運動選項，分析時合併解讀；下一版問卷須先消除重複語意。
- 開放文字僅做主題歸納，不在 PRD 留存可識別原文；若日後蒐集聯絡資料或敏感資訊，須另設告知、用途、權限與保存期限。

#### 18.4.2 主要發現

防曬習慣：每天使用 12 人（44.4%）、每週數次 2 人（7.4%）、很少使用 6 人（22.2%）、長時間戶外才使用 5 人（18.5%）、海邊／爬山／運動才使用 2 人（7.4%）。

| 使用問題 | 人數 | 27 份占比 |
| --- | ---: | ---: |
| 忘記補擦 | 17 | 63.0% |
| 覺得補擦麻煩 | 14 | 51.9% |
| 不知道何時補擦 | 10 | 37.0% |
| 看不懂產品標示 | 10 | 37.0% |
| 擔心海洋環境影響 | 8 | 29.6% |
| 流汗、游泳或擦拭後不確定是否要補擦 | 6 | 22.2% |
| 擔心刺激或特殊皮膚狀況 | 6 | 22.2% |

若只能保留一項功能，選擇為：即時 UV 資訊 9 人（33.3%）、補擦提醒 8 人（29.6%）、個人化防曬建議 7 人（25.9%）、衣物／遮蔭建議 2 人（7.4%）、防曬品管理 1 人（3.7%）。

進一步以使用頻率交叉觀察：

| 只能保留一項功能 | 每天使用防曬 `n=12` | 非每天使用 `n=15` |
| --- | ---: | ---: |
| 補擦提醒 | 5 | 3 |
| 個人化防曬建議 | 5 | 2 |
| 即時 UV 資訊 | 1 | 8 |
| 衣物／遮蔭建議 | 1 | 1 |
| 防曬品管理 | 0 | 1 |

此交叉結果顯示：UV 資訊可能較適合作為低習慣使用者的進站理由；已有每日防曬習慣者較重視提醒與個人化。因各組樣本很小，只能形成研究假設，不能宣稱因果或穩定族群差異。

最願意提供的輸入為：所在地 UV 21 人（77.8%）、上次塗抹時間 15 人（55.6%）、目前在室內或戶外 10 人（37.0%）；SPF／PA 6 人（22.2%），流汗、游泳、擦拭、活動狀態各約 5 人。這支持先詢問少數、直接影響當次提醒的資訊，其餘改為事件回報或漸進展開。

希望收到提醒的方式為：手機桌面小工具 15 人（55.6%）、LINE 12 人（44.4%）、智慧手錶 8 人（29.6%）、瀏覽器通知 4 人（14.8%）、App／網站內提醒 3 人（11.1%）、Email 2 人（7.4%）。這是偏好，不等於 PWA 可直接實作或使用者會授予通知權限；桌面小工具、穿戴裝置與 LINE 均須另做平台可行性、成本與送達可靠性驗證。

海邊內容需求為：下水前如何處理 17 人、游泳後如何補擦 17 人、海洋環境資訊 12 人、毛巾擦拭後如何處理 10 人、衣物／遮蔭 7 人、耐水 40／80 分鐘 6 人、椰子油比較 4 人；2 人不需要海邊資訊。這支持以行動時機為主、材料爭議為輔的海邊內容排序。

被勾選為「不需要」較多的功能為：Email 提醒 13 人、兒童模式 11 人、每個身體部位各顯示倒數 10 人、產品推薦／購買連結 10 人、分享 9 人、遊戲化 9 人、家庭／隊友管理 7 人、帳號 5 人。第 12 題存在前述矛盾答案，這些結果只用來檢查刪減方向，不代表功能永久禁止。

主要棄用風險為：要求太多個資 20 人（74.1%）、步驟太多 18 人（66.7%）、介面複雜 12 人（44.4%）、廣告／產品推薦 11 人（40.7%）、通知太頻繁 11 人（40.7%）、強制註冊 8 人（29.6%）；不準確提醒、輸入太多產品資訊與隱私疑慮各 7 人（25.9%）。

在嚴格遵守「最多選 3 項」的 21 份第 15 題回覆中，最能提高採用意願的條件為：操作簡單 12 人（57.1%）、沒有廣告／強迫推銷 10 人（47.6%）、只需少量資料 10 人（47.6%）、LINE／手機通知 6 人（28.6%）、清楚隱私說明 5 人（23.8%）、不用註冊 4 人（19.0%）。

概念有用程度平均 6.78／10、中位數 6，13 人給 7 分以上；未來使用意願平均 6.26／10、中位數 6，9 人給 7 分以上。結果代表中度興趣，尚不足以宣稱高採用意願或產品市場契合。

#### 18.4.3 對產品決策的影響

1. **維持提醒優先、UV 支援**：核心工作仍是開始、理解與完成補擦提醒；今日 UV 是首頁重要資訊與低習慣使用者入口，不改成壓過提醒 CTA 的主產品。
2. **維持訪客優先與資料最小化**：P0 不強制註冊；優先使用所在地／手動地區、上次塗抹時間與室內外等少數當次資訊。產品標示、部位及事件資料只在確實影響顯示或提醒時詢問。
3. **把變動狀況改成快速事件**：流汗、游泳、擦拭與洗手放在 `回報狀況`，不全部塞入啟動表單；常用事件與最近設定可重用。
4. **後端保留部位差異，前端降低計時器負擔**：可追蹤手背等局部狀況，但進行中頁優先顯示最早需要處理的部位與一個主提醒環，不同部位不以多個同等醒目的倒數競爭注意力。
5. **商業中立列為 P0 信任條件**：不加入廣告、業配、付費排名、強迫推銷或產品購買連結；`我的防曬品` 是私人紀錄與快速重用，不是推薦或銷售榜單。
6. **通知偏好不直接等於開發優先級**：P0 先完成網站內與受支援的瀏覽器／PWA 路徑；LINE 可列 P1 管道候選。手機小工具與智慧手錶只列研究項目，完成 OS、PWA、後端、權限、成本及送達測試後才能排期。
7. **維持刪除兒童與隊友模式**：與既有刪減決策一致；Email、分享與遊戲化不列 P0。若後續研究出現明確族群與任務，再另寫需求與安全邊界。
8. **海邊內容先回答行動問題**：優先處理下水前、游泳後、擦拭後與遮蔭／衣物，再解釋耐水標示、海洋環境與椰子油；不得把環境疑慮變成以純椰子油取代合規防曬品的建議。
9. **產品／敏感肌推薦暫不納入**：問卷開放文字雖出現產品、膚質與刺激疑問，但在醫療邊界、利益揭露、健康宣稱與信任設計完成前，不提供個人化產品優劣或治療性推薦。

#### 18.4.4 下一輪研究

- 修正第 4、15 題的表單限制，前端實際阻止超選；將「都需要／都不需要」設為互斥選項。
- 合併第 7 題重複的戶外活動語意；將「即時 UV」改為「最新可用 UV 資料」，避免暗示即時且零延遲。
- 分開詢問「想要哪種提醒」與「願意授予通知權限／接受的頻率」，並測試關閉通知、靜默時段與錯過提醒後的理解。
- 加入定位方式偏好：自動定位、手動選地區、只在本機保存、不提供位置；不得把願意看所在地 UV 解讀為同意 GPS。
- 以手機低擬真原型測試：訪客進入、拒絕定位、開始提醒、回報洗手／游泳、局部補擦、通知頻率與閱讀海邊內容。
- 下一輪招募須記錄招募方式，補足不同性別、防曬頻率、年齡層、裝置及無障礙需求；若要估計市場比例，須另做抽樣設計與足夠樣本，而非沿用本次方便樣本。
- 功能只有在同時通過使用者任務、技術可行性、安全／法務與易用性驗證後才能升級優先級，不得僅依票數排入 P0。

---

## 19. 驗收條件

每項 AC 以括號標示適用範圍。Core Beta 必須通過所有標示 P0 的情境；P0.5／P1／P2 AC 只有在相應公開內容、帳號通知或研究功能納入當次發布時才成為阻擋條件。`P0／P1` 表示 P0 先驗收本機路徑，P1 再驗收帳號或伺服器路徑。

### AC-01：拒絕權限仍可使用（P0）

**Given** 使用者拒絕定位、登入與通知權限  
**When** 使用者手動選擇地區，或選擇 `暫不提供地區`  
**Then** 手動地區可顯示具來源與時間的 UVI；不提供地區時明示「目前沒有所在地 UVI」，但兩條路徑都能以 Guest 啟動 App 內防曬提醒，且不得捏造所在地或 UVI。

### AC-02：UVI 為 0 與 CWA 分級邊界（P0）

**Scenario A：觀測值為 0**  
**Given** 有效觀測 UVI＝0  
**When** 首頁載入  
**Then** 顯示「觀測值為 0／低量級，但不代表零風險」，不除以零、不顯示無限安全；只有另有可靠日照／時間判定時才標示夜間，且分類名稱須使用當次核准的 CWA 對照表。

**Scenario B：CWA 分級邊界**  
**Given** UVI 分別為 2、3、5、6、7、8、10、11  
**When** 顯示測站觀測或區域預報  
**Then** 依序正確落入低量級、中量級、高量級、過量級與危險級，並顯示資料類型、時間與來源；不得產生全體適用的固定曬傷分鐘或安全曝曬倒數。

### AC-03：資料過期（P0）

**Given** 最近觀測已超過 `stale` 門檻但尚未達 `unusable`  
**When** 顯示 UVI  
**Then** 明確標記資料過期與時間，不稱為即時值；超過 `unusable` 門檻後不得繼續顯示成目前 UVI。

### AC-04：資料缺測（P0）

**Given** 最近測站 UVIndex 無效  
**When** 系統選站  
**Then** 尋找下一個有效站；皆無效時使用明示的預報或顯示無有效資料，不自行填 0。

### AC-05：提醒語義（P0）

**Given** 任何有效 Session 或部位狀態  
**When** 顯示時間型提醒、無時間行動卡或通知  
**Then** 使用「建議檢查／補擦」語義；有數字期限時同時顯示「不是安全曝曬時間」，無可信期限時不得製造倒數、`00:00` 或安全百分比。

### AC-06：游泳與耐水標示（P0）

**Scenario A：開始入水**  
**Given** 產品標示耐水 40 分鐘  
**When** 使用者以可信的實際入水時間記錄 `water_start`  
**Then** 系統以新的 `activityIntervalId` 建立不晚於實際入水時間加 40 分鐘的提醒。

**Scenario B：離水或擦拭**  
**Given** 有一個進行中的水上區間  
**When** 使用者記錄對應 `water_end` 或擦毛巾事件  
**Then** 受影響部位立即標記應補擦，原水上期限不再提供新的剩餘時間。

**Scenario C：未補擦便再次入水**  
**Given** 相關部位在離水後尚未確認重新塗抹  
**When** 使用者再次記錄入水  
**Then** 部位保持 `REAPPLY_DUE`，不得取得新的 40 分鐘期限。

**Scenario D：補擦後再次入水**  
**Given** 使用者已確認重新塗抹相關部位  
**When** 之後再次以可信起點記錄入水  
**Then** 才建立新的 `activityIntervalId` 與相應期限。

**Scenario E：不合法或重疊的區間**  
**Given** 同一批部位已有未關閉的 `activityIntervalId`，或 `water_end` 找不到相同 ID、相同部位集合與合法起點  
**When** 客戶端新增、重送或更正水上事件  
**Then** P0 reducer 與 P1 API 拒絕重疊、孤兒、重複結束及 `activityEndedAt < activityStartedAt`；相同冪等鍵重送只回傳原結果，不建立第二個區間。

### AC-07：局部補擦（P0）

**Given** 使用者只補擦鼻／雙頰區 `face_nose_cheeks` 與前頸 `neck_front`  
**When** 提交局部補擦  
**Then** 只更新這兩個部位的 `lastAppliedAt`、`labelReadyAt`、`zoneDueAt` 與狀態；其他部位完全不變，Session 仍以最需要注意的外露部位作彙總。

### AC-08：自動建議不得延長（P2）

**Given** 既有提醒將於 20 分鐘後到期  
**When** 感測器推測使用者進入室內  
**Then** 在使用者確認前不得延長提醒；即使確認，也保留各部位原 `lastAppliedAt`。

### AC-09：重新開啟（P0）

**Given** 頁面關閉 30 分鐘後重新開啟  
**When** App 恢復  
**Then** 依第 8.1 節所屬模式選擇可信時間並重算狀態，不依賴背景 JavaScript timer；Guest 不得被誤當成有伺服器 Session。

### AC-10：通知去重（P1）

**Given** Session revision 已更新  
**When** 舊通知工作到達排程時間  
**Then** Worker 在送出前重驗 revision、部位狀態與 `expiresAt` 並使舊工作失效；系統只保留一個符合唯一鍵的有效 logical delivery，外部供應商重送則由冪等操作防止重複修改狀態。

### AC-11：AI 結果確認（P2）

**Given** AI 辨識出 SPF 50+、PA++++  
**When** 使用者尚未確認  
**Then** 候選值不得寫入產品或修改進行中 Session。

### AC-12：離線狀態（P0）

**Given** 使用者離線  
**When** 開啟已快取 App  
**Then** 可看到最後資料與時間，但清楚顯示遠端通知、AI、同步與新氣象不可用。

### AC-13：刪除帳號（P1）

**Given** 使用者確認刪除帳號  
**When** 刪除流程完成  
**Then** Profile、產品、產品私人備註、Session、token、通知工作與 OAuth 關聯依政策清除，並提供完成結果。

### AC-14：鎖定畫面隱私（P1）

**Given** 使用者有產品私人備註或未來選填敏感資訊  
**When** 系統發送通知  
**Then** 通知預設不顯示姓名、疾病、療程或其他敏感內容。

### AC-15：健康文案發布（P0）

**Given** 一則衛教內容沒有有效來源或審查已過期  
**When** 發布流程執行  
**Then** 系統阻止發布。

### AC-16：耐水標示未知（P0）

**Given** 產品沒有耐水標示或使用者看不清楚  
**When** 使用者開始水上活動  
**Then** 系統不得自行套用 40／80 分鐘，必須提示產品水中防護未知，並在離水或擦拭後立即提醒重新塗抹。

### AC-17：曝曬前標示等待（P0）

**Given** 產品標示需在曝曬前 15 分鐘使用  
**When** 使用者剛記錄塗抹  
**Then** `ProductLabelSnapshotV1` 保存 `preExposureWaitStatus=explicit_minutes` 與 15 分鐘，相應部位進入 `LABEL_WAIT`，只顯示依標示等待與搭配遮蔽的提示；等待前後均不得顯示已驗證防護或安全曝曬。沒有明確數字時不得自行填入 15 分鐘。

### AC-18：產品標示不完整（P0）

**Given** 產品 SPF、PA、廣效宣稱或塗抹部位有缺漏  
**When** 使用者啟動 Session  
**Then** 系統分欄顯示已知與未知資訊，允許繼續使用提醒，但不得將未知默認為足夠防護，並顯示經專業審查的多重防護建議。

### AC-19：未提供地區仍可啟動提醒（P0）

**Given** 使用者未允許定位、未手動選地區且選擇 `暫不提供地區`  
**When** 完成部位、方法、產品與實際塗抹時間確認  
**Then** 系統原子建立本機 Session 並依產品標示及回報狀況運作；首頁 UVI 顯示資料不足與新增地區入口，但不得以 UVI 缺失阻擋提醒、套用假值或改變產品期限。

### AC-20：一次性特殊狀況（P0）

**Given** 使用者選擇醫師避光、破損／起泡皮膚或可能全身性／熱疾病症狀  
**When** 顯示對應流程  
**Then** 三類訊息分開呈現、不得改成數字權重，選擇預設不保存或上傳；紅旗與就醫文案必須來自有效醫療審查版本。

### AC-21：手動地區（P0）

**Given** 使用者拒絕定位並選擇行政區  
**When** 取得 UV 資料  
**Then** 系統依指定版本 `RegionUvMapping` 顯示代表站觀測或區域預報、資料時間與類型；固定 mapping 版本、相同來源快照與相同輸入時，選站結果可重現。

### AC-22：Guest 與帳號資料邊界（P0／P1）

**Given** 未登入 Guest 建立產品與 Session  
**When** 檢查網路請求與後端資料庫  
**Then** 產品、Session、部位與事件只存在 IndexedDB，後端沒有匿名 Session；只有使用者選擇升級後才經 migration API 上傳。

### AC-23：裝置時鐘向後跳（P0）

**Scenario A：基準未改變卻向後跳**  
**Given** Session revision、期限、原因集合與 ruleset 均未改變，部位尚有 20 分鐘，裝置時鐘向後調整 30 分鐘  
**When** App 回到前景  
**Then** 系統標記 `CLOCK_UNTRUSTED`，剩餘時間不得增加，並要求成功重新校時；單純點選「我確認」不得清除此狀態。

**Scenario B：合法補擦不是時鐘異常**  
**Given** 使用者在可信現在確認一筆合法補擦，導致 Session revision 與部位期限更新  
**When** 新期限比上次畫面所見更晚  
**Then** 系統重設該部位的比較基準並正常顯示新期限，不得僅因剩餘時間增加而標記 `CLOCK_UNTRUSTED`。

### AC-24：離線同步衝突（P1）

**Given** 登入帳號離線建立事件，但伺服器 Session 已在另一裝置結束  
**When** outbox 恢復同步  
**Then** 伺服器回傳 `409` 與允許動作，Session 不自動重開，後續相依操作暫停直到使用者決定。

### AC-25：多裝置通知目的地（P1；限選定管道支援多目的地時）

**Given** 當次選定管道支援多目的地，且同一帳號有兩個有效 destination  
**When** 一個部位建立提醒工作  
**Then** 系統建立一個 logical Job、包含該 Job 的 Bundle，並對兩個 destination 建立兩筆獨立 Delivery；逐一記錄結果，撤銷其中一個 destination 不影響另一個。

### AC-26：外露部位與防護方法（P0）

**Scenario A：不同部位使用不同方法**  
**Given** 使用者選擇 `face_nose_cheeks`、`ears`、`neck_back`，並分別記錄防曬產品、其他外用產品、衣物完整覆蓋  
**When** Session 建立  
**Then** 必須使用 `BODY_ZONE_V3` 產生一筆含三個穩定 `zoneInstanceId` 的 SessionStartedEvent 及三筆 ZoneMethodEvent；耳朵的其他外用產品不具防曬提醒資格時使 Session 成為 `ATTENTION_REQUIRED`，不得因臉部已記錄塗抹而消失。衣物覆蓋記在 `skinExposureStatus／methodComponents`，遮蔭只記在情境，不得混入部位方法。

**Scenario B：只有衣物完整覆蓋**  
**Given** 使用者將某部位記錄為 `被衣物完整遮住`，且未選擇衣物下方也有擦防曬  
**When** Session 建立並顯示該部位狀態  
**Then** 該部位保存 `skinExposureStatus=clothing_covered`、`methodComponents=[clothing]` 及 `timingStatus=not_applicable`，不得要求塗抹時間、建立 Application、`zoneDueAt` 或倒數；畫面顯示「已記錄衣物遮住，不計算補擦時間」與 `更新衣物狀態`。

**Scenario C：衣物下方曾擦防曬後重新外露**  
**Given** 某部位 10:00 記錄合格防曬產品，接著以衣物完整覆蓋，原一般期限為 12:00，且 `sunscreen` component 從未被移除  
**When** 使用者在 11:00 或 12:10 明確更新為衣物已移開、皮膚外露  
**Then** 衣物完整覆蓋期間不得顯示補擦倒數；11:00 外露時依原 `appliedAt` 顯示到 12:00 的剩餘時間，12:10 外露時立即顯示建議補擦。兩者都不得從衣物移開時間重新起算；若原 Application 已失效或方法不確定，改顯示無時間行動卡。

### AC-27：同一離線期間的相依事件（P1）

**Given** 裝置在 base revision 5 離線記錄 `water_end`，接著記錄局部重新塗抹，client sequence 分別為 1、2  
**When** 恢復連線並提交同一 `syncBatchId`  
**Then** 伺服器只對 revision 5 驗證一次，按 sequence 原子套用兩個事件並回傳最終 revision；第二個事件不得僅因第一個事件已增加 revision 而得到 409。

### AC-28：未來或晚到事件（P0／P1）

**Scenario A：未來事件**  
**Given** 客戶端提交晚於可信現在／`serverNowUtc` 10 分鐘的塗抹事件  
**When** P0 本機 reducer 或 P1 API 驗證  
**Then** 拒絕套用；P1 回傳 `422 FUTURE_EVENT_TIME`，不得延長任何部位期限。

**Scenario B：晚到的較舊事件**  
**Given** 客戶端提交早於同部位最新有效事件的晚到事件  
**When** 事件流重建狀態  
**Then** 可保留為歷史但不得覆蓋較新的部位狀態，除非使用通過驗證的明確更正流程。

### AC-29：Quiet hours 與旅行時區（P1）

**Given** 使用者選擇 `Asia/Taipei` 的 22:00–07:00 quiet hours，之後瀏覽器偵測到另一個 IANA time zone  
**When** 排程通知  
**Then** 跨午夜區間依 `Asia/Taipei` 判斷；系統只提示是否更新時區，在使用者確認前不得自動改變 quiet hours。

### AC-30：預報 freshness（P0 fallback／P1 預報卡）

**Given** `F-D0047-091` 預報的 `validTo` 已過，或發布後超過 14 小時仍無更新  
**When** 每次讀取 WeatherSnapshot  
**Then** 動態計算為 `unusable` 並停止當作目前 UVI；資料列不得因先前寫入 `fresh` 而永久維持新鮮。

### AC-31：通知呈現與偏好（P0／P1；遠端情境限當次選定管道）

**Scenario A：選定管道已啟用**  
**Given** 登入使用者透過 `PATCH /v1/me/notification-preferences` 設定 `Asia/Taipei`、22:00–07:00 quiet hours，並啟用本次選定遠端管道  
**When** 重新讀取偏好並建立通知工作  
**Then** API 回傳相同設定，選定管道依 quiet hours 與 time-sensitive 選項處理。

**Scenario B：選定管道已關閉**  
**Given** 同一使用者關閉本次選定遠端管道  
**When** 新的邏輯提醒工作建立  
**Then** 該管道不得建立 Delivery，App 內仍顯示真實狀態。

**Scenario C：P0 前景聲音與震動漸進增強**  
**Given** 使用者在目前裝置明確啟用並測試前景短提示音或震動  
**When** App 開啟且提醒狀態到達需要注意的節點  
**Then** 畫面必須先顯示可存取的視覺／文字狀態；聲音只嘗試播放一次短提示，震動只在 feature detection 支援時嘗試一次短震動。不支援、被阻擋或裝置未實際發聲／震動時不得阻止提醒、循環重試或宣稱硬體已執行。

**Scenario D：Web Push 聲音與震動只能作為請求**  
**Given** 使用者已啟用 Web Push，並選擇 `聲音（跟隨手機設定）` 或 `震動（支援的裝置）`  
**When** 系統傳送測試通知或正式提醒  
**Then** 只在平台支援時提交相應非靜音／震動提示，並顯示「實際聲音與震動由手機、瀏覽器及系統設定決定」。Delivery 成功不得被記錄或顯示為已聽見、已震動；靜音、專注／勿擾、省電或平台不支援時不得用自訂音訊、持續震動或重複推播繞過。

### AC-32：首頁與即時權限詢問（P0）

**Scenario A：尚未主動要求權限**  
**Given** 無進行中 Session 的新 Guest 進入首頁  
**When** 尚未主動選擇定位、通知、相機或登入功能  
**Then** 畫面不得自動彈出這些權限或登入要求。

**Scenario B：手動地區完成**  
**Given** Guest 不提供定位  
**When** 手動選擇地區並取得可用 UVI 或明確的資料不可用狀態  
**Then** 首屏只有一個視覺上主要的 `開始防曬提醒` CTA，且拒絕任何非必要權限仍可繼續。

**Scenario C：暫不提供地區**  
**Given** Guest 不提供定位也不選行政區  
**When** 選擇 `暫不提供地區`  
**Then** 首頁不顯示所在地 UVI 數字，但保留單一主要的 `開始防曬提醒` CTA，且不得反覆要求定位。

### AC-33：沿用設定不沿用時間（P0）

**Given** 本機有一筆仍留存的最近設定  
**When** 使用者選擇 `沿用並確認`  
**Then** 系統以臉部、耳朵、頸部等大群組顯示完整追蹤部位、方法與產品摘要；只有包含實際防曬或其他外用產品的部位才要求確認新的塗抹時間，全部為衣物完整覆蓋時不得要求時間。最近設定若含舊版 `none_reported` 或 `unknown` 方法，不得直接沿用，必須請使用者改選目前已擦防曬產品或被衣物完整遮住，否則取消建立。在最終提交前不得建立 Session，也不得沿用舊 `lastAppliedAt`、期限或狀態。無設定變更時，從選擇沿用到建立 Session 不超過兩個確認畫面。

### AC-34：部位組合與批次方法（P0）

**Scenario A：套用組合後建立例外**  
**Given** 使用者套用 `BODY_ZONE_PRESET_V3.face_ears_neck` 並為全部部位批次選擇防曬產品  
**When** 使用者將耳朵改為 `被衣物完整遮住`  
**Then** 提交前確認區以臉部、耳朵、頸部大群組顯示，耳朵例外保持可見；只有同一大群組內的子部位方法、產品、時間或狀態不同時才展開原子部位。`zoneInstanceId` 只供資料驗證與提交使用，不得顯示給一般使用者；提交時建立相應 `ZoneMethodEvent`。

**Scenario B：例外後再次批次操作**  
**Given** 耳朵已被個別修改為 `被衣物完整遮住`  
**When** 使用者再次對其餘部位批次套用防曬產品  
**Then** 耳朵不得被靜默覆蓋；若操作範圍包含耳朵，必須先明確提示並取得確認。

**Scenario C：不逐一選擇部位的快速提醒**  
**Given** 首次使用者選擇 `快速提醒（推薦）` 並完成本次情境選擇  
**When** 使用者進入快速提醒／塗抹時間畫面  
**Then** 系統將該情境的版本化建議組合自動寫入 SetupDraft，並以白話大群組顯示將追蹤的部位摘要，不要求逐項瀏覽原子部位清單；只有使用者選擇 `調整部位` 時才以 Bottom Sheet 展開細項。自動套用不得建立 Application 或 Session；使用者仍須在提交前確認區檢查完整 `BODY_ZONE_V3` 摘要並按下 `開始提醒`，且系統不得加入摘要中未揭露的部位。

### AC-35：情境式產品欄位、臨時產品與未知值（P0）

**Scenario A：一般戶外新增產品**  
**Given** 一般戶外使用者看不清楚耐水標示  
**When** 新增產品並選擇 `看不清楚／不確定`  
**Then** 可繼續建立 Session，非水上情境不強迫展開耐水進階欄位。

**Scenario B：之後進入水上活動**  
**Given** 進行中 Session 使用耐水標示未知的產品  
**When** 使用者選擇準備下水或記錄 `water_start`  
**Then** 介面展開顯示標示未知及其保守影響，不得猜測或套用 40／80 分鐘。

**Scenario C：只在本次使用的產品**  
**Given** 使用者不想把本次產品加入 `我的防曬品`  
**When** 確認產品身分、必要標示與本次塗抹  
**Then** 系統以 `productId=null` 及不可變 `ProductLabelSnapshotV1` 建立 ApplicationEvent；不強迫保存產品，也不得因沒有 product ID 就丟失標示、期限資格或稽核依據。

### AC-36：進行中 Session 資訊層級（P0）

**Given** 耳朵因擦毛巾成為 `REAPPLY_DUE`，其他部位仍為 `TRACKING`  
**When** 開啟進行中 Session  
**Then** 首屏先以到期靜態卡顯示耳朵、擦毛巾原因與 `建議現在補擦`，並提供狀態相符的主要動作 `記錄已補擦` 與次要動作 `回報狀況`；不得顯示假的 `00:00` 圓環、只顯示其他部位的較晚倒數，或把耳朵警示藏入收合區。

### AC-37：補擦預選不等於提交（P0）

**Given** 耳朵與後頸已到期，臉部仍在追蹤中  
**When** 使用者點選 `記錄已補擦`，取消後頸、加選臉部並確認  
**Then** 介面可預選耳朵與後頸，但只為最後確認的耳朵與臉部建立一筆或多筆、依產品標示 snapshot 與時間分組的 `ApplicationEvent`；同一 `applicationConfirmationId` 內的 `zoneInstanceIds[]` 彼此互斥，且每個最後確認部位恰好出現一次。後頸及其他部位狀態不得改變。

### AC-38：白話狀態與非顏色提示（P0）

**Scenario A：逐部位計時狀態**  
**Given** 同一畫面同時包含 `UNRECORDED`、`REAPPLY_SOON` 與 `REAPPLY_DUE`  
**When** 以一般視覺、灰階、高對比或螢幕閱讀器操作  
**Then** 每一狀態均有第 5.7.5 節的白話文字與可存取名稱，不得只靠紅黃綠、內部代碼或無替代文字的圖示區分。

**Scenario B：Session、記錄與不適用狀態**  
**Given** 畫面包含 `ATTENTION_REQUIRED`、`physical_method_reported`、`none_reported`、`unknown` 與 `not_applicable`  
**When** 顯示 Session 摘要與完整部位清單  
**Then** 分別使用第 5.7.5 節對應白話文字，`not_applicable` 同時顯示實際記錄方式，不得誤寫成已完成產品防護或仍有補擦倒數。

### AC-39：草稿保留與可稽核更正（P0／P1）

**Scenario A：未提交草稿**  
**Given** 使用者已完成部位與產品選擇但尚未提交  
**When** 發生暫時網路錯誤、返回上一步或關閉後在 24 小時內返回  
**Then** 內容保留為明確標示且具期限的本機草稿，不得成為有效 Session，也不得包含尚未確認的塗抹時間。

**Scenario B：已提交事件更正**  
**Given** 使用者剛提交錯誤的補擦部位  
**When** 選擇取代或完全撤銷該次補擦確認  
**Then** 系統追加同 owner 與 Session 的不可變 ApplicationConfirmationGroup：取代時帶 `groupAction=replace`、`correctionOfGroupId`、完整新部位集合與互斥事件分區；撤銷時帶 `groupAction=void`。系統在同一交易重建受影響部位、Session 彙總並寫入 NotificationJob／Bundle 的取消與重建 outbox，且不得直接刪除原 group 或事件。

### AC-40：相對時間與可信時鐘（P0）

**Scenario A：可信時鐘**  
**Given** 裝置具有可信時間，且使用者選擇 `30 分鐘前`  
**When** 設定新的塗抹時間  
**Then** 以 `trustedCurrentTime - 30 分鐘` 換算並顯示絕對時間供確認。

**Scenario B：不可信時鐘**  
**Given** 裝置目前為 `CLOCK_UNTRUSTED`  
**When** 使用者嘗試設定新的塗抹時間  
**Then** 成功取得符合第 8.1 節的線上時間樣本前不得解除此狀態、增加剩餘時間或接受未來時間；不得提供「我確認現在時間」等手動解除按鈕。離線時只保留較短的保守狀態，並允許記錄不晚於保守現在的過去事件；重新連線校準成功後，使用者確認的實際塗抹事件才可依正常規則建立新的較晚期限。

### AC-41：初始情境與水上活動起點（P0）

**Scenario A：準備下水**  
**Given** 使用者在啟動流程選擇水上活動  
**When** 選擇 `準備下水` 並提交  
**Then** 只寫入 `initialContext=water_preparing`，不建立耐水期限。

**Scenario B：已在水中且知道時間**  
**Given** 使用者選擇 `已在水中` 並確認可信的實際入水時間  
**When** 提交 Session  
**Then** 寫入 `initialContext=water_active`，並在同一原子操作建立含 `startTimeConfidence=confirmed`、實際 `activityStartedAt`、新 `activityIntervalId` 及非空 `zoneInstanceIds[]` 的 `water_start`，否則整筆失敗。

**Scenario C：已在水中但時間不確定**  
**Given** 使用者選擇 `已在水中` 但不知道何時入水  
**When** 選擇 `不確定` 並提交  
**Then** 寫入 `initialContext=water_active` 與 `startTimeConfidence=unknown` 的 `water_start`，`activityStartedAt` 保持空值，不以提交時間冒充起點，不建立 40／80 分鐘期限；含防曬產品的相關部位進入 `untimed_action`，首屏顯示需立即確認的靜態行動卡，Session 為 `ATTENTION_REQUIRED`。

**Scenario D：不合法的入水起點**  
**Given** 客戶端提交 `startTimeConfidence=confirmed` 但 `activityStartedAt` 缺失、晚於 `effectiveOccurredAt` 或晚於可信現在，或提交 `unknown` 卻帶有 `activityStartedAt`  
**When** P0 reducer 或 P1 API 驗證原事件或 replace 更正  
**Then** 拒絕整筆操作，不建立或延長任何耐水期限；P1 回傳可識別的 `422 INVALID_WATER_START_TIME`。

**Scenario E：未知起點的合法離水與全域區間**  
**Given** Session 已有一個 `startTimeConfidence=unknown`、`activityStartedAt=null` 的未關閉水上區間  
**When** 使用者記錄 `water_end`，或嘗試用不同部位集合建立第二個重疊 `water_start`  
**Then** 合法終點必須沿用起點完整部位集合，且 `activityEndedAt <= effectiveOccurredAt` 並不晚於可信現在；系統可關閉區間但不得計算水中分鐘。任何第二個重疊區間均須拒絕，不因部位集合不同而例外。

### AC-42：已有進行中 Session（P0／P1）

**Given** Guest 或帳號已有一個未結束 Session  
**When** 回到首頁、開啟舊的沿用入口或再次嘗試啟動提醒  
**Then** 首頁主要 CTA 依目前 `primaryAction.actionKind` 顯示最高優先操作，並提供進入完整提醒頁的次要入口；使用者再次明確啟動新提醒時顯示 `查看目前提醒`、`結束目前提醒並開始新的`、`取消` 三個選項，沿用入口不得直接建立第二個 Session。只有 `SessionEndedEvent` 成功後才能建立新草稿，且不得帶入舊塗抹時間、事件或期限；直接呼叫建立 API 時依 owner 單一 Session 規則回傳目前狀態或 `409`，不得靜默覆寫。

### AC-43：最近使用產品排序（P0）

**Given** 最近 90 天曾有 5 個不同產品的 ApplicationEvent，其中一個產品已刪除、另一產品所在的 confirmation group 已被 void，另外三個仍可存取  
**When** 開啟產品選擇步驟  
**Then** 只顯示依第 5.7.3 節規則去重及穩定排序後最多 3 個仍有效產品；不得從已刪除產品的 snapshot 重建產品，也不得顯示已作廢 confirmation group 的產品。

### AC-44：部位方法更正不變條件（P0／P1）

**Given** 某追蹤部位只有一筆有效的初始 ZoneMethodEvent  
**When** 嘗試把 replace payload 改成另一個 `zoneInstanceId`／`bodyZoneCode`，或單獨 void 該唯一事件  
**Then** P0 reducer 與 P1 API 均拒絕操作；只有明確的 `ZoneTrackingCommand` 能原子追加舊 instance 的 `tracking_ended`，並建立／啟用新 instance、`tracking_started` 與完整初始方法。Batch 後每個 active 部位都必須有唯一 tracking state 及合法方法組合；已 ended instance 不得再參與 `sessionNextDueAt`、`primaryAction` 或通知。

### AC-45：更正鏈唯一 leaf（P0／P1）

**Given** 兩個 replace／void 操作都以同一個目前有效 event 或 ApplicationConfirmationGroup 作為 correction target，且起始 Session revision 相同  
**When** 第一個操作已成功提交後，第二個操作才到達 reducer 或 API  
**Then** 第一個 successor 成為唯一有效 leaf；P0 reducer 拒絕第二個 successor，P1 整批回傳 `409 CORRECTION_CONFLICT`。不得形成分支，也不得以接收時間、客戶端時間或任意排序選擇其中之一。

### AC-46：海邊防曬 Q&A 入口與狀態中立（P0）

**Given** 使用者正在 `water_preparing`、`water_active`、耐水標示未知或水上事件流程，且已有尚未提交的情境、部位或產品草稿  
**When** 開啟 `FAQ_BEACH_SUN_V1` 後返回  
**Then** 原步驟及草稿完整保留；閱讀、展開來源或關閉 Q&A 不得建立事件、儲存產品、改變 Session 狀態或建立／延長任何提醒期限。

### AC-47：純椰子油不得取得防曬期限（P0）

**Given** 使用者表示唯一塗抹的外用產品為純椰子油，且包裝沒有防曬產品或 SPF 標示  
**When** 嘗試把它新增為防曬產品，或以它啟動一般戶外／水上 Session  
**Then** 系統可讓使用者把它保存為一般外用產品紀錄，或建立不具期限資格的 ApplicationEvent，但不得把它標成已確認防曬產品，也不得建立一般 120 分鐘或耐水 40／80 分鐘期限；相關外露部位顯示 `目前沒有可建立防曬期限的產品紀錄`，並提供物理防護建議及第 13.4 節入口。系統不得只依暱稱自動判定，產品身分須由使用者確認包裝標示。

### AC-48：含椰子油的完整防曬成品（P0）

**Given** 最終成品含椰子油，但包裝明確標示其為防曬產品，且使用者可分別確認 SPF、UVA／廣效與耐水標示的已知或未知狀態  
**When** 使用者新增產品並建立 Session  
**Then** 系統依該最終成品的實際標示與第 6.2 節規則處理，不得因「含椰子油」自動判為有效、無效、提高 SPF 或海洋安全；未知欄位仍採既有保守路徑。

### AC-49：海洋環境宣稱與 Q&A 發布 gate（P0）

**Given** Q&A 或其他內容包含純椰子油 SPF、`reef-safe`、海洋友善、珊瑚安全或特定成分環境影響宣稱  
**When** 發布流程執行  
**Then** 每一題均須具有可追溯來源、研究／測試材料、情境限制、有效審查者、審查日期及再審日期；宣稱「天然必然安全」「零傷害」「百分之百海洋友善」、把缺乏直接研究寫成安全證明，或把 SPF 換算成安全曝曬時間時，發布流程必須阻止上線。

### AC-50：重用已保存產品（P0）

**Given** 使用者已有一款未修改且狀態為 `目前使用` 的已確認防曬產品  
**When** 啟動新的補擦提醒並選擇該產品  
**Then** 系統只要求確認本次使用的產品、部位與塗抹時間，不得再次詢問是否有 SPF 標示。

### AC-51：洗手只影響手背（P0）

**Given** 手背與臉部均有進行中的產品提醒  
**When** 使用者記錄 `hand_wash` 並接受預設受影響部位  
**Then** 系統只將 `hand_backs` 提前為檢查／補擦，臉部及其他未勾選部位的期限不變。

### AC-52：耳朵可從常用組合移除（P0）

**Given** 使用者套用含耳朵的常用部位組合  
**When** 在確認清單取消耳朵  
**Then** Session 不包含 `ears`，且介面不得阻止提交或在背景自動加回。

### AC-53：過去產品不干擾快速選擇且安全封鎖不可直接恢復（P0）

**Given** 使用者將一款產品標記為已用完、停止使用或封存，或曾回報該產品異常／使用後不適  
**When** 再次啟動提醒  
**Then** 該產品不出現在快速選擇，仍可在本機過去紀錄查看或刪除。只有 `conditionStatus=no_issue_reported` 且未被 ProductSafetyEvent 封鎖的一般已用完／封存／自行停止產品可恢復；回報異常或不適者不得直接恢復。異常事件可對不同批次／新包裝建立新版；不適事件不得只靠新包裝或同配方新批次繞過，只有另一款產品或使用者明確確認配方不同時建立獨立紀錄，且不得宣稱系統已判定可用。自助匯出只有在 P1 帳號功能納入當次版本時提供，不得成為 P0 查看與刪除的前提。

### AC-54：手機版優先流程（P0）

**Given** 使用者以 360、390 或 430 CSS px 寬的受支援手機瀏覽器開啟服務  
**When** 從首頁建立一個 Session  
**Then** 使用者可在不縮放、不橫向捲動、無必要控制項被鍵盤或瀏覽器列遮擋的情況下完成地區、部位、產品、時間與確認操作；桌面版差異不得改變流程所需資訊。

### AC-55：免個資訪客模式（P0）

**Scenario A：不登入也能完成核心任務**  
**Given** 新使用者尚未登入或註冊  
**When** 選擇以訪客身分開始  
**Then** 可完成 UVI 查詢、手動地區、產品本機紀錄與前景提醒，不被要求輸入 Email、電話、姓名、生日或性別；若之後選擇登入，系統才先顯示會上傳的本機資料與合併選項。

**Scenario B：登入價值不誤導提醒準確度**  
**Given** 訪客已完成第一次提醒，或主動查看備份、跨裝置、歷史、匯出或遠端通知功能  
**When** 系統呈現訪客與登入差異或登入提示  
**Then** 必須明確說明兩者使用相同 UVI 資料與提醒規則；只可列出當前版本已實際提供的登入功能，並同時說明訪客資料可能因清除網站資料、解除安裝或更換裝置而遺失。不得使用「登入才更準確」「完整防護」或健康風險作為施壓文案。

**Scenario C：登入前由使用者決定遷移內容**  
**Given** 訪客裝置已有產品、Session、事件、私人備註或特殊狀況資料  
**When** 使用者完成登入並準備啟用同步  
**Then** 系統先顯示分類預覽與是否上傳的選擇；私人備註、產品安全狀態、SetupDraft 與一次性特殊狀況預設不勾選。使用者確認前不得上傳，完成後須顯示實際同步狀態，不得只因已登入就宣稱資料已備份。

### AC-56：醫療情境不作個別判定（P0）

**Given** 使用者選擇有醫囑、皮膚疾病／症狀、用藥或近期療程，或詢問自己是否可曝曬、可使用某產品或需等待多久  
**When** 系統呈現回應  
**Then** 系統先比對當次有效醫療審查版本的 `redFlagCodes`；命中者改走 AC-99，不命中者才適用本項。一般路徑不得要求輸入疾病名稱、病理資料、處方、照片或病史，也不得產生個別化醫療結論、延長提醒期限或保證安全；必須清楚說明本服務不提供診斷或治療建議，並引導使用者依醫囑或詢問醫師／原療程院所。

### AC-57：提醒頁沒有進行中提醒（P0）

**Given** 使用者目前沒有進行中的提醒  
**When** 點選底部導覽的 `提醒`  
**Then** 系統留在提醒頁，顯示「目前沒有進行中的防曬提醒」與單一主要按鈕 `開始防曬提醒`；不得自動跳回首頁或顯示空白畫面。

### AC-58：特殊狀況入口位置（P0）

**Given** 使用者位於一般首頁  
**When** 檢查首頁與 `更多` 頁  
**Then** `我有特殊狀況` 不出現在首頁首屏，並在 `更多` 頁以清楚文字常駐；提醒設定或進行中提醒可依情境提供次要的就近入口。

### AC-59：簡潔與白話介面（P0）

**Given** 使用者操作任何 P0 手機頁面  
**When** 檢查標題、狀態、主要按鈕與必要說明  
**Then** 每頁只有一個視覺上最主要的動作，畫面不顯示未解釋的內部術語，必要說明先以一句白話摘要呈現，且四個底部入口均同時具有圖示與文字。

### AC-60：UV知識庫入口與分類（P0.5）

**Given** 使用者開啟 `更多`  
**When** 進入 `UV知識庫`  
**Then** 系統顯示六個白話分類、搜尋及內容卡；底部導覽仍維持四項，不新增第五個主入口。

### AC-61：情境式衛教不改變提醒（P0／P0.5）

**Given** 使用者在選產品、水上活動、洗手或提醒畫面，開啟當次版本已納入的 P0 最低 Q&A／運行說明，或 P0.5 `UV知識庫`文章  
**When** 閱讀後返回  
**Then** 原操作位置與草稿仍保留，且產品、部位、事件、Session 與提醒期限均未因閱讀內容而改變。

### AC-62：衛教卡分層與趣味邊界（P0.5）

**Given** 一篇內容使用問題卡、小測驗、迷思判決或情境攻略  
**When** 使用者閱讀或作答  
**Then** 先看到一句白話答案與可執行動作，能主動展開原理及來源；系統不得以答錯羞辱、恐嚇、健康分數、公開排名、錯誤安全保證或獎勵壓力促進互動。

### AC-63：內容發布與著作權 gate（P0／P0.5）

**Given** 任一當次版本納入的 P0 最低內容或 P0.5 `UV知識庫`內容缺少有效來源、審查者、審查日期、再審日期，或包含未授權第三方文章、標題、圖片／插圖  
**When** 發布流程執行  
**Then** 系統阻止發布；已發布內容審查過期時改顯示 `內容更新中` 與經核准的安全替代說明。

### AC-64：視覺提醒頁資訊順序（P0）

**Given** 使用者開啟進行中的提醒頁  
**When** 首屏完成渲染  
**Then** 依序看見符合 `primaryAction` 的主要狀態元件、目前原因、狀態感知主要動作與 `回報狀況`；不需先捲過完整部位清單或事件歷史。只有存在可信起訖時間時顯示提醒環，並同時顯示相對時間、絕對時間及非安全曝曬提示；已到期或無可信期限時改用靜態行動卡。

### AC-65：操作按鈕層級與正確提交（P0）

**Given** 某部位即將到期或建議現在檢查  
**When** 使用者查看狀態摘要  
**Then** 可補擦狀態以至少 44 CSS px 高的 `記錄已補擦` 為主要按鈕，`回報狀況` 為次要按鈕；若主要需求是補齊方法、確認產品或處理未知入水時間，主要按鈕須改成對應動作。任何按鈕都不得在未確認實際部位、產品、事件與時間前延長期限。

### AC-66：視覺狀態不製造安全感（P0）

**Given** 提醒處於追蹤、即將到期或已到期  
**When** 主要狀態元件、部位卡與時間軸更新  
**Then** 畫面不得將提醒環稱為防護量／安全百分比，不以綠色表示安全，不只靠顏色區分；到期後顯示 `建議現在檢查／補擦` 的靜態卡，不顯示負數、持續閃爍 `00:00`、暫停或任意增加時間。

### AC-67：視覺倒數無障礙（P0）

**Given** 使用者啟用螢幕閱讀器或減少動態效果  
**When** 提醒持續運行並跨越即將到期或已到期節點  
**Then** 螢幕閱讀器取得白話剩餘時間與狀態但不逐秒朗讀；減少動態效果下無旋轉、脈衝或閃爍，所有重要資訊仍可由文字及圖示理解。

### AC-68：日間高對比色票（P0）

**Given** 使用者首次開啟服務或選擇 `日間高對比`  
**When** 首頁與提醒頁渲染  
**Then** 使用第 5.7.7 節 token，主要／次要文字、按鈕、圓環及有意義邊框分別通過適用的 WCAG 2.2 AA 文字或非文字對比；不得因 hover、disabled 以外狀態或 opacity 使必要資訊低於門檻。

### AC-69：狀態不只靠顏色（P0）

**Given** 提醒分別處於進行中、即將檢查、建議現在處理、尚未記錄／不確定及衣物／遮蔽  
**When** 使用者以灰階或色覺差異模擬查看  
**Then** 仍可透過文字、圖示、邊框／線型與原因辨識每一狀態；綠色不得被用作安全或無風險訊號。

### AC-70：顯示模式選擇（P0）

**Given** 使用者進入 `更多 → 顯示設定`  
**When** 選擇 `跟隨手機`、`日間高對比` 或 `夜間模式`  
**Then** 設定保存於目前裝置並立即套用，不要求定位或環境光權限；三種模式的核心內容與功能相同，夜間模式也通過相同適用對比驗證。

### AC-71：戶外實機可讀性（P0）

**Given** 代表性 Android／iPhone 位於正午直射陽光或樹蔭，並涵蓋第 17.3.1 節亮度、保護貼與眼鏡條件  
**When** 受測者執行 UVI 判讀、主要狀態元件、部位狀態、`記錄已補擦`／`回報狀況` 及模式切換任務  
**Then** 不需猜測顏色、橫向捲動或依賴遮住螢幕才能找到必要資訊；失敗條件與修訂決策須留存，未完成不得宣稱適合戶外閱讀。

### AC-72：防曬提醒優先的公開架構（P0.5）

**Given** 使用者由首頁、搜尋引擎或公開內容頁進入防曬晴報員  
**When** 檢查首屏主要行動與主要內部連結  
**Then** 無進行中 Session 時優先顯示 `開始防曬提醒`，已有 Session 時優先顯示目前 `primaryAction.actionKind` 對應操作；`今日UV` 提供資料與次要入口，不得取代提醒主 CTA。

### AC-73：公開文章 SEO 模板與健康審查（P0.5）

**Given** 一篇 UV知識庫文章準備公開索引  
**When** 發布 gate 執行  
**Then** 頁面具有唯一 title／description／canonical、單一 H1、麵包屑、10 秒答案、H2／H3 內容層級、作者／審查者／日期、宣稱附近來源、文末參考資料與相關工具入口；缺少有效健康審查或使用誇張保證時阻止發布。

### AC-74：個人頁面不可索引（P0／P0.5）

**Given** 未登入第三方、搜尋引擎爬蟲或知道網址的人嘗試存取帳號、產品、Session、提醒歷史、設定或權限回呼頁  
**When** 系統處理請求  
**Then** 不回傳其他使用者或訪客的私人內容，頁面具正確存取控制、快取限制與 `noindex`；不得僅依賴 robots.txt 或難以猜測的網址保護資料。

### AC-75：結構化資料與行動 SEO 驗證（P0.5）

**Given** 公開品牌頁、分類頁或文章頁進入發布候選版本  
**When** 執行行動版 Lighthouse／Core Web Vitals、HTML、Rich Results、sitemap、canonical 與內部連結檢查  
**Then** 無阻擋索引的非預期設定，結構化資料與可見內容一致，圖片具適當文字替代與尺寸；測試結果、例外及修訂決策均留存。

### AC-76：AEO 不得干擾核心任務（P0.5）

**Given** 首頁、提醒頁或公開內容頁加入 AEO 內容或版面調整  
**When** 以代表性手機執行開始提醒、確認已補擦及回報狀況任務  
**Then** 主要 CTA 未被答案、FAQ、推薦文章或搜尋文字遮住，操作步驟未增加，提醒規則未改變；核心任務完成率、時間、錯誤率或無障礙結果惡化時阻止發布。

### AC-77：答案單元清楚、完整且可追溯（P0.5）

**Given** 一個公開頁面回答補擦、SPF／PA、耐水、洗手、流汗、游泳、UVI 或特殊狀況問題  
**When** 使用者與內容審查者閱讀答案  
**Then** 能在前 1～3 句取得直接答案，並看見適用條件、下一步、限制／不確定性及重要來源／審查日期；不得把一般答案表述成個人安全曝曬時間或醫療結論。

### AC-78：禁止 AEO 大量變體與隱藏內容（P0.5）

**Given** 發布流程發現同義問句、城市／族群／季節模板頁、AI 專用副本、隱藏文字或不可見結構化資料  
**When** 內容沒有獨立的使用者價值，或標記內容與畫面不一致  
**Then** 系統阻止發布並要求合併、canonical／重新導向或移除；不得以增加 AI 引用或長尾覆蓋作為例外。

### AC-79：首次使用維持少量必要輸入（P0）

**Given** 訪客首次開始防曬提醒  
**When** 系統需要取得地區、塗抹時間、部位、方法或產品資訊  
**Then** 畫面只先詢問建立本次有效提醒所需的欄位；進入部位步驟時自動套用一組情境建議並顯示摘要，不要求逐一選擇，只有按下 `調整部位` 才以 Bottom Sheet 展開細項。防護方式第一層只顯示 `已擦防曬產品` 與 `被衣物完整遮住`，只有實際外用產品部位才詢問塗抹時間，全部為衣物完整覆蓋時直接確認。自動套用只寫入 SetupDraft，必須經最終確認才建立 Session；若無法確認任何可用方法，提供返回與離開但不建立 Session。定位可拒絕並改用手動地區或 `暫不提供地區`，非必要細節可稍後補充，且不得要求先註冊帳號。

### AC-80：商業中立（P0／P0.5）

**Given** 使用者查看 P0 首頁、提醒、我的防曬品與最低 Q&A／說明，或當次已納入的 P0.5 `UV知識庫`／搜尋內容  
**When** 畫面呈現產品資訊或下一步建議  
**Then** 不得顯示廣告、業配、付費排名、聯盟分潤購買連結或偽裝成中立建議的產品推薦；我的防曬品只顯示使用者自行保存的資料與中立標示說明。

### AC-81：部位狀態簡化呈現（P0）

**Scenario A：多個部位只有一個主要狀態元件**  
**Given** 同一 Session 有兩個以上部位且處理時間不同  
**When** 使用者開啟進行中提醒頁  
**Then** 首屏只以一個符合最高優先 `primaryAction` 的主要狀態元件呈現下一步，其餘部位依大群組摘要展開查看；若最高優先狀態無可信期限則顯示靜態行動卡，即使其他部位有較晚數字期限也不得用該數字掩蓋主要行動。不得同時呈現多個同等醒目的倒數。

**Scenario B：全部只有衣物覆蓋**  
**Given** 所有 active 追蹤部位均已確認只有衣物覆蓋，且沒有產品期限或需要處理的未知狀態  
**When** 使用者開啟進行中提醒頁  
**Then** 首屏使用 `presentationType=untimed_action_card`、`variant=neutral_physical` 顯示「已記錄衣物覆蓋」，不得放入「需要處理」或以錯誤色呈現；主要動作為 `回報狀況`，次要動作才是 `更新防護方式`。

### AC-82：局部補擦只重設已確認部位（P0）

**Given** 手背已到期、臉部即將到期且手臂仍在一般追蹤  
**When** 使用者點擊 `記錄已補擦`，保留預選手背、加選臉部並確認當下具期限資格的實際產品與時間  
**Then** 只有手背與臉部建立新的 `ApplicationEvent` 並更新各自期限；手臂與其他未選部位的塗抹時間、期限及狀態完全不變，完成訊息列出實際更新部位並提供更正。

### AC-83：事件快速預選仍需確認（P0）

**Given** 使用者有手背、臉部、頸部及手臂的獨立部位狀態  
**When** 回報洗手  
**Then** 介面預選手背並在不超過三個必要動作內完成確認；使用者可修改受影響部位，提交後只重建最後確認部位，且畫面顯示狀態變更原因。

### AC-84：多部位通知合併但不延後（P1）

**Given** 同一 Session 有多個部位在相同時間或已核准的短合併視窗內到期  
**When** 遠端通知工作準備投遞  
**Then** 重新驗證各部位與 Session revision，只顯示一則摘要通知，不重複轟炸；摘要不得延後最早到期提醒，點擊後顯示目前真實部位狀態，已更新或失效部位不得繼續列入。

### AC-85：事件因果與方法重啟不復活舊塗抹（P0／P1）

**Scenario A：一般破壞事件只由之後的有效補擦解除**  
**Given** 手背在 10:00 記錄洗手，並有一筆 09:59、10:00 或 10:01 的有效 Application  
**When** reducer 重建事件因果  
**Then** 09:59 與 10:00 的 Application 均不得解除洗手原因，只有嚴格晚於 10:00 的 10:01 Application 可解除；離線重播、晚到與更正後仍得到相同結果，不得依封包抵達順序決定。

**Scenario B：移除並重新啟用防曬方法**  
**Given** 某部位 09:00 有合格 Application，10:00 將目前方法改為只有衣物，11:00 再改回含 `sunscreen`，且該次 activation command 沒有建立新 Application  
**When** reducer 重建一般期限  
**Then** 10:00 起 `generalDueAt` 與適用的 `activeWaterDeadline` 均為 null；11:00 後也不得恢復或引用 09:00 的舊 Application，只有不早於 11:00 的新 Application 才能建立一般期限，且仍須符合水上起點先後規則才能建立耐水期限。

**Scenario C：初始回填時間早於 Session 建立時間**  
**Given** 使用者於 10:00 提交 StartSessionCommand，並在同一原子操作確認某部位實際於 09:30 塗抹合格產品  
**When** reducer 建立初始 sunscreen activation 與一般期限  
**Then** 該 Application 的 09:30 `appliedAt` 作為 activation 邊界並依規則建立期限，不得因早於 SessionStartedEvent 的 10:00 而被排除；舊 Session 或舊 activation 的 Application 仍不得混入。

**Scenario D：衣物覆蓋期間一般原因暫不適用但不消失**  
**Given** 手背 10:00 記錄洗手，10:01 改為衣物覆蓋，且沒有晚於洗手的合格 Application  
**When** reducer 先顯示衣物狀態，之後使用者再次改為外露且含 `sunscreen`  
**Then** 衣物覆蓋期間不顯示 `REAPPLY_DUE` 或事件數字期限；再次外露後原洗手原因重新成為待處理狀態，直到有嚴格晚於 10:00 的合格 Application。不得把衣物覆蓋誤當成已解除洗手原因。

### AC-86：沒有可信期限時不製造倒數（P0）

**Given** 最高優先部位為尚未記錄方法、產品身分未知、入水起點未知，或具有尚未解除的立即事件  
**When** 進行中頁面衍生 `primaryAction`  
**Then** `presentationType=untimed_action_card` 或 `due_card`，`actionAt=null` 時不顯示圓環、任意分鐘、`00:00` 或百分比；主要按鈕須對應補資料、確認時間、採取防護或記錄補擦。

### AC-87：產品標示 snapshot 與較短標示優先（P0）

**Given** 使用者確認產品標示建議 90 分鐘補擦，並以該產品建立 ApplicationEvent  
**When** 產品日後被修改、封存或刪除  
**Then** 該事件仍保留當時的 `ProductLabelSnapshotV1`，一般期限不晚於 90 分鐘；不得因產品主檔後來改成 120 分鐘而延長舊事件，也不得把私人備註、品牌或購買紀錄寫入 snapshot。

### AC-88：開始 Session 必須原子成功（P0／P1）

**Scenario A：含產品與水上事件的原子建立**  
**Given** `StartSessionCommandV1` 包含三個追蹤部位、兩筆 ApplicationEvent，且初始情境為已在水中  
**When** 其中一個部位方法、產品 snapshot 或 `water_start` 驗證失敗，或相同 Idempotency-Key 被重送  
**Then** 第一次失敗不得留下空 Session、部分 zone、Application 或通知工作；合法重送只回傳同一個完整 Session。P0 IndexedDB 與 P1 API 使用相同命令 schema 與測試向量。

**Scenario B：沒有外用產品時不建立 Application**  
**Given** P0 新設定流程的所有追蹤部位均為衣物完整覆蓋，且每個部位的 `skinExposureStatus + methodCertainty + methodComponents[]` 組合合法  
**When** 提交 `StartSessionCommandV1`  
**Then** 系統可原子建立 Session 與 ZoneMethodEvents，ApplicationConfirmationGroup／ApplicationEvents 必須為空，且不得要求或偽造塗抹時間。

**Scenario C：方法組合不合法**  
**Given** 當前新建命令的任一部位出現 `none_reported`、`unknown`、`unrecorded`、重複 method component、`methodCertainty=confirmed` 但集合為空，或 `skinExposureStatus=unknown` 卻搭配非 unknown certainty  
**When** P0 共用驗證器或 P1 API 處理命令  
**Then** 整筆以相應 `422` 拒絕且不留下部分 Session；`none_reported`、`unknown` 與 `unrecorded` 只能由版本化舊資料重播、遷移或復原流程處理，不得猜測、靜默修正或當成新建 Session 的有效方法。

### AC-89：可信時間 API 不得被快取（P0／P1）

**Given** 客戶端請求 `/v1/time`  
**When** CDN、Service Worker、瀏覽器或測試代理處理回應  
**Then** 請求帶每次隨機 `clientNonce`；回應具有 `no-store／no-cache` 要求、UTC `serverNowUtc`、原樣 echo 的 nonce 與伺服器 `requestId`。客戶端以 monotonic 樣本計算 RTT／偵測跳變，以 wall-clock 請求／回應中點估算 UTC offset。快取回應、nonce 不符、重播、wall-clock 異常或 RTT 超標均不得把時鐘標記為可信；不得用只有 monotonic epoch 的資料計算 UTC offset。

### AC-90：Guest 不會在未登入時建立遠端通知（P0／P1）

**Given** Guest 在本機建立進行中 Session  
**When** 使用者未登入、未遷移 Session 或未完成通知管道設定  
**Then** 後端沒有該 Guest 的 Session、NotificationDestination、Job、Bundle 或 Delivery；介面只承諾 App 開啟時的狀態恢復。登入後也必須先顯示遷移範圍並取得明確選擇，才可建立遠端排程。

### AC-91：通知 Bundle 部分失效時重建（P1）

**Given** 一個 Bundle 合併臉部 10:30 與手背 10:32 的兩個 Job  
**When** 使用者在 10:20 只補擦臉部，使 Session revision 更新  
**Then** 舊 Bundle 設定 `cancelledAt`，手背 Job 以不晚於原 10:32 的時間及版本化 `mergeWindowVersion` 重建新 Bundle；已被 worker claim 的舊 Delivery 在送出前亦因 revision 不符停止，通知不得繼續列出臉部或延後手背。Delivery 的 claim 逾 `claimExpiresAt` 後才可由另一 worker reclaim，不得永久停在 `claimed`。

### AC-92：離線更新與舊通知風險必須可見（P1）

**Given** 登入使用者離線補擦或結束 Session，且伺服器仍可能保有舊排程  
**When** 操作尚為 `pending_sync`  
**Then** App 內立即依本機有效事件顯示狀態，同時清楚標示 `尚未同步` 與 `舊通知可能仍會送達`；不得宣稱所有裝置已更新。同步後依伺服器結果消除提示或進入可處理的衝突流程。

### AC-93：通知目的地隔離與唯一綁定（P1）

**Scenario A：當次只納入 Web Push**  
**Given** P1 當次版本納入 Web Push，不論是否納入 LINE Messaging  
**When** 使用者建立、測試、更新或撤銷 Web Push destination  
**Then** 只接受 WebPushDestination 所需的 endpoint、`p256dh`、`auth` 與 device binding；加密、active endpoint 唯一性、測試通知、聲音／震動偏好作為非保證提示、失效及撤銷均須通過驗收，不得要求或建立 LINE 欄位，也不得保存「實際已發聲／已震動」等無法由 Web Push 證明的狀態。

**Scenario B：當次只納入 LINE Messaging**  
**Given** P1 當次版本納入 LINE Messaging，不論是否納入 Web Push  
**When** 使用者完成 link／follow，或 webhook 回報 followed／blocked／invalid  
**Then** 只依驗證過的 LINE Messaging recipient 更新 LineDestination；LINE Login identity 不得直接當作 Messaging recipient，且不得要求 Web Push 的 device、endpoint 或金鑰欄位。

**Scenario C：兩種管道同時納入時互不干擾**  
**Given** 同一帳號同時具有 Web Push 與 LINE Messaging destination  
**When** Web Push subscription 失效、使用者解除 LINE Login，或 LINE webhook 回報封鎖  
**Then** 只更新經驗證對應的 destination；任一管道錯誤、測試或撤銷不得刪除或改寫另一管道。此跨管道情境只有在兩種管道都納入當次版本時執行，Scenario A／B 則各自是所納入管道的必要發布門檻。

**Scenario D：Web Push endpoint 跨帳號重新綁定**  
**Given** 同一 `endpointHash` 目前屬於帳號 A，之後裝置登出並由帳號 B 要求綁定  
**When** 建立新 destination  
**Then** 單一交易先撤銷 A 的 active ownership，再啟用 B；active endpoint 全域只能有一位 owner。競爭請求必須由資料庫唯一限制拒絕，不得讓 A 與 B 同時收到通知或讓 A 的舊 destination 殘留為 active。

### AC-94：BODY_ZONE_V3 與自訂部位隱私（P0／P1）

**Given** Session 包含標準 `hand_backs` 與兩個 `custom` 部位  
**When** 建立、顯示、通知與分析  
**Then** 每個 custom 具有不同且穩定的 `zoneInstanceId`，標準部位在同一 Session 不重複；自訂文字只在本機或已授權帳號畫面顯示，不進入遠端通知、第三方分析或一般日誌。

### AC-95：暫停通知不等於暫停提醒（P1）

**Given** 使用者有進行中的 Session 與已啟用遠端通知  
**When** 選擇 `暫停通知`，期間有部位到期，之後再恢復  
**Then** App 內期限、原因與狀態持續計算且不得延長；暫停期間不投遞，恢復時只依目前 revision 建立仍有意義的後續工作，不一次補送多則過期通知。

### AC-96：結束 Session 與舊通知（P0／P1）

**Given** 使用者選擇 `結束本次提醒`  
**When** 確認操作  
**Then** 系統追加 SessionEndedEvent、保存結束原因、使未投遞 Job／Bundle 失效並回到無進行中提醒狀態；舊通知被點開時只顯示 `這次提醒已結束`，不得重啟、補擦或修改舊 Session。

### AC-97：過期、異常或使用後不適的產品（P0）

**Scenario A：開始前已知不可用**  
**Given** 產品已過期、使用者回報外觀／氣味異常，或先前記錄使用後感到不適  
**When** 選擇該產品開始新 Session  
**Then** 系統不得讓它建立新的時間型期限，明示停止使用並依產品標示、藥師或醫療專業建議處理；不得診斷不適原因。

**Scenario B：進行中才回報**  
**Given** 該產品已有進行中的部位期限  
**When** 使用者回報產品異常或使用後感到不適  
**Then** 系統原子追加粗粒度 ProductSafetyEvent、將相關部位轉為不具期限的注意狀態並使未投遞的產品期限通知失效；既有 Application snapshot 保持不可變。介面不要求輸入症狀、照片或病史，並提供經審查的一般停止使用／就醫界線。

**Scenario C：安全封鎖的顯示解除與產品封鎖分離**  
**Given** 某部位目前因產品 A 的 ProductSafetyEvent 顯示安全事件行動卡  
**When** 使用者稍後再次記錄產品 A、改用具期限資格的產品 B，或改為衣物覆蓋  
**Then** 再次記錄產品 A 不得解除原因或建立期限；改用產品 B 且具有新的合格 Application，或改為衣物覆蓋後，該部位可不再顯示產品 A 的目前行動卡，但產品 A 的安全狀態仍維持封鎖且不得直接恢復。

**Scenario D：較新的不合格使用不得回退舊產品期限**  
**Given** 某部位先有具期限資格的產品 A Application，之後記錄較新的過期、異常、身分未知或非防曬產品 B Application  
**When** reducer 計算目前期限  
**Then** `currentApplication` 必須是較新的 B 且期限為 null，不得跳過 B、回退使用 A 的舊 Application 或舊 snapshot 建立一般／耐水期限。

**Scenario E：異常與不適的新紀錄條件不同**  
**Given** 產品 A 曾有 `product_abnormal_reported` 或 `product_discomfort_reported`  
**When** 使用者手動輸入新包裝／不同批次，或明確確認配方不同並嘗試建立新版  
**Then** 異常事件可讓不同批次或新包裝建立獨立紀錄並重新確認標示；不適事件不得只靠新包裝或同配方新批次解除封鎖，只有另一款產品或使用者明確確認配方不同時建立獨立紀錄。兩者均不得由系統宣稱對該使用者適合或安全。

### AC-98：提醒規則必須連到核准證據（P0／P1）

**Given** 某規則會建立、提前、取消或阻止期限  
**When** ruleset 進入發布候選  
**Then** 每個 `ruleId` 均存在於已核准 ReminderRuleSet，並透過 ReminderRuleEvidenceLink 連到有效 Claim Registry 項目、審查者與日期；缺漏或審查過期時阻止發布。規則升版不得在無遷移政策下靜默改寫進行中 Session。

### AC-99：急症分流優先於一般免責（P0）

**Given** 使用者選擇的情境命中當次有效醫療審查版本的 `redFlagCodes`  
**When** 分流頁開啟，包含離線狀態  
**Then** 第一個主要動作是經台灣醫療／法務審查的 119 或緊急就醫指引，一般免責聲明置於其後且不得延誤動作；離線包仍須有最低必要分流文字。系統不得要求症狀自由文字、照片或自行判定診斷。

### AC-100：個資權利與短效分析界線（P0／P1）

**Given** 個資告知、資料設定頁或產品分析進入發布候選  
**When** 法務與隱私測試執行  
**Then** 告知分別列出查詢／閱覽、複製本、補充／更正、停止蒐集處理利用及刪除等權利，自助匯出另列為產品功能；分析 `flowId` 必須在 `createdAt + 24 小時` 到期刪除且不得續期、跨裝置或連回帳號，原始搜尋字詞、自訂部位、產品、健康、精確位置與提醒時間線均不得送往第三方分析。未完成可重識別風險評估前不得宣稱匿名或去識別化。

---

## 20. 上線門檻

### 20.1 P0 Core Beta 門檻

Core Beta 用於驗證手機版核心提醒流程，可採限定招募且不要求完整公開內容／SEO；但下列安全與正確性門檻不可延後：

1. 皮膚科或相關醫療專業人員審查當次版本所有健康文案、產品異常／使用後不適流程、醫療拒答與急症分流；離線最低必要分流內容亦完成驗證。
2. 台灣法務審查 P0 個資告知、法定權利文字、Guest 本機資料、保存政策、短效假名化分析及海洋友善／環境宣稱邊界。
3. TFDA 法規專業人員完成 intended use、宣傳範圍及產品屬性初步評估；不得只靠免責聲明取代。
4. 具海洋環境或珊瑚研究能力的審查者核准 `FAQ_BEACH_SUN_V1`，確認未把實驗室危害外推為真實海域必然結果，也未把證據不足、天然、礦物或 `reef-safe` 寫成零傷害保證。
5. CWA dataset、授權、UVI 分類、選站、缺值、觀測／預報類型及過期策略通過固定輸入測試；拒絕定位、手動地區及 `暫不提供地區` 三條路徑均可完成。
6. ReminderRuleSet、Claim Registry 與 ReminderRuleEvidenceLink 完整；所有會建立、提前、取消或阻止期限的 `ruleId` 有有效證據與審查。P0 reducer 的公式、因果及錯誤案例具固定測試向量。
7. `StartSessionCommandV1` 原子性、`BODY_ZONE_V3`、逐部位狀態、局部補擦、事件嚴格先後、產品 snapshot、較短標示優先、水上區間不重疊、過期／異常／不適產品、結束 Session 及舊狀態恢復均通過測試。
8. `/v1/time` 不可快取、RTT／單調時間校準、時鐘前後跳、合法補擦重設比較基準及無可信期限時不製造倒數均通過測試。
9. PWA／一般瀏覽器在關閉頁面後重新開啟，能由時間戳恢復 App 內正確狀態；Core Beta 不宣稱 Guest 關閉頁面後一定會收到遠端通知。
10. Android Chrome、iOS Safari、iOS 主畫面 Web App、Desktop Chrome／Edge及定位、通知、相機拒絕矩陣完成；P0 前景短提示音／震動須以 feature detection、明確啟用、測試、阻擋與不支援降級驗收，且不得循環播放或宣稱一定發聲／震動。Guest 本機資料查看、刪除、草稿到期與儲存失敗降級完成演練。
11. 完成適用的 OWASP Web 風險、輸入驗證、權限邊界、CSP、日誌遮罩與本機資料清除測試。
12. 完成 WCAG 2.2 AA、自動對比、灰階／色覺差異及第 17.3.1 節戶外實機可讀性測試；有時間、已到期、無時間、資料不足與錯誤狀態均不能只靠顏色。
13. 至少一輪手機原型易用性測試涵蓋 Guest、無地區、沿用最近設定、`快速提醒` 一鍵情境建議組合、自己選擇部位、部位大群組、局部補擦、回報狀況、無時間行動卡及結束 Session。須分別量測部位步驟放棄率、完成時間、直接接受／調整比例及誤解率；受測者須能指出「現在要做什麼」、實際追蹤部位，並理解建議組合仍需本人確認，不得把局部補擦理解為全部重設。問卷票數本身不視為易用性驗證。
14. 當次 Core Beta 納入的最低 Q&A／運行說明已有來源、審查、到期、著作權與發布阻擋機制；P0.5 `UV知識庫`完整六類內容未完成不阻擋限定 Beta，但不得在 P0 導覽顯示空白、未審查或未納入範圍的入口。
15. 產品分析符合第 16.5 節：短效 flowId、原始搜尋字詞與敏感欄位不外送；安全日誌與產品分析分開。

### 20.2 P0.5 公開網站門檻

在 Core Beta 全數通過後，正式公開索引與內容導流另須完成：

1. UV知識庫六類入口、首發內容清單、搜尋同義詞、分層閱讀、情境返回、內容過期、第三方素材授權與責任人核准。
2. 防曬提醒優先的資訊架構通過行動任務測試；公開內容、`今日UV` 與 SEO 區塊不得遮住 `開始防曬提醒` 或 active Session 的最高優先操作。
3. 唯一 title／description／canonical、語意化 heading、XML sitemap、重新導向、個人頁面存取控制與 `noindex`、可見內容一致的 `Organization`／`WebSite`／`BreadcrumbList`／`Article` 或 `BlogPosting` schema 通過驗證。
4. 目前 UVI、個人狀態與會快速過期的數值不得寫進長效 title、meta description、OG 或結構化資料；地區／即時工具頁的索引政策依第 13.6.6 節執行。
5. AEO 答案單元與核心任務並列測試；若 FAQ、答案或推薦文章增加提醒步驟、降低可讀性、任務成功率或無障礙結果，即使搜尋曝光上升也不得發布。
6. 行動版效能、HTML、Rich Results、canonical、內部連結及 Search Console／Bing 可用功能驗證完成；不得為長尾流量大量建立低價值變體頁。

### 20.3 P1 帳號與遠端通知門檻

若當次發布納入 P1 功能，另須完成：

1. 帳號、Google／LINE Login identity、Guest migration 預覽與選擇、跨裝置衝突、自助匯出、刪除、法定權利申請流程及第三方 token 撤銷演練。
2. 依第 4.2 與 8.4 節選定的遠端通知管道完成小規模可靠性 pilot；在延遲、背景限制與已知失敗資料足夠前，不得把「關閉頁面仍會提醒」當成無條件承諾。
3. NotificationJob／Bundle／Destination／Delivery 四層、最早提醒不延後、revision 重驗、部分失效重建、quiet hours、多目的地、暫停通知、結束 Session、舊通知與管道撤銷通過競爭及冪等測試。
4. Web Push 與 LINE Messaging 分別完成建立、驗證、測試、失效與撤銷流程；LINE Login subject 不得取代 Messaging recipient。
5. 離線 batch、相依操作、`pending_sync`／舊通知風險提示、更正衝突、Webhook 簽章／防重播及 IDOR 測試完成。
6. 訪客／登入比較表由實際 capability flags 產生；登入提示的觸發時機、頻率上限、稍後選項與不得宣稱登入提高準確度均通過測試。Guest migration 必須先預覽、由使用者選擇並回報實際同步狀態。
7. 若當次版本要公開 `general_unlocalized` 整體提醒，必須先完成獨立 scope、state、event schema、命令驗證、reducer、通知、更正、遷移、分析邊界及新增驗收案例；缺少任一項時只能研究，不得出現在正式提交流程。
8. Web Push 若提供聲音／震動偏好，必須在代表性 Android、iPhone 主畫面 Web App、靜音、專注／勿擾、省電、權限拒絕及平台不支援情境完成矩陣測試；設定頁、測試通知、Delivery 與分析均不得把提交成功誤報為實際已響鈴／震動，也不得使用自訂鈴聲、持續震動或重複推播模擬鬧鐘。

### 20.4 P2 功能門檻

若當次發布納入 P2 功能，另須依實際範圍完成：

1. AI 標籤辨識的人工確認、模型版本、影像隱私、第三方條款與失敗降級測試。
2. 前景感測建議的權限、feature detection、誤判確認與不得延長期限測試。

每次發布必須通過 Core Beta 門檻及當次實際納入的 P0.5／P1／P2 條件；未開發的後續功能不阻擋限定 Core Beta。任何適用的安全、醫療、法務、資料來源或提醒規則證據門檻未完成時，不得對外發布，也不得使用「個人化醫療」「預防疾病」「精準」「保證提醒」或「安全曝曬」等宣傳語招募。

---

## 21. 主要參考來源

### 使用者研究

- `2026-07 防曬功能需求問卷：網頁製作的功能需求問卷（回覆）`：27 份探索性回覆，採彙總分析；屬內部需求探索資料，不是科學研究、全體使用者比例或市場規模證據。限制與可用範圍見第 18.4 節。

### 防曬與 UV

- [WHO：Protecting against skin cancer](https://www.who.int/news-room/questions-and-answers/item/radiation-protecting-against-skin-cancer)
- [WHO：Ultraviolet radiation fact sheet](https://www.who.int/news-room/fact-sheets/detail/ultraviolet-radiation)
- [CWA：紫外線指數預報服務、低量／中量／高量／過量／危險分級](https://www.cwa.gov.tw/Data/knowledge/announce/service13.pdf)
- [FDA：Sunscreen—how to help protect your skin from the sun](https://www.fda.gov/drugs/understanding-over-counter-medicines/sunscreen-how-help-protect-your-skin-sun)
- [FDA：Tips to stay safe in the sun](https://www.fda.gov/consumers/consumer-updates/tips-stay-safe-sun-sunscreen-sunglasses)
- [FDA：防曬標示、廣效及耐水 40／80 分鐘效能測試](https://www.fda.gov/regulatory-information/search-fda-guidance-documents/labeling-and-effectiveness-testing-sunscreen-drug-products-over-counter-human-use-small-entity)
- [FDA：OTC000039 Final Administrative Order（2026；美國 OTC 防曬標示與測試規範，非台灣法規）](https://www.accessdata.fda.gov/drugsatfda_docs/omuf/order/supportDoc/OTC000039/Final_Administrative_Order.pdf)
- [AAD：Sunscreen FAQs](https://www.aad.org/media/stats-sunscreen)
- [CIE：Standard Erythema Dose review](https://www.cie.co.at/publications/standard-erythema-dose-review)
- [TFDA：水上活動與海邊防曬選擇及補擦](https://www.fda.gov.tw/TC/siteListContent.aspx?id=51160&sid=13694)
- [TFDA：化粧品標示、保存、變質辨識、不適處理與通報](https://www.fda.gov.tw/TC/siteListContent.aspx?id=51158&sid=13694)

### 純椰子油與植物油防曬

- [Ácsová et al.（2021）：植物油體外與 ISO 人體 SPF 研究](https://pubmed.ncbi.nlm.nih.gov/33721242/)
- [Kaur & Saraf（2010）：產生椰子油 SPF 7～8 說法的稀釋液體外研究](https://pmc.ncbi.nlm.nih.gov/articles/PMC3140123/)
- [Natural photoprotection under scrutiny（2026）：植物油 UVA／UVB 體外評估](https://link.springer.com/article/10.1007/s43630-026-00911-2)
- [Skin Cancer Foundation：椰子油與自製防曬迷思](https://www2.skincancer.org/risk-factors/skin-cancer-myths-misinformation/)

### 海邊防曬與海洋環境

- [National Academies：Understanding Sunscreens in Aquatic Environments](https://nap.nationalacademies.org/resource/26381/interactive/)
- [NOAA：Skincare Chemicals and Marine Life](https://oceanservice.noaa.gov/news/sunscreen-corals.html)
- [Smithsonian Ocean：The Truth About Corals and Sunscreen](https://ocean.si.edu/ecosystems/coral-reefs/truth-about-corals-and-sunscreen)
- [國家海洋研究院：防曬乳成分對珊瑚影響之評估](https://www.namr.gov.tw/ch/home.jsp?dataserno=202110080008&id=50&mcustomize=research_view.jsp&parentpath=0%2C7)
- [海洋保育署：珊瑚礁區活動與海洋友善防曬提醒](https://www.oca.gov.tw/ch/home.jsp?id=176)

### 科學宣稱限制

- [Wild et al.：Global dimming／brightening](https://pubmed.ncbi.nlm.nih.gov/15879214/)
- [Nambour 防曬與黑色素瘤追蹤研究](https://pubmed.ncbi.nlm.nih.gov/21135266/)
- [防曬塗抹厚度與 SPF 研究](https://pubmed.ncbi.nlm.nih.gov/22548392/)
- [Fitzpatrick 自評限制研究](https://pubmed.ncbi.nlm.nih.gov/24928709/)

### Web、PWA 與第三方服務

- [CWA：O-A0003-001 10 分鐘綜觀氣象資料](https://opendata.cwa.gov.tw/dataset/observation/O-A0003-001)
- [CWA：F-D0047-091 鄉鎮未來一週天氣預報](https://opendata.cwa.gov.tw/dataset/forecast/F-D0047-091)
- [W3C：Geolocation](https://www.w3.org/TR/geolocation/)
- [W3C：Ambient Light Sensor](https://www.w3.org/TR/ambient-light/)
- [W3C：Geofencing 歷史狀態](https://www.w3.org/standards/history/geofencing/)
- [Chrome：Notification Triggers](https://developer.chrome.com/docs/web-platform/notification-triggers/)
- [MDN：Push API](https://developer.mozilla.org/en-US/docs/Web/API/Push_API)
- [MDN：Vibration API 與有限瀏覽器支援](https://developer.mozilla.org/en-US/docs/Web/API/Vibration_API)
- [MDN：ServiceWorkerRegistration.showNotification 與通知選項](https://developer.mozilla.org/en-US/docs/Web/API/ServiceWorkerRegistration/showNotification)
- [web.dev：系統通知的聲音、震動與平台差異](https://web.dev/articles/push-notifications-display-a-notification)
- [MDN：PWA 離線與背景運作](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Guides/Offline_and_background_operation)
- [MDN：Periodic Background Sync 支援限制](https://developer.mozilla.org/en-US/docs/Web/API/Web_Periodic_Background_Synchronization_API)
- [Chrome：背景分頁計時器與節流](https://developer.chrome.com/blog/background_tabs)
- [WebKit：Web Push for Home Screen web apps](https://webkit.org/blog/13878/web-push-for-web-apps-on-ios-and-ipados/)
- [Apple Developer：網站與 Web App 傳送 Web Push](https://developer.apple.com/documentation/usernotifications/sending-web-push-notifications-in-web-apps-and-browsers)
- [Apple WWDC23：Web App 通知聲與系統設定](https://developer.apple.com/videos/play/wwdc2023/10120/?time=253)
- [LINE：Getting user IDs](https://developers.line.biz/en/docs/messaging-api/getting-user-ids/)
- [Gemini API：模型停止服務資訊](https://ai.google.dev/gemini-api/docs/deprecations)
- [Gemini API：Rate limits](https://ai.google.dev/gemini-api/docs/rate-limits)
- [Gemini API：Terms](https://ai.google.dev/gemini-api/terms)
- [W3C：WCAG 2.2 對比、色彩使用與非文字對比](https://www.w3.org/TR/WCAG22/)
- [Android 無障礙工具：陽光直射等極端光照下的色彩對比](https://support.google.com/accessibility/android/answer/7158390?hl=zh-Hant)
- [Apple Human Interface Guidelines：Color](https://developer.apple.com/design/Human-Interface-Guidelines/color)

### 計時器與視覺進度介面參考

- [Apple：iPhone 計時器的標籤、預設、最近使用、多計時器與控制方式](https://support.apple.com/en-gb/guide/iphone/iph8241d6b2a/ios)
- [Apple Human Interface Guidelines：Progress Indicators](https://developer.apple.com/design/human-interface-guidelines/progress-indicators)
- [Time Timer：以可見面積呈現剩餘時間的視覺計時器](https://www.timetimer.com/collections/applications)
- [W3C WAI-ARIA：progressbar 值與 `aria-valuetext`](https://www.w3.org/WAI/ARIA/apg/practices/range-related-properties/)
- [W3C：以 live region 傳達重要進度狀態](https://www.w3.org/WAI/WCAG21/Techniques/aria/ARIA25)

以上只供互動與無障礙模式研究；防曬晴報員不得照搬一般計時器的暫停、延長或安全完成語意。

### 台灣法規與產品屬性

- [個人資料保護法](https://law.moj.gov.tw/LawClass/LawAll.aspx?pcode=I0050021)
- [個人資料保護法第 3 條：當事人權利](https://law.moj.gov.tw/LawClass/LawSingle.aspx?pcode=I0050021&flno=3)
- [TFDA：醫療器材屬性判定](https://www.fda.gov.tw/TC/siteContent.aspx?sid=11754)
- [TFDA：醫療器材軟體分類分級參考指引](https://www.fda.gov.tw/TC/siteListContent.aspx?id=41637&sid=11652)

### 衛教資訊架構參考（不作科學宣稱來源）

- [EZSKIN「陳皮靠腰」：主題分類、搜尋、問題式文章卡與閱讀時間](https://www.ezskin.com.tw/blog)
- [EZSKIN 防曬分類：以日常疑問作文章入口](https://www.ezskin.com.tw/blog/categories/___%E9%98%B2%E6%9B%AC)
- [輕鬆美膚皮膚百科：以使用者可觀察的症狀與問題分類](https://www.skindoctor.com.tw/blog)
- [光茵樂活文章頁：問題式標題、比較、QA、專業審閱、來源與內容到產品的內部連結](https://www.nanobiolight.com/blogs/%E5%85%89%E8%8C%B5%E5%B0%8F%E5%AD%B8%E5%A0%82/82234)

以上只供資訊架構與溝通方式研究，不是防曬晴報員健康宣稱的證據來源；不得轉錄其文章、標題、圖片或具辨識度系列名稱。

### SEO 與公開內容

- [Google 搜尋中心：SEO 入門指南](https://developers.google.com/search/docs/fundamentals/seo-starter-guide?hl=zh-TW)
- [Google 搜尋中心：建立實用、可靠、以使用者為優先的內容](https://developers.google.com/search/docs/fundamentals/creating-helpful-content)
- [Google 搜尋中心：生成式 AI 搜尋最佳實務與 AEO／GEO 迷思](https://developers.google.com/search/docs/fundamentals/ai-optimization-guide)
- [Google 搜尋中心：AI Overviews／AI Mode 與網站的技術要求](https://developers.google.com/search/docs/appearance/ai-features)
- [Google 搜尋中心：網站使用生成式 AI 內容的準確性、品質與揭露原則](https://developers.google.com/search/docs/fundamentals/using-gen-ai-content)
- [Google 搜尋中心：電商網站導覽與內部連結架構](https://developers.google.com/search/docs/specialty/ecommerce/help-google-understand-your-ecommerce-site-structure)
- [Google 搜尋中心：Article／BlogPosting 結構化資料](https://developers.google.com/search/docs/appearance/structured-data/article)
- [Google 搜尋中心：結構化資料一般規範](https://developers.google.com/search/docs/appearance/structured-data/sd-policies)
- [Google 搜尋中心：圖片 SEO 最佳實務](https://developers.google.com/search/docs/appearance/google-images)
- [Bing Webmaster Blog：AI Performance 引用與 grounding query 報表](https://blogs.bing.com/webmaster/February-2026/Introducing-AI-Performance-in-Bing-Webmaster-Tools-Public-Preview)

> 來源連結與供應商政策可能更新。每次正式發布前須檢查來源是否仍有效，並由相應專業人員確認內容沒有超出證據範圍。

---

## 22. 版本修訂紀錄

| 版本 | 日期 | 文件狀態 | 編修／核准責任人 |
| --- | --- | --- | --- |
| 3.10 | 2026-08-07 | 目前草案；設定流程由三畫面縮為兩畫面（獨立最終確認畫面改為 S-05 的提交前確認區，確認的實質要求不變）、F-05 擴為防曬裝備清單四品類、明訂本機匯出，待產品、設計、工程、醫療內容、海洋環境內容與法務聯合審查 | 待指定 |
| 3.9 | 2026-07-31 | 已由 3.10 取代；保留為推薦部位自動套用、最終確認邊界與首頁最高優先操作的一致性修訂基準 | 待指定 |
| 3.8 | 2026-07-28 | 已由 3.9 取代；保留為提醒邏輯、資料模型、無時間狀態、通知、醫療法務與發布分期的一致性修訂基準 | 待指定 |
| 3.7 | 2026-07-28 | 已由 3.8 取代；保留為逐部位獨立計時、首屏集中提醒、批次補擦及合併通知規格基準 | 待指定 |
| 3.6 | 2026-07-28 | 已由 3.7 取代；保留為探索性需求問卷分析與決策邊界修訂基準 | 待指定 |
| 3.5 | 2026-07-28 | 已由 3.6 取代；保留為使用者體驗優先 AEO 內容規格修訂基準 | 待指定 |
| 3.4 | 2026-07-28 | 已由 3.5 取代；保留為防曬提醒優先 SEO 架構修訂基準 | 待指定 |
| 3.3 | 2026-07-27 | 已由 3.4 取代；保留為固定品牌溝通語氣修訂基準 | 待指定 |
| 3.2 | 2026-07-27 | 已由 3.3 取代；保留為品牌標語定稿基準 | 待指定 |
| 3.1 | 2026-07-27 | 已由 3.2 取代；保留為中文品牌名稱修訂基準 | 待指定 |
| 3.0 | 2026-07-27 | 已由 3.1 取代；保留為戶外高對比色彩系統修訂基準 | 待指定 |
| 2.9 | 2026-07-27 | 已由 3.0 取代；保留為視覺提醒與主要操作位置修訂基準 | 待指定 |
| 2.8 | 2026-07-27 | 已由 2.9 取代；保留為防曬小學堂內容專區修訂基準 | 待指定 |
| 2.7 | 2026-07-27 | 已由 2.8 取代；保留為簡潔導覽與提醒空狀態修訂基準 | 待指定 |
| 2.6 | 2026-07-27 | 已由 2.7 取代；保留為醫療情境轉介與免責修訂基準 | 待指定 |
| 2.5 | 2026-07-27 | 已由 2.6 取代；保留為手機優先與訪客模式修訂基準 | 待指定 |
| 2.4 | 2026-07-25 | 已由 2.5 取代；保留為我的防曬品與手背流程修訂基準 | 待指定 |
| 2.3 | 2026-07-25 | 已由 2.4 取代；保留為 PWA 運行與通知修訂基準 | 待指定 |
| 2.2 | 2026-07-22 | 已由 2.3 取代；保留為海邊衛教修訂基準 | 待指定 |
| 2.1 | 2026-07-22 | 已由 2.2 取代；保留為易用性修訂基準 | 待指定 |
| 2.0 | 2026-07-22 | 已由後續版本取代；保留為安全與可行性修訂基準 | 待指定 |

版本日期表示文件修訂日期，不代表公開發布或專業核准日期；核准後須另記錄核准人、日期與範圍，不得由本表推定。

### 22.1 版本 3.9

- 進入 S-05 時依使用者已選情境自動將版本化建議部位寫入 SetupDraft，時間選擇器立即可用；不再要求先按下接受推薦。
- 自動套用只建立可修改的草稿預設值，不代表使用者已確認塗抹，不建立方法事件、Application 或 Session；S-06 仍須顯示完整摘要並由使用者按下 `開始提醒`。
- 防護方式與部位的例外調整統一由 S-05 Bottom Sheet 開啟，保留關閉、返回與草稿恢復行為。
- active Session 首頁主要 CTA 改為直接依 `primaryAction.actionKind` 顯示最高優先操作；提醒頁保留完整部位、原因、事件與 Session 管理。
- 同步修訂 AC-34、AC-42、AC-72、AC-79，以及 Release Manifest、Screen Inventory、Copy Deck、TDD 與 RTM。

### 22.2 版本 3.8

- 新增 P0 前景短提示音／震動及 P1 Web Push 系統聲音／震動偏好；兩者均採明確啟用、feature detection、測試與安靜降級，不提供自訂鈴聲、持續響鈴或繞過靜音／專注模式。
- 明定通知提交、供應商接受或畫面顯示都不能證明裝置實際發聲／震動；正式文案只能描述為依裝置與系統設定「可能」發出聲音或震動。
- 從新設定介面移除 `尚未採取防護` 與防護方式 `不確定`；若使用者無法確認已擦防曬、衣物完整覆蓋或次要的其他外用產品，提供返回／離開且不建立 Session。內部舊版、遷移與損壞復原狀態仍保留，不得直接沿用成新 Session。
- 將防護方式第一層收斂為 `已擦防曬產品`、`被衣物完整遮住`，衣物下方也有擦防曬改為選填補充；`其他外用產品` 移至次要入口，並移除語義模糊的 `兩者都有` 主選項。
- 明定衣物覆蓋屬條件型狀態而非時間型防護，不以 UPF、布料或覆蓋時間產生倒數；衣物移開後沿用仍有效的原始塗抹時間重建期限，不得重新起算。
- 將首次部位流程改為 `快速提醒（推薦）`：依使用者主動選擇的情境提供單一建議組合與白話摘要，只有需要調整時才展開原子部位；最終仍由使用者確認並提交完整 `BODY_ZONE_V3`。
- 將完全不指定部位的 `general_unlocalized` 定位為 P1 研究選項；未完成獨立資料模型、規則、通知、更正、遷移與驗收前，不得以假部位或空部位資料投入正式流程。
- 新增訪客／登入能力比較與價值式登入提示：核心 UVI 與提醒規則相同，登入價值聚焦於已實作的備份、跨裝置、歷史、匯出及遠端通知；補齊提示頻率、遷移預覽、敏感資料預設不選與同步狀態限制。
- 將首屏統一為由 `primaryAction` 決定的主要狀態元件：只有可信起訖時間顯示提醒環；到期、未知起點、產品不具資格或資料不足改用靜態行動卡。
- 部位 schema 升級為 `BODY_ZONE_V3`，加入 zone instance、UI 大群組、常用組合、自訂部位隱私及耳朵、嘴唇、頭皮、手背、足部等完整原子部位。
- 補齊 `ProductLabelSnapshotV1`、較短標示優先、臨時產品、過期／異常／使用後不適與私人備註本機優先規格。
- 定義 `effectiveOccurredAt` 等事件時間語義、嚴格先後因果、水上區間不變條件、不可變 SessionEndedEvent 與 `StartSessionCommandV1` 原子建立流程。
- 新增 `methodCertainty` 合法組合與不可變 `ZoneTrackingEvent`；只有實際外用產品部位詢問塗抹時間，移除／重新啟用防曬方法時不得復活舊 Application。
- 將 ProductSafetyEvent 從通用事件原因公式分離；異常／不適產品不得直接恢復，改用其他合格產品或衣物只解除目前行動卡，不清除原產品安全封鎖。
- 將遠端通知拆為 Job、Bundle、Destination、Delivery 四層，補齊部分失效重建、暫停通知、Guest 邊界、Web Push／LINE 隔離及離線舊通知風險。
- 補齊 `/v1/time` nonce echo、wall-clock／monotonic 分工、不可快取、合法事件重設時鐘比較基準與 IndexedDB 多 context 一致性；同步補齊 Delivery claim 到期與 Web Push endpoint 唯一 owner。
- 修正 CWA UVI 分級、海洋內容中立、醫療／急症分流、台灣個資法權利及短效假名化分析界線。
- 將 Core Beta、P0.5 公開網站、P1 帳號／通知與 P2 功能分開設定上線門檻，維持核心易用性優先於 SEO／AEO。
- 新增 AC-19 與 AC-85 至 AC-100，並同步修訂既有 AC 的欄位、文案及狀態語義。

### 22.3 版本 3.7

- 正式保留逐部位獨立記錄與計時，採用「底層分開計時、首屏集中提醒」原則。
- 規定首屏只顯示一個主要提醒環，以最早需處理部位作為 Session 摘要，其他部位依需要處理、即將處理、稍後處理分組。
- 補齊 `已補擦` 的預選、批次選擇、最終確認、局部更新及完成後更正規格。
- 補齊洗手、游泳、擦拭、摩擦與流汗事件的預選範圍，所有預選均須由使用者確認。
- P0 不預設拆分左右側；沿用外露部位與產品設定時不得沿用舊塗抹時間。
- 新增多部位遠端通知合併原則，但禁止為合併摘要延後最早到期提醒。
- 新增 AC-82 至 AC-84，並擴充手機原型易用性上線門檻。

### 22.4 版本 3.6

- 新增 27 份探索性功能需求問卷的資料品質、題目層級有效性、樣本偏差與不可外推範圍。
- 彙整防曬痛點、單一核心功能、最低輸入、通知偏好、海邊內容、不需要功能、棄用風險與採用條件。
- 以每日／非每日防曬使用者交叉觀察建立「UV 作為入口、提醒與個人化服務習慣使用者」假設，但明定小樣本不能推論因果。
- 維持提醒優先、UV 支援、訪客優先、最少輸入、事件回報與兒童／隊友模式刪除決策。
- 新增 P0 商業中立、單一主提醒環與通知技術可行性邊界，不把小工具、智慧手錶或 LINE 偏好直接承諾為 P0。
- 新增 AC-79 至 AC-81、問卷導向的上線前手機原型測試及下一輪問卷修正清單。

### 22.5 版本 3.5

- 明定優先順序為 `安全與正確性 → 使用者完成核心任務 → 易讀與無障礙 → SEO／AEO 可發現性 → 曝光與互動指標`。
- 將 AEO 定義為讓公開內容更容易被搜尋引擎與問答系統正確理解及引用，不視為獨立排名捷徑或引用保證。
- 新增直接答案、適用條件、下一步、限制／不確定性、來源與審查日期的答案單元。
- 禁止隱藏 AI 內容、近義問句／城市模板大量頁面、虛假提及、偽造作者／引用及未審查的自動健康內容。
- 明確不把 `llms.txt`、特殊 AI schema、Markdown 副本或內容極小切片列為 Google AEO 必要條件。
- AEO 僅調整公開內容，不得增加提醒步驟、改變倒數規則或在核心首屏插入長篇內容。
- 新增 AC-76 至 AC-78、AEO 發布否決門檻，以及 Google／Bing 官方 AI 搜尋參考。

### 22.6 版本 3.4

- 公開 SEO 與首頁主要行動均以 `防曬提醒` 為第一優先，`今日UV` 定位為支援判斷與導流的環境資訊。
- 明確區分 App 內四項底部導覽與公開可索引的內容架構，不為 SEO 增加第五個 App 主入口。
- 使用者可見的衛教專區名稱由 `防曬小學堂` 統一改為 `UV知識庫`。
- 新增公開／不可索引頁面邊界、個人頁面存取控制、canonical、sitemap、永久網址及重新導向規格。
- 新增 SEO 文章模板、作者與審查資訊、10 秒答案、頁內目錄、宣稱附近來源、提醒優先 CTA、分類與標籤治理。
- 新增 BlogPosting／Article、BreadcrumbList、圖片 SEO、行動效能、Search Console 與不含敏感資料的成效指標。
- 新增 AC-72 至 AC-75、SEO 發布門檻及 Google Search Central 參考來源。

### 22.7 版本 3.3

- 取消由使用者切換簡潔直接、溫和鼓勵或詳細說明等品牌人格。
- 固定採用「溫和明確、簡潔有行動感」的提醒語氣。
- 規定重要提醒依序說明狀況、原因與一個主要行動，補充原理改由可展開內容承載。
- 新增一般提醒、洗手事件、資料不足、提交完成與系統限制的標準句型。
- 保留健康風險情境提高嚴肅程度的例外，但不得使用恐嚇或無法證實的保證。

### 22.8 版本 3.2

- 品牌標語定為 `記得防曬，也記得適時補擦`。
- 標語用於首頁、品牌介紹及宣傳素材；不得單獨解讀為能保證避免曬傷，仍須搭配產品定位與必要免責說明。

### 22.9 版本 3.1

- 中文品牌名稱由原工作名稱統一改為 `防曬晴報員`，介面短稱為 `晴報員`。
- 品牌意象結合晴天／晴況與情報員／播報員，角色定位為提供日光風險情報、操作建議與適時提醒。
- 同步更新標題、產品介紹、通知、安裝教學、運行 Q&A、內容角色及參考資料說明中的名稱。
- 明確限制不得自稱官方氣象預報員、醫療專業人員或暗示自行產製官方 UVI。
- 英文工作名稱暫保留 `Sunshield Advisor`，正式定稿前須完成品牌、商標、網域與英文語感確認。

### 22.10 版本 3.0

- 新增 P0 戶外高對比淺色介面與完整日間 design token，列出主要文字、背景、品牌、提醒狀態及邊框色碼與對比。
- 規定提醒狀態同時使用文字、圖示、邊框與色彩，禁止綠色安全暗示、白字亮黃、低透明度淡字、大面積毛玻璃及文字疊圖。
- 新增圓環線寬／尺寸、按鈕配色、最低字級／字重與圖示筆畫規格。
- `更多 → 顯示設定` 新增跟隨手機、日間高對比與夜間模式，不依賴環境光感測器。
- 新增戶外直射陽光、樹蔭、亮度、保護貼、太陽眼鏡、色覺差異及代表裝置實機測試。
- 新增 AC-68 至 AC-71、發布門檻及 W3C／Android／Apple 色彩參考。

### 22.11 版本 2.9

- 將進行中提醒頁固定為視覺提醒環、狀態原因、`已補擦`／`回報事件`、部位卡及事件時間軸。
- `已補擦` 設為主要按鈕、`回報事件` 為次要按鈕，置於狀態摘要後且不需捲過完整內容。
- 視覺提醒環同時顯示相對與絕對時間，只代表距離建議檢查的進度，不代表防護剩餘量或安全百分比。
- 禁止暫停、增加／延長時間、負數倒數、閃爍 `00:00` 及以綠色表示安全。
- 新增部位狀態卡、事件時間軸、減少動態效果與螢幕閱讀器更新規格。
- 新增 AC-64 至 AC-67 及計時器／視覺進度介面參考。

### 22.12 版本 2.8

- 新增 `更多 → 防曬小學堂`，維持四項底部導覽，不增加第五個主入口。
- 建立 `今天怎麼防曬`、`看懂防曬乳`、`不只防曬乳`、`不同場合怎麼做`、`防曬迷思破解`、`曬後與特殊狀況` 六類架構。
- 規定每篇採問題式標題、10 秒答案、立即行動、常見誤解、可展開原理、醫療轉介與來源／審查資訊。
- 新增先猜猜看、迷思判決、情境攻略卡與晴報員一句話，並禁止羞辱、恐嚇、錯誤安全感、公開排名與獎勵壓力。
- 新增操作情境的就近衛教入口、返回保留草稿、內容著作權與過期替代規則。
- 新增 AC-60 至 AC-63 及小學堂發布門檻。

### 22.13 版本 2.7

- 新增「簡潔、白話、可預期導覽」易用性原則，每頁只突出一個主要動作。
- 確定手機底部導覽為 `首頁`、`提醒`、`防曬品`、`更多`，圖示必須搭配文字。
- 將 `我有特殊狀況` 的常駐入口移至 `更多`；提醒設定與進行中頁面仍可提供情境式入口。
- 沒有進行中提醒時，提醒頁留在原頁顯示說明與 `開始防曬提醒`，不自動跳回首頁。
- 新增 AC-57 至 AC-59，驗收提醒空白狀態、特殊狀況入口與簡潔白話介面。

### 22.14 版本 2.6

- 明確定位本服務不是醫療服務、診斷工具或治療建議；已有皮膚疾病、症狀、病理、醫囑、用藥或療程者，以醫師／醫療專業人員個別指示優先。
- 特殊狀況入口不得蒐集疾病名稱、病理、處方、照片或病史，並停止一般提醒推論。
- 對「能否曝曬、能否使用某產品、需等待多久」等個別醫療問題，規定不得判定，改採依醫囑或詢問醫師／原療程院所的轉介文案。
- 新增醫療內容與法務發布審查要求及 AC-56，驗收不作個別醫療判定。

### 22.15 版本 2.5

- 明確指定手機板為產品、互動與發布驗收的優先基準；桌面版維持相容與效率增強。
- 新增 360、390、430 CSS px 的單手操作、無橫向捲動與必要控制項可見性要求。
- 將預設入口定義為免註冊、免聯絡資料的訪客模式；登入／註冊為使用者主動選擇的升級功能。
- 明確列出訪客可用的本機核心功能，以及資料可能隨清除網站資料、解除安裝或換裝置而消失的限制。
- 新增 AC-54、AC-55，驗收手機優先流程與訪客個資最小化。

### 22.16 版本 2.4

- 新增 P0「我的防曬品」：區分目前使用與過去紀錄，支援使用狀態、購買／開封／效期及私人備註。
- 將產品身分確認移至新增、複製或編輯產品；啟動提醒只選擇本次使用產品，不重複詢問 SPF 身分。
- 部位代碼升級為 `BODY_ZONE_V2`，以 `hand_backs` 明確表示手背，並新增只影響手背的 `hand_wash` 事件。
- 耳朵保留在常見外露組合，但必須獨立顯示並可取消，不得強制納入。
- 收斂單人核心產品範圍，移除未採用的多人功能及其資料模型、API、隱私、驗收與上線門檻。
- 新增 AC-50 至 AC-53，驗收保存產品重用、洗手影響範圍、耳朵取消及過去產品快速選擇。

### 22.17 版本 2.3

- 確認 PWA 適合作為第一版平台，但將 P0 前景提醒與 P1 關閉頁面後的遠端通知能力明確分開。
- 新增關閉分頁、滑掉 PWA、滑掉單一通知、權限撤銷、離線及系統省電等情境的送達限制。
- 規定關閉後提醒採後端 `NotificationJob`、排程 Worker、狀態重驗及 Web Push，不得依賴頁面背景倒數。
- 新增 Android／Chrome 與 iPhone／Safari 的手機安裝方式、通知啟用順序及解除安裝／清除資料提醒。
- 新增低耗電策略，禁止持續 GPS、分鐘級 CWA 輪詢及背景視覺倒數，並要求代表裝置實測。
- 新增紫外線資料準確度與顯示規格，區分測站觀測、區域預報及每日最大值。
- 在 Q&A 新增「手機安裝方式教學」與產品運行原理。
- 在 Q&A 新增「倒數時間的運作原理」，說明絕對時間戳、逐部位期限、事件提前及非安全曝曬倒數。

### 22.18 版本 2.2

- 新增 P0 `FAQ_BEACH_SUN_V1` 海邊防曬 Q&A，涵蓋純椰子油 SPF 證據、舊有 SPF 7～8 體外研究限制、耐水 40／80 分鐘、海洋友善宣稱、UPF 衣物、完整成品及補擦方式。
- 新增水上情境就近入口、閱讀 Q&A 不修改狀態的互動規則，以及醫療、海洋環境與法務聯合內容審查門檻。
- 防曬產品手動新增前先確認包裝是否明確標示防曬／SPF；沒有標示的純植物油或一般身體油不得建立 120／40／80 分鐘期限。
- 新增海洋環境宣稱的證據欄位與發布限制，禁止把「天然」「缺乏研究」或 `reef-safe` 行銷文字寫成零影響保證。
- 新增 AC-46 至 AC-49，驗收 Q&A 狀態中立、純椰子油保守處理、含椰子油完整成品及環境宣稱發布 gate。
- 補充植物油防曬、TFDA 海邊防曬、耐水測試與水域環境研究來源。

### 22.19 版本 2.1

- 新增易用性原則與低摩擦操作框架，明確採用核心任務優先、漸進式揭露、批次設定與即時權限詢問。
- 將啟動提醒整理為三階段流程，加入最近設定重用、常見部位組合、相對時間快捷選項及情境式產品欄位。
- 明確規定沿用上次設定不得沿用舊塗抹時間、提醒期限或狀態，所有建議均須由使用者確認。
- 新增進行中 Session 的資訊層級、固定快捷動作、白話狀態、草稿恢復與可稽核更正規格。
- 增加易用性 KPI、可重現測量協定、分版本上線門檻及 AC-32 至 AC-45。
- 補上水上活動未知起點、事件更正不變條件、多產品事件互斥、常用部位 preset 映射與最近產品排序規則。

### 22.20 版本 2.0

- 將產品核心由「個人曬傷／安全倒數」改為「UV 風險與補擦事件提醒」。
- 移除 `150 × W_skin ÷ UVI`、SPF 時間乘法及疾病／汗水／窗邊倍率。
- 移除性別刻板化語氣與 Fitzpatrick 個人時間權重。
- 將定位改為最近有效官方測站，補上來源、距離、時間及 stale 狀態。
- 指定 `F-D0047-091` 為鄉鎮預報 fallback，區分觀測與預報 freshness，移除未經驗證的逐時／分鐘級 UVI 規劃。
- 將強制登入改為 guest-first。
- 增加完整外露部位、逐部位防護方法、事件稽核與 per-zone 提醒狀態，局部補擦不再改寫其他部位。
- 定義 Guest／登入／離線三種時間與儲存權威、時鐘跳變保守規則及原子離線 batch 同步。
- 修正 LINE Login、Messaging API、PWA、通知、離線與感測器能力邊界。
- 將通知拆成偏好、目的地、邏輯工作與逐目的地投遞，補上 quiet hours 時區與 at-least-once 冪等規則。
- 將 AI 掃描移至 P2，加入人工確認、模型生命週期、隱私與刪除要求。
- 補齊資料模型、API、隱私、資安、非功能需求與 AC-01 至 AC-31。
- 公式改為可搜尋文字，移除 base64 公式圖片與殘留編輯標記。
