The header on every screen: warm ivory, 44px lockup at the left, one hairline rule below.

```jsx
<BrandHeader logoBase="../../assets/logo" />
```

It is not a nav bar — the three destinations (提醒 / 裝備 / 更多) live in `BottomNav`. Keep the trailing slot empty unless there is a genuinely global, quiet status to show.
